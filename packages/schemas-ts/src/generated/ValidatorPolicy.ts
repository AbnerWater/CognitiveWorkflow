/**
 * AUTO-GENERATED by scripts/codegen/generate-ts-schemas.mjs.
 * Do not edit by hand. Change packages/schemas (Python cw_schemas), then run make codegen.
 */

/**
 * 多候选 / 多 judge 仲裁模式（D-NC-7 v0.2 占位）
 */
export type ArbitrationMode =
  | "single_judge"
  | "multi_judge"
  | "programmatic_first";
/**
 * ToolRegistry 中已注册的校验器 ID（如 citation_checker / schema_strict_v2）
 */
export type ValidatorId = string;
/**
 * ToolRegistry 中已注册的额外校验器（D-NC-3：禁止执行用户代码）
 */
export type ExtraValidators = ExtraValidatorRef[];
/**
 * strict：严格校验；lenient：允许部分缺失但触发修复；programmatic_only：跳过 LLM 校验
 */
export type ValidatorMode = "strict" | "lenient" | "programmatic_only";
/**
 * 流式中是否允许 partial output（与 Pydantic AI RunContext.partial_output 对齐）
 */
export type PartialOutputAllowed = boolean;

/**
 * 节点输出校验策略（§8）。
 */
export interface ValidatorPolicy {
  arbitration?: ArbitrationMode;
  extra_validators?: ExtraValidators;
  mode?: ValidatorMode;
  partial_output_allowed?: PartialOutputAllowed;
}
/**
 * ValidatorPolicy.extra_validators 引用（§8）。
 */
export interface ExtraValidatorRef {
  options?: Options;
  validator_id: ValidatorId;
}
/**
 * 校验器参数
 */
export interface Options {
  [k: string]: unknown;
}
