/**
 * @cw/schemas — CognitiveWorkflow TypeScript types.
 *
 * 单一真理来源在 packages/schemas（Python cw_schemas）；本包由 make codegen 自动派生。
 */

export const CW_SCHEMAS_VERSION = "0.1.0";

export type * from "./generated/index.js";
