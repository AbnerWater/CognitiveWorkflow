# M1.5.28 Runtime Shutdown Helper Review

Date: 2026-06-20

Reviewers:

- Initial reviewer: independent Codex subagent `Confucius` (`019ee53b-535a-75b1-be21-d7d98912d5fa`)
- Re-reviewer: independent Codex subagent `Noether` (`019ee543-cc87-76f2-a6d2-3aafe85108a7`)

Result: PASS after repair

## Findings

- High, fixed: the first implementation targeted `/shutdown`, but the accepted API tree and runtime app route require `/cw/v1/system/shutdown`. Fixed by changing `RUNTIME_SHUTDOWN_REQUEST_PATH` to `/system/shutdown` and updating the test / runbook expectations.

## Scope Checked

- Changed files stayed within the intended W1.5.28 boundary: `apps/desktop/src/main/runtime-ipc-handlers.ts`, `apps/desktop/src/main/runtime-ipc-handlers.test.ts`, `apps/desktop/src/main/index.ts`, and `docs/04_runbook/m1.5-progress.md`.
- No package manifest, accepted spec, ADR, schema, renderer UI, or generated schema output changed.
- No dependency, Electron import, real `ipcMain`, real `BrowserWindow`, app lifecycle, `localStorage`, or `sessionStorage` usage was introduced.
- `requestRuntimeShutdown()` now reuses the existing `RuntimeIpcMainHandlers.fetch()` path and `buildRuntimeIpcFetchRequest()` with `method: "POST"`.
- The helper posts `/system/shutdown` against runtime base URLs ending in `/cw/v1`, producing `/cw/v1/system/shutdown`, matching `specs/api/http_sse.md` and `apps/runtime/src/cw_runtime/api/app.py`.
- Tests cover the generated URL, `POST` method, injected `Authorization`, injected `X-Cw-Client`, empty body, and 202 empty response handling.

## Commands Run By Reviewers

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
```

The initial review commands passed before the endpoint fix but found the route mismatch. The re-review commands passed after repair; desktop tests reported 105 passed, 0 failed.

## Residual Risks

W1.5.28 remains a pure helper-only TypeScript slice. It does not wire real Electron quit / reload lifecycle, wait for actual sidecar shutdown, broadcast shutdown state, verify shutdown against a live FastAPI sidecar, or replace the existing signal-based sidecar stop fallback.
