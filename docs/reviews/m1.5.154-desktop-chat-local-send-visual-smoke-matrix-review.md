# M1.5.154 Review - Desktop Chat Local Send Visual Smoke Matrix

## Scope

- Slice: W1.5.154 Desktop Chat Box local send visual smoke matrix.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`

## Checklist

- `chat-enabled-desktop` remains dev-only visual smoke matrix coverage.
- Matrix case URLs strip base query/hash and only add safe `streamEvent` / `chatBox` enums.
- Matrix manifest keeps `chatLocalEvidence` to metadata-only sequence/count/status/intent/action/history summaries.
- Child JSON `failures` values are summarized by count and are not echoed into manifest or stderr.
- No production runtime/session/store/React/API/IPC/storage/dependency/spec/schema drift.

## Result

Initial independent review by Kepler found:

- P2: child `result.failures` strings were imported verbatim into matrix manifest/stderr, which could leak raw Chat Box draft text if the enabled-chat visual smoke child failed.
- P3: the runbook verification command referenced this review file before the file existed.

Both findings were fixed in this slice. The runner now records child JSON failures as `case JSON contains N failure(s)`, and the matrix test suite includes an enabled-chat fake failure containing raw draft text to assert stdout, stderr, and manifest redaction. This review artifact now exists and is covered by the targeted Prettier command.

Kepler re-review PASS: no remaining blocker/P1/P2/P3 findings. The re-review confirmed the P2 redaction fix, the enabled-chat child failure regression, and the review artifact closure for the runbook command.

## Reviewer Commands

- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs` PASS.
- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs` PASS, 13/13 tests.
- `pnpm exec prettier --check apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs docs/04_runbook/m1.5-progress.md docs/reviews/m1.5.154-desktop-chat-local-send-visual-smoke-matrix-review.md` PASS.
- `git diff --check` PASS, only LF-to-CRLF warnings.
