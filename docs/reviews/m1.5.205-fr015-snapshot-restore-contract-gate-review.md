# W1.5.205 FR-015 Snapshot Restore Contract Gate Review

| Item         | Value                                          |
| ------------ | ---------------------------------------------- |
| Slice        | W1.5.205                                       |
| Scope        | FR-015 snapshot restore Proposed ADR gate      |
| Reviewer     | Raman (`019f0a2f-0374-7ac2-ab7a-ee85e1a94795`) |
| Final Status | PASS after P2 repair                           |

## Reviewed Files

- `docs/03_decisions/0012-fr015-snapshot-ledger-restore-contract.md`
- `docs/03_decisions/README.md`
- `docs/04_runbook/m1.5-fr015-snapshot-restore-contract-gate.json`
- `docs/04_runbook/m1.5-progress.md` W1.5.205 section
- `apps/desktop/package.json`
- `apps/desktop/scripts/m1-5-fr015-snapshot-restore-contract-gate.cjs`
- `apps/desktop/scripts/m1-5-fr015-snapshot-restore-contract-gate.test.cjs`
- `apps/desktop/scripts/m1-5-runtime-flow-contract-acceptance-gate.cjs`

## Findings

Initial review found one P2:

- The FR-015 gate rejected only the exact shortened API table literal `GET    /{workflow_id}/snapshots`, so a premature accepted API change written as `GET /cw/v1/workflows/{workflow_id}/snapshots` could pass the gate.

Repair:

- `m1-5-fr015-snapshot-restore-contract-gate.cjs` now rejects workflow snapshot ledger, restore, and continue routes through a method plus route-suffix matcher that covers shortened, `/workflows/...`, and `/cw/v1/workflows/...` forms.
- `m1-5-fr015-snapshot-restore-contract-gate.test.cjs` now includes full-route snapshot and restore regressions.

No remaining P0/P1/P2/P3 findings after repair.

## Conservative Boundaries

- ADR-0012 remains `Proposed`.
- FR-015 remains partial and no FR item is accepted.
- `EXIT-P1-1` remains `not_ready`.
- No accepted spec, schema, generated schema, runtime behavior, Desktop production behavior, dependency, packaging, or baseline docx content was changed.
- Gate output remains metadata-only and does not include raw prompt, model output, uploaded-file bytes, response bodies, artifact bodies, raw workflow change summaries, raw refs payloads, full paths, secure paths, cache paths, or output directory values.
- The ADR-0011 index matcher repair still validates ID, filename, title, status, and date; it only removes dependency on Markdown table column spacing.

## Checks

- `node apps/desktop/scripts/m1-5-fr015-snapshot-restore-contract-gate.cjs --check` PASS.
- `node --test apps/desktop/scripts/m1-5-fr015-snapshot-restore-contract-gate.test.cjs` PASS.
- `node apps/desktop/scripts/m1-5-runtime-flow-contract-acceptance-gate.cjs --check` PASS.
- `node --test apps/desktop/scripts/m1-5-runtime-flow-contract-acceptance-gate.test.cjs` PASS.
- `pnpm exec prettier --check ...` on touched files PASS.
- `git diff --check -- . ":(exclude)CLAUDE.md"` PASS with CRLF warnings only.

Owner additionally ran `pnpm --filter @cw/desktop test` after repair: PASS, 450 passed.
