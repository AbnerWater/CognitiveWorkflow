# W1.5.184 Semi-auto HITL Desktop Bridge Review

## Scope

- Slice: W1.5.184
- Area: Desktop runtime workbench semi-auto HITL decision submit surface.
- Primary files: `apps/desktop/src/renderer/runtime-workbench-session.ts`, `runtime-workbench-interaction.ts`, `runtime-workbench-host-session.ts`, `runtime-workbench-shell-presenter.ts`, `runtime-workbench-shell-react.tsx`, `runtime-workbench-shell-react.test.tsx`, `apps/desktop/src/preload/contract.test.ts`.
- Explicit non-scope: `CLAUDE.md` unrelated dirty change.

## Independent Review

Reviewer: Arendt

Initial findings:

- P2: Desktop narrowed `custom_value` to `string`, while runtime `POST /runs/{run_id}/decisions` accepts `Any | None`. This blocked structured HITL edit/resume payloads even though snapshots did not retain raw values.
- P3: HITL status displayed `Ready` when no runtime bridge was available because idle status did not account for `canSubmitDecision=false`.

Fixes:

- `RuntimeWorkbenchHumanDecisionInput` and `submit_human_decision` commands now accept a JSON custom value (`null`, string, finite number, boolean, arrays, objects) with depth validation.
- `submitHumanDecision()` preserves structured `custom_value` in the POST body while snapshots keep only `customValuePresent`.
- Human decision status now labels idle unavailable runtime as `Unavailable` with warning tone.
- Focused tests now submit a structured custom value containing `raw_human_custom_value` and assert it is absent from the resulting snapshot.

Delta review:

- PASS. Arendt verified the prior P2 and P3 are closed: structured JSON `customValue` is accepted through session/interaction and preserved in POST body, snapshots retain only `customValuePresent`, structured raw-value regression covers redaction, and idle unavailable HITL now reports `Unavailable` / warning.

## Verification

- `pnpm --filter @cw/desktop exec tsc --noEmit` PASS.
- `pnpm --filter @cw/desktop run build:ts && node --test apps/desktop/dist/preload/contract.test.js apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js` PASS: 147 tests passed.
- `uv run pytest apps/runtime/tests/test_m1_3_8_human_decision_resolve.py -q` PASS: 6 tests passed.
- `pnpm --filter @cw/desktop run test` PASS: 395 tests passed.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 482 passed / 1 skipped; desktop 395 passed.

## Residual Risk

- W1.5.184 still does not implement pending-decision discovery, full pause/resume A4 evidence, or FR-018 acceptance; those remain future evidence-refresh work.
