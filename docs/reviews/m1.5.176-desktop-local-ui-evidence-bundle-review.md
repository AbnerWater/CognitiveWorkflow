# W1.5.176 Desktop Local UI Evidence Bundle Review

## Scope

W1.5.176 prepares a machine-checkable local UI evidence bundle for `TRACK-LOCAL-UI-EVIDENCE` from W1.5.174. The bundle covers only FR-001, FR-002, FR-003, FR-005, FR-006, and FR-020.

This review confirms the slice does not claim A4 acceptance, does not advance `EXIT-P1-1`, and does not modify production runtime/renderer behavior, dependencies, accepted specs, ADRs, generated schemas, packaging, updater, or baseline docx files.

## Findings

- Initial independent review by Anscombe (`019efd08-a7f7-72d2-b477-50fc839e865a`) found two P2 gaps:
  - Visual-smoke field validation only matched field names, not expected values.
  - Bundle source checks did not exactly mirror evidence-map/checklist gaps and commands.
- Fixed by validating concrete `field=value` assertions in the visual-smoke runner, recording exact source checklist status/readiness/commands/remaining gaps, mirroring evidence-map `missing_evidence`, and adding negative drift tests.

## Verification

- `node apps/desktop/scripts/m1-5-local-ui-evidence-bundle.cjs --check`: PASS; output is limited to sanitized conservative summary fields.
- `node --test apps/desktop/scripts/m1-5-local-ui-evidence-bundle.test.cjs`: PASS; 11 tests passed.
- `pnpm --filter @cw/desktop run visual-smoke:matrix`: PASS; 8 cases, `failures=[]`.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `pnpm --filter @cw/desktop run test`: PASS; 350 tests passed.
- `.\dev.ps1 lint`: PASS.
- `.\dev.ps1 codegen`: PASS; generated schemas have no content diff.
- `.\dev.ps1 typecheck`: PASS.
- `.\dev.ps1 test`: PASS; Python `475 passed, 1 skipped`, desktop `350 passed`.
- Targeted Prettier check: PASS for touched files.
- `.\dev.ps1 format-check`: ruff format check PASS; Prettier still fails on the existing 39-file repo-wide baseline debt outside this slice.
- `git diff --check`: PASS with only Windows LF/CRLF warnings.

Delta independent review by Anscombe: PASS, no new findings. Prior P2/P3 findings are closed. Residual risk is limited to static-review-only scope for the subagent review and broad FR-002 React text markers that are now supplementary to exact visual-smoke assertions.

## Residual Risk

The bundle is a preparation artifact. FR items remain `pending_a4_review` until a reviewer records end-to-end UX acceptance evidence, including the missing gaps listed per FR.
