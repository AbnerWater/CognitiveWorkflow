# W1.5.170 Desktop Readiness Ledger Review

## Scope

W1.5.170 adds a machine-readable M1.5 readiness ledger and a Node regression test that keeps the ledger aligned with the current approved desktop dependency boundary. The slice is limited to runbook/readiness documentation, the ledger test, and desktop package test wiring.

## Owner Checks

- `node --test apps/desktop/scripts/m1-5-readiness-ledger.test.cjs` PASS: 3 tests passed.
- `pnpm --filter @cw/desktop run typecheck` PASS.
- `pnpm --filter @cw/desktop run test` PASS: desktop Node tests `314 passed`.
- `pnpm exec prettier --check apps/desktop/package.json apps/desktop/scripts/m1-5-readiness-ledger.test.cjs docs/04_runbook/m1.5-readiness-ledger.json docs/04_runbook/m1.5-progress.md docs/reviews/m1.5.170-desktop-readiness-ledger-review.md` PASS.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS; `packages/schemas-ts/src/generated/**` has no content diff.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python pytest `475 passed, 1 skipped`; desktop Node tests `314 passed`.
- `.\dev.ps1 format-check`: ruff format check PASS; Prettier still fails on the standing 39-file repo-wide baseline debt. W1.5.170 touched files pass targeted Prettier.
- `git diff --check` PASS with only Windows LF/CRLF warnings.

## Independent Review

- Sartre (`019efd73-24b3-7b80-afbb-e337483f966a`) initial review: FAIL.
- P2: `CLAUDE.md` is modified in the working tree and appears in an unrestricted `git diff --stat`. This is a standing unrelated change and must not be reverted or staged for W1.5.170; final closure uses an explicit staged/scoped diff.
- P3: `apps/desktop/scripts/m1-5-readiness-ledger.test.cjs` asserted `phase_1_exit_readiness.length === 12` and prevented `ready` / `verified`, but did not assert the exact `EXIT-P1-1` through `EXIT-P1-12` ID set.
- Resolution: added exact Phase 1 exit ID assertions to the ledger regression test; `node --test apps/desktop/scripts/m1-5-readiness-ledger.test.cjs` PASS after the fix.
- Sartre staged/scoped re-review: PASS with no P0/P1/P2/P3. The re-review confirmed the staged diff only contains W1.5.170 package test wiring, ledger JSON, ledger test, runbook/review, the P3 is closed, and there is no dependency/spec/runtime/production behavior drift. `CLAUDE.md` remains unstaged and excluded from this commit.

## Decision

- PASS. W1.5.170 is complete with the unrelated `CLAUDE.md` worktree change excluded from the commit.
