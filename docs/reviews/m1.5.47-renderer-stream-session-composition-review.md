# M1.5.47 Renderer Stream Session Composition Review

Date: 2026-06-21
Reviewer: Bernoulli (`019ee73f-c6bc-7352-9941-d9a7b00068ad`)
Verdict: PASS after fixes

## Scope

- `apps/desktop/src/renderer/runtime-stream-session.ts`
- `apps/desktop/src/preload/contract.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

- P2: `dispose()` originally disposed interaction/view model before `store.stop()`, so a session snapshot could expose `store.status=stopped` with stale interaction view status. Fixed by stopping the store before disposing interaction/view model, and adding a dispose snapshot consistency assertion.
- P2: `eventTypes` override originally accepted any regex-shaped value such as `model.fake`. Fixed by typing overrides as `RuntimeStreamKnownEventType[]` and adding runtime membership validation against `RUNTIME_STREAM_ALL_EVENT_TYPES`.
- P3: default channel event tests were spot checks. Fixed by asserting exact run/planning event lists and no duplicates.

## Verification

- `pnpm --filter @cw/desktop run typecheck` passed.
- `pnpm --filter @cw/desktop run test` passed with 155 tests.
- `.\dev.ps1 lint` passed.
- `.\dev.ps1 typecheck` passed.
- `.\dev.ps1 test` passed with Python 475 passed / 1 skipped and desktop 155 passed.
- `.\dev.ps1 codegen` passed.
- `git diff --check` passed with only Git LF-to-CRLF working-tree warnings.

## Review Notes

- The slice remains pure TypeScript and pre-Electron: no Electron import, React/Vite/Tailwind UI, storage API, new IPC channel, production dependency, package/lockfile, accepted spec, ADR, schema, or codegen behavior change.
- Run/planning default event sets align with the SSE channel boundary and include only `system.heartbeat` from system events.
- Explicit event override is not a forward-compat escape hatch; it must be one of the StreamEvent spec event types mirrored by `RUNTIME_STREAM_ALL_EVENT_TYPES`.
- Bernoulli's re-review verdict was PASS with no remaining findings or open questions.

## Residual Risks

- This slice does not validate real Electron/browser EventSource behavior, live sidecar replay, React rendering, actual full reload UI rebuild, cross-window sharing, runtime StreamEvent schema validation, or generated synchronization for the hand-written event constants.
