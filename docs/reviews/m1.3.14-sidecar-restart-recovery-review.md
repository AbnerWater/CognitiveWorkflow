# W1.3.14 Sidecar Restart Recovery Review

| 项       | 值                                  |
| -------- | ----------------------------------- |
| Stage    | W1.3.14                             |
| Scope    | Sidecar restart recovery foundation |
| Reviewer | Independent subagent Kuhn           |
| Verdict  | PASS                                |

## Scope

Reviewed changes in:

- `apps/runtime/src/cw_runtime/runs/lifecycle.py`
- `apps/runtime/src/cw_runtime/runs/__init__.py`
- `apps/runtime/tests/test_m1_3_14_runtime_recovery.py`
- `docs/04_runbook/m1.3-progress.md`

The review checked alignment with `workflow_run.md §4.2`, D-WR-4, D-WR-8, `runtime_harness.md §13`, and `stream_event.md` recovery event constraints.

## Initial Findings

Initial verdict: FAIL.

- High: recovery scanned only `runs/*/run.json`, so a run directory missing `run.json` was silently skipped instead of surfacing `RH_RUN_DIR_CORRUPTED`.
- High: schema-invalid `run.json` raised raw Pydantic `ValidationError` instead of being wrapped as `RH_RUN_DIR_CORRUPTED`.

## Fixes

- Recovery now iterates each directory below `.agent-workflow/runs/` and requires `run.json`.
- `_read_workflow_run_document()` wraps missing, invalid JSON, non-object JSON, and Pydantic schema validation failures as `RunError("RH_RUN_DIR_CORRUPTED")`.
- Added focused tests for missing `run.json` and schema-invalid `run.json`.

## Verification

- `uv run pytest apps/runtime/tests/test_m1_3_14_runtime_recovery.py -q`: 8 passed.
- `uv run pytest apps/runtime/tests/test_m1_3_4_run_lifecycle.py apps/runtime/tests/test_m1_3_14_runtime_recovery.py -q`: 17 passed.
- `uv run mypy --strict apps/runtime/src/cw_runtime/runs apps/runtime/tests/test_m1_3_14_runtime_recovery.py`: passed.
- `uv run ruff check apps/runtime/src/cw_runtime/runs apps/runtime/tests/test_m1_3_14_runtime_recovery.py`: passed.
- `git diff --check`: passed with LF/CRLF warnings only.

## Final Verdict

PASS. The two initial high findings are closed. No dependency diff, no public API wiring, no direct `pydantic_ai` import in the reviewed run package, and no LangGraph checkpointer reconstruction were introduced.
