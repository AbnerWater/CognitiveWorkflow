# M1.5.139 Runtime Stream Visual Smoke Matrix Missing Output Review

## Scope

W1.5.139 adds automated regression coverage for two failure-manifest paths in the dev-only Runtime Stream visual smoke matrix: a case process that writes no JSON evidence, and a case process that exits nonzero without JSON evidence. The matrix runner was already hardened to write `matrix.json` before failing; this slice proves those paths are covered by the standard desktop Node test.

Files reviewed:

- `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
- `docs/04_runbook/m1.5-progress.md`

Out of scope:

- Production runtime/session/store/React shell behavior.
- Accepted spec / ADR / schema / generated schema changes.
- Dependency, runtime API, IPC / `window.cw`, renderer storage, React Flow, Tailwind, packaging/updater changes.
- Raw payload, `rawData`, metadata object/key/value display.

## Independent Review

Reviewer: Mill (`019efa2a-6308-7930-9946-3ff113d77163`)

Initial result: PASS, no blocker/P1/P2/P3 findings.

Final result: PASS, no blocker/P1/P2/P3 findings.

Mill confirmed:

- Missing JSON evidence is summarized with `ENOENT`, output lengths, and no raw child output values.
- Nonzero child exits are summarized with the exit code, `ENOENT`, output lengths, and no raw child output values.
- Both regressions cover all 6 matrix cases.
- The diff is limited to tests, runbook, and this review artifact.

## Verification

- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs` PASS: 6/6 tests.
- `pnpm --filter @cw/desktop run test` PASS: 276/276 tests.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS with no generated diff.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 475 passed / 1 skipped; desktop 276/276 tests.
- Targeted Prettier check PASS for touched files.
- `git diff --check` PASS with Git CRLF warnings only.

Additional closure gates are tracked in `docs/04_runbook/m1.5-progress.md`.
