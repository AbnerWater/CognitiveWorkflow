# M1.5.12 Runtime Connection Registry Review

Date: 2026-06-20
Reviewer: independent subagent `Locke`
Result: PASS

## Scope Reviewed

- `apps/desktop/src/main/runtime-connection-registry.ts`
- `apps/desktop/src/main/runtime-connection-registry.test.ts`
- `apps/desktop/src/main/runtime-orchestration.ts`
- `apps/desktop/src/main/runtime-orchestration.test.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/package.json`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking findings.

## Confirmed Alignment

- No accepted spec or ADR files were modified.
- No production dependency or lockfile change was introduced.
- No Electron import, `ipcMain` registration, `BrowserWindow` creation, renderer UI, EventSource/SSE client, native file-lock implementation, or token/port persistence was introduced.
- Registry state is in-memory only and backed by a process-local `Map`; it does not read from or write to disk.
- Active `runtime.lock` handoff uses the injected in-memory resolver, not token/port data from `runtime.lock`.
- `startRuntimeOrchestration()` registers after sidecar READY and unregisters on `stop()`.
- `runtime.lock` content remains limited to the existing pid/acquired_at/adapter_id fields.

## Verification

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

Observed result: PASS. Desktop tests reported 73 passed. `git diff --check` emitted only CRLF replacement warnings.

## Residual Risks

- Unexpected sidecar exit after READY is still a future cleanup path; current registry cleanup happens through `session.stop()`.
- Project-root matching currently uses `path.resolve()`; true multi-window wiring may need Windows casing or symlink canonicalization tightening.
