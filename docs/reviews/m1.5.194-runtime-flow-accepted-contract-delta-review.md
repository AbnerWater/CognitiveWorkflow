# W1.5.194 Runtime Flow Accepted Contract Delta Review

Status: PASS
Date: 2026-06-27
Reviewer: Codex local contract review

## Scope Reviewed

- ADR-0011 transition from `Proposed` to `Accepted`.
- New accepted `specs/schemas/runtime_actions.md` contract.
- `specs/api/http_sse.md` submit-instruction endpoints and Desktop artifact handoff contract.
- `specs/runtime_harness.md` metadata-only instruction/artifact action persistence boundary.
- `packages/schemas` runtime action models, exports, tests, and generated `packages/schemas-ts` artifacts.
- W1.5 runtime-flow contract gate transition from pending human confirmation to accepted contract delta.
- Reference-management repair-plan Markdown anchor matching hardening, added only to keep existing plan validation compatible with Prettier-formatted runtime harness tables.

## Findings

No P0/P1/P2/P3 findings.

## Review Notes

- The slice consumes explicit ADR acceptance and does not implement runtime endpoint handlers, preload/main handoff, renderer Chat dispatch, or A4 evidence.
- FR-008 and FR-017 remain not accepted and not implemented; `EXIT-P1-1` remains `not_ready`.
- The new runtime action schemas keep raw Chat instruction text limited to authenticated runtime requests/runtime-controlled records; evidence and snapshots remain metadata-only.
- Artifact action results expose `destination_kind` rather than absolute paths and forbid raw destination-path fields through schema `extra="forbid"` tests.
- Codegen outputs for `RuntimeInstructionRequest`, `RuntimeInstructionAccepted`, `ArtifactActionRequest`, and `ArtifactActionResult` are present in TS and JSON schema form.

## Verification Evidence

- `uv run pytest packages/schemas/tests/test_w1_5_194_runtime_actions.py -q` PASS.
- `.\dev.ps1 codegen` PASS.
- `node apps/desktop/scripts/m1-5-runtime-flow-contract-acceptance-gate.cjs --check` PASS.
- `node --test apps/desktop/scripts/m1-5-runtime-flow-contract-acceptance-gate.test.cjs` PASS.
- `node --test apps/desktop/scripts/m1-5-reference-management-repair-plan.test.cjs` PASS.
- `uv run mypy packages/schemas --strict` PASS.
- `pnpm --filter @cw/desktop run test` PASS.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test-py` PASS.

## Residual Risk

W1.5.194 is a contract delta only. The next implementation slice must still add real runtime submit-instruction handlers and Desktop artifact native handoff before FR-008 / FR-017 can be reviewed for closure.
