# M1.5.156 Review - Desktop Chat Single Visual Smoke Error Redaction

## Scope

- Slice: W1.5.156 Desktop Chat Box single visual smoke hard-exception redaction.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-evidence.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`

## Checklist

- Normal single visual smoke assertions and persisted JSON evidence structure remain unchanged.
- Chat draft fragments recorded by the current smoke run are redacted from step-level hard exception messages.
- Top-level visual smoke stderr output does not directly print an unsanitized Error object after chat draft fragments have been recorded.
- Preflight invalid/missing env behavior still fails before Electron import and does not leak raw URL or viewport values.
- No production runtime/session/store/React/API/IPC/storage/dependency/spec/schema drift.

## Result

Cicero independent review PASS: no blocker/P1/P2/P3 findings.

The review confirmed the diff stays in the intended dev-only surface: visual smoke evidence helper, single visual smoke runner/test, runbook, and review artifact. Draft fragments are recorded before Chat Box input work, `runSmokeStep()` redacts step error messages before rethrowing, and top-level `main().catch` sanitizes stack/message before stderr output. Preflight still runs before `require("electron")`.

Residual risk is limited to exact-fragment redaction. If future smoke drafts include escaped variants or new freeform evidence fields, the sanitizer/test fixture should be extended.

## Reviewer Commands

- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs` PASS, 13/13 tests.
- `git diff --check` PASS, only LF-to-CRLF warnings.
- Parent-run verification also covered syntax checks, real enabled Chat Box visual smoke, `pnpm --filter @cw/desktop run test`, `.\dev.ps1 lint`, `.\dev.ps1 codegen`, `.\dev.ps1 typecheck`, `.\dev.ps1 test`, targeted Prettier, and the known `.\dev.ps1 format-check` baseline.
