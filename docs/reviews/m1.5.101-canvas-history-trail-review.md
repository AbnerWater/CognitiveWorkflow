# W1.5.101 Canvas History Trail Review

## Scope

Reviewed W1.5.101 focused Canvas history trail changes in:

- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`

The slice stays inside the existing React shell seam. It does not modify package manifests, lockfiles, accepted specs, ADRs, schema packages, IPC contracts, runtime code, renderer storage, `window.cw`, raw host/session/controller/store access, React Flow, Tailwind, packaging, updater, or persistence surfaces.

## Initial Finding

- P2: history checkpoint selection updated selected node and Inspector state, but did not hand focus to the newly selected Canvas node. In real Electron smoke, checkpoint selection produced `selectedWorkflowCanvasNodeId=end` while `focusedWorkflowCanvasNodeId=repair_task`, leaving keyboard continuity out of sync after the clicked history button was removed by history truncation.

## Fix

- `handleInspectorHistorySelectClick()` now focuses the selected Canvas node button with `preventScroll: true` after validating history index and node id.
- React test coverage now asserts focus moves to `review_task` after selecting the `2:review_task` checkpoint.
- Electron visual smoke now asserts checkpoint selection leaves selected node, Inspector node, and focused node all on `end`.

## Re-review

Bernoulli (`019ef31c-e980-7e12-a075-a057379f5a43`) re-reviewed the current diff and reported no blocker / P1 / P2 / P3 findings.

Re-review verification:

- `git diff --check` passed with LF-to-CRLF warnings only.
- `pnpm --filter @cw/desktop run typecheck` passed.
- `pnpm --filter @cw/desktop run test` passed: 252 tests.
- Desktop visual smoke at `1280x720` passed with `failures=[]`, `messages=[]`, `horizontalOverflow=0`.
- Smoke JSON after history checkpoint: selected node `end`, focused node `end`, history depth `2`, trail `0:context_task,1:repair_task`.

## Local Verification

- `pnpm --filter @cw/desktop run typecheck` PASS.
- `pnpm --filter @cw/desktop run build` PASS.
- `pnpm --filter @cw/desktop run test` PASS, 252 tests passed.
- Electron visual smoke PASS across desktop, mobile, and mobile scroll views; JSON failures/messages stayed empty.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 475 passed / 1 skipped; desktop 252 passed.
- `.\dev.ps1 codegen` PASS, no generated diff remained.
- `git diff --check` PASS with CRLF warnings only.
