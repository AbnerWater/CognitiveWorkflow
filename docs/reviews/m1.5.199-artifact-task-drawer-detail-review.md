# W1.5.199 Artifact Task Drawer Detail Review

Status: PASS
Date: 2026-06-27
Reviewer: James independent subagent (`019f0953-ed19-7a90-b22f-42d681c0edce`)

## Scope Reviewed

- `apps/desktop/src/renderer/runtime-workbench-shell-presenter.ts`.
- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`.
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`.
- `apps/desktop/src/renderer/runtime-workbench-interaction.ts`.
- `apps/desktop/src/preload/contract.test.ts`.
- W1.5.199 runbook and A4 runtime-flow evidence artifacts.

## Findings

Initial review failed with three findings:

- P1: Task Drawer artifact detail used the active/first-node fallback rather than explicit selected-node evidence.
- P2: Task Drawer rendered unvalidated artifact paths and preview text.
- P2: W1.5.199 bookkeeping referenced a missing review file and stale verification results.

All findings were fixed before final review. No remaining P0/P1/P2/P3 findings.

## Review Notes

- Task Drawer artifact detail now uses an explicitly selected Canvas node and does not use the active/first-node fallback for FR-017 evidence.
- The React regression first verifies `No node selected`, then clicks `workflowCanvasNodeSelect=context_task`, and only then expects the selected-node artifact detail.
- Task Drawer artifact path rendering is limited to sanitizer-approved project-relative paths. Absolute paths, URI-style paths, backslashes, `.` / `..`, `secure`, and `cache` segments are hidden from the Task Drawer detail.
- Task Drawer no longer renders artifact preview text. The unsafe path/preview regression verifies neither value enters the visible Task Drawer detail or the dispatched artifact action command.
- FR-017 is represented only as `runtime_bridge_needs_a4_review`; no A4/FR acceptance is claimed and `EXIT-P1-1` remains `not_ready`.
- Scoped diff excludes the standing unrelated `CLAUDE.md` file.

## Verification Evidence

- `pnpm --filter @cw/desktop build` PASS.
- `node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js` PASS: 59 passed.
- `pnpm --filter @cw/desktop test` PASS: 431 passed.
- `git diff --check` PASS with Git CRLF warnings only.
- Independent re-review PASS after fixes.

## Residual Risk

W1.5.199 prepares FR-017 as an A4 runtime bridge review input only. A4 acceptance still requires a separate reviewer decision record, and FR-017 remains unaccepted until that review is recorded.
