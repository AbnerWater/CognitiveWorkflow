# M1.5.33 Main Shutdown Lifecycle Composer Review

Date: 2026-06-21

Reviewer: Arendt (`019ee5f9-5cab-7a93-bdee-2ab80999bff6`)

Verdict: PASS

## Scope

- `apps/desktop/src/main/bootstrap.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No findings.

## Review Notes

- The diff is limited to the four requested W1.5.33 files.
- No package, lockfile, accepted spec, ADR, schema, or production dependency changes were found.
- `bootstrap.ts` keeps W1.5.33 as pure injected TypeScript, without Electron import or real `app` / `BrowserWindow` / `ipcMain` / `ipcRenderer` wiring.
- `installRuntimeMainLifecycleShutdown()` registers injected `before-quit` and `close`, shares one shutdown promise, prevents only non-terminal events, calls requested finalizers after success or failure, records failure reason, and removes both listeners on unregister.
- Tests cover shared app/window shutdown, unregister without runtime startup, and failed shutdown retrying requested exits.
- The runbook states the injection-only boundary and does not claim real Electron wiring, renderer reload, live sidecar shutdown, cross-window broadcast, or StreamEvent behavior.

## Reviewer Commands

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

Reviewer results:

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 120 passed
- `git diff --check`: PASS, with only LF-to-CRLF normalization warnings
- Targeted package/spec/ADR/schema diff check: PASS, no output

## Residual Risks

- This remains an injected lifecycle composer.
- Real Electron event ordering and live sidecar shutdown behavior still need validation in the dependency-gated Electron wiring slice.
