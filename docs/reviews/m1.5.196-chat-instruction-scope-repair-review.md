# W1.5.196 Chat Instruction Scope Repair Review

Status: PASS
Date: 2026-06-27
Reviewer: Zeno independent subagent

## Scope Reviewed

- React Chat Box current-node/global Workflow scope selection.
- Renderer dispatch of `submit_chat_instruction` with optional structured `nodeId`.
- Metadata-only local Chat submission receipt and preview state.
- Regression coverage for current-node scoped Chat instruction dispatch and no-current-node fallback prevention.

## Findings

- Initial P1 fixed: current-node scope originally reused the Canvas display selection helper, which falls back to the first node when no selected/active node exists. W1.5.196 now uses a strict selected-or-active helper for Chat scope and does not use first-node fallback.
- Initial P2 fixed: regression coverage originally covered only the active-node happy path. W1.5.196 now includes a no-selected/no-active Canvas regression that disables `Current node` and proves default Workflow submit omits `nodeId`.
- Final findings: no open P0/P1/P2/P3 findings after updating verification evidence counts.

## Verification Evidence

- `pnpm --filter @cw/desktop typecheck` PASS.
- `pnpm --filter @cw/desktop build` PASS.
- `node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js` PASS: 57 passed.
- `pnpm --filter @cw/desktop test` PASS: 423 passed.
- `pnpm exec prettier --check apps/desktop/src/renderer/runtime-workbench-shell-react.tsx apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx docs/04_runbook/m1.5-progress.md docs/reviews/m1.5.196-chat-instruction-scope-repair-review.md` PASS.
- `git diff --check` PASS, with Git CRLF warnings only.

## Residual Risk

This slice repairs FR-008 Chat scope routing only. It does not refresh the W1.5.186 evidence map, does not add A4 manifest/capture entries, does not close FR-008, and does not change `EXIT-P1-1=not_ready`.
