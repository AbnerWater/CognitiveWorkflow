# W1.5.204 Workflow History Timeline Review

## Scope

Review of the W1.5.204 FR-015 follow-up: accepted `GET /cw/v1/workflows/{workflow_id}/history`, Desktop Version Snapshot workflow-history refresh, metadata-only timeline projection, focused tests, and runbook evidence refresh.

W1.5.204 does not implement restore-to-snapshot, continue execution, a new snapshot ledger projection API, A4 reviewer acceptance, or FR-015 closure.

## Independent Review

Reviewer: Aristotle (`019f0a13-5ea0-7ce0-9baf-fe1bd42c2f5d`)

Initial findings:

- P1: `WorkflowHistoryEntry` treated spec-optional `git_tag`, `derived_from_draft_id`, and `change_summary` as required-nullable fields. Fixed by giving each field a default and adding a regression where the endpoint accepts entries that omit those fields.
- P2: `CLAUDE.md` is dirty but out of W1.5.204 scope. It remains unstaged and must not be included in this slice commit.
- P3: W1.5.204 verification results were missing from the progress runbook. Fixed by adding the W1.5.204 verification result section.

Reviewer-confirmed constraints:

- No restore-to-snapshot implementation is required for this slice.
- FR-015 and A4 status remain conservative: `accepted_items=0`, `EXIT-P1-1=not_ready`, and restore/continue remain explicit gaps.
- No accepted spec, ADR, schema, generated-schema, or dependency changes were introduced.
- Desktop timeline projection is metadata-only; raw response bodies and `change_summary` are not retained in renderer snapshots.

## Verification

- `uv run ruff check apps/runtime/src/cw_runtime/api/app.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/harness/project.py apps/runtime/tests/test_m1_3_4_run_endpoints.py` PASS.
- `uv run mypy --strict apps/runtime/src/cw_runtime/api/app.py apps/runtime/src/cw_runtime/harness/project.py apps/runtime/tests/test_m1_3_4_run_endpoints.py` PASS.
- `uv run pytest apps/runtime/tests/test_m1_3_4_run_endpoints.py -q` PASS: 8 passed.
- `pnpm --filter @cw/desktop test` PASS: 443 passed.
- `node apps/desktop/scripts/m1-5-runtime-flow-repair-plan.cjs --check` PASS.
- `node --test apps/desktop/scripts/m1-5-runtime-flow-repair-plan.test.cjs apps/desktop/scripts/m1-5-fr-evidence-map.test.cjs apps/desktop/scripts/m1-5-ux-acceptance-checklist.test.cjs apps/desktop/scripts/m1-5-readiness-ledger.test.cjs` PASS: 23 passed.
- `git diff --check` PASS with CRLF warnings only.

## Result

PASS after repair. No remaining P0/P1/P2/P3 findings are recorded for the W1.5.204 scope.
