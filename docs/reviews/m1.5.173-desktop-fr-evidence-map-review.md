# W1.5.173 Desktop FR Evidence Map Review

## Scope

W1.5.173 adds a machine-readable evidence map for FR-001 through FR-020. The map consumes the W1.5.172 UX acceptance checklist and classifies every FR into candidate evidence, partial UI evidence, partial runtime flow, backend-only desktop gaps, dependency-gated work, or missing implementation.

The slice is limited to the evidence map artifact, the map regression test, desktop package test wiring, and runbook/review documentation. It does not modify the baseline UIUX docx, install dependencies, implement missing FR behavior, or change production runtime, IPC, storage, schema, spec, ADR, packaging, updater, or React shell behavior.

## Verification

- `node --test apps/desktop/scripts/m1-5-fr-evidence-map.test.cjs`: PASS, 5 tests.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `pnpm --filter @cw/desktop run test`: PASS, 328 tests.
- `.\dev.ps1 lint`: PASS.
- `.\dev.ps1 codegen`: PASS, no generated schema content diff remained.
- `.\dev.ps1 typecheck`: PASS.
- `.\dev.ps1 test`: PASS, Python 475 passed / 1 skipped and desktop 328 passed.
- `pnpm exec prettier --check apps/desktop/package.json apps/desktop/scripts/m1-5-fr-evidence-map.test.cjs docs/04_runbook/m1.5-fr-evidence-map.json docs/04_runbook/m1.5-progress.md docs/reviews/m1.5.173-desktop-fr-evidence-map-review.md`: PASS.
- `.\dev.ps1 format-check`: ruff format check PASS; Prettier still fails on the standing 39-file repo-wide baseline debt. W1.5.173 touched files pass targeted Prettier.
- `git diff --check`: PASS with only Windows LF/CRLF warnings.

## Findings

- Nietzsche (`019efdae-4be4-7203-ad21-3bdb445881dd`) independent review: PASS with no blocking findings.
- The review confirmed there is no A4 or `EXIT-P1-1` overclaim, FR-001 through FR-020 IDs and statuses mirror W1.5.172 exactly, summary counts are item-derived, FR-004 and FR-019 remain blocked by `DEP-REACT-FLOW`, FR-013 remains `missing_implementation`, and candidate items FR-009/010/016 have verification commands.
- The review confirmed `apps/desktop/package.json` only wires the new test into the existing test command and that the scoped diff does not touch dependencies, spec, ADR, schema, generated schema, runtime, or production renderer behavior.
- Residual unrelated worktree change: `CLAUDE.md` remains dirty and is excluded from the W1.5.173 commit.

## Verdict

PASS. W1.5.173 is complete as a conservative FR evidence map for A4 preparation. It does not mark any FR accepted and does not make `EXIT-P1-1` ready.
