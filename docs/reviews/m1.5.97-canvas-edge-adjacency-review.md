# M1.5.97 Canvas Edge Adjacency Review

Date: 2026-06-23

Reviewer: independent subagent Ohm (`019ef2a7-9119-7313-b90b-4df924170ecd`)

## Scope

Review the W1.5.97 desktop shell diff for the no-new-dependency, focused Canvas selected-edge adjacency scaffold:

- selected-node incoming/outgoing edge highlighting in the focused Canvas
- preview Canvas remaining noninteractive
- read-only Inspector route lists derived from existing Canvas edges
- visual smoke coverage for selecting `repair_task` and verifying adjacent edge direction
- absence of dependency, spec, ADR, schema, generated-code, IPC, storage, React Flow, drag/connect, or persistence drift

## Result

PASS. No blocker, P1, or P2 findings.

The review confirmed the diff is contained to React shell, CSS, visual smoke, and React tests before documentation closure. It found no package / lockfile / accepted spec / ADR / schema / generated schema diff, and no React Flow / Tailwind dependency. The focused Canvas adjacency remains local React UI projection from the selected node and current Canvas snapshot; preview Canvas remains noninteractive, and the Inspector route lists do not dispatch runtime commands or access raw host/session/controller/store.

## Resolved Review Item

- P3: the initial visual smoke covered selected-edge counts and direction counts, while exact `review_to_repair` / `repair_to_context` identity was only asserted in the React test. The main rollout tightened `runtime-workbench-visual-smoke.cjs` to collect and assert selected, incoming, outgoing, and Inspector edge ids, closing the browser-level coverage gap.

## Residual Risk

- Visual smoke still validates DOM/count/id/layout invariants rather than pixel-level diffs or a complete overlap detector.
- Real React Flow editing, drag/connect behavior, WorkflowGraph persistence, PlanningSession draft history, WorkflowPatch application, and git snapshot writes remain outside W1.5.97 scope.

## Verification

- Reviewer observed `pnpm --filter @cw/desktop run typecheck` PASS.
- Reviewer observed `pnpm --filter @cw/desktop run test` PASS, 252 tests.
- Reviewer observed desktop Electron visual smoke PASS at 1280x720 against local Vite on port 5174.
- Main rollout reran `pnpm --filter @cw/desktop run typecheck` PASS after addressing the P3.
- Main rollout reran `pnpm --filter @cw/desktop run build` PASS.
- Main rollout reran `pnpm --filter @cw/desktop run test` PASS with 252 tests.
- Main rollout reran Electron visual smoke PASS for desktop 1280x720, mobile 390x844, and mobile scrollY 900 / 1180 / 1440 after adding exact edge-id assertions.
