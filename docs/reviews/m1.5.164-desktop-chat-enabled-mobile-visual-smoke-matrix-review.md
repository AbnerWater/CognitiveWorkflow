# M1.5.164 Review - Desktop Chat Enabled Mobile Visual Smoke Matrix

## Scope

- Slice: W1.5.164 Desktop Chat Box enabled visual smoke matrix mobile coverage.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
  - `apps/desktop/src/renderer/runtime-workbench-shell.css`
  - `docs/04_runbook/m1.5-progress.md`
  - `docs/reviews/m1.5.164-desktop-chat-enabled-mobile-visual-smoke-matrix-review.md`

## Checklist

- `chat-enabled-mobile` runs the enabled Chat Box matrix path at 390x844 with `chatBox=enabled`.
- Mobile enabled Chat Box keeps `horizontalOverflow=0` after local submission history rows render.
- Matrix manifest records only metadata-only focus/local evidence for both enabled desktop and mobile cases.
- Matrix failure summaries do not persist raw draft text, preview text, local submission text, raw child failures, query/hash secrets, or output directory paths.
- Changes remain scoped to matrix/test, a CSS-only responsive layout fix, and docs; they do not introduce production runtime logic, API, IPC/window.cw, renderer storage, dependency, accepted spec, schema, generated schema, or ADR drift.

## Result

Noether independent review found no P0/P1/P2 implementation issues.

- P3: runbook/review docs still marked W1.5.164 as in-progress/pending after verification. Fixed by recording the final verification results and marking the slice done.
- P3: runbook scope omitted the intentional CSS-only responsive layout fix. Fixed by documenting the 520px mobile Chat Box local submission history row adjustment and narrowing the boundary statement.
- P3: review artifact was still a placeholder and omitted `runtime-workbench-shell.css`. Fixed by adding CSS to the scope and replacing pending placeholders with this review result.

Final status after remediation: PASS.

## Reviewer Commands

- Parent verification before review:
  - `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
  - `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
  - `pnpm --filter @cw/desktop run typecheck`
  - `pnpm --filter @cw/desktop run test`
  - Electron visual smoke matrix with `chat-enabled-mobile`
  - targeted Prettier check for touched files
  - `.\dev.ps1 lint`
  - `.\dev.ps1 codegen`
  - `.\dev.ps1 typecheck`
  - `.\dev.ps1 test`
  - `.\dev.ps1 format-check`
  - `git diff --check`

## Parent Verification Summary

- Matrix runner syntax PASS.
- Matrix focused tests PASS: 14 tests passed with 8-case manifest expectations.
- Focused desktop typecheck/test PASS.
- Real visual smoke matrix initially failed on `chat-enabled-mobile` with `horizontalOverflow=136`; the failure correctly surfaced mobile layout overflow.
- Real visual smoke matrix PASS after CSS remediation: 8 cases, `chat-enabled-mobile` metadata-only evidence showed `chatBoxMode=enabled`, viewport `390x844`, focused-after-expand=true, horizontal overflow `0`, first sequence `1`, and resend sequence `5`.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS with no generated diff.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 475 passed / 1 skipped; desktop 302 passed.
- `.\dev.ps1 format-check` FAIL only on standing 39-file repo-wide Prettier baseline; touched files passed targeted Prettier.
- `git diff --check` PASS with only Git CRLF warnings.

## Remediation Verification

- Final targeted Prettier check PASS after documentation closure.
- Final `git diff --check` PASS with only Git CRLF warnings.
