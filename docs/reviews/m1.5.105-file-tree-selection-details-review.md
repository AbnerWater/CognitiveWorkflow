# M1.5.105 File Tree selection details independent review

Reviewer: Lorentz (`019ef3a4-6803-7aa2-93f4-8787dfd004b9`)

Date: 2026-06-23

## Scope

Review current uncommitted W1.5.105 diff for local read-only File Tree selection details.

Expected write scope:

- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
- `docs/04_runbook/m1.5-progress.md`
- `docs/reviews/m1.5.105-file-tree-selection-details-review.md`

Out of scope:

- package / lockfile / dependency changes
- accepted spec / ADR / schema / codegen changes
- IPC / runtime bridge changes
- renderer storage, `window.cw`, raw host/session/controller/store access
- Node fs or real filesystem enumeration
- React Flow, Tailwind, Forge, packaging, updater
- persistence, mutation commands, real file opening, Git diff, version snapshot writes

## Findings

Lorentz reported no blocker or P1 findings.

P2: the File Tree implementation handled both Enter and Space key activation, but the React shell regression test only covered Enter. A regression in the Space path could have shipped while the focused React test still passed.

Resolution: the React shell test now dispatches Space on `runtime_stream`, asserts the selected node moves to `runtime_stream`, and verifies details path `No active stream` with status `Idle`. The same test still covers click selection of `workflow_graph`, Enter selection of `accepted_specs`, and exactly one selected node.

## Verification

Lorentz reported as passed before the P2 fix:

```powershell
git diff -- apps/desktop/src/renderer/runtime-workbench-shell-react.tsx apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx apps/desktop/src/renderer/runtime-workbench-shell.css apps/desktop/scripts/runtime-workbench-visual-smoke.cjs
pnpm --filter @cw/desktop run build:ts
node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js
node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs
pnpm exec prettier --check apps/desktop/src/renderer/runtime-workbench-shell-react.tsx apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx apps/desktop/src/renderer/runtime-workbench-shell.css apps/desktop/scripts/runtime-workbench-visual-smoke.cjs
pnpm --filter @cw/desktop run visual-smoke
```

Additional verification after addressing the P2:

```powershell
pnpm exec prettier --write apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run build
node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js
pnpm --filter @cw/desktop run test
```

The post-fix React shell test passed with 16 tests. The desktop package test passed with 253 tests. Prior Electron visual smoke evidence covered desktop 1280x720, mobile 390x844, and mobile scrollY 900 / 1180 / 1440, with File Tree metrics confirming initial `workspace_root` selection and clicked `workflow_graph` details.

## Scope Check

- No package / lockfile / dependency changes.
- No accepted spec / ADR / schema / generated schema diff.
- No IPC / runtime bridge, renderer storage, `window.cw`, Node fs, persistence, React Flow, Tailwind, packaging, or updater changes.
- File Tree selection remains local React state derived from the frozen shell snapshot.
- The details panel is read-only and does not dispatch runtime or mutation commands.
