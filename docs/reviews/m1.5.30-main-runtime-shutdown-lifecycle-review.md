# M1.5.30 Main Runtime Shutdown Lifecycle Review

| Item        | Value                                           |
| ----------- | ----------------------------------------------- |
| Work item   | W1.5.30 Desktop main runtime shutdown lifecycle |
| Reviewed at | 2026-06-21                                      |
| Reviewer    | Euclid (`019ee5cb-68b4-7ce1-8c80-54c60c69a31d`) |
| Verdict     | PASS                                            |

## Scope

- `apps/desktop/src/main/bootstrap.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`
- `apps/desktop/src/main/runtime-startup-controller.ts`
- `apps/desktop/src/main/runtime-startup-controller.test.ts`
- `apps/desktop/src/main/index.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

None.

The reviewer confirmed the diff is limited to the requested pre-Electron TypeScript lifecycle adapter and runbook files, with no dependency, spec, ADR, or schema drift and no Electron import or wiring in production code.

## Reviewer Checks

- `shutdown(signal?)` is exposed and cached by the injected main bootstrap adapter.
- Shutdown unregisters stable IPC handlers first, then stops runtime with the caller-provided signal.
- Idle, failed, blocked, and timed-out runtime states do not get started by shutdown.
- Owned shutdown options pass through from `StartRuntimeWithLifecycleOptions` to owned orchestration startup.
- Reused existing runtime remains non-owned and `stop()` returns `false`.

## Reviewer Commands

- `pnpm --filter @cw/desktop run typecheck` - PASS
- `pnpm --filter @cw/desktop run test` - PASS, 111 tests passed
- `git diff --check` - PASS, with only line-ending normalization warnings on modified files
- Targeted diff and status checks confirmed no package, lockfile, spec, or ADR drift

## Residual Risks

- Real Electron `before-quit` and window reload wiring remain outside this slice.
- Cross-window shutdown status broadcast remains outside this slice.
- Live FastAPI sidecar shutdown validation remains outside this slice.
- Shutdown StreamEvent emission remains outside this slice.
