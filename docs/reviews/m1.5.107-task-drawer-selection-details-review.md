# W1.5.107 Task Drawer Selection Details Review

Date: 2026-06-23

Reviewer: Sagan (`019ef451-61ab-70a2-b48a-48cbe01734ac`)

Scope reviewed:

- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`

## Initial Findings

Sagan found three P3 issues in the first review:

- Task Drawer selection initialized from the first item instead of explicitly preferring `active_panel`.
- Task Drawer item nodes used `role="option"` while the container was still a plain `<dl>`, leaving the selectable group without `role="listbox"`.
- Electron visual smoke covered the real Enter path but did not cover the real Space key path.

No blocker / P1 / P2 findings were reported.

## Fixes Applied

- `selectedItemId` now explicitly prefers the `active_panel` item before falling back to the first item.
- The Task Drawer item container now has `role="listbox"` and `aria-label="Task drawer items"`.
- `runtime-workbench-visual-smoke.cjs` now drives Space on `visible_items`, validates the resulting details, and then drives Enter on `runtime_stream`.

## Re-review Result

Sagan re-reviewed the current uncommitted diff and reported no remaining blocker / P1 / P2 / P3 findings.

The re-review confirmed:

- The three prior P3 findings are resolved.
- The diff is limited to the expected frontend files before documentation was added.
- No package / lockfile / dependency / spec / ADR / schema / generated schema drift was found.
- No IPC/runtime bridge, renderer storage, Node fs, raw host/session/controller/store, React Flow, Tailwind, persistence, packaging, or updater drift was found.

## Validation Evidence

Owner-run validation after fixes:

- `pnpm exec prettier --write apps/desktop/src/renderer/runtime-workbench-shell-react.tsx apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx apps/desktop/src/renderer/runtime-workbench-shell.css apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`: PASS / unchanged.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`: PASS.
- `git diff --check`: PASS, only LF-to-CRLF warnings.
- `pnpm --filter @cw/desktop run build`: PASS.
- `node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js`: PASS, 18 tests.
- Electron visual smoke: desktop 1280x720 PASS, mobile 390x844 PASS, mobile scrollY 900 / 1180 / 1440 PASS. Parallel mobile smoke emitted known Windows Electron GPU cache warnings but exited 0.

Task Drawer metrics from the owner desktop smoke JSON:

- Initial: `active_panel` / `Lifecycle` / `neutral`.
- Click: `unread_events` / `0` / `neutral`.
- Space: `visible_items` / `5` / `neutral`.
- Enter: `runtime_stream` / `Idle` / `neutral`.

Reviewer rerun validation:

- `git diff --check`: PASS, only LF-to-CRLF warnings.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`: PASS.
- `pnpm --filter @cw/desktop run build`: PASS.
- `node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js`: PASS, 18 tests.
- Desktop Electron smoke via `visual-smoke.html`: PASS, `failures=0`, `messages=0`.
