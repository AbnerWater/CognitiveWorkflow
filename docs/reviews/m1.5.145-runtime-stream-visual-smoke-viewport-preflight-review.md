# W1.5.145 Runtime Stream Visual Smoke Viewport Preflight Review

## Scope

- Reviewed uncommitted W1.5.145 changes for the dev-only single-case Runtime Workbench visual smoke viewport / scroll preflight contract.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-preflight.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs`

## Independent Review

- Reviewer: Ramanujan (`019efa8f-b114-7c71-a264-3295c53e6af4`)
- Result: PASS, no blocker / P1 / P2 / P3 findings.

The reviewer confirmed:

- `CW_VISUAL_SMOKE_WIDTH` and `CW_VISUAL_SMOKE_HEIGHT` require positive integers.
- `CW_VISUAL_SMOKE_SCROLL_Y` requires a non-negative integer.
- Error messages include only the env var name and rule; they do not echo raw env values.
- Script-level invalid viewport failures resolve preflight before Electron import and do not write PNG / JSON output artifacts.
- No package or lockfile diff is present; the change stays inside the approved W1.5 minimal dependency and spec boundary.

## Follow-Up Fix

The initial review noted a residual test gap: the spawned script-path failure test covered invalid width, while height and scroll were covered only at helper level.

W1.5.145 addressed this by expanding the spawned script-path test to loop over `CW_VISUAL_SMOKE_WIDTH`, `CW_VISUAL_SMOKE_HEIGHT`, and `CW_VISUAL_SMOKE_SCROLL_Y`, each with a secret nonnumeric value.

## Re-Review

- Result: PASS.
- The reviewer confirmed the follow-up closes the residual gap: width, height, and scroll are all covered through the spawned script path with sanitized stderr, no Electron require guard fire, and no output artifacts.

## Verification

```powershell
node --test apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs
pnpm --filter @cw/desktop run test
```

Both commands passed during implementation before full-gate closure.
