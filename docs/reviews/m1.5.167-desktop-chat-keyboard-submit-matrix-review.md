# M1.5.167 Review - Desktop Chat Keyboard Submit Matrix Evidence

## Scope

- Slice: W1.5.167 Desktop Chat Box visual smoke matrix keyboard submit/focus evidence.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`
  - `docs/reviews/m1.5.167-desktop-chat-keyboard-submit-matrix-review.md`

## Checklist

- Single visual smoke keeps the first enabled Chat Box local send on the keyboard shortcut path and exposes only metadata-only trigger evidence.
- Matrix runner fails closed if enabled Chat Box cases do not report `trigger=keyboard`, `modifier=ctrl`, prevented keyboard default, and textarea focus after submit.
- Matrix manifest keeps trigger/focus evidence as enum/boolean metadata and does not expose raw draft, preview, local submission text, raw child failure text, query/hash secrets, or output directory paths.
- Changes remain dev-only runner/test/docs and do not introduce production runtime, API, IPC/window.cw, renderer storage, dependency, accepted spec, schema, generated schema, or ADR drift.

## Result

PASS after review fixes.

Independent reviewer: Rawls (`019efd20-3b0c-72c2-8517-2d6f5970a245`).

Initial review found:

- P2: matrix trigger evidence used loose string comparison, so string `"true"` could satisfy boolean trigger evidence, and raw invalid child values could be copied into manifest/failure output.
- P3: disabled Chat Box absence guard did not reject `chatLocalSubmitTriggerMetrics`.

Fixes applied:

- `runtime-workbench-visual-smoke-matrix.cjs` now normalizes trigger evidence through a whitelist, requires boolean fields with `=== true`, emits normalized evidence only, and avoids echoing raw invalid child values.
- Disabled Chat Box guard now rejects `chatLocalSubmitTriggerMetrics`.
- Matrix tests now cover non-keyboard trigger redaction, string boolean rejection, and disabled trigger metrics fail-closed behavior.

Rawls复审确认 P2/P3 均关闭，无新增 P0/P1/P2/P3。

## Reviewer Commands

Reviewer performed read-only static review. Local verification:

- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs`
- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
- `pnpm --filter @cw/desktop run typecheck`
- `pnpm --filter @cw/desktop run test`
- `.\dev.ps1 lint`
- `.\dev.ps1 codegen`
- `.\dev.ps1 typecheck`
- `.\dev.ps1 test`
- `git diff --check`
