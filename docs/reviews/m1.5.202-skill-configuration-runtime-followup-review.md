# W1.5.202 Skill Configuration Runtime Follow-up Review

Status: PASS after repair
Date: 2026-06-27
Reviewer: Huygens independent subagent reused for W1.5.202 (`019f0926-d493-77c3-a70b-36c052dfba83`)

## Scope Reviewed

- `apps/desktop/src/renderer/runtime-workbench-session.ts`.
- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`.
- `apps/desktop/src/renderer/runtime-workbench-host-session.ts`.
- `apps/desktop/src/renderer/runtime-workbench-shell-visual-smoke.tsx`.
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`.
- `apps/desktop/src/preload/contract.test.ts`.
- W1.5.202 runbook updates in `docs/04_runbook/`.
- Related A4 manifest, capture execution, review package, decision handoff, evidence map, checklist, readiness ledger, and remaining runtime-flow repair-plan updates.

## Findings

Initial review found two issues; both were repaired before commit.

- P2: The touched-file Prettier gate failed for W1.5.202 runbook JSON files.
- P3: A failed `/projects/{project_id}/mcps` refresh after switching projects could retain the previous project's sanitized MCP metadata in the active Skill management snapshot.

## Repairs Applied

- Ran Prettier over the touched TS/TSX/CJS/JSON/Markdown files and reformatted the W1.5.202 runbook JSON artifacts.
- Changed `refreshSkills()` to reuse previous Skill/MCP snapshot entries only when the active project id matches the requested project id.
- Changed failed `/mcps` refresh handling to clear `mcpEntries`, preventing stale `serverId` / `hasSecretRef` metadata from another project.
- Added a regression test that refreshes an old project successfully, then refreshes a new project with `/mcps` returning 503 and verifies stale MCP metadata plus raw failure body are absent from the snapshot.
- Updated superseded desktop bridge and UX gap tests so their current evidence-map assertions match W1.5.202: FR-014 is now `runtime_bridge_needs_a4_review`, current A4-ready bridge items are 7, and partial bridge follow-up items are 2.

## Review Notes

- Huygens confirmed no scoped diff in accepted spec, ADR, schema, generated schema, dependency, or runtime files.
- FR-014 is `runtime_bridge_evidence_available` / `runtime_bridge_needs_a4_review`, not accepted.
- `accepted_items=0` and `EXIT-P1-1=not_ready` remain locked.
- Happy-path Skill/MCP snapshot drops raw params, raw `command_or_url`, and raw `secret_ref`.
- Re-review found only stale archive metadata; this file now records the W1.5.202 reuse of Huygens and the current 441-test desktop suite result.
- Final Huygens re-review PASS; no remaining P0/P1/P2/P3 findings.
- The standing unrelated `CLAUDE.md` worktree change remains excluded from this slice.

## Verification Evidence

- `pnpm --filter @cw/desktop run typecheck` PASS.
- `pnpm --filter @cw/desktop run build` PASS.
- Focused renderer/preload tests PASS: 162 passed after the stale MCP regression.
- `uv run pytest apps/runtime/tests/test_m1_3_2_project_endpoints.py -q` PASS.
- Focused runbook validators PASS for runtime-flow repair plan, A4 manifest, A4 capture execution, A4 review package, and A4 decision handoff.
- Grouped runbook script tests PASS: 50 passed after W1.5.202 updates.
- `pnpm --filter @cw/desktop test` PASS: 441 passed after repair.

## Residual Risk

W1.5.202 provides runtime bridge evidence only. FR-014 still requires separate A4 UX acceptance review before any acceptance or phase-exit readiness claim.
