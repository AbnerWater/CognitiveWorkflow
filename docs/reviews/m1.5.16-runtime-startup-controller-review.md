# M1.5.16 Runtime Startup Controller Review

Date: 2026-06-20
Reviewer: independent subagent `McClintock`
Result: PASS

## Scope Reviewed

- `apps/desktop/src/main/runtime-startup-controller.ts`
- `apps/desktop/src/main/runtime-startup-controller.test.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/package.json`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking findings.

## Evidence

- `startRuntimeWithLifecycle()` calls lifecycle first with `connectionRegistry.resolver(projectRoot)`.
- `start_sidecar` and `cleanup_then_start` route to owned startup and delegate to `startRuntimeOrchestration()` with registry and caller options.
- `reuse_existing` uses only the handed-off in-memory connection, creates runtime IPC handlers from that connection, and `stop()` returns `false` so the shared sidecar is not stopped.
- `block_startup` and `timeout_waiting_for_existing` fail closed without starting a sidecar.
- Tests cover owned start, reuse, block, timeout, and inconsistent reuse decision.
- No production dependency additions, Electron imports, `ipcMain`, `BrowserWindow`, renderer UI, EventSource/SSE client, token/port persistence, or `runtime.lock` schema changes were found in the reviewed diff.

## Commands

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 85/85 tests
- `git diff --check`: PASS, LF/CRLF warnings only

## Residual Risk

This remains a pure TypeScript pre-Electron controller. Real Electron IPC registration, preload/renderer bridge behavior, `system.runtime_ready` emission, and cross-window broadcast remain future dependency-gated slices.
