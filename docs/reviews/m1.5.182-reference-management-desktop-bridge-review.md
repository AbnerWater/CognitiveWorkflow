# W1.5.182 Reference Management Desktop Bridge Review

Reviewer: Wegener (`019efedc-27bd-7572-bd26-d7978970c319`)
Date: 2026-06-25

## Scope

Review W1.5.182 runtime-backed reference management bridge across runtime harness/API, desktop IPC, workbench session/interaction, React shell, tests, and runbook evidence. The review was read-only.

## Initial Findings

- P1: `preload/contract.test.ts` still expected the older workbench command/status projection and would fail the full desktop test suite.
- P1: reference auto-commit did not preserve unrelated user staged state through the spec-required stash/commit/unstash invariant.
- P2: multipart parsing split on boundary bytes inside file content.
- P2: React import state retained raw file base64 after successful dispatch.
- P3: entry toggles could use the mutable form project id instead of the active reference project id.

## Fixes Reviewed

- Updated preload contract expectations for `referenceManagement` snapshot, status item, and command ids.
- Moved reference auto-commit through staged-change inspection, staged stash, path-scoped `git commit --only`, and indexed stash restore.
- Tightened multipart parsing to recognize only real `CRLF--boundary` delimiters.
- Cleared file input/base64 state after successful React import dispatch.
- Routed enable/disable commands through `activeProjectId` when present.

## Delta Review

The first delta review found two remaining P2s:

- Multipart parser accepted `CRLF--boundary` without validating the delimiter suffix.
- Auto-commit stash/add/commit/pop was not guarded by `.agent-workflow/locks/git.lock`.

Both were fixed. The final delta review was PASS with no blocker/P1/P2 findings:

- `_find_multipart_boundary()` now accepts only suffix `CRLF` or `--`, preserving file content such as `CRLF--boundary-not-a-delimiter`.
- Reference import and enable/disable run manifest write plus stash/add/commit/pop under `git.lock`.
- Stash restore verifies the expected `refs/stash` top before `stash pop --index`.
- Runtime regression covers pseudo-boundary file content, user staged/untracked preservation, git lock release, and `chore(refs)` commit order.

## Verification Noted

- `pnpm --filter @cw/desktop exec tsc --noEmit` PASS.
- `uv run pytest apps/runtime/tests/test_m1_3_2_project_endpoints.py::test_reference_endpoints_import_and_toggle_runtime_manifest -q` PASS.
- `git diff --check -- apps/runtime/src/cw_runtime/api/app.py apps/runtime/src/cw_runtime/harness/project.py apps/runtime/tests/test_m1_3_2_project_endpoints.py` PASS, LF/CRLF warnings only.

## Residual Risk

- W1.5.182 intentionally does not implement DELETE for references because `runtime_harness.md` currently locks import/enable/disable commit policy only.
- A4 acceptance remains out of scope; FR-013 should not be marked UX accepted from this slice alone.
