# M1.5.104 Canvas type focus details independent review

Reviewer: Laplace (`019ef37f-1b05-7781-bb80-a15211a4e851`)

Date: 2026-06-23

## Scope

Review current uncommitted W1.5.104 diff for focused Canvas type focus details.

Expected write scope:

- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`

Out of scope:

- package / lockfile / dependency changes
- accepted spec / ADR / schema / codegen changes
- IPC / runtime bridge changes
- renderer storage, `window.cw`, raw host/session/controller/store access
- React Flow, Tailwind, Forge, packaging, updater
- persistence, drag/connect, real WorkflowGraph writeback
- PlanningSession draft history, WorkflowPatch application, git snapshot writes

## Findings

Laplace reported no blocker, P1, or P2 findings.

P3: Electron smoke originally selected `repair_task` before clicking the `repair_task` node type match, so a no-op match click handler could still pass in Electron. The React shell test already covered a state-changing `context_task` to `repair_task` path, so this was a smoke-strength gap rather than a product blocker.

Resolution: the Electron smoke now selects `context_task` before clicking the `repair_task` node type match, records pre-match metrics, asserts the details still list `repair_task`, and then asserts the match click selects and focuses `repair_task`. The downstream history trail expectations were updated to reflect the extra real local Canvas selection steps.

## Verification

Laplace reported as passed:

```powershell
pnpm --filter @cw/desktop run typecheck
git diff --check
```

Additional verification after addressing the P3:

```powershell
node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs
pnpm --filter @cw/desktop run visual-smoke
```

The post-fix Electron visual smoke passed for desktop 1280x720, mobile 390x844, and mobile scrollY 900 / 1180 / 1440. The desktop JSON evidence had `failures=[]`, `messages=[]`, pre-match selected node `context_task`, pre-match node details `repair_task`, match-selected node `repair_task`, match-focused node `repair_task`, and edge details `edge:normal:2`.

## Scope Check

- No package / lockfile / dependency changes.
- No accepted spec / ADR / schema / generated schema diff.
- No IPC / runtime bridge, renderer storage, persistence, React Flow, Tailwind, packaging, or updater changes.
- Type focus details remain derived from the local frozen Canvas snapshot and render only on the focused Canvas surface.
- Edge type focus details remain read-only; only node type focus details expose local Canvas selection.
