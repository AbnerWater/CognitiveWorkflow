# M1.5.19 Runtime Registry Filesystem Alias Fixture Review

| Item        | Value                                                                 |
| ----------- | --------------------------------------------------------------------- |
| Milestone   | M1.5 / W1.5.19                                                        |
| Reviewed at | 2026-06-20                                                            |
| Reviewer    | Independent subagent `Raman` (`019ee47d-e035-7691-99f0-2ec78afba6e2`) |
| Verdict     | PASS                                                                  |

## Scope

- `apps/desktop/src/main/runtime-connection-registry.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking or non-blocking findings.

## Evidence

- Boundary is respected: the diff is test evidence and runbook only, uses Node stdlib, and does not add dependencies, production registry changes, Electron imports, IPC registration, renderer code, SSE/EventSource behavior, token/port persistence, or `runtime.lock` schema changes.
- The new test creates an isolated temp fixture, creates a Windows directory junction on this checkout, and cleans the fixture in `finally`.
- The registry is created with `projectRootRealpath` unset, so the test exercises the default `fs.realpathSync.native()` path rather than an injected resolver.
- The skip path only covers filesystem alias creation capability; it does not catch registry assertions or hide canonicalization failures.
- On this Windows checkout, the focused desktop test reported 96 passed / 0 skipped, including `matches real filesystem directory aliases with default realpath`.
- The reviewer checked package/dependency manifests and found no dependency diff, and checked `$env:TEMP` for `cw-runtime-registry-realpath-*` residue with no leftovers.

## Commands

- `pnpm --filter @cw/desktop run typecheck` PASS
- `pnpm --filter @cw/desktop run test` PASS, 96 passed / 0 skipped
- `git diff --check` PASS, with LF-to-CRLF warnings only
- `git diff --name-only -- package.json apps/desktop/package.json pnpm-lock.yaml pnpm-workspace.yaml pyproject.toml uv.lock` produced no output

## Residual Risk

- W1.5.19 closes the W1.5.18 residual risk for a real filesystem directory alias fixture on this Windows checkout.
- Remaining scope is unchanged: this is still registry test evidence only, not Electron multi-window wiring, sidecar IPC broadcasting, renderer `EventSource` / `fetch`, token or port persistence, or `runtime.lock` schema / native lock behavior.
