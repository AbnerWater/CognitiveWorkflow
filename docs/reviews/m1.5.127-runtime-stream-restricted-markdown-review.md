# W1.5.127 Runtime Stream Restricted Markdown Review

## Scope

- Slice: W1.5.127 Desktop Runtime Stream `content` restricted Markdown rendering.
- Reviewer: Herschel (`019ef75f-43a7-77c3-b6a7-6f54cc0bfb5d`).
- Mode: read-only independent review of the uncommitted diff.

## Verdict

PASS. No findings.

## Checks Run

- `pnpm --filter @cw/desktop run typecheck` PASS.
- `git diff --check` PASS, with only LF/CRLF conversion warnings.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs` PASS.
- Post-check `git status --short` showed only the expected 5 modified renderer/test/smoke/css files.

Owner validation also covered targeted Prettier, full desktop tests, full repository lint/typecheck/test/codegen gates, the five-viewport Electron visual smoke matrix, and final diff hygiene.

## Boundary Confirmation

The renderer uses React element construction for restricted Markdown and does not use `dangerouslySetInnerHTML` or raw HTML injection. `<script>`, `<iframe>`, arbitrary HTML tags, Markdown images, and remote image URLs are not materialized as executable or image DOM. Link filtering rejects empty/control/whitespace hrefs, protocol-relative URLs, and non-whitelisted schemes; only relative, `https:`, and `mailto:` links become anchors.

The diff does not expose raw `payload`, `rawData`, metadata objects, metadata keys, or metadata values, and does not introduce runtime/API/IPC/`window.cw`/storage/fs behavior, dependency, package, spec, schema, generated, or ADR changes.

## Residual Risks

- The renderer intentionally implements only the spec's restricted Markdown subset; full CommonMark behavior, HTML passthrough, remote images, and artifact opening remain out of scope for this slice.
