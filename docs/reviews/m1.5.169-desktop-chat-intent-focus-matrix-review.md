# W1.5.169 Desktop Chat Intent Focus Matrix Review

## Scope

W1.5.169 adds metadata-only matrix evidence for Chat Box intent-switch focus retention. The slice is limited to dev-only visual smoke scripts, matrix tests, runbook, and this review note.

## Owner Checks

- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs` PASS。
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs` PASS。
- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs` PASS：15 tests passed。
- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs` PASS：22 tests passed。
- `pnpm --filter @cw/desktop run typecheck` PASS。
- `pnpm --filter @cw/desktop run test` PASS：desktop Node tests `311 passed`。
- Electron visual smoke matrix PASS：8 cases, `failures=[]`, enabled desktop/mobile both record `chatIntentSwitchFocusEvidence` with `repair` + focused `true`.
- `pnpm exec prettier --write` / targeted Prettier check PASS for touched files.
- `.\dev.ps1 lint` PASS。
- `.\dev.ps1 codegen` PASS；generated schemas have no content diff.
- `.\dev.ps1 typecheck` PASS。
- `.\dev.ps1 test` PASS：pytest `475 passed, 1 skipped`; desktop Node tests `311 passed`.
- `.\dev.ps1 format-check` expected baseline result：ruff format check PASS; Prettier still fails on the known repo-wide 39-file baseline debt.
- `git diff --check` PASS with LF/CRLF warnings only.

## Independent Review

- Dewey (`019efd55-c36f-7142-b569-2d4eee88b2fa`) PASS.
- Findings: no blocker/P1/P2/P3.
- Scope review: diff stays dev-only for visual smoke runner/matrix/test plus runbook/review; no spec/schema/dependency/runtime/IPC/storage drift found. `CLAUDE.md` remains dirty and outside this slice.
- Code review: runner captures intent-switch metrics after `Revise -> Repair` and before draft input; runner fail-closes active/details/preview intent and focus; matrix evidence is strict/sanitized to `"repair"` and boolean `true`, with invalid values represented as `null` in manifest evidence.
- Test review: success payloads, false focus, string focus, child-failure fixture compatibility, and disabled local-submit guard behavior are covered.
- Dewey commands: `node --check` on both smoke scripts, focused smoke tests, focused matrix tests, desktop typecheck/test, `.\dev.ps1 lint`, `.\dev.ps1 typecheck`, `.\dev.ps1 test`, `.\dev.ps1 format-check`, and `git diff --check`.
- Dewey did not run write-capable `.\dev.ps1 codegen` or rerun the real Electron matrix; owner ran both and recorded results above.

## Decision

- PASS. W1.5.169 is ready to commit with `CLAUDE.md` excluded.
