# M1.5.161 Review - Desktop Chat Intent Focus Retention

## Scope

- Slice: W1.5.161 Desktop Chat Box local intent focus retention.
- Files reviewed:
  - `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
  - `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `docs/04_runbook/m1.5-progress.md`
  - `docs/reviews/m1.5.161-desktop-chat-intent-focus-retention-review.md`

## Checklist

- Clicking a valid Chat Box draft intent restores focus to the current draft textarea after local intent state updates.
- Intent focus retention does not change draft text, intent details, preview target/action, local send history, or sequence behavior.
- Focus retention does not introduce runtime command, IPC/window.cw, renderer storage, persistence, dependency, accepted spec, schema, generated schema, or ADR drift.
- Visual smoke checks real DOM focus retention without persisting raw draft text or weakening existing evidence redaction.

## Result

Linnaeus independent review found no P0/P1/P2 implementation issues.

- P3: runbook/review docs still marked the slice as in-progress/pending while implementation and parent verification had already completed. Fixed by recording the W1.5.161 verification results, known format-check baseline, and this review outcome.
- Residual test gap: initial focused tests and visual smoke asserted focus for `revise` / `repair`, while `ask` and malformed intent relied on shared handler/guard shape. Fixed by adding explicit React regression coverage for malformed intent fail-closed behavior and `ask` focus retention.

Final status after remediation: PASS.

## Reviewer Commands

- Parent verification already run before review:
  - `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `pnpm --filter @cw/desktop run typecheck`
  - `pnpm --filter @cw/desktop run test`
  - Electron visual smoke with `chatBox=enabled`
  - targeted Prettier check for touched files
  - `.\dev.ps1 lint`
  - `.\dev.ps1 codegen`
  - `.\dev.ps1 typecheck`
  - `.\dev.ps1 test`
  - `.\dev.ps1 format-check`
  - `git diff --check`

## Parent Verification Summary

- Focused desktop typecheck/test PASS.
- Real enabled Chat Box visual smoke PASS: intent switching restored focus to the draft textarea while local history and redaction evidence stayed metadata-only.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS with no generated diff.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 475 passed / 1 skipped; desktop 301 passed.
- `.\dev.ps1 format-check` FAIL only on standing 39-file repo-wide Prettier baseline; touched files passed targeted Prettier.
- `git diff --check` PASS with only Git CRLF warnings.

## Remediation Verification

- `pnpm --filter @cw/desktop run typecheck` PASS after residual test-gap remediation.
- `pnpm --filter @cw/desktop run test` PASS after residual test-gap remediation: desktop 301 passed.
- Final targeted Prettier check PASS; `git diff --check` PASS with only Git CRLF warnings.
