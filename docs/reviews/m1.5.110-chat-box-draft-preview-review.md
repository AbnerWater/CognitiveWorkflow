# M1.5.110 Chat Box Draft Preview Review

Date: 2026-06-23

Reviewer: independent subagent Darwin (`019ef4b1-558e-7da1-a9d0-d111999f774f`)

## Scope

Review the W1.5.110 desktop shell diff for the no-new-dependency, local Chat Box draft preview scaffold:

- renderer-local derived preview state for empty / blocked / ready draft conditions
- visible preview of current draft text or empty state, current intent, and readiness reason
- stable visual-smoke metrics for preview state, reason, ready flag, intent, body state, and visible text
- React coverage for empty, drafted, and cleared preview states
- absence of runtime commands, IPC/runtime bridge use, `window.cw`, storage, Node fs, raw host/session/controller/store access, persistence, mutation, message send behavior, schema/spec/ADR/codegen/dependency drift, React Flow, Tailwind, packaging, or updater drift

## Result

PASS. No blocker, P1, P2, or P3 findings.

The review confirmed the implementation stays within the approved local Desktop boundary. The preview is renderer-local derived state/rendering in `runtime-workbench-shell-react.tsx`; it does not add runtime commands, IPC, `window.cw`, storage, Node fs, raw host/session/controller/store access, persistence, mutation, send behavior, schema/spec/ADR/codegen/dependency drift, React Flow, Tailwind, packaging, or updater drift.

## Checked

- `RuntimeWorkbenchShellChatDraftPreview` derives `empty`, `blocked`, and `ready` states from local draft word count plus `chatBox.enabled`.
- Preview data attributes expose only derived state, reason, ready flag, intent, intent label, and body empty/draft state.
- Draft text is rendered visibly in the preview body, but is not copied into a raw data attribute.
- React assertions cover empty preview, `revise` and `repair` intent selection, blocked preview with draft body, collapse/expand persistence, and Clear preserving intent.
- Visual smoke metrics and invariants cover preview state/reason/ready/intent/body/text across initial, draft, and cleared states.

## Residual Risk

- The reviewer did not run the full desktop React test command, Electron visual smoke, or full `dev.ps1` gates; those were run by the main rollout.
- Real chat command routing, runtime mutation, prompt persistence, API/IPC bridge behavior, and send handling remain outside W1.5.110 scope.

## Verification

- Reviewer observed `git diff --check` PASS with only normal CRLF warnings.
- Reviewer observed a diff-only forbidden-boundary scan with no added forbidden references.
- Reviewer observed `pnpm --filter @cw/desktop exec tsc --noEmit` PASS.
- Main rollout ran `pnpm --filter @cw/desktop run typecheck` PASS.
- Main rollout ran `pnpm --filter @cw/desktop run test` PASS, 256 tests.
- Main rollout ran Electron visual smoke PASS for desktop 1280x720, mobile 390x844, and mobile scrollY 900 / 1180 / 1440.
- Main rollout ran full gates: `.\dev.ps1 lint` PASS, `.\dev.ps1 codegen` PASS, `.\dev.ps1 typecheck` PASS, and `.\dev.ps1 test` PASS.
