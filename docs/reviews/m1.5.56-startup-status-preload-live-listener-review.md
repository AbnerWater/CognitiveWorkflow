# M1.5.56 Startup Status Preload Live Listener Review

Date: 2026-06-21
Reviewer: independent subagent `Peirce`
Result: PASS

## Scope Reviewed

- `apps/desktop/src/preload/contract.ts`
- `apps/desktop/src/preload/runtime-bridge.ts`
- `apps/desktop/src/preload/api.ts`
- `apps/desktop/src/preload/contract.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking findings.

## Confirmed Alignment

- The implementation stays preload-side pure TypeScript.
- No Electron import, dependency wiring, production dependency, package or lockfile change, accepted spec / ADR change, schema change, generated-code content change, or new IPC channel was introduced.
- The startup live listener reuses the existing `cw:runtime:startup-status` channel.
- `RuntimeBridge.onStartupStatus(listener)` is preserved through the frozen preload API.
- Startup live payloads are shallow-cloned per status object before renderer callbacks.
- Renderer listener exceptions are isolated from the preload IPC listener chain.
- Non-array or invalid payloads deterministically reach listeners as an empty status array.
- Unsubscribe behavior is idempotent.
- Existing shutdown status listener/API behavior remains covered and passing.

## Verification

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

Observed result: PASS. Desktop tests reported 169 passed / 0 failed. `git diff --check` passed with LF-to-CRLF warnings only.

## Residual Risks

- This slice remains a pure TypeScript injected preload listener.
- Real Electron `ipcRenderer.on/off` wiring, renderer startup status store / React panel, main window enumeration, live sidecar startup UI, runtime StreamEvent schema validation, and Electron dependency wiring remain later work.
