# M1.3.12 AgentAdapter Foundation Review

- Date: 2026-06-17
- Scope: `cw_runtime.adapters` protocol foundation and ClaudeCodeAdapter session seam.
- Reviewer: independent subagent Dalton
- Final verdict: PASS

## Initial Findings

- FAIL: Claude permission prompts emitted only `human.gate_required`; `tool.approval_required` parent event was missing.
- FAIL: Several StreamEvent payload keys drifted from `stream_event.md`, including model deltas, tool call payloads, human gate payloads, and attempt lifecycle payloads.
- FAIL: ClaudeCode capabilities did not match the accepted AgentAdapter snapshot.
- FAIL: `AA_RESUME_INVALID_KIND` was mapped to `AdapterErrorKind.DEFERRED_TOOL`.
- FAIL: W1.3.12 runbook omitted `model.request_completed` from the documented event surface.

## Fixes

- Added `tool.approval_required -> human.gate_required` event emission with `parent_event_id` linkage.
- Aligned model/tool/human/lifecycle payload key fields with `stream_event.md`.
- Updated ClaudeCode capabilities to `structured_output=false`, `mcp=true`, `evidence_lookup_tool=true`, and `multi_modal={image}`.
- Remapped `AA_RESUME_INVALID_KIND` to `AdapterErrorKind.INVALID_PACK` with `FailureType.FORMAT_ERROR`.
- Expanded tests to assert payload shape and updated the runbook event list.

## Verification

- `uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q` passed.
- `uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_3_12_claude_code_adapter.py` passed.
- `uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_3_12_claude_code_adapter.py` passed.

No remaining findings.
