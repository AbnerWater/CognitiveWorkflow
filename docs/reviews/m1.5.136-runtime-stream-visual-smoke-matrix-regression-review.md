# M1.5.136 Runtime Stream Visual Smoke Matrix Regression Review

## Scope

W1.5.136 turns the W1.5.135 visual smoke matrix failure-manifest hardening into automated desktop test coverage. The test runs the real matrix script with a temporary fake Electron CLI so it can exercise child process handling and manifest writing without launching real Electron, starting a Vite dev server, or relying on an external port.

Files reviewed:

- `apps/desktop/package.json`
- `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
- `docs/04_runbook/m1.5-progress.md`

Out of scope:

- Production runtime/session/store/React shell behavior.
- Accepted spec / ADR / schema / generated schema changes.
- Dependency, runtime API, IPC / `window.cw`, renderer storage, React Flow, Tailwind, packaging/updater changes.
- Raw payload, `rawData`, metadata object/key/value display.

## Independent Review

Reviewer: Bohr (`019ef9ce-aac7-7a01-9791-ad639d905e6e`)

Final result: PASS, no blocker/P1/P2/P3 findings.

Bohr confirmed the worktree delta is limited to `apps/desktop/package.json` and the new matrix regression test file. The new test uses `CW_VISUAL_SMOKE_ELECTRON_CLI` with a fake CLI, executes the real matrix script as a child process, reads back `matrix.json`, and covers the requested failure paths:

- Invalid JSON with raw child stdout/stderr redaction.
- Valid but non-object JSON root rejection.
- Console message value redaction with count-only manifest failure.

Bohr also confirmed there are no production runtime/renderer/spec/schema/dependency/API/IPC/window.cw/storage changes, and no React Flow/Tailwind/packaging/updater drift.

## Verification

- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs` PASS: 3/3 tests.
- `pnpm --filter @cw/desktop run test` PASS: 273/273 tests.
- `git diff --check` PASS with Git CRLF warning only.
- Targeted Prettier check PASS for touched files.

Additional closure gates are tracked in `docs/04_runbook/m1.5-progress.md`.
