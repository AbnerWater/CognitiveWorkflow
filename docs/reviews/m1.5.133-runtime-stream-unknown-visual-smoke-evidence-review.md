# M1.5.133 Runtime Stream Unknown Visual Smoke Evidence Review

## Scope

W1.5.133 is a dev-only evidence hardening slice for the Runtime Stream visual smoke runner. It preserves W1.5.132 known/unknown StreamEvent assertions and adds stable JSON evidence for responsive smoke runs.

Files reviewed:

- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`

Out of scope:

- Production runtime/session/store/React shell behavior.
- Accepted spec / ADR / schema / generated schema changes.
- Dependency, runtime API, IPC / `window.cw`, renderer storage, React Flow, Tailwind, packaging/updater changes.
- Raw payload, `rawData`, metadata object/key/value display.

## Independent Review

Reviewer: Hilbert (`019ef974-ed3f-7302-a218-187581d6462c`)

Result: PASS, no findings.

The reviewer confirmed:

- The diff is limited to the dev-only Electron visual smoke runner.
- `parseTargetLocation()` records only `origin`, `pathname`, and derived `streamEventMode`; it does not serialize the full URL, query string, hash, credentials, or token-like params.
- The JSON additions are smoke evidence only: target location, requested viewport/scroll, and Electron capture size.
- W1.5.132 semantics remain intact: `streamEventMode` still drives `expectedStreamEvent()`, known/unknown expectations, and expanded detail/metadata type-status text assertions.
- The representative unknown mobile deep-scroll JSON evidence was clean: `failures=[]`, `horizontalOverflow=0`, requested/capture size `390x844`, and expanded detail/metadata both include `Unknown event type`.

## Verification

- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
- `pnpm exec prettier --write apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
- `pnpm exec prettier --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
- `pnpm --filter @cw/desktop run typecheck`
- `pnpm --filter @cw/desktop run test`
- Electron visual smoke known desktop: `1280x720`, `scroll=0`, `visual-smoke.html`
- Electron visual smoke unknown desktop: `1280x720`, `scroll=0`, `visual-smoke.html?streamEvent=unknown`
- Electron visual smoke unknown mobile: `390x844`, `scroll=0`, `visual-smoke.html?streamEvent=unknown`
- Electron visual smoke unknown mobile scroll: `390x844`, `scroll=900`, `visual-smoke.html?streamEvent=unknown`
- Electron visual smoke unknown mobile deep scroll: `390x844`, `scroll=1440`, `visual-smoke.html?streamEvent=unknown`

Additional closure gates are tracked in `docs/04_runbook/m1.5-progress.md`.
