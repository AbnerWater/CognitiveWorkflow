# M1.5.111 Chat Box Intent Context Preview Review

Date: 2026-06-23

Reviewer: independent subagent Pauli (`019ef4cd-6f5d-7580-9347-03f1ef3e4e10`)

## Scope

Review the W1.5.111 desktop shell diff for the no-new-dependency, local Chat Box intent context preview scaffold:

- renderer-local `intent -> target/action` derivation for `Ask`, `Revise`, and `Repair`
- visible preview rows for Target and Action alongside draft body, Intent, and Reason
- stable visual-smoke metrics for target/action values and visible context text
- React coverage for initial ask context, revise context, repair draft context, and clear preserving repair context
- absence of runtime commands, message send, API/IPC/runtime bridge use, `window.cw`, storage, Node fs, raw host/session/controller/store access, persistence, mutation, dependency/package/lockfile, spec/ADR/schema/generated schema, React Flow, Tailwind, packaging, or updater drift

## Result

PASS. No blocker, P1, P2, or P3 findings.

The review confirmed the current uncommitted diff was limited to three desktop files. The implementation is renderer-local intent-to-target/action derivation plus preview rendering. The new data attributes only contain enumerated target/action/intent/reason/body-state values and do not put prompt text or model output into attributes.

## Checked

- `ask` derives `workflow` / `question`; `revise` derives `draft` / `change_request`; `repair` derives `repair` / `repair_review`.
- Preview attributes expose only derived enum-like values, not raw prompt text, model output, or draft text.
- React coverage checks initial ask context, revise context, repair draft context, and clear preserving repair context.
- Visual smoke metrics/assertions cover initial ask, repair draft, and clear preserving repair target/action plus visible context text.
- Diff review found no runtime command, send behavior, API/IPC bridge, storage, Node fs, persistence, mutation, dependency, spec, ADR, schema, generated schema, React Flow, Tailwind, packaging, or updater drift.

## Residual Risk

- Reviewer did not run desktop React test/build or Electron visual smoke because the review was read-only; the main rollout ran those gates.
- Real chat command routing, send handling, prompt persistence, API/IPC bridge behavior, runtime mutation, and workflow patch application remain outside W1.5.111 scope.

## Verification

- Reviewer observed `git diff --check` PASS with only normal CRLF warnings.
- Reviewer observed added-line forbidden-boundary scan over `git diff -U0` with no matches.
- Reviewer observed `pnpm --filter @cw/desktop exec tsc --noEmit` PASS.
- Main rollout ran `pnpm --filter @cw/desktop run typecheck` PASS.
- Main rollout ran `pnpm --filter @cw/desktop run test` PASS, 256 tests.
- Main rollout ran Electron visual smoke PASS for desktop 1280x720, mobile 390x844, and mobile scrollY 900 / 1180 / 1440.
- Main rollout ran full gates: `.\dev.ps1 lint` PASS, `.\dev.ps1 codegen` PASS, `.\dev.ps1 typecheck` PASS, and `.\dev.ps1 test` PASS.
