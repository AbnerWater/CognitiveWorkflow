# M1.5.166 Review - Desktop Chat Enabled Mobile Matrix Layout

## Scope

- Slice: W1.5.166 Desktop Chat Box visual smoke matrix mobile history layout evidence.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`
  - `docs/reviews/m1.5.166-desktop-chat-enabled-mobile-matrix-layout-review.md`

## Checklist

- Matrix runner keeps `chat-enabled-mobile` in the default 8-case manifest.
- Matrix-level enabled mobile Chat Box evidence fails closed if capped local history row columns drift away from 2 or if row `scrollWidth > clientWidth`.
- Matrix manifest keeps only metadata-only layout evidence and does not expose raw draft, preview, local submission text, raw child failure text, query/hash secrets, or output directory paths.
- Changes remain dev-only runner/test/docs and do not introduce production runtime, API, IPC/window.cw, renderer storage, dependency, accepted spec, schema, generated schema, or ADR drift.

## Result

No findings.

Independent reviewer: Anscombe (`019efd08-a7f7-72d2-b477-50fc839e865a`).

The review confirmed the scoped diff only, ignoring the pre-existing `CLAUDE.md` worktree change. The matrix runner reuses the mobile layout helper, applies the gate only to enabled cases at width <= 520, fails closed on `columns !== 2` or `scrollWidth > clientWidth`, and emits only numeric `chatLocalHistoryLayoutEvidence` in the manifest. The scoped diff does not touch production runtime/UI/API/IPC/storage/dependencies/accepted specs/schema/generated schema.

## Reviewer Commands

- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
- `git diff --check -- apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs docs/04_runbook/m1.5-progress.md docs/reviews/m1.5.166-desktop-chat-enabled-mobile-matrix-layout-review.md`

Reviewer command result: PASS; `git diff --check -- ...` reported only Windows CRLF warnings.
