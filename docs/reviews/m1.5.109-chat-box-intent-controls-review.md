# W1.5.109 Chat Box Intent Controls Review

Date: 2026-06-23

Reviewer: Newton (`019ef493-8343-7c00-9ce3-8a06322c14a1`)

Scope reviewed:

- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`

## Findings

No blocker / P1 / P2 / P3 findings were reported.

Newton confirmed:

- The diff stays scoped to local renderer Chat Box intent state/buttons/details, styling, focused React assertions, and visual-smoke metrics/click coverage.
- No package / lockfile / dependency / spec / ADR / schema / generated schema drift was found.
- No runtime / IPC / `window.cw` / storage / Node fs / raw host/session/controller/store / persistence / mutation / send behavior was found in the changed renderer implementation.

Reviewer residual risk:

- Newton did not rerun the full React test suite or Electron visual smoke.
- The initial reviewed diff did not separately click `revise`; owner subsequently added React and visual-smoke coverage for `Revise`.

## Validation Evidence

Owner-run validation:

- `pnpm exec prettier --write apps/desktop/src/renderer/runtime-workbench-shell-react.tsx apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx apps/desktop/src/renderer/runtime-workbench-shell.css apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`: PASS.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`: PASS.
- `git diff --check`: PASS, only LF-to-CRLF warnings.
- `pnpm --filter @cw/desktop run test`: PASS, 256 tests.
- Electron visual smoke: desktop 1280x720 PASS, mobile 390x844 PASS, mobile scrollY 900 / 1180 / 1440 PASS.
- `.\dev.ps1 lint`: PASS.
- `.\dev.ps1 codegen`: PASS, no generated diff remained.
- `.\dev.ps1 typecheck`: PASS.
- `.\dev.ps1 test`: PASS, Python 475 passed / 1 skipped; desktop 256 passed.

Chat Intent metrics from owner smoke JSON:

- Initial: intent `ask`, label `Ask`.
- Interaction path: `revise` click followed by `repair` click.
- Draft input: active/details intent `repair`, label `Repair`, value `Review repair plan now`, length `22`, words `4`, send-enabled `false`.
- Clear: value empty, length `0`, words `0`, intent remains `repair`.
- Responsive matrix: desktop and all mobile scroll captures had `failures` count `0` and horizontal overflow `0`.

Reviewer-run validation:

- `git diff --check`: PASS, only LF-to-CRLF warnings.
