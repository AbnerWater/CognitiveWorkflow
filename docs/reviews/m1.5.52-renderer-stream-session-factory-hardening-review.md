# M1.5.52 Review - Renderer Stream Session Factory Hardening

## Scope

- `apps/desktop/src/preload/contract.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Result

PASS after one review-driven fix.

## Initial Finding

- P1: The first W1.5.52 draft overclaimed factory-level `replayState` default coverage. The test created `replayState` and passed it through `createSession()`, proving session-level propagation rather than a factory default.

## Fix

- Updated the factory default test to keep `replayState` out of factory/session defaults and assert `clientOptions.replayState === undefined`.
- Updated the runbook wording to state that `replayState` is session-owned, avoiding shared replay cursors across sessions.
- Kept the per-session override test covering `sessionReplayState` propagation.

## Checks

- Confirmed factory defaults cover `runtime`, `eventSourceFactory`, default `projectId`, stream filters, scheduler, and event/connection/replay callbacks.
- Confirmed factory-level full reload callback propagation through the store wrapper and `full_reload_required` latch.
- Confirmed factory-level `onError` works as session listener error fallback.
- Confirmed per-session overrides cover planning channel, `projectId`, filters, scheduler, replay state, event/connection/replay/full reload callbacks, and `onError`.
- Confirmed no production API, dependency, package/lockfile, accepted spec, ADR, schema, or codegen behavior changes.
- Confirmed no Electron / `BrowserWindow` import, no new IPC channel, no React / Vite / Tailwind / UI wiring, no `localStorage` / `sessionStorage`, and no real network connection.

## Verification

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
```

Results:

- `pnpm --filter @cw/desktop run typecheck` PASS.
- `pnpm --filter @cw/desktop run test` PASS, 160 passed / 0 failed.

## Open Questions

None.

## Residual Risk

This remains a pure TypeScript renderer stream test-hardening slice. It does not validate real Electron, browser EventSource, React rendering, live FastAPI SSE replay, cross-window stream sharing, or runtime StreamEvent schema validation.
