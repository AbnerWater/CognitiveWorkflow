# M1.5.151 Desktop Chat Local Send History Review

## Scope

- Slice: W1.5.151 Desktop Chat Box local send history.
- Reviewed implementation files:
  - `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
  - `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
  - `apps/desktop/src/renderer/runtime-workbench-shell.css`
  - `docs/04_runbook/m1.5-progress.md`
- Boundary: local renderer-only Chat Box metadata history. No runtime mutation, IPC, `window.cw`, renderer storage, persistence, accepted spec, schema, generated schema, dependency, React Flow, Tailwind, packaging, or updater change.

## Independent Review

- Reviewer: Gibbs (`019efafc-90bd-7c52-9714-a1d8b35dd44f`).
- Initial result:
  - P2: runbook had W1.5.151 marked done but no W1.5.151 verification result section.
  - P3: raw draft leakage test only checked root attributes, not descendant history attributes.
  - Residual risk: focused test did not explicitly prove the 3-item history cap.
- Fixes:
  - Moved stale W1.5.150 result content back to the tail W1.5.150 block and added the W1.5.151 verification result block.
  - Added recursive descendant attribute collection in the React shell test helper and used it for local submission leak checks.
  - Expanded the history test to send four drafts, assert count `3`, latest sequence `4`, previous sequence retained, and sequence `1` trimmed.
- Re-review result:
  - P3 closed.
  - 3-item cap and oldest-entry trim covered.
  - Focused build/test passed with 32 React shell tests.
  - `git diff --check -- <focused files>` passed with only CRLF warnings.

## Main-Agent Verification

- `pnpm --filter @cw/desktop run build:ts && node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js` PASS: React shell focused suite 32 passed.
- `pnpm --filter @cw/desktop run test` PASS: desktop 294 passed.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS; no generated schema content diff.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 475 passed / 1 skipped; desktop 294 passed.
- `.\dev.ps1 format-check`: ruff format check PASS; Prettier still fails on the known repo-wide 38-file baseline, with W1.5.151 touched files not listed in the warning set.
- `git diff --check` PASS with Git CRLF warnings only.
