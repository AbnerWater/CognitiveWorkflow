# W1.4.42 Independent Review — Project MCP Approval Bridge

Reviewer: independent subagent Cicero
Result: PASS

## Scope

Reviewed the W1.4.42 diff for projecting project `.agent-workflow/mcp.config.json` entries with `requires_approval=true` into the ClaudeCodeAdapter `can_use_tool` bridge when `enable_can_use_tool_bridge=true`.

Authority checked:

- `specs/runtime_harness.md §2.8`
- `specs/schemas/node_contract.md §5.2`
- `specs/protocols/agent_adapter.md §11`
- `docs/04_runbook/m1.4-progress.md` W1.4.41/W1.4.42

Files reviewed:

- `apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py`
- `apps/runtime/tests/test_m1_3_12_claude_code_adapter.py`
- `docs/04_runbook/m1.4-progress.md`

## Findings

None.

## Verification

```powershell
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
git diff --check
```

Subagent result:

- Focused pytest passed: 27 tests.
- Focused strict mypy passed: no issues in 2 source files.
- Focused ruff passed.
- `git diff --check` passed; LF/CRLF warnings only.

## Boundary Assessment

The diff matches the W1.4.42 boundary:

- Bridge disabled: project `requires_approval=true` still fails closed.
- Bridge enabled: project `requires_approval=true` becomes approval-required `mcp__<server>__*` and is removed from SDK auto-approved `allowed_tools`.
- `secret_ref` behavior remains fail-closed without resolver/decryptor.
- No accepted spec, ADR, public schema, production dependency, real provider smoke, OAuth, MCP invocation, or cross-process pending permission persistence changes were introduced.
