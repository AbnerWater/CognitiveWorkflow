# M1.5.22 Runtime Startup Status IPC Contract Review

| Item        | Value                                                                  |
| ----------- | ---------------------------------------------------------------------- |
| Milestone   | M1.5 / W1.5.22                                                         |
| Reviewed at | 2026-06-20                                                             |
| Reviewer    | Independent subagent `Popper` (`019ee4ae-68f4-7ff2-ad32-b681b07c6833`) |
| Verdict     | PASS                                                                   |

## Scope

- `apps/desktop/src/shared/runtime-ipc.ts`
- `apps/desktop/src/shared/runtime-ipc.test.ts`
- `apps/desktop/src/main/runtime-startup-status.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/src/preload/contract.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking or non-blocking findings.

## Evidence

- Boundary is respected: the diff is pure TypeScript shared IPC contract, main factory registration, preload type contract, tests, and runbook updates.
- No production dependency, package manifest, Electron import, real `ipcMain.handle` / `ipcRenderer.invoke`, `BrowserWindow`, renderer UI, SSE/EventSource client, StreamEvent emission, token/port persistence, or `runtime.lock` schema change was introduced.
- `cw:runtime:startup-status` is added to the stable shared channel list.
- The public startup status shape lives in the shared IPC contract, and the main startup status mapper aliases that shared shape to avoid field drift.
- The startup-status registration returns a copy of recorded status history and does not trigger lazy startup when called before `connectionInfo()` or `fetch()`.
- The returned status shape does not include token, `base_url`, prompt text, model output, or runtime fetch data.

## Commands

- `pnpm --filter @cw/desktop run typecheck` PASS
- `pnpm --filter @cw/desktop run test` PASS, 98 passed / 0 skipped
- `git diff --check` PASS, with LF-to-CRLF warnings only
- `git diff --name-only -- ":(glob)**/package.json" pnpm-lock.yaml pnpm-workspace.yaml pyproject.toml uv.lock package-lock.json yarn.lock npm-shrinkwrap.json` produced no output

## Residual Risk

- This remains module-level IPC contract coverage, not real Electron `ipcMain.handle`, preload `contextBridge`, renderer usage, structured-clone behavior, or cross-window broadcast.
- It does not emit `system.runtime_ready` StreamEvents and does not implement an EventSource/SSE client.
- Future lifecycle sources must keep the public `reason` field sanitized.
