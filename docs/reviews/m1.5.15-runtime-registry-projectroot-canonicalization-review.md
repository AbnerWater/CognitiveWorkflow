# M1.5.15 Runtime Registry ProjectRoot Canonicalization Review

Date: 2026-06-20
Reviewer: independent subagent `Singer`
Result: PASS

## Scope Reviewed

- `apps/desktop/src/main/runtime-connection-registry.ts`
- `apps/desktop/src/main/runtime-connection-registry.test.ts`
- `apps/desktop/src/main/index.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking findings.

## Evidence

- Scope stayed limited to the requested registry, test, export, and runbook files; there were no `package.json` or lockfile changes.
- The diff adds no Electron import, `ipcMain`, `BrowserWindow`, renderer UI, EventSource/SSE client, runtime.lock schema change, or token/port persistence.
- Default case mode is Windows `case_insensitive`, otherwise `case_sensitive`.
- `register()`, `get()`, `resolver()`, and `unregister()` use a canonical lookup key, while `RuntimeConnectionRegistryEntry.projectRoot` keeps the `path.resolve()` display casing.
- `resolver(projectRoot)` benefits through `get(projectRoot)`.
- `unregister(projectRoot, connection)` still refuses mismatched connection cleanup, preserving newer session protection.
- Tests cover configured case-insensitive and case-sensitive projectRoot behavior.

## Commands

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 80/80 tests
- `git diff --check`: PASS, LF/CRLF warnings only

## Residual Risk

This slice closes projectRoot casing mismatches in the in-memory registry. Symlink, junction, and 8.3 short-path equivalence still require a future realpath-aware canonicalization design.
