# W1.5.7 Desktop Runtime IPC Main Handlers Review

Date: 2026-06-20

Reviewer: independent subagent `019ee3b8-4895-7013-95b1-2dd091d8bdfc`

## Scope

- Changed files under review:
  - `apps/desktop/src/main/runtime-ipc-handlers.ts`
  - `apps/desktop/src/main/runtime-ipc-handlers.test.ts`
  - `apps/desktop/src/shared/runtime-ipc.ts`
  - `apps/desktop/src/shared/runtime-ipc.test.ts`
  - `apps/desktop/src/preload/contract.ts`
  - `apps/desktop/src/main/index.ts`
  - `apps/desktop/package.json`
  - `docs/04_runbook/m1.5-progress.md`
- Specs and decisions checked:
  - `docs/roadmap.md` M1.5
  - `specs/api/http_sse.md` §7
  - `specs/runtime_harness.md` §9.1 / §9.3
  - `docs/03_decisions/0006-electron-desktop-shell.md`

## Result

PASS

No blocking findings.

## Review Notes

- `createRuntimeIpcMainHandlers()` stays inside the requested skeleton boundary: it is a pure factory with injected `connectionInfo` and optional `fetchImpl`, with no Electron import or `ipcMain` registration.
- Runtime connection normalization is loopback-only and reconstitutes canonical `/cw/v1` base URLs.
- Fetch path/header validation is reused through shared IPC helpers.
- Preload now delegates header injection to the same shared helper, reducing main/preload drift.
- No disk token persistence, hidden dependency change, sidecar startup, runtime lock acquisition, BrowserWindow, renderer UI, or real Electron wiring was found in the diff.
- Runbook scope and boundary language match the implementation.

## Commands Run

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 51 tests passed
- `git diff --check`: PASS, CRLF warnings only

## Residual Risks

- W1.5.7 remains a module-level skeleton: no real Electron `ipcMain.handle`, no sidecar lifecycle orchestration, no `runtime.lock` integration, and no EventSource/SSE client verification yet.
- The accepted API spec has some wording tension between main-process token memory and §7 returning `{base_url, token}` to renderer. W1.5.7 follows §7 and does not persist the token.
