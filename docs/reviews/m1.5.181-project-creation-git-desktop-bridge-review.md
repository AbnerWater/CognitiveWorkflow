# M1.5.181 Project Creation Git Desktop Bridge Review

Independent reviewer: Ramanujan (`019efea6-a6f2-74b2-be6f-67bb8f48e91e`)

## Findings

No findings.

## Scope Checked

- `POST /cw/v1/projects` alignment with `specs/api/http_sse.md`.
- Mandatory Git initialization alignment with `specs/runtime_harness.md`.
- No accepted spec / ADR changes.
- No raw task background or runtime response leakage into command payloads or snapshots.
- `create_project` command integration through session, interaction, host, presenter, shortcuts, and React shell.
- Test coverage for `git_initialized === true`, missing runtime / invalid input blocking, `git_initialized: false` rejection, no task background payload, and no Git bypass marker.

## Reviewer Notes

- Desktop bridge sends relative `/projects` with `schema_version`, `display_name`, `host_path`, and optional `settings_overrides`.
- Desktop bridge requires `git_initialized === true` and a non-empty `first_commit_sha` before reporting success.
- React keeps task background local and omits it from the dispatched `create_project` command.
- Session snapshots retain structured project creation metadata only and ignore extra runtime response fields.

## Residual Risk

Reviewer initially noted that coverage was split between React fake-session dispatch and direct session fake-fetch tests. This was addressed by adding interaction-level coverage that dispatches `create_project` through the real interaction object into `runtime-workbench-session` and fake `runtime.fetch`.
