# M1.5.32 Window Close Shutdown Review

Date: 2026-06-21

Reviewer: Descartes (`019ee5eb-e0d6-7a32-b71a-5459f00e33f8`)

Verdict: PASS

## Scope

- `apps/desktop/src/main/bootstrap.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No findings.

## Review Notes

- The uncommitted diff is limited to the requested W1.5.32 files.
- No package, lockfile, accepted spec, ADR, schema, or production dependency changes were found.
- Targeted search found no real `electron` import or require.
- `installRuntimeWindowLifecycleShutdown()` is an injected BrowserWindow-like adapter, not real Electron wiring.
- The close listener prevents non-terminal `close`, deduplicates pending runtime shutdown through one cached promise, allows terminal retry closes, and calls `window.close()` on both success and failure.
- Tests cover first close prevent, pending close deduplication, unregister-before-close without runtime startup, and failure retry without permanently blocking window close.
- The runbook states the injection-only boundary and does not claim real Electron wiring, renderer reload, live sidecar shutdown, or StreamEvent coverage.

## Reviewer Commands

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

Reviewer results:

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 117 passed / 0 failed / 0 skipped
- `git diff --check`: PASS, with only LF-to-CRLF normalization warnings
- Targeted package/spec/ADR/schema diff check: PASS, no output

## Residual Risks

- This remains injection-level coverage only.
- Real Electron `BrowserWindow.on("close")`, renderer reload or `beforeunload`, cross-window shutdown broadcast, live FastAPI sidecar shutdown, and shutdown StreamEvent verification remain future wiring risks.
