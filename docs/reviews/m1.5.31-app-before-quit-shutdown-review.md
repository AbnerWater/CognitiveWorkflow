# M1.5.31 App Before-Quit Shutdown Review

Date: 2026-06-21

Reviewer: Copernicus (`019ee5d8-b95d-7811-bb35-068d88feda9c`)

Verdict: PASS

## Scope

- `apps/desktop/src/main/bootstrap.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No findings.

## Review Notes

- The uncommitted diff is limited to the requested W1.5.31 files.
- No package, lockfile, spec, ADR, schema, runtime, or production dependency changes were found.
- The new lifecycle surface is injected TypeScript only and does not import Electron or wire real `BrowserWindow`, `ipcMain`, or `ipcRenderer` behavior.
- `installRuntimeAppLifecycleShutdown()` prevents non-terminal `before-quit`, deduplicates pending shutdown through one cached promise, allows terminal retry quits, and calls `app.quit()` on both success and failure.
- Tests cover success and pending deduplication, unregister-before-quit without runtime startup, and failure retry without permanently blocking app quit.

## Reviewer Commands

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

Reviewer results:

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 114 passed / 0 failed
- `git diff --check`: PASS, with only LF-to-CRLF normalization warnings
- Targeted dependency/spec/ADR diff check: PASS, no output

## Residual Risks

- This remains injection-level coverage only.
- Real Electron `app.on("before-quit")`, live FastAPI sidecar shutdown, cross-window shutdown state, reload behavior, and shutdown StreamEvent verification remain future wiring risks.
