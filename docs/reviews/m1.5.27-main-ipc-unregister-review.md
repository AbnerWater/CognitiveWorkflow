# M1.5.27 Main IPC Unregister Review

Date: 2026-06-20

Reviewer: independent Codex subagent `Einstein` (`019ee520-8532-7122-9757-2208eb4764d2`)

Result: PASS

## Findings

None.

## Scope Checked

- Changed files stayed within the intended W1.5.27 boundary: `apps/desktop/src/main/bootstrap.ts`, `apps/desktop/src/main/index.ts`, `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`, and `docs/04_runbook/m1.5-progress.md`.
- No package manifest, accepted spec, ADR, schema, renderer UI, or generated schema output changed.
- No dependency, Electron import, real `ipcMain`, real `BrowserWindow`, app lifecycle, `localStorage`, or `sessionStorage` usage was introduced.
- `CwMainIpcMain` now includes the injected Electron-like `removeHandler(channel)` capability alongside `handle(channel, listener)`.
- `installRuntimeIpcMainHandlers()` returns an idempotent `unregister()` function that removes the stable runtime IPC channels only once and does not start the runtime.
- `unregisterRuntimeIpcMainChannels()` exposes the channel removal helper for future real Electron shutdown / reload wiring.
- Tests cover stable channel removal order, handler map cleanup, second unregister no-op behavior, unchanged startup status, and no runtime startup during unregister.

## Commands Run By Reviewer

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
```

All commands passed; desktop tests reported 104 passed, 0 failed, 0 skipped.

## Residual Risks

W1.5.27 remains a pure injected TypeScript IPC lifecycle cleanup. It does not validate real Electron `ipcMain.removeHandler()` wiring, app/window shutdown lifecycle, renderer/preload integration, SSE/EventSource behavior, or runtime sidecar shutdown semantics.
