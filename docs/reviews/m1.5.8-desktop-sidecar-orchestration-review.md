# W1.5.8 Desktop Sidecar Orchestration Review

Date: 2026-06-20

Reviewer: independent subagent `019ee3c8-3b5e-7aa2-9d6f-3351acc798de`

## Scope

- Changed files under review:
  - `apps/desktop/src/main/runtime-orchestration.ts`
  - `apps/desktop/src/main/runtime-orchestration.test.ts`
  - `apps/desktop/src/main/index.ts`
  - `apps/desktop/package.json`
  - `docs/04_runbook/m1.5-progress.md`
- Specs and decisions checked:
  - `docs/roadmap.md` M1.5
  - `specs/api/http_sse.md` §7
  - `specs/runtime_harness.md` §9.1 / §9.3
  - `docs/03_decisions/0006-electron-desktop-shell.md`

## Result

PASS

No blocking findings.

## Review Notes

- `startRuntimeOrchestration()` composes command discovery, `runtime.lock` acquisition/release, sidecar startup, and IPC handler creation while staying inside the module-level skeleton boundary.
- Startup failure releases the acquired lock before rethrowing.
- `stop()` sends the sidecar signal and releases the lock once.
- No Electron import, `ipcMain` registration, `BrowserWindow`, renderer UI, EventSource/SSE client, token persistence, accepted spec/ADR edit, or production dependency change was found.
- `apps/desktop/package.json` only adds the new orchestration test to the existing desktop test script.
- Runbook claims match the implementation and declared boundary.

## Commands Run

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 54 tests passed
- `git diff --check`: PASS, CRLF warnings only

## Residual Risks

- W1.5.8 remains module-level orchestration only: no real Electron wiring, no existing sidecar port handoff, no native `flock` / `LockFileEx`, no renderer `window.cw.runtime`, and no EventSource/SSE verification yet.
