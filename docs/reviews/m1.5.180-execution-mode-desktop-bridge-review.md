# W1.5.180 Execution Mode Desktop Bridge Review

Date: 2026-06-25

Reviewer: Sartre (`019efd73-24b3-7b80-afbb-e337483f966a`)

Scope: current W1.5.180 Desktop workbench execution mode bridge diff, excluding unrelated dirty `CLAUDE.md`.

## Result

No blocking findings after the P3 follow-up was fixed.

## Findings

- P0/P1/P2: none.
- P3: `runNodeOnce()` had React dispatch coverage but lacked direct session-level contract coverage for `runtime.fetch` path/init, no request body, structured-only snapshot storage, failed response status, and blocked branches.

## Resolution

- Added direct `runtime-workbench-session` coverage in `runtime-workbench-shell-react.test.tsx` for:
  - `POST /runs/{run_id}/nodes/{node_id}:run-once` path construction.
  - `projectId` / `idempotencyKey` forwarding with no request body.
  - Success and failed HTTP response snapshots without retaining response body text/object content.
  - `mode_not_step`, `runtime_unavailable`, and `invalid_target` blocked snapshots without runtime dispatch.

## Verification

- Reviewer ran `pnpm --filter @cw/desktop run typecheck` PASS.
- Reviewer ran `pnpm --filter @cw/desktop run test` PASS, 381 tests before P3 fix.
- Main follow-up ran `pnpm --filter @cw/desktop run typecheck` PASS after P3 fix.
- Main follow-up ran `pnpm --filter @cw/desktop run test` PASS after P3 fix, 384 tests.

## Residual Risk

- W1.5.180 only wires the Desktop workbench mode controls and run-once dispatch surface. It does not prove backend run-once execution success, A4 acceptance, or `EXIT-P1-1` readiness.
- `CLAUDE.md` remains an unrelated dirty file and is intentionally excluded from this slice.
