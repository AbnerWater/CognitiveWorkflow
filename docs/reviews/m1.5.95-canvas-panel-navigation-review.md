# M1.5.95 Canvas Panel Navigation Review

Date: 2026-06-23

Reviewer: independent subagent Heisenberg (`019ef26e-6a2b-7393-879f-26313b6c799a`)

## Scope

Review the W1.5.95 desktop shell diff for the no-new-dependency, read-only Canvas panel navigation/focus scaffold:

- `RuntimeWorkbenchPanelId` / `show_canvas_panel` / `Ctrl+0`
- Canvas tab, Dock item, shell action, shortcut hint, and focused read-only Canvas surface
- visual smoke coverage for Canvas focused surface
- absence of spec/ADR/package/codegen drift

## Result

PASS. No blocker, P1, or P2 findings.

The review confirmed the diff stays inside desktop shell, smoke, test, and runbook files. It found no accepted spec / ADR / package / generated schema change, no React Flow or Tailwind dependency, no renderer storage API use, no direct Electron / IPC / `window.cw` access, and no raw runtime bridge or host/session/controller/store access in changed production renderer files.

## P3 Findings

- React action-mapping coverage initially asserted `show_stream_panel` but not `show_canvas_panel`.
- DOM keyboard binding coverage initially exercised `Ctrl+2` / `Ctrl+1` but not `Ctrl+0`.

Both P3 findings were fixed in this slice:

- `runtime-workbench-shell-react.test.tsx` now asserts `show_canvas_panel` maps to `{ type: "show_canvas_panel" }`.
- `contract.test.ts` now exercises real DOM keyboard binding for `Ctrl+0`, verifies Canvas becomes active, then continues through `Ctrl+2`.

## Verification

- `pnpm --filter @cw/desktop run typecheck` PASS after P3 fixes.
- `pnpm --filter @cw/desktop run test` PASS after P3 fixes, 251 tests passed.
- Electron visual smoke before P3 test-only fixes passed across desktop, mobile, and mobile scroll positions with `failures=[]`, `messages=[]`, and `horizontalOverflow=0`.
