# M1.4.47 External HTTP MCP Provider Review

## Scope

- Slice: W1.4.47 Project MCP external HTTP provider smoke.
- Reviewed files:
  - `apps/runtime/tests/test_m1_3_2_project_harness.py`
  - `docs/04_runbook/m1.4-progress.md`
- Review mode: independent read-only subagent review.

## Result

PASS.

No actionable findings were identified.

## Evidence

- The diff is limited to the requested test and M1.4 runbook files.
- No accepted spec, ADR, public schema, production code, or dependency file was modified.
- The smoke provider starts as a separate Python subprocess using `sys.executable`, binds `127.0.0.1:0`, writes its endpoint to a temp file, and is accessed over HTTP by `ProjectMCPHttpDiscoveryClient`.
- Coverage includes `ProjectMCPDiscoveryRunner + ProjectMCPHttpDiscoveryClient` lock snapshot discovery and concrete `tools/call` invocation against the same subprocess provider.
- Cleanup uses startup failure handling, timeout termination, and a test-level `finally` path that stops the subprocess.

## Boundary Check

- This slice validates a local stdlib subprocess HTTP MCP provider smoke, not a public internet or third-party provider.
- It does not implement or claim OAuth, secure-store credential injection into the MCP HTTP client, AdapterRegistry/API integration, real provider upstream `cancel()`, Claude Agent SDK real-provider HITL, or end-to-end runtime API MCP invocation.

## Residual Risk

- The provider is intentionally protocol-minimal and does not enforce request path, session, or header correctness.
- The 5s startup timeout and 2s client timeout should be fine for local loopback but could still be flaky on a severely overloaded Windows runner.
