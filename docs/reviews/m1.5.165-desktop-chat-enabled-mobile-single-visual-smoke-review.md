# M1.5.165 Review - Desktop Chat Enabled Mobile Single Visual Smoke

## Scope

- Slice: W1.5.165 Desktop Chat Box single visual smoke mobile history layout evidence.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`
  - `docs/reviews/m1.5.165-desktop-chat-enabled-mobile-single-visual-smoke-review.md`

## Checklist

- Single Electron visual smoke records metadata-only Chat Box history layout evidence.
- `chatBox=enabled` mobile visual smoke fails closed if local history rows no longer use the 2-column mobile layout.
- Persisted evidence does not include raw draft text, preview text, local submission text, raw errors, query/hash secrets, or output directory paths.
- Changes remain dev-only runner/test/docs and do not introduce production runtime, API, IPC/window.cw, renderer storage, dependency, accepted spec, schema, generated schema, or ADR drift.

## Result

- Kepler (`019efce6-7795-7c70-a169-15741931fff7`) reported no P0/P1 findings.
- P2 finding: local-history `clientWidth` / `scrollWidth` metrics were collected as evidence but not fail-closed; a two-column row with `scrollWidth > clientWidth` could pass. Fixed by adding `runtime-workbench-visual-smoke-layout.cjs` and routing single visual smoke through a mobile-only `scrollWidth <= clientWidth` gate.
- P3 finding: runbook/review still contained pending closure placeholders. Fixed by recording real W1.5.165 verification results below.
- Residual risk: this remains dev-only single-smoke evidence for the current Chat Box fixture; broader responsive coverage stays in the W1.5.164 visual smoke matrix.

## Reviewer Commands

- Independent reviewer ran `node --test apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs` before the P2 fix: 14/14 passed.
- Main agent follow-up after P2 fix:
  - `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs` PASS.
  - `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-layout.cjs` PASS.
  - `node --test apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs` PASS: 15/15 passed.
  - `pnpm --filter @cw/desktop run typecheck` PASS.
  - `pnpm --filter @cw/desktop run test` PASS: 303 Node tests passed.
  - Real single smoke PASS at `390x844`: `failures=0`, `horizontalOverflow=0`, `columns=2`, `clientWidth=294`, `scrollWidth=294`, `focused=true`, `firstSequence=1`, `historyIds=4,3,2`, `resendSequence=5`.
  - `.\dev.ps1 lint` PASS.
  - `.\dev.ps1 codegen` PASS, no generated diff retained.
  - `.\dev.ps1 typecheck` PASS.
  - `.\dev.ps1 test` PASS: Python `475 passed, 1 skipped`; desktop Node `303 passed`.
  - `.\dev.ps1 format-check` still fails only on standing repo-wide Prettier baseline debt (39 files); targeted Prettier for this slice passed.
  - `git diff --check` PASS with LF/CRLF warnings only.
