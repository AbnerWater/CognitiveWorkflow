# M1.5.43 Renderer Stream Reconnect Controller Review

Reviewer: Zeno (`019ee6bd-0cca-7cf1-9299-b0547bd7af9e`)

Verdict: PASS after fix

## Scope Reviewed

- `apps/desktop/src/renderer/runtime-stream-client.ts`
- `apps/desktop/src/preload/contract.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

- Initial finding: full reload terminal path closed the active source but retained managed renderer subscription callbacks.
- Fix: `full_reload` now calls `clearManagedSubscriptions()`, and `close()` reuses the same helper.
- Regression coverage: the replay-miss test captures the returned unsubscribe function and asserts it returns `false` after terminal full reload.
- Final review: no remaining findings.

## Verification

- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `pnpm --filter @cw/desktop run test`: PASS, 142 tests.
- `git diff --check`: PASS, with only Git LF-to-CRLF working-tree warnings.
- Protected path diff check: PASS, no package, lockfile, accepted spec, ADR, schema, or generated schema output diff.
- Boundary grep: PASS, no new Electron import, no new `cw:runtime:*` channel, no `BrowserWindow`, no `localStorage` or `sessionStorage`. The only `ipcRenderer` hit is the existing injected test fixture shape.

## Notes

- The controller remains pure TypeScript renderer-side logic.
- Real Electron or browser `EventSource` behavior, live FastAPI SSE replay, React full reload UI rebuild, cross-window stream sharing, and runtime `StreamEvent` schema validation remain later integration work.
