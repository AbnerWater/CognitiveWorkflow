# M1.5.29 Graceful Shutdown Orchestration Review

| Item        | Value                                             |
| ----------- | ------------------------------------------------- |
| Work item   | W1.5.29 Desktop graceful shutdown orchestration   |
| Reviewed at | 2026-06-20                                        |
| Reviewer    | Linnaeus (`019ee55b-f19a-7fc0-a439-3d5f8a4bcacd`) |
| Verdict     | PASS                                              |

## Scope

- `apps/desktop/src/main/runtime-orchestration.ts`
- `apps/desktop/src/main/runtime-orchestration.test.ts`
- `apps/desktop/src/main/index.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

None.

The reviewer confirmed the owned sidecar shutdown sequence attempts authenticated `POST /cw/v1/system/shutdown`, treats only HTTP 202 as accepted, waits for sidecar exit before skipping signal fallback, and preserves caller signal fallback on non-202 or exit timeout. The reused existing runtime boundary remains unchanged.

After the first PASS, the implementation was tightened to handle a shutdown request that throws before returning a Promise. A focused test now covers synchronous shutdown request failure falling back to the caller-provided signal. The incremental review of that delta also returned PASS.

## Reviewer Commands

- `pnpm --filter @cw/desktop run typecheck` - PASS
- `pnpm --filter @cw/desktop run test` - PASS, 109 tests passed after the request-failure fallback coverage was added
- Supplemental `git diff --check` over the requested files - PASS, with only line-ending normalization warnings

## Residual Risks

- Coverage remains module-level; this does not yet verify real Electron app quit/reload lifecycle.
- No live FastAPI sidecar shutdown verification is included in this slice.
- Reused existing runtimes are still not stopped by this owned orchestration session.
- Signal fallback keeps the existing behavior of cleaning ownership immediately after `kill()` rather than waiting for a post-signal process exit.
