# M1.5.142 Runtime Stream Visual Smoke Matrix Invalid URL Source Review

## Scope

W1.5.142 closes the residual explicit regression gaps from W1.5.141 by adding
test-only coverage for invalid URL source variants in the dev-only Runtime
Stream visual smoke matrix. The runner behavior is unchanged.

Files reviewed:

- `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
- `docs/04_runbook/m1.5-progress.md`

Out of scope:

- Production runtime/session/store/React shell behavior.
- Accepted spec / ADR / schema / generated schema changes.
- Dependency, runtime API, IPC / `window.cw`, renderer storage, React Flow,
  Tailwind, packaging/updater changes.
- Raw payload, `rawData`, metadata object/key/value display.

## Independent Review

Reviewer: Lorentz (`019efa5d-df61-7c31-9c79-fb3e910d8d5e`)

Initial result: FAIL with one P3 documentation finding.

- P3: the runbook marked W1.5.142 as done before the full planned gate results
  and this review artifact had been recorded. The test implementation itself
  had no blocker/P1/P2 findings.

Fix applied:

- Added this review artifact.
- Updated the runbook after closure gates completed, including review result
  and full verification evidence.

Final result: PASS after documentation closure; no blocker/P1/P2 findings were
reported for the test changes.

Lorentz confirmed:

- The invalid legacy-only URL case exercises the intended source.
- The invalid matrix URL with valid legacy fallback case proves matrix URL
  precedence does not silently fall back to legacy.
- `runMatrix()` clears inherited parent URL env before setting the requested
  mode.
- The new assertions prove fail-fast before fake Electron by requiring empty
  stdout and no `matrix.json`.

## Verification

- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
  PASS: 12/12 tests.
- `pnpm --filter @cw/desktop run test` PASS: 282/282 tests.

Additional closure gates are tracked in `docs/04_runbook/m1.5-progress.md`.
