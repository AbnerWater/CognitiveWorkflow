# M1.4.51 Project Config API Reads Review

## Scope

- Slice: W1.4.51 Project config API reads.
- Reviewed files:
  - `apps/runtime/src/cw_runtime/api/app.py`
  - `apps/runtime/tests/test_m1_3_2_project_endpoints.py`
  - `docs/04_runbook/m1.4-progress.md`
- Review mode: independent read-only subagent review and re-review after repair.

## Result

PASS after repair.

## Findings

- Initial review found a P2 spec-boundary issue: malformed project config JSON was returned as `RH_RUN_DIR_CORRUPTED`, but that error code is locked to corrupted `runs/<run_id>/` critical files in `specs/runtime_harness.md`.
- The repair changed missing and malformed project config resources to return existing API-level `RES_NOT_FOUND`, avoiding a new or mis-scoped manifest corruption error code.

## Evidence

- The three accepted GET routes are registered under `/cw/v1/projects/{project_id}/skills`, `/cw/v1/projects/{project_id}/mcps`, and `/cw/v1/projects/{project_id}/adapters`.
- The endpoints read only the corresponding `.agent-workflow/*.config.json` manifest files for projects registered in the current runtime process.
- Auth, Origin guard, and security headers continue to flow through the shared `/cw/v1` middleware.
- Tests cover successful reads, unknown project, missing config file, and malformed config JSON.

## Boundary Check

- No accepted spec, ADR, public schema, or dependency file was modified.
- This slice does not implement PATCH enable/disable/version/connect, manifest schema canonicalization, MCP/provider startup, secure-store reads, `secret_ref` resolution, AdapterRegistry wiring, or real-provider E2E.
