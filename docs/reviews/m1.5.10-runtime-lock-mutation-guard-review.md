# M1.5.10 Runtime Lock Mutation Guard Review

Date: 2026-06-20
Reviewer: independent subagent `Franklin`
Result: PASS

## Scope Reviewed

- `apps/desktop/src/main/runtime-lock.ts`
- `apps/desktop/src/main/runtime-lock.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking findings.

## Confirmed Alignment

- No accepted spec or ADR files were modified.
- No production dependency or lockfile change was introduced.
- No Electron import, `ipcMain` registration, `BrowserWindow` creation, renderer UI, sidecar start, EventSource/SSE client, or token/port persistence was introduced.
- `acquireRuntimeLock()` validates `staleMs` and passes it to stale lock cleanup, lease release, and mutation guard acquisition.
- Fresh `.mutation` guard files are not removed; callers continue waiting until timeout.
- Stale `.mutation` guard files are removed only after parsing succeeds, the age exceeds the stale threshold, and a second read confirms the guard content did not change.
- Corrupt and unreadable `.mutation` guard files fail closed.
- The runbook describes W1.5.10 as a recovery improvement for the existing skeleton and does not claim native `flock` / `LockFileEx` completion.

## Verification

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

Observed result: PASS. Desktop tests reported 61 passed during independent review. After review, corrupt and unreadable mutation guard direct tests were added and the desktop suite reported 63 passed.

## Residual Risks

- This remains an exclusive-create + `.mutation` guard skeleton, not a native cross-platform file-lock implementation.
- Real Electron multi-window lifecycle behavior and existing sidecar reuse remain in later slices.
