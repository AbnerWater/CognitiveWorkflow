# M1.5.153 Review - Desktop Chat Local Send Visual Smoke

## Scope

- Slice: W1.5.153 Desktop Chat Box local send visual smoke.
- Files reviewed:
  - `apps/desktop/src/renderer/runtime-workbench-shell-visual-smoke.tsx`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-preflight.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`

## Checklist

- `chatBox=enabled` is dev-only visual smoke fixture state.
- Default visual smoke route keeps the existing disabled chat guard coverage.
- New JSON evidence records only metadata/count/sequence fields for local submissions.
- New helper checks local submission text and attributes for raw draft leakage.
- No runtime API, IPC, storage, persistence, schema, generated schema, dependency, React Flow, Tailwind, packaging, or updater changes.

## Result

Helmholtz PASS: no blocker/P1/P2/P3 found.

The review confirmed the diff is limited to the dev visual-smoke fixture,
preflight, runner, tests, and runbook. Default visual-smoke URLs still map
missing or unknown `chatBox` query values to `disabled`, while
`chatBox=enabled` is only consumed by the dev visual-smoke fixture.

The enabled runner path exercises local Send, capped recent history, Clear
history, and resend sequencing. The generated enabled JSON showed
`failures=[]`, first local sequence `1`, history ids `4,3,2`, local section
absent after clear, and resend sequence `5`.

Boundary note: the full visual-smoke JSON still includes pre-existing
`chatDraftMetrics` draft-preview data from earlier Chat Box draft-preview
coverage. W1.5.153 only claims the new local submission/history evidence is
metadata-only.

## Reviewer Commands

- `git diff --check` PASS, only LF-to-CRLF warnings.
- `node --test apps\desktop\scripts\runtime-workbench-visual-smoke.test.cjs`
  PASS, 11/11 tests.
- `node --check apps\desktop\scripts\runtime-workbench-visual-smoke.cjs` PASS.
- `pnpm --filter @cw/desktop run visual-smoke` against
  `http://127.0.0.1:5174/visual-smoke.html?chatBox=enabled` PASS with
  `failures=[]`.
- `pnpm --filter @cw/desktop run visual-smoke` against
  `http://127.0.0.1:5174/visual-smoke.html` PASS with `failures=[]`.
