# M1.5.35 Shutdown Status IPC Contract Review

Date: 2026-06-21

Reviewer: Euler (`019ee621-2dca-7d02-a207-45b2a33c3be1`)

Verdict: PASS

## Scope

- `apps/desktop/src/shared/runtime-ipc.ts`
- `apps/desktop/src/shared/runtime-ipc.test.ts`
- `apps/desktop/src/preload/contract.ts`
- `apps/desktop/src/preload/runtime-bridge.ts`
- `apps/desktop/src/preload/api.ts`
- `apps/desktop/src/preload/contract.test.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`
- `apps/desktop/src/main/bootstrap.ts`
- `apps/desktop/src/main/index.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No findings.

## Review Notes

- The diff is limited to pure TypeScript shared IPC / preload contract work and the W1.5 runbook.
- No package, lockfile, accepted spec, ADR, schema, codegen output, or production dependency changes were found.
- No Electron import, real `ipcMain` / `ipcRenderer`, `BrowserWindow`, renderer UI, cross-window broadcast, SSE, or StreamEvent wiring was found.
- `RUNTIME_IPC_CHANNELS` and `RuntimeIpcChannel` include `cw:runtime:shutdown-status` while preserving the existing channel order.
- `bootstrap.ts` reuses shared shutdown status types for main shutdown status, reducing main/preload drift.
- The main factory default shutdown-status registration returns `[]`, provider responses are copied, and querying shutdown status does not trigger runtime startup.
- Preload `shutdownStatus()` maps to the new channel and returns copied status objects.
- The runbook states the correct boundary and avoids claiming real Electron, renderer UI, broadcast, SSE, or StreamEvent behavior.

## Reviewer Commands

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

Reviewer results:

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 123 passed
- `git diff --check`: PASS, with only LF-to-CRLF normalization warnings
- Targeted package/lock/spec/ADR/schema/codegen diff check: PASS, no output

## Residual Risks

- This remains contract-level coverage only.
- Real Electron handler installation, real app/window lifecycle wiring, cross-window broadcast, renderer UI, renderer reload / beforeunload, live FastAPI sidecar shutdown, and shutdown StreamEvent verification remain future wiring risks.
