# M1.5.163 Review - Desktop Chat Expand Focus Visual Smoke Matrix

## Scope

- Slice: W1.5.163 Desktop Chat Box expand focus visual smoke matrix evidence.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`
  - `docs/reviews/m1.5.163-desktop-chat-expand-focus-visual-smoke-matrix-review.md`

## Checklist

- `chat-enabled-desktop` matrix case fails if enabled Chat Box focus evidence is missing or not focused after expand.
- Matrix manifest records only metadata-only `chatFocusEvidence` for expanded/focused/count values.
- Matrix manifest and failure summaries do not persist raw draft text, preview text, local submission text, raw child failures, query/hash secrets, or output directory paths.
- Changes remain dev-only matrix/test/docs and do not introduce production runtime, API, IPC/window.cw, renderer storage, dependency, accepted spec, schema, generated schema, or ADR drift.

## Result

Maxwell independent review found no P0/P1/P2 implementation issues.

- P3: runbook/review docs still marked the slice as in-progress/pending while implementation and parent verification had already completed. Fixed by recording the W1.5.163 verification results, known format-check baseline, and this review outcome.
- P3: matrix tests only had positive focus evidence coverage. Fixed by adding `visual smoke matrix rejects unfocused enabled chat evidence`, which proves `chat-enabled-desktop` fails when `chatDraftInputFocused=false` while preserving raw draft/query/hash/output-dir redaction.

Final status after remediation: PASS.

## Reviewer Commands

- Parent verification already run before review:
  - `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
  - `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
  - `pnpm --filter @cw/desktop run typecheck`
  - `pnpm --filter @cw/desktop run test`
  - Electron visual smoke matrix with `chat-enabled-desktop`
  - targeted Prettier check for touched files
  - `.\dev.ps1 lint`
  - `.\dev.ps1 codegen`
  - `.\dev.ps1 typecheck`
  - `.\dev.ps1 test`
  - `.\dev.ps1 format-check`
  - `git diff --check`

## Parent Verification Summary

- Matrix runner syntax PASS.
- Matrix focused tests PASS: 14 tests passed after negative focus-evidence remediation.
- Focused desktop typecheck/test PASS.
- Real visual smoke matrix PASS: 7 cases, `chat-enabled-desktop` captured metadata-only `chatFocusEvidence` with expanded=true, draft input count=1, and focused-after-expand=true.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS with no generated diff.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 475 passed / 1 skipped; desktop package test was 301 passed before remediation and 302 passed after adding the negative matrix test.
- `.\dev.ps1 format-check` FAIL only on standing 39-file repo-wide Prettier baseline; touched files passed targeted Prettier.
- `git diff --check` PASS with only Git CRLF warnings.

## Remediation Verification

- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs` PASS after adding the negative focus-evidence fixture: 14 tests passed.
- `pnpm --filter @cw/desktop run test` PASS after remediation: desktop 302 passed.
- Final targeted Prettier check PASS after documentation closure.
- Final `git diff --check` PASS with only Git CRLF warnings.
