# W1.5.201 Project Reference Runtime Follow-up Review

Status: PASS
Date: 2026-06-27
Reviewer: Confucius independent subagent (`019f0994-9fa3-7f61-b4b2-e5f1d0170699`)

## Scope Reviewed

- `apps/desktop/src/renderer/runtime-workbench-interaction.ts`.
- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`.
- `apps/desktop/src/renderer/runtime-workbench-shortcuts.ts`.
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`.
- W1.5.201 runbook updates in `docs/04_runbook/`.
- Related A4 manifest, capture execution, review package, decision handoff, evidence map, checklist, readiness ledger, and repair-plan updates.

## Findings

Initial review found two issues; both were repaired and re-reviewed.

- P2: W1.5.201 evidence artifacts referenced this review file before it existed, leaving the FR-011 evidence trace incomplete.
- P3: The React `Create + import` handler cleared the selected initial reference file after any resolved dispatch, including failed project creation or failed reference import snapshots.

## Repairs Applied

- Added this review artifact so FR-011 evidence references resolve.
- Changed `Create + import` cleanup to run only when returned shell snapshot shows `projectCreation.status="succeeded"`, `referenceManagement.status="succeeded"`, and a non-null `lastReferenceId`.
- Added a React shell regression that preserves the selected initial reference file when combined creation returns a failed snapshot.

## Re-review Notes

- Confucius re-review PASS; no remaining findings.
- The review artifact exists and closes the FR-011 evidence trace.
- `Create + import` clears the selected file only after project creation succeeds, reference import succeeds, and `lastReferenceId` is non-null.
- Regression coverage verifies both the success cleanup path and the failed combined-creation preservation path.
- FR-011 moves to `runtime_bridge_needs_a4_review` but remains unaccepted.
- A4 package/capture/decision artifacts now contain 9 pending items, 6 runtime bridge items, and 3 excluded partial runtime bridge items.
- `accepted_items=0` and `EXIT-P1-1=not_ready` remain locked.
- No spec, ADR, schema, generated schema, dependency, packaging, updater, or baseline docx change is included.
- The standing unrelated `CLAUDE.md` worktree change remains excluded from this slice.

## Verification Evidence

- `pnpm --filter @cw/desktop build` PASS.
- `node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js` PASS: 62 passed after the failure-preservation regression.
- Focused runbook validators PASS for runtime-flow repair plan, A4 manifest, A4 capture execution, A4 review package, and A4 decision handoff.
- Grouped runbook script tests PASS: 50 passed after repair.
- `pnpm --filter @cw/desktop test` PASS: 440 passed after repair.
- `uv run pytest apps/runtime/tests/test_m1_3_2_project_endpoints.py -q` PASS.
- `git diff --check` PASS with Git CRLF warnings only after repair.

## Residual Risk

W1.5.201 provides runtime bridge evidence only. FR-011 still requires separate A4 UX acceptance review before any acceptance or phase-exit readiness claim.
