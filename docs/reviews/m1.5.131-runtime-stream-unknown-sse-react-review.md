# M1.5.131 Runtime Stream Unknown SSE React Review

## Scope

W1.5.131 is a test-only coverage slice for the Runtime Stream unknown-event path. It verifies that a real React shell session, backed by `createRuntimeFetchEventSourceFactory()` and a loopback HTTP SSE server, ingests an SSE event whose `type` is outside the current frontend known-event table and renders it as an unknown event without leaking raw payload or metadata values.

Files reviewed:

- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`

Out of scope:

- Production logic changes.
- Accepted spec / ADR / schema / generated schema changes.
- Dependency, runtime API, IPC / `window.cw`, renderer storage, React Flow, Tailwind, packaging/updater changes.
- Raw payload, `rawData`, metadata object/key/value display.

## Independent Review

Reviewer: Dewey (`019ef7c7-9ef1-7dd0-aa05-d6a7358f7f1d`)

Result: PASS, no findings.

Key review points:

- The new test uses real loopback SSE coverage rather than static store injection.
- The event fixture uses unknown `adapter.experimental_event` and exercises the React shell + fetch-backed session path.
- The UI asserts unknown badge/type status and confirms raw payload/metadata values are not rendered.
- Existing known loopback/full-reload coverage remains intact.
- Diff boundary is limited to test coverage, with no production/spec/generated/dependency/API/IPC/storage drift.

## Verification

- `pnpm exec prettier --write apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `pnpm --filter @cw/desktop run typecheck`
- `pnpm --filter @cw/desktop run build:ts`
- `node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js`
- `pnpm --filter @cw/desktop run test`
- `.\dev.ps1 lint`
- `.\dev.ps1 codegen`
- `git diff --check`

Additional closure gates are tracked in `docs/04_runbook/m1.5-progress.md`.
