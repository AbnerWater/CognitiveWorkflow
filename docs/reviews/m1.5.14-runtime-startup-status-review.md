# M1.5.14 Runtime Startup Status Review

Date: 2026-06-20
Reviewer: independent subagent `Mill`
Result: PASS

## Scope Reviewed

- `apps/desktop/src/main/runtime-startup-status.ts`
- `apps/desktop/src/main/runtime-lifecycle.ts`
- `apps/desktop/src/main/runtime-lifecycle.test.ts`
- `apps/desktop/src/main/index.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking findings.

## Evidence

- `RuntimeStartupStatus` stays narrow: it carries status kind, lifecycle action, attempt, lock status, severity, message, lifecycle flags, and optional reason; it does not carry token or `base_url`.
- Mapping covers `start_sidecar`, `cleanup_then_start`, `reuse_existing`, `wait_for_existing`, `timeout_waiting_for_existing`, and `block_startup`.
- `onStatus` is optional and emitted only from startup lifecycle decision/transition paths.
- Tests cover start, wait/reuse, timeout, block, and cleanup snapshot cases.
- No dependency file diff was present in `package.json`, `apps/desktop/package.json`, or `pnpm-lock.yaml`.
- Targeted scans found no Electron import, `ipcMain`, `BrowserWindow`, renderer UI, storage API, or EventSource/SSE client in scoped TypeScript files.

## Commands

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 78/78 tests
- `git diff --check`: PASS, LF/CRLF warnings only
- Targeted forbidden-surface and token/base_url scans: PASS

## Residual Risk

This remains module-level startup status mapping. Real Electron lifecycle registry wiring, IPC handler registration, renderer UI, cross-window broadcast, EventSource/SSE client behavior, and project-root canonicalization remain future slices.
