# M1.5.26 Main Bootstrap Adapter Review

Date: 2026-06-20

Reviewer: independent Codex subagent `Poincare` (`019ee504-7eab-7093-b7ab-9b6b9bb7c596`)

Result: PASS

## Findings

None.

## Scope Checked

- Changed files stayed within the intended W1.5.26 boundary: `apps/desktop/src/main/bootstrap.ts`, `apps/desktop/src/main/index.ts`, `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`, and `docs/04_runbook/m1.5-progress.md`.
- No package manifest, accepted spec, ADR, schema, renderer UI, or generated schema output changed.
- No Electron import, real `ipcMain`, real `BrowserWindow`, app lifecycle, `localStorage`, or `sessionStorage` usage was introduced.
- `bootstrap.ts` defines the injected Electron-like `ipcMain.handle` interface.
- `installRuntimeIpcMainHandlers()` creates `RuntimeIpcStartupHandlers` without eager runtime startup, then registers existing runtime IPC registrations.
- `registerRuntimeIpcMainChannelRegistrations()` registers each channel through injected `handle()`, adapting connection / startup-status handlers without payload and fetch with payload forwarding.
- `index.ts` exports the install / register API and public injected types for a future real Electron main entry.
- Tests cover stable channel registration, no eager startup, startup-status no-start behavior, connection-info lazy startup, fetch payload forwarding, and reserved header fail-closed behavior.

## Commands Run By Reviewer

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

All commands passed; desktop tests reported 103 passed, 0 failed. The reviewer also checked the untracked `apps/desktop/src/main/bootstrap.ts` with a no-index diff and found no whitespace errors.

## Residual Risks

W1.5.26 remains a pure injected TypeScript adapter. It does not validate real Electron `ipcMain.handle`, `BrowserWindow`, app lifecycle, packaged main/preload loading, renderer integration, cross-window broadcast, or `system.runtime_ready` emission.
