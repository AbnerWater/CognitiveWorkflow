# M1.5.49 Renderer Stream Full Reload Reset Review

- Reviewer: Sagan (`019ee770-5c7c-7a62-aff8-83eb7eeef29b`)
- Scope: W1.5.49 renderer pure TypeScript full reload reset latch
- Result: PASS

## Scope Reviewed

- `apps/desktop/src/renderer/runtime-stream-store.ts`
- `apps/desktop/src/renderer/runtime-stream-session.ts`
- `apps/desktop/src/preload/contract.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No findings.

## Verification

Sagan independently ran:

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
```

Result: both commands passed; desktop test reported 156 passed.

## Review Notes

- `Last-Event-ID` replay miss remains mapped to `412` / `SE_SSE_REPLAY_NOT_FOUND` / full reload required semantics.
- `full_reload_required` still blocks direct `start()` and does not create a new client.
- `resetFullReloadRequired()` only clears the terminal latch after the UI has completed full reload reconstruction: old events, `totalEvents`, and `fullReloadDecision` are cleared and the store returns to `idle`.
- Session reset updates the interaction read baseline through `markAllRead()`, so a fresh stream after reset reports new unread events correctly.
- No production dependency, Electron/React import, storage API, accepted spec/ADR/schema, or codegen behavior change was introduced.

## Residual Boundary

This slice is still a pure TypeScript lifecycle latch. It does not implement real Electron/browser EventSource behavior, live FastAPI SSE replay, React full reload UI rebuild, cross-window stream sharing, or runtime StreamEvent schema validation.
