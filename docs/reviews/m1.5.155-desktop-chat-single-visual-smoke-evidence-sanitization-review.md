# M1.5.155 Review - Desktop Chat Single Visual Smoke Evidence Sanitization

## Scope

- Slice: W1.5.155 Desktop Chat Box single visual smoke evidence sanitization.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-evidence.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`

## Checklist

- Single visual smoke can still use raw draft text in-memory for runtime assertions.
- Persisted `.json` evidence removes raw/freeform Chat Box text fields.
- Persisted failures/messages redact the draft text fragments used by the current smoke run.
- Matrix local evidence remains metadata-only and keeps sequence/count/status/intent/action/history summaries.
- No production runtime/session/store/React/API/IPC/storage/dependency/spec/schema drift.

## Result

Euclid independent review PASS: no blocker/P1/P2/P3 findings.

The review confirmed the diff stays in the intended dev-only surface. The sanitizer removes the four persisted Chat Box text fields, recursively redacts known draft fragments, and the runner records each draft through `inputChatDraft()` before final sanitized JSON write. Matrix-required metadata remains available because sequence/count/status/intent/target/action/characters/words/history ids/statuses/clear fields are not in the removal set. Focused unit coverage checks field removal, redaction, metadata preservation, and non-mutation.

Residual risk is limited to future new chat freeform evidence fields, which must be added to `CHAT_TEXT_EVIDENCE_FIELDS`. Early hard exceptions before final evidence serialization remain outside this persisted JSON sanitization scope.

## Reviewer Commands

- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-evidence.cjs` PASS.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs` PASS.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs` PASS.
- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs` PASS, 12/12 tests.
- Electron enabled Chat Box visual smoke PASS, persisted JSON had `failures=[]`, no raw draft fragments, no persisted chat text fields, and retained first sequence/history/resend metadata.
- Electron visual smoke matrix PASS, 7 cases, manifest `failures=[]`, no raw draft fragments, and retained enabled-chat local evidence metadata.
- `pnpm --filter @cw/desktop run test` PASS, 298/298 tests.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS, generated content had no diff.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS, Python 475 passed / 1 skipped and desktop 298 passed.
- Targeted Prettier check PASS.
- `.\dev.ps1 format-check`: ruff PASS; Prettier still fails on the existing 38-file repo-wide baseline debt.
- `git diff --check` PASS, only LF-to-CRLF warnings.
