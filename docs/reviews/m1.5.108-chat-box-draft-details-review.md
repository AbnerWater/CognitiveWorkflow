# W1.5.108 Chat Box Draft Details Review

Date: 2026-06-23

Reviewer: Lovelace (`019ef479-b2cb-79d3-b48f-e4a9b69d1c68`)

Scope reviewed:

- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`

## Findings

No blocker / P1 / P2 / P3 findings were reported.

Lovelace confirmed:

- The diff stays scoped to the expected desktop scaffold files before closure documentation was added.
- No package / lockfile / dependency / spec / ADR / schema / generated schema drift was found.
- No renderer `window.cw`, storage API, IPC, Electron import, Node fs, persistence, controller, or store drift was found.

## Validation Evidence

Owner-run validation:

- `pnpm exec prettier --write apps/desktop/src/renderer/runtime-workbench-shell-react.tsx apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx apps/desktop/src/renderer/runtime-workbench-shell.css apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`: PASS.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`: PASS.
- `git diff --check`: PASS, only LF-to-CRLF warnings.
- `pnpm --filter @cw/desktop run build`: PASS.
- `node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js`: PASS, 19 tests.
- `pnpm --filter @cw/desktop run test`: PASS, 256 tests.
- Electron visual smoke: desktop 1280x720 PASS, mobile 390x844 PASS, mobile scrollY 900 / 1180 / 1440 PASS. Parallel mobile smoke emitted known Windows Electron GPU cache warnings but exited 0.

Chat Draft metrics from the owner desktop smoke JSON:

- Input: value `Review repair plan now`, length `22`, words `4`, status `Idle`, send-enabled `false`, Send button disabled `true`.
- Clear: value empty, length `0`, words `0`, status `Idle`, send-enabled `false`, Send button disabled `true`.

Reviewer-run validation:

- `git diff --check`: PASS, only LF-to-CRLF warnings.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`: PASS.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js`: PASS, 19 tests.
- Renderer forbidden API search on touched renderer files: no matches.

The reviewer did not rerun the build because the review was read-only and `pnpm --filter @cw/desktop run build` writes `dist/`; owner build evidence covered that path.
