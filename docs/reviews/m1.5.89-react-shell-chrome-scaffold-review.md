# W1.5.89 React Shell Chrome Scaffold Review

## Scope

Review target: W1.5.89 desktop React shell chrome scaffold.

- `apps/desktop/src/renderer/runtime-workbench-shell-presenter.ts`
- `apps/desktop/src/renderer/runtime-workbench-shell-adapter.ts`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/src/renderer/runtime-workbench-shell-visual-smoke.tsx`
- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/src/preload/contract.test.ts`
- `docs/04_runbook/m1.5-progress.md`

This review checks the no-new-dependency shell chrome scaffold only. It does not approve React Flow canvas, file tree, real workflow graph editing, chat command routing, Task Drawer mutation commands, snapshot/versioning, packaging/updater, native menu, multi-window work, spec changes, schema changes, ADR changes, generated schema changes, or IPC changes.

## Reviewer

Independent subagent Dalton (`019ef1bc-3046-75d2-a9ae-51c6ca091114`).

## Findings

No findings.

The review did not identify dependency/spec/ADR/schema/codegen/IPC drift, presenter DTO leakage, React access to host/session/controller/store internals, Dock overclaiming, obvious CSS overflow, weak visual smoke invariants, or inaccurate W1.5.89 runbook claims in the reviewed diff.

## Residual Risk

- Visual smoke checks viewport sizing, shell chrome presence, console warning/error messages, horizontal overflow, selected lifecycle state, and key DOM counts, but it does not do pixel comparison or full element-overlap detection.
- Canvas, Tasks, and Chat Box remain scaffold surfaces only. Real canvas editing, task mutation, and chat command routing need later dependency-gated slices.

## Accepted Checks

- Drift: no dependency, spec, ADR, schema, generated schema, or IPC contract diff was introduced.
- DTO boundary: shell chrome data is presenter-derived from the existing workbench snapshot and frozen before React consumes it.
- Adapter isolation: the adapter deep-clones and freezes the chrome DTO before exposing it through the external-store contract.
- React boundary: React renders the chrome DTO and routes Dock panel changes through the existing `setActivePanel` interaction path.
- Scaffold boundary: Canvas and Tasks Dock items are visible but disabled; Chat Box is visible but disabled and does not claim command routing.
- Responsive evidence: desktop and mobile smoke captures reported `horizontalOverflow: 0`, no page warning/error messages, and expected Dock / Task Drawer / Chat Box DOM counts.

## Verification Evidence

- `pnpm exec prettier --check apps/desktop/src/renderer/runtime-workbench-shell-presenter.ts apps/desktop/src/renderer/runtime-workbench-shell-adapter.ts apps/desktop/src/renderer/runtime-workbench-shell-react.tsx apps/desktop/src/renderer/runtime-workbench-shell.css apps/desktop/src/renderer/runtime-workbench-shell-visual-smoke.tsx apps/desktop/scripts/runtime-workbench-visual-smoke.cjs apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx apps/desktop/src/preload/contract.test.ts docs/04_runbook/m1.5-progress.md`
- `pnpm --filter @cw/desktop run typecheck`
- `pnpm --filter @cw/desktop run test`
- `pnpm --filter @cw/desktop run visual-smoke` for:
  - `%TEMP%\cw-w1-5-89-desktop-1280.png`
  - `%TEMP%\cw-w1-5-89-mobile-rerun.png`
  - `%TEMP%\cw-w1-5-89-mobile-timeline-rerun.png`
  - `%TEMP%\cw-w1-5-89-mobile-drawer-chat.png`
- `.\dev.ps1 lint`
- `.\dev.ps1 typecheck`
- `.\dev.ps1 test`
- `.\dev.ps1 codegen`
- `.\dev.ps1 format-check` remains blocked only by the existing 38-file repo-wide Prettier baseline debt; touched files pass targeted Prettier.
- `git diff --check`
