# W1.5.185 Version Snapshot Bridge Review

## Scope

- Slice: W1.5.185 Desktop Version Snapshot bridge.
- Reviewed files: desktop workbench session / interaction / host / presenter / React shell / shortcuts / visual smoke fixture / preload contract tests / runbook.
- Contract boundary: `POST /cw/v1/workflows/{workflow_id}/snapshot` only.

## Independent Review

- Reviewer: Russell (`019eff81-10f2-7ae0-9e77-54fda5057cc2`).
- Result: initial review found one P2 test gap, no P0/P1.

### Findings

- P2: workflow snapshot failure paths were implemented but not directly covered by focused tests. Missing cases were invalid workflow id/no fetch, runtime unavailable, non-OK response body redaction, and malformed successful response with `blockedReason="response_invalid"`.

## Fixes

- Added focused React shell session tests for invalid workflow id, missing runtime bridge, non-OK response body redaction, and invalid successful snapshot response.
- Confirmed retained `versionSnapshot` state only includes status, method, path, workflow id, snapshot id, commit sha, created_at, statusCode, blockedReason, and capability flag.

## Verification

- `pnpm --filter @cw/desktop exec tsc --noEmit` PASS.
- `pnpm --filter @cw/desktop run build:ts && node --test apps/desktop/dist/renderer/runtime-workbench-shell-react.test.js apps/desktop/dist/preload/contract.test.js` PASS: focused desktop suites 152 passed after P2 fix.
- `pnpm --filter @cw/desktop run test` PASS: desktop 400 passed after P2 fix.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 482 passed / 1 skipped; desktop 400 passed.
- `.\dev.ps1 codegen` PASS; `packages/schemas-ts/src/generated/**` had no content diff.
- `pnpm exec prettier --check` touched files PASS.
- `.\dev.ps1 format-check`: ruff format check PASS; repo-wide Prettier still fails on existing 39-file baseline debt; W1.5.185 touched files were not in the warning list.
- `git diff --check` PASS with Git CRLF warnings only.

## Residual Risk

- Restore-to-snapshot and continue execution remain unimplemented because accepted API/spec material does not define a restore-to-snapshot HTTP contract in this slice.
- Full phase exit evidence for FR-015 still requires runtime/Git-backed snapshot semantics beyond this explicit dispatch surface.
