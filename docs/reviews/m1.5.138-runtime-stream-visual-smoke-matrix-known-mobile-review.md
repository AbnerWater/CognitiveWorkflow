# M1.5.138 Runtime Stream Visual Smoke Matrix Known Mobile Review

## Scope

W1.5.138 adds `known-mobile` coverage to the dev-only Runtime Stream visual smoke matrix. Earlier slices covered known desktop, unknown desktop/mobile/scroll, failure-manifest hardening, and fake success-path regression coverage; this slice proves that the spec-known `model.text_delta` path is also part of the mobile responsive matrix.

Files reviewed:

- `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.cjs`
- `apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs`
- `docs/04_runbook/m1.5-progress.md`

Out of scope:

- Production runtime/session/store/React shell behavior.
- Accepted spec / ADR / schema / generated schema changes.
- Dependency, runtime API, IPC / `window.cw`, renderer storage, React Flow, Tailwind, packaging/updater changes.
- Raw payload, `rawData`, metadata object/key/value display.

## Independent Review

Reviewer: Poincare (`019efa1b-88e8-75b1-b775-4f1a12408702`)

Initial result: P3 finding.

Finding:

- P3: the first review file revision was still a pending template. Reviewer, initial result, final result, and verification were not recorded even though the implementation had been reviewed.

Resolution:

- This report now records the reviewer, P3 finding, implementation checks, and verification results.

Final result: PASS, no blocker/P1/P2 findings. P3 resolved in this report.

Poincare confirmed:

- The matrix now contains `known-mobile` in addition to existing known/unknown cases.
- The fake success-path test proves `known-mobile` has 390x844 requested and observed viewport evidence with `failures=[]`.
- Existing failure-manifest regressions now cover all 6 cases.
- The diff is limited to dev-only visual smoke automation, tests, and documentation.

## Verification

- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke-matrix.test.cjs` PASS: 4/4 tests.
- `pnpm --filter @cw/desktop run test` PASS: 274/274 tests.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS with no generated diff.
- `.\dev.ps1 typecheck` PASS after serial rerun. The first run overlapped with codegen and hit a transient generated-file race.
- `.\dev.ps1 test` PASS: Python 475 passed / 1 skipped; desktop 274/274 tests.
- Targeted Prettier check PASS for touched files.
- `git diff --check` PASS with Git CRLF warnings only.

Additional closure gates are tracked in `docs/04_runbook/m1.5-progress.md`.
