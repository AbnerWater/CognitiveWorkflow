# W1.5.174 Desktop UX Gap Repair Plan Review

## Scope

W1.5.174 adds a machine-readable repair plan for Desktop UX acceptance gaps. It converts the W1.5.173 FR evidence map into six repair tracks: A4 candidate evidence, local UI evidence, runtime-backed desktop flows, backend-only desktop bridge work, dependency-gated React Flow Canvas work, and missing reference-management implementation.

The slice is limited to the repair plan artifact, the plan regression test, desktop package test wiring, and runbook/review documentation. It does not modify the baseline UIUX docx, install dependencies, implement missing FR behavior, or change production runtime, IPC, storage, schema, spec, ADR, packaging, updater, or React shell behavior.

## Verification

- `node --test apps/desktop/scripts/m1-5-ux-gap-repair-plan.test.cjs`: PASS, 5 tests.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `pnpm --filter @cw/desktop run test`: PASS, 333 tests.
- `.\dev.ps1 lint`: PASS.
- `.\dev.ps1 codegen`: PASS, no generated schema content diff remained.
- `.\dev.ps1 typecheck`: PASS.
- `.\dev.ps1 test`: PASS, Python 475 passed / 1 skipped and desktop 333 passed.
- `pnpm exec prettier --check apps/desktop/package.json apps/desktop/scripts/m1-5-ux-gap-repair-plan.test.cjs docs/04_runbook/m1.5-ux-gap-repair-plan.json docs/04_runbook/m1.5-progress.md docs/reviews/m1.5.174-desktop-ux-gap-repair-plan-review.md`: PASS.
- `.\dev.ps1 format-check`: ruff format check PASS; Prettier still fails on the standing 39-file repo-wide baseline debt. W1.5.174 touched files pass targeted Prettier.
- `git diff --check`: PASS with only Windows LF/CRLF warnings.

## Findings

- Archimedes (`019efdbf-c113-7ae1-b7d4-3ffc70b548a4`) independent review: PASS with no findings.
- The review confirmed there is no A4 or `EXIT-P1-1` overclaim, FR-001 through FR-020 are assigned exactly once, every repair track matches the W1.5.173 `acceptance_readiness` bucket, and `TRACK-REACT-FLOW-DEPENDENCY` remains limited to FR-004 and FR-019 behind `DEP-REACT-FLOW`.
- The review confirmed `apps/desktop/package.json` only wires the new test into the existing test command and that the scoped diff does not touch dependencies, spec, ADR, schema, generated schema, runtime, or production renderer behavior.
- Initial residual risk: the focused dependency-boundary test covered the main forbidden package names but not every proposed package from W1.5.171, such as `@electron-forge/plugin-vite`, `postcss`, and `autoprefixer`.
- Resolution: `apps/desktop/scripts/m1-5-ux-gap-repair-plan.test.cjs` now dynamically enumerates every `dependency_gates[*].proposed_packages[*].name` from `docs/04_runbook/m1.5-dependency-gate-proposal.json` and asserts none are installed. Focused test re-ran PASS with 5 tests. Archimedes delta re-review PASS confirmed the coverage gap is closed.
- Residual unrelated worktree change: `CLAUDE.md` remains dirty and is excluded from the W1.5.174 commit.

## Verdict

PASS. W1.5.174 is complete as a conservative UX acceptance gap repair plan. It does not implement missing FR behavior, does not mark any FR accepted, and does not make `EXIT-P1-1` ready.
