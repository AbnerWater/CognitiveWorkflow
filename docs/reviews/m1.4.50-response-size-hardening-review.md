# M1.4.50 Response Size Hardening Review

## Scope

- Slice: W1.4.50 Project MCP response size hardening.
- Reviewed files:
  - `apps/runtime/src/cw_runtime/harness/project.py`
  - `apps/runtime/tests/test_m1_3_2_project_harness.py`
  - `docs/04_runbook/m1.4-progress.md`
- Review mode: independent read-only subagent review.

## Result

PASS.

No blocking or actionable findings were identified.

## Evidence

- The diff is limited to runtime harness code, project harness tests, the M1.4 runbook, and this review note.
- No accepted spec, ADR, public schema, or dependency file was modified.
- Stdio JSON-RPC responses use bounded `readline()` and reject oversized UTF-8 responses before JSON parsing.
- Streamable HTTP SSE still checks each line and now also checks aggregate multi-`data:` event size.
- Legacy HTTP+SSE fallback now checks aggregate multi-`data:` event size before parsing JSON event data.
- New tests cover stdio oversized response, Streamable HTTP SSE split oversized event, and legacy HTTP+SSE split oversized event with a monkeypatched small limit.

## Boundary Check

- This slice is a harness hardening change only.
- It does not implement real external provider compatibility, OAuth refresh/token exchange, AdapterRegistry/API wiring, MCP result schema changes, real provider upstream `cancel()`, or Claude Agent SDK real-provider HITL.

## Residual Risk

- Stdio still uses `text=True` subprocess stdout, so `readline(size)` bounds characters before the explicit UTF-8 byte-size guard. This rejects oversized responses before JSON parsing, but it is not a strict raw-byte memory ceiling.
- Coverage remains fake-provider smoke, not a real external provider compatibility matrix at the default 4 MiB limit.
