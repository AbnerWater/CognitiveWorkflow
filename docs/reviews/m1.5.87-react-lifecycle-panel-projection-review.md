# W1.5.87 React Lifecycle Panel Projection Review

## Scope

Review target: W1.5.87 desktop React lifecycle panel projection.

- `apps/desktop/src/renderer/runtime-workbench-host-session.ts`
- `apps/desktop/src/renderer/runtime-workbench-shell-presenter.ts`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/src/preload/contract.test.ts`
- `docs/04_runbook/m1.5-progress.md`

The review checked that the existing lifecycle panel presenter/view-model/interaction/session chain is projected into the React workbench shell without adding a new IPC surface, dependency, accepted spec change, or privileged renderer capability.

## Independent Review

Reviewer: Tesla (`019ef0b4-f47e-7ee3-b8a0-be837073372d`)

Findings:

- No blocking findings.

Non-blocking gaps:

- The initial runbook claim said the tests covered active lifecycle shell snapshot clone/freeze boundaries, but the first test pass mostly asserted null/shape and React behavior.
- No browser screenshot or visual overlap check was run for the responsive lifecycle panel layout.

Resolution:

- Added an active lifecycle panel fixture in `apps/desktop/src/preload/contract.test.ts` that asserts the shell presenter clones and freezes the active lifecycle interaction snapshot, nested panel/view/timeline arrays, selected timeline item, and command id arrays without exposing controller/adapter/runtime objects.
- The visual screenshot gap remains documented as non-blocking. Static CSS review plus SSR/client React tests cover rendering structure and command dispatch, but not pixel-level layout.

## Verification

- `pnpm --filter @cw/desktop run typecheck`
- `pnpm --filter @cw/desktop run test` (249 passed / 0 skipped)
- `.\dev.ps1 lint`
- `.\dev.ps1 typecheck`
- `.\dev.ps1 test` (Python 475 passed / 1 skipped; desktop 249 passed / 0 skipped)
- `.\dev.ps1 codegen`
- `git diff --check`

Known baseline:

- `.\dev.ps1 format-check` still fails on existing repo-wide Prettier baseline debt. This slice did not broaden that debt; targeted Prettier check passed for touched TypeScript and CSS files.

## Boundary Check

- No accepted spec, ADR, schema, generated schema, package, or lockfile drift.
- No renderer `localStorage` / `sessionStorage`.
- No direct renderer Electron/IPC import.
- No new IPC channel or privileged preload surface.
- No token, runtime port, controller, adapter, or runtime bridge persisted or exposed through shell snapshots.
