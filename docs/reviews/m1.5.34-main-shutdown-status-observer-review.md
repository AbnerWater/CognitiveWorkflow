# M1.5.34 Main Shutdown Status Observer Review

Date: 2026-06-21

Reviewer: Meitner (`019ee609-1f5c-7bc1-bb20-73b0fac7177a`)

Verdict: PASS

## Scope

- `apps/desktop/src/main/bootstrap.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/src/main/runtime-ipc-main-factory.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No findings.

## Review Notes

- The diff is limited to the expected W1.5.34 files.
- No package, lockfile, accepted spec, ADR, schema, codegen output, or production dependency changes were found.
- `installRuntimeMainLifecycleShutdown()` preserves W1.5.33 shared shutdown semantics: app/window events still share one shutdown promise, pending events prevent, terminal/unregistered events do not, and finalizers only retry requested exits.
- Status objects remain pure TypeScript main-process data and add no IPC channel, Electron import, renderer exposure, StreamEvent, or SSE behavior.
- Status payloads contain lifecycle metadata only and no token, `base_url`, prompt text, model output, or other runtime secret material.
- `statusHistory()` returns a new array, and observer exceptions are caught after snapshot/history update without blocking shutdown or requested finalizers.
- Initial residual risk around `shutdown_failed` and `unregistered` status-shape assertions was resolved with test-only additions, then incrementally re-reviewed.

## Reviewer Commands

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

Reviewer results:

- `pnpm --filter @cw/desktop run typecheck`: PASS
- `pnpm --filter @cw/desktop run test`: PASS, 122 passed
- `git diff --check`: PASS, with only LF-to-CRLF normalization warnings
- Targeted package/lock/spec/ADR/schema diff check: PASS, no output

## Residual Risks

- This remains injection-level coverage only.
- Real Electron event ordering, cross-window broadcast, renderer exposure, renderer reload / beforeunload behavior, live FastAPI sidecar shutdown, and shutdown StreamEvent verification remain future wiring risks.
