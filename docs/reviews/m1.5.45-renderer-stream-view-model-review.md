# M1.5.45 Renderer Stream View Model Review

Reviewer: Pauli (`019ee6fc-2d5c-73c2-b0ec-0b193991dd01`)

Verdict: PASS after fix

## Scope Reviewed

- `apps/desktop/src/renderer/runtime-stream-view-model.ts`
- `apps/desktop/src/preload/contract.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

- Initial finding: the view-model category validator accepted `evidence`, inherited from an older renderer client type, but accepted `StreamEvent` spec routes EvidencePack under `context` and does not define an `evidence` category.
- Fix: `runtime-stream-view-model.ts` now defines a local 12-value `RuntimeStreamViewCategory` union that matches `specs/schemas/stream_event.md`.
- Regression coverage: the unsafe-options test asserts `categories: ["evidence"]` is rejected.
- Final review: no remaining findings.

## Verification

- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `pnpm --filter @cw/desktop run test`: PASS, 149 tests.
- `.\dev.ps1 lint`: PASS.
- `.\dev.ps1 typecheck`: PASS.
- `.\dev.ps1 test`: PASS.
- `.\dev.ps1 codegen`: PASS, with no generated schema git diff.
- `git diff --check`: PASS, with only Git LF-to-CRLF working-tree warnings.
- Protected path diff check: PASS, no package, lockfile, accepted spec, ADR, schema, generated schema output, baseline docx, `secure/**`, or `cache/**` diff.
- Boundary grep: PASS, no Electron import, no `BrowserWindow`, no storage API, no new IPC channel, no real network, and no React / Vite / Tailwind wiring.

## Notes

- The view model remains pure TypeScript renderer-side presentation state over the existing stream event store.
- Real Electron or browser `EventSource` behavior, live FastAPI SSE replay, React stream panel rendering, actual full reload UI rebuild, cross-window stream sharing, runtime `StreamEvent` schema validation, virtualization, and Electron dependency wiring remain later integration work.
