# W1.5.191 Runtime Flow Contract ADR Review

| Field       | Value                                    |
| ----------- | ---------------------------------------- |
| Slice       | W1.5.191                                 |
| Scope       | Runtime-flow Desktop action contract ADR |
| Reviewer    | Planck                                   |
| Review type | Independent subagent read-only review    |
| Result      | PASS                                     |
| Date        | 2026-06-26                               |
| Agent ID    | 019f0228-8fc2-7ad3-ad07-15ba0072c904     |

## Verdict

PASS. No P0, P1, P2, or remaining P3 findings.

## Reviewed Files

- `docs/03_decisions/0011-runtime-flow-desktop-actions-contract.md`
- `docs/03_decisions/README.md`
- `docs/04_runbook/m1.5-progress.md` W1.5.190 / W1.5.191 sections

## Findings

- P0: none.
- P1: none.
- P2: none.
- P3: none after archiving this review file.

Initial review noted one P3: the W1.5.191 verification plan referenced this review file before it existed. This file closes that archival gap.

## Confirmed Scope

- ADR-0011 is `Proposed`, not `Accepted`, and explicitly does not change accepted specs by itself.
- W1.5.191 does not claim A4 acceptance, FR acceptance, or `EXIT-P1-1` readiness.
- W1.5.190 remains `review_failed`; W1.5.191 records a contract decision slice only.
- The scoped diff is docs-only for W1.5.191 and excludes unrelated dirty `CLAUDE.md` plus the W1.5.190 implementation files.

## Confirmed Contract Direction

- Chat Box instruction routing must not overload FR-007 `run-once`; the follow-up implementation needs a spec-owned runtime instruction command schema and endpoint before it can satisfy FR-008.
- Artifact Open/Download must not stop at metadata-only content fetch; the follow-up implementation needs a preload/main native handoff with observable action result before it can satisfy FR-017.
- Evidence boundaries remain metadata-only: raw prompt, model output, uploaded-file bytes, response body, artifact body, and draft text must not enter snapshots, visual-smoke evidence, runbook evidence, OTel attributes, command history, or review artifacts.

## Verification

- `pnpm exec prettier --check docs/03_decisions/0011-runtime-flow-desktop-actions-contract.md docs/03_decisions/README.md docs/04_runbook/m1.5-progress.md` PASS.
- `git diff --check -- docs/03_decisions/0011-runtime-flow-desktop-actions-contract.md docs/03_decisions/README.md docs/04_runbook/m1.5-progress.md docs/reviews/m1.5.190-runtime-flow-actions-review.md` PASS with Git LF/CRLF warnings only.

## Residual Risk

ADR-0011 is still Proposed. FR-008 and FR-017 remain unclosed until a follow-up accepted spec/schema/API contract is landed and a real runtime/main/preload/renderer implementation passes independent review.
