# M1.5.99 Canvas Traversal History Review

Date: 2026-06-23

Reviewer: independent subagent Copernicus (`019ef2d6-7cfa-7631-9bac-7f1d425d9c84`)

## Scope

Review the W1.5.99 desktop shell diff for the no-new-dependency, focused Canvas Inspector traversal history scaffold:

- local Canvas selection state with bounded traversal history
- Inspector Back button behavior after node clicks and adjacent route navigation
- preview Canvas remaining noninteractive
- CSS and mobile layout for the Inspector heading and Back button
- React test and Electron visual smoke coverage for Back navigation
- absence of dependency, spec, ADR, schema, generated-code, IPC, storage, React Flow, drag/connect, or persistence drift

## Result

PASS. No blocker, P1, P2, or P3 findings.

The review confirmed the code diff was scoped to the React shell, CSS, React test, and visual smoke before documentation closure. It found no package / lockfile / accepted spec / ADR / schema / generated schema diff, and no React Flow / Tailwind dependency. Traversal history remains local React state, validates selected node ids through existing Canvas helpers, and does not dispatch runtime commands or access raw host/session/controller/store.

## Checked

- Focused Canvas selection now stores `{ selectedNodeId, history }`, pushes the previous valid node only when selection changes, and caps history depth.
- Inspector Back pops the latest history item and restores the selected node without touching runtime state.
- Preview Canvas remains noninteractive because Inspector rendering and edge selection markers stay gated by focused-surface `selectable` state.
- CSS keeps the Inspector heading and Back button `min-width: 0`, wraps text, and collapses to one column on narrow mobile viewports.
- React coverage exercises `context_task -> repair_task -> review_task -> end -> Back`.
- Visual smoke records history/back metrics, clicks Back in the real renderer, and validates selected node, Inspector node, history depth, and Back target.

## Residual Risk

- Visual smoke still validates DOM/count/id/layout invariants rather than pixel-level diffs or a complete overlap detector.
- Real React Flow editing, drag/connect behavior, WorkflowGraph persistence, PlanningSession draft history, WorkflowPatch application, and git snapshot writes remain outside W1.5.99 scope.

## Verification

- Reviewer observed `pnpm --filter @cw/desktop run typecheck` PASS.
- Reviewer observed `pnpm --filter @cw/desktop run test` PASS, 252 tests.
- Reviewer observed Electron visual smoke PASS against `http://127.0.0.1:5174/visual-smoke.html` at `1280x720` and `390x844`, with `messages=[]` and `failures=[]`.
- Reviewer observed `git diff --check` PASS with only normal CRLF warnings.
- Main rollout ran Electron visual smoke PASS for desktop 1280x720, mobile 390x844, and mobile scrollY 900 / 1180 / 1440.
- Main rollout ran full gates: `.\dev.ps1 lint` PASS, `.\dev.ps1 typecheck` PASS, `.\dev.ps1 test` PASS, and `.\dev.ps1 codegen` PASS.
