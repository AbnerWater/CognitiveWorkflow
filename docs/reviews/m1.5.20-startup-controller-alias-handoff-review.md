# M1.5.20 Startup Controller Alias Handoff Review

| Item        | Value                                                                 |
| ----------- | --------------------------------------------------------------------- |
| Milestone   | M1.5 / W1.5.20                                                        |
| Reviewed at | 2026-06-20                                                            |
| Reviewer    | Independent subagent `Hegel` (`019ee488-8f6a-76b0-b45a-1263b232814f`) |
| Verdict     | PASS                                                                  |

## Scope

- `apps/desktop/src/main/runtime-startup-controller.test.ts`
- `docs/04_runbook/m1.5-progress.md`

## Findings

No blocking or non-blocking findings.

## Evidence

- Boundary is respected: the diff is test evidence and runbook only, with no dependency manifest changes, production code changes, Electron imports, IPC registration, renderer behavior, SSE/EventSource behavior, token/port persistence, or `runtime.lock` schema changes.
- The new test creates a real temp filesystem alias, uses a Windows directory junction on this checkout, and writes an active `runtime.lock` through the alias path.
- `projectRootRealpath` remains default; only `projectRootCaseSensitivity` is set.
- The test does not inject a fake `lifecycleResolver`, so `startRuntimeWithLifecycle()` uses the default `resolveRuntimeStartupLifecycle()` path.
- The test asserts `reused_existing`, existing `connectionInfo()`, `stop() === false`, and no orchestration start.
- The `t.skip()` path only wraps alias creation capability; controller, registry, and lifecycle assertions are outside that path, and the suite ran with 0 skipped.
- The reviewer checked `$env:TEMP` and found no leftover `cw-startup-controller-realpath-*` or `cw-runtime-registry-realpath-*` directories.

## Commands

- `pnpm --filter @cw/desktop run typecheck` PASS
- `pnpm --filter @cw/desktop run test` PASS, 97 passed / 0 skipped
- `git diff --check` PASS, with LF-to-CRLF warnings only
- `git diff --name-only -- ":(glob)**/package.json" pnpm-lock.yaml pnpm-workspace.yaml pyproject.toml uv.lock package-lock.json yarn.lock npm-shrinkwrap.json` produced no output

## Residual Risk

- This remains module-level pre-Electron coverage, not real Electron `ipcMain` wiring, preload/renderer behavior, SSE/EventSource, or cross-window broadcast.
- It does not validate native `flock` / Windows `LockFileEx`; it validates current `runtime.lock` file inspection plus in-memory handoff behavior.
