# M1.5.98 Canvas Inspector Route Navigation Review

Date: 2026-06-23

Reviewer: independent subagent Zeno (`019ef2c3-2dfb-7ca3-9549-22839c839666`)

## Scope

Review the W1.5.98 desktop shell diff for the no-new-dependency, focused Canvas Inspector adjacent-node route navigation scaffold:

- Inspector route buttons derived from selected-node incoming/outgoing edges
- local selected-node switching through validated adjacent node ids
- preview Canvas remaining noninteractive
- CSS and mobile layout for route rows and buttons
- React test and Electron visual smoke coverage for route navigation
- absence of dependency, spec, ADR, schema, generated-code, IPC, storage, React Flow, drag/connect, or persistence drift

## Result

PASS. No blocker, P1, P2, or P3 findings.

The review confirmed the code diff was scoped to React shell, CSS, React test, and visual smoke before documentation closure. It found no package / lockfile / accepted spec / ADR / schema / generated schema diff, and no React Flow / Tailwind dependency. Route navigation remains local React state, validates node ids before selection, and does not dispatch runtime commands or access raw host/session/controller/store.

## Checked

- Preview Canvas remains noninteractive because `selectable` is focused-only, Inspector rendering is gated to focused Canvas, and edge selected/direction markers remain selected-node projections.
- Inspector buttons derive adjacent node ids from the current edge/current node pair, so incoming route buttons target the source node and outgoing route buttons target the target node.
- CSS keeps route rows `min-width: 0`, wraps long node ids, and falls back to one column on narrow mobile viewports.
- React coverage clicks `repair_task -> review_task -> end`.
- Visual smoke clicks `review_to_repair -> review_task` in the real renderer and validates post-navigation selected node, Inspector title, edge ids, row counts, and route targets.

## Residual Risk

- Visual smoke still validates DOM/count/id/layout invariants rather than pixel-level diffs or a complete overlap detector.
- Real React Flow editing, drag/connect behavior, WorkflowGraph persistence, PlanningSession draft history, WorkflowPatch application, and git snapshot writes remain outside W1.5.98 scope.

## Verification

- Reviewer observed `pnpm --filter @cw/desktop run typecheck` PASS.
- Reviewer observed `pnpm --filter @cw/desktop run test` PASS, 252 tests.
- Reviewer observed Electron visual smoke PASS against `http://127.0.0.1:5174/visual-smoke.html`, with `messages=[]`, `failures=[]`, and route metrics selecting `review_task`.
- Reviewer observed `git diff --check` PASS with only normal CRLF warnings.
- Main rollout ran Electron visual smoke PASS for desktop 1280x720, mobile 390x844, and mobile scrollY 900 / 1180 / 1440.
- Main rollout ran full gates: `.\dev.ps1 lint` PASS, `.\dev.ps1 typecheck` PASS, `.\dev.ps1 test` PASS, and `.\dev.ps1 codegen` PASS.
