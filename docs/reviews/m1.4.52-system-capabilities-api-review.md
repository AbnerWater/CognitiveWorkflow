# M1.4.52 System Capabilities API Review

## Scope

- Slice: W1.4.52 System capabilities API wiring.
- Reviewed files:
  - `apps/runtime/src/cw_runtime/api/app.py`
  - `apps/runtime/tests/test_m1_3_1_system_endpoints.py`
  - `docs/04_runbook/m1.4-progress.md`
- Review mode: independent read-only subagent review.

## Result

PASS.

No blocking or actionable findings were identified.

## Evidence

- `/cw/v1/system/capabilities` returns only `AdapterCapabilities[]` from `descriptor.capabilities`.
- The response does not expose adapter descriptors, adapter ids, or default configs.
- PydanticAIAdapter keeps `mcp=False` and `cancel=False`.
- ClaudeCodeAdapter exposes the existing MCP / HITL / cancel capability snapshot without starting a provider.
- The API import path does not import `pydantic_ai` or `claude_agent_sdk`; those remain lazy behind SDK session use paths.
- `create_app(..., adapter_registry=...)` is keyword-only with a default, preserving existing callers.

## Boundary Check

- No accepted spec, ADR, public schema, or dependency file was modified.
- This slice does not implement default execution AdapterRegistry wiring, real provider startup, real provider cancel proof, real-provider HITL, or AdapterRegistry-backed run execution E2E.
