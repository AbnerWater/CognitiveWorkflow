# M1.5.157 Review - Desktop Chat Single Visual Smoke Escaped Redaction

## Scope

- Slice: W1.5.157 Desktop Chat Box single visual smoke escaped hard-exception redaction.
- Files reviewed:
  - `apps/desktop/scripts/runtime-workbench-visual-smoke-evidence.cjs`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs`
  - `docs/04_runbook/m1.5-progress.md`

## Checklist

- Existing raw/trimmed chat draft redaction behavior remains intact.
- JSON string escaped draft variants are redacted from hard-exception text.
- Non-sensitive error context remains available for debugging.
- Normal single visual smoke assertions and persisted JSON evidence structure remain unchanged.
- No production runtime/session/store/React/API/IPC/storage/dependency/spec/schema drift.

## Result

Parfit independent review found one P3 documentation completion issue: the task table marked W1.5.157 done before this file and the runbook preserved the final verification results. The P3 is fixed by recording the W1.5.157 verification evidence in `docs/04_runbook/m1.5-progress.md` and replacing the pending review placeholders here.

No blocker/P1/P2 findings were found in the code/test change. Raw and trimmed chat draft redaction remain intact, JSON string escaped draft variants are added to the sensitive fragment set, and the new regression covers quote/newline/backslash escaping while preserving non-sensitive error context. The diff stays in the intended dev-only surface with no production runtime/session/store/React/API/IPC/storage/dependency/spec/schema drift.

Residual risk remains exact-fragment matching. Future transformed or partial draft echoes, or new freeform chat evidence fields, should add explicit sanitizer coverage.

## Reviewer Commands

- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke-evidence.cjs` PASS.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs` PASS.
- `node --test apps/desktop/scripts/runtime-workbench-visual-smoke.test.cjs` PASS, 14/14 tests.
- Parent-run verification also covered real enabled Chat Box visual smoke, `pnpm --filter @cw/desktop run test`, `.\dev.ps1 lint`, `.\dev.ps1 codegen`, `.\dev.ps1 typecheck`, `.\dev.ps1 test`, targeted Prettier, `.\dev.ps1 format-check` known baseline, and `git diff --check`.
