# W1.5.123 Runtime Stream Protocol Provenance Review

## Scope

- Slice: W1.5.123 Desktop Runtime Stream protocol provenance read-only details.
- Reviewer: Lovelace (`019ef6fe-c712-7500-8b56-3693421c7e26`).
- Mode: read-only independent review of the uncommitted diff.

## Verdict

PASS. No findings.

## Checks Run

- `git diff --check`: PASS with Git LF-to-CRLF warnings only.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `pnpm --filter @cw/desktop run test`: PASS, 265 tests passed.
- Read-only diff review for AGENTS/spec boundary, StreamEvent top-level field conformance, dependency/package drift, schema/generated drift, host/shell clone propagation, raw `payload` / `rawData` exposure, runtime/API/IPC/window.cw/storage/fs behavior, and UI/test/visual-smoke coverage.

Reviewer note: Electron visual smoke was not run by the reviewer. Owner validation covered the full desktop/mobile smoke matrix.

## Boundary Confirmation

The diff stays within the intended desktop renderer/local UI boundary. It adds top-level `schema_version` as `schemaVersion` in the Runtime Stream view-model, propagates it through host and shell snapshot cloning, and renders it beside existing `seq` and `createdAt` in expanded event detail, selected-event body, and selected-event metadata. Host/shell snapshots still omit `payload` and `rawData`.

## Residual Risks

- Malformed or empty `schema_version` degrades to `null`; this matches the renderer's forward-compatible read-only tolerance rather than schema validation.
- Visual smoke uses representative provenance values rather than max-length IDs or timestamps; existing wrapping plus `horizontalOverflow=0` remain the enforced UI guard.
