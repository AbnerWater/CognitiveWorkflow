# W1.5.175 Desktop A4 Evidence Manifest Review

## Scope

W1.5.175 prepares a conservative A4 review input manifest for the W1.5.174 `TRACK-A4-CANDIDATE-EVIDENCE` repair track. The manifest is limited to FR-009, FR-010, and FR-016, and keeps every item at `pending_a4_review`.

The slice adds a manifest validator, a focused Node test, and standard desktop test wiring. It also stabilizes the existing real sidecar stream smoke test teardown on Windows by adding `fs.rm` retry options for transient SQLite `EBUSY` cleanup after sidecar shutdown.

The slice does not mark A4 acceptance, does not make `EXIT-P1-1` ready, does not install dependencies, and does not change accepted specs, ADRs, schema/generated schema inputs, production runtime behavior, IPC, renderer storage, packaging, updater, or React shell behavior.

## Verification

- `node apps/desktop/scripts/m1-5-a4-evidence-manifest.cjs --check`: PASS, sanitized conservative summary only.
- `node --test apps/desktop/scripts/m1-5-a4-evidence-manifest.test.cjs`: PASS, 6 tests.
- `pnpm --filter @cw/desktop run typecheck`: PASS.
- `pnpm --filter @cw/desktop run test`: PASS, 339 tests.
- `.\dev.ps1 lint`: PASS.
- `.\dev.ps1 codegen`: PASS, no generated schema content diff remained.
- `.\dev.ps1 typecheck`: PASS.
- `.\dev.ps1 test`: first run exposed Windows `EBUSY` cleanup in `runtime-sidecar-stream-smoke.test.ts`; after the test-only retry fix, rerun PASS with Python 475 passed / 1 skipped and desktop 339 passed.
- `pnpm exec prettier --check apps/desktop/package.json apps/desktop/src/main/runtime-sidecar-stream-smoke.test.ts apps/desktop/scripts/m1-5-a4-evidence-manifest.cjs apps/desktop/scripts/m1-5-a4-evidence-manifest.test.cjs docs/04_runbook/m1.5-a4-evidence-manifest.json docs/04_runbook/m1.5-progress.md docs/reviews/m1.5.175-desktop-a4-evidence-manifest-review.md`: PASS.
- `.\dev.ps1 format-check`: ruff format check PASS; Prettier still fails on the standing 39-file repo-wide baseline debt. W1.5.175 touched files pass targeted Prettier.
- `git diff --check`: PASS with only Windows LF/CRLF warnings.

## Findings

- Rawls (`019efd20-3b0c-72c2-8517-2d6f5970a245`) independent review: PASS with no findings.
- The review confirmed A4 and `EXIT-P1-1` stay conservative: the manifest keeps `a4_evidence_inputs_prepared_not_accepted`, `exit_p1_1_status=not_ready`, `accepted_items=0`, and every review item at `pending_a4_review`.
- The review confirmed scope is limited to `TRACK-A4-CANDIDATE-EVIDENCE` and FR-009 / FR-010 / FR-016, matching the W1.5.174 repair track.
- The review confirmed the runner enforces repair-plan/evidence-map consistency, matrix case existence, default case count, zero accepted count, and sanitized-summary-only output.
- The review confirmed package wiring only adds the new manifest test to the existing desktop `test` script, and no dependency/spec/ADR/schema/generated/runtime/production renderer behavior drift is present.
- The review confirmed the Windows cleanup stabilization is test-only and appropriate for transient SQLite `EBUSY` cleanup after sidecar shutdown.
- Residual risk: the independent review was static only; command execution evidence is recorded in the verification section above.
- Residual unrelated worktree change: `CLAUDE.md` remains dirty and is excluded from the W1.5.175 commit.

## Verdict

PASS. W1.5.175 is complete as a conservative A4 evidence manifest and runner slice. It prepares reviewer inputs only, does not accept any FR, and keeps `EXIT-P1-1` not ready.
