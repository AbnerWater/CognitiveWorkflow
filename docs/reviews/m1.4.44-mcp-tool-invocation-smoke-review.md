# M1.4.44 MCP Tool Invocation Smoke Review

- Reviewer: independent subagent Bernoulli
- Scope: local MCP `tools/call` smoke for existing runtime harness stdio, Streamable HTTP, and legacy HTTP+SSE clients.
- Result: PASS

## Findings

No actionable findings.

## Evidence

- Diff stayed within `apps/runtime/src/cw_runtime/harness/project.py`, `apps/runtime/tests/test_m1_3_2_project_harness.py`, and `docs/04_runbook/m1.4-progress.md`.
- No accepted spec, ADR, public schema, or dependency files were changed.
- `tools/call` reuses existing JSON-RPC request/id/deadline and sanitized error paths for stdio.
- Streamable HTTP covers JSON and SSE responses, session headers, notification skipping, and response-id matching.
- Legacy HTTP+SSE continues to POST to the endpoint event URL and match SSE `message` responses by id.
- Error construction remains sanitized and does not include `command_or_url`, `secret_ref`, provider body/message, or arguments.
- Tests cover stdio invocation, HTTP JSON/SSE invocation, HTTP invocation error sanitization, and legacy invocation.
- Runbook wording is scoped to local fake-server smoke and does not claim real provider, API, adapter registry, OAuth, or secure-store injection support.

## Residual Risks

- Stdio and legacy invocation error sanitization rely on the same shared JSON-RPC error paths already exercised by discovery-error tests; direct invocation-error tests currently cover HTTP.
- Real external MCP providers, credential injection, OAuth, adapter registry/API integration, and aggregate SSE/tool-result size hardening remain outside W1.4.44.
