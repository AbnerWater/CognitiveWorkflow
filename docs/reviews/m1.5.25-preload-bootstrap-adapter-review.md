# M1.5.25 Preload Bootstrap Adapter Review

Date: 2026-06-20

Reviewer: independent Codex subagent `Herschel` (`019ee4ea-3675-79b3-83bf-2fcc462c2e25`)

Result: PASS

## Findings

None.

## Scope Checked

- Changed files stayed within the intended W1.5.25 boundary: `apps/desktop/src/preload/bootstrap.ts`, `apps/desktop/src/preload/contract.test.ts`, and `docs/04_runbook/m1.5-progress.md`.
- No package manifest, accepted spec, ADR, schema, renderer UI, or generated schema output changed.
- No Electron import, real `contextBridge`, real `ipcRenderer`, real `ipcMain`, `BrowserWindow`, `localStorage`, or `sessionStorage` usage was introduced.
- `bootstrap.ts` defines injected Electron-like `contextBridge` and `ipcRenderer` interfaces without importing Electron.
- `createRuntimeIpcRendererInvoke()` adapts `ipcRenderer.invoke(channel, payload)` into the existing `RuntimePreloadIpcInvoke`.
- `installCwPreloadApi()` exposes the frozen `CwDesktopApi` under fixed key `cw`.
- Tests cover install-time exposure without eager IPC, returned / exposed API identity, runtime channel and payload mapping, and malformed `init` fail-before-invoke behavior.

## Commands Run By Reviewer

```powershell
pnpm --filter @cw/desktop run typecheck
pnpm --filter @cw/desktop run test
git diff --check
```

All commands passed; desktop tests reported 102 passed, 0 failed. The reviewer also checked the untracked `bootstrap.ts` with `git diff --check --no-index`, which reported only LF/CRLF warning and no whitespace errors.

## Residual Risks

W1.5.25 remains a pure injected preload bootstrap adapter. It does not validate real Electron `contextBridge` structured clone behavior, real `ipcRenderer.invoke`, real `ipcMain.handle`, renderer integration, or packaged preload loading.
