# M1.5.158 Review - Desktop Chat Keyboard Submit

## Scope

- Slice: W1.5.158 Desktop Chat Box local send keyboard submit.
- Files reviewed:
  - `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
  - `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
  - `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `docs/04_runbook/m1.5-progress.md`

## Checklist

- `Ctrl+Enter` and `Meta+Enter` submit through the same metadata-only local send path as the Send button.
- Empty or disabled drafts do not create local submissions.
- Plain Enter remains textarea input behavior and is not treated as submit.
- Local history remains metadata-only and does not persist raw draft text.
- No production runtime/session/store/API/IPC/`window.cw`/storage/dependency/spec/schema drift.

## Result

Dewey independent review found no P0/P1/P2 issues.

- P3: runbook/review docs still marked the independent review as pending. Fixed by recording the final Dewey finding and remediation.
- P3: disabled keyboard-submit guard was implementation-covered but not explicitly test-covered. Fixed by extending the disabled Chat Box guard test to dispatch `Ctrl+Enter` and assert no local submission, no runtime command, and draft retention.

Final status after remediation: PASS.

## Reviewer Commands

- Parent verification already run before review:
  - `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`
  - `pnpm --filter @cw/desktop run typecheck`
  - `pnpm --filter @cw/desktop run test`
  - Electron visual smoke with `chatBox=enabled` and first local send via real `Ctrl+Enter` keyboard event
  - targeted Prettier check for touched files
  - `.\dev.ps1 lint`
  - `.\dev.ps1 codegen`
  - `.\dev.ps1 typecheck`
  - `.\dev.ps1 test`
  - `.\dev.ps1 format-check`
  - `git diff --check`

## Parent Verification Summary

- Focused desktop typecheck/test PASS.
- Real enabled Chat Box visual smoke PASS: first local send used keyboard submit; local history and redaction evidence stayed metadata-only.
- `.\dev.ps1 lint` PASS.
- `.\dev.ps1 codegen` PASS with no generated diff.
- `.\dev.ps1 typecheck` PASS.
- `.\dev.ps1 test` PASS: Python 475 passed / 1 skipped; desktop 301 passed.
- `.\dev.ps1 format-check` FAIL only on standing 38-file repo-wide Prettier baseline; touched files passed targeted Prettier.
- `git diff --check` PASS with only Git CRLF warnings.

## Remediation Verification

- `pnpm --filter @cw/desktop run typecheck` PASS after P3 remediation.
- `pnpm --filter @cw/desktop run test` PASS after P3 remediation: desktop 301 passed; the disabled guard coverage was added to an existing test.
- Targeted Prettier check PASS after P3 remediation.
- `git diff --check` PASS after P3 remediation with only Git CRLF warnings.
