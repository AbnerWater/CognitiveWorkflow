# W1.5.129 Runtime Stream Unknown Event Fallback Review

Reviewer: Hume (`019ef796-df2f-7270-88f2-89c1dd919671`)

Scope:

- `apps/desktop/src/renderer/runtime-workbench-shell-react.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell-react.test.tsx`
- `apps/desktop/src/renderer/runtime-workbench-shell.css`
- `apps/desktop/scripts/runtime-workbench-visual-smoke.cjs`

## Findings

No blocker/P1/P2 findings.

The diff stays within the intended desktop renderer UI/test/visual-smoke scope. It does not modify accepted specs, schemas, generated schema output, dependencies, runtime API, preload IPC, or package metadata.

The D-SE-7 shell-layer behavior is acceptable for this slice: unknown event types are classified against the current known type set, labeled in Timeline and Selection UI, and kept on safe payload/metadata summaries instead of exposing raw payload or metadata values.

The visual smoke script change is acceptable. It adds known-type metrics/assertions for the current known-event fixture and makes `clickPanel` wait briefly before failing with structured diagnostics. It does not broaden product behavior.

## Reviewer Verification

- `git diff --check -- ...` PASS with only LF-to-CRLF warnings.
- `pnpm --filter @cw/desktop build:ts` PASS.
- `node --test dist/renderer/runtime-workbench-shell-react.test.js` PASS, 29/29.
- `node --check apps/desktop/scripts/runtime-workbench-visual-smoke.cjs` PASS.

Reviewer did not run full repo gates or Electron visual smoke. Main rollout validation covered those gates and the full desktop/mobile/scrolled Electron visual smoke matrix.

## Residual Notes

End-to-end ingestion of unknown SSE event names is outside this UI/test/visual-smoke diff. This review treats it as a later runtime/session protocol slice, not a W1.5.129 finding.
