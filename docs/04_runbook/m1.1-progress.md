# M1.1 工程地基 · 进度日志

> 本文档跟踪 M1.1 milestone（W1.1.1 ~ W1.1.11）的实际推进；与 `docs/roadmap.md §5.2` 的计划清单对照。

| Work item | 描述 | 状态 | 备注 |
|---|---|---|---|
| W1.1.1 | monorepo 骨架 | ✅ done | apps/{desktop,runtime} + packages/{schemas,schemas-ts,ui} + scripts/{codegen,git-hooks,release} + docs/03_decisions/ |
| W1.1.2 | uv workspace + pyproject 子包 stub | ✅ done | 根 pyproject.toml + 2 个子包 pyproject |
| W1.1.3 | pnpm workspace + package.json stub | ✅ done | 根 package.json + 3 个子包 package.json |
| W1.1.4 | AGENTS.md + CLAUDE.md + .claude/skills/（5 个） | 🟡 partial | AGENTS.md / CLAUDE.md 完成；5 个 skill 待 next batch |
| W1.1.5 | ADR 0001~0010 | ✅ done | 全部 10 份；0010 状态 Proposed |
| W1.1.6 | pre-commit hook | ✅ done | .pre-commit-config.yaml + 4 个自定义 hook |
| W1.1.7 | Makefile（6 个 target） | ✅ done | help/install/dev/build/test/typecheck/lint/format/codegen/clean/phase-status 共 11 targets |
| W1.1.8 | CI 三平台矩阵 | ✅ done | ci.yml + release.yml 双管线 |
| W1.1.9 | Lint / Typecheck 全量配置 | 🟡 partial | ruff / mypy 已配；eslint / tsc 待 M1.5 落地 |
| W1.1.10 | Test 框架 | ✅ done | pytest (cw_schemas + cw_runtime smoke test)；vitest 在 M1.5 |
| W1.1.11 | 类型流 codegen | ✅ done | generate-json-schemas.py + generate-ts-schemas.mjs（M1.1 stub） |

## 已建立的工程纪律

- ADR-0001 ~ ADR-0010 锁定核心架构决策
- AGENTS.md 锁定贡献规则、禁止行为、commit 规范
- pre-commit hook 4 道防线：secure 路径 / 已知凭证 / Co-Authored-By Claude / Engine import pydantic_ai
- CI 双管线：push+PR 全量 / tag 触发 release pipeline
- 包名规约：`cw_schemas / cw_runtime / cw_ui` + `@cw/desktop / @cw/schemas / @cw/ui`

## 下一步动作

1. **本机执行依赖安装**（验证骨架可工作）：
   ```bash
   cd D:\00_AI\CognitiveWorkflow\CognitiveWorkflow
   uv sync --all-extras
   pnpm install
   pre-commit install
   pre-commit install --hook-type commit-msg

   # 三连验证
   make lint
   make typecheck
   make test
   make codegen
   ```

2. **W1.1.4 收尾**：5 个 `.claude/skills/`（add-adr / new-node-spec / lift-failure-to-reflection / pre-push-review / update-baseline-decision）

3. **进入 M1.2**：开始把 7 份 schema spec 落到 cw_schemas Pydantic 模型

## 验证标准（EXIT-M1.1）

- [ ] `make dev` 三平台拉起 hello-world（M1.5 后才能完成）
- [x] CI 三平台矩阵全绿 — yaml 已写，待首次 push 后验证
- [x] ADR 0001~0010 全部状态明确
- [ ] pre-commit hook 拦截测试用例 3 条均成功（待本机 install + 提交带 secret 的测试 commit 验证）
- [x] spec ↔ packages 映射初稿落 docs/spec_code_mapping.md（M1.2 产出 Pydantic 模型时填充）
