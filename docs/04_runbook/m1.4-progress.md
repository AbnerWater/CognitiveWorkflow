# M1.4 AgentAdapter 首发两家 · 进度日志

> 本文档跟踪 M1.4 milestone 的阶段切片；与 `docs/roadmap.md §5.2` 的 AgentAdapter 首发计划对照。

| Work item | 描述                                                                                            | 状态 |
| --------- | ----------------------------------------------------------------------------------------------- | ---- |
| W1.4.1    | PydanticAIAdapter foundation：entry point 模块、session seam、事件转译、AttemptOutcome 基础闭环 | done |
| W1.4.2    | PydanticAIAdapter SDK session：lazy SDK factory、StructuredDict output、usage/messages 回填     | done |
| W1.4.3    | PydanticAIAdapter SDK event stream：run_stream_events 桥接、细粒度 model/tool raw events        | done |
| W1.4.4    | PydanticAIAdapter SDK usage limits：token limits 透传、UsageLimitExceeded 结构化失败            | done |
| W1.4.5    | PydanticAIAdapter SDK toolsets：registry seam、toolset 透传、MCP approval wrapper               | done |
| W1.4.6    | PydanticAIAdapter SDK retry policy：AgentRetries 映射、unsupported retry shapes fail closed     | done |
| W1.4.7    | PydanticAIAdapter SDK human/deferred resume：DeferredToolRequests/Results 桥接                  | done |
| W1.4.8    | PydanticAIAdapter SDK attempt timeout：`RetryPolicy.timeout_seconds` adapter 层总时限控制       | done |
| W1.4.9    | PydanticAIAdapter user cancel iterator exit：主动取消唤醒并终止 run/resume iterator             | done |
| W1.4.10   | PydanticAIAdapter toolset registry：已构造 SDK toolset 的注册式解析与缺失 fail closed           | done |
| W1.4.11   | PydanticAIAdapter model retry：adapter-owned retryable model/session exception retry            | done |
| W1.4.12   | PydanticAIAdapter reported cost guard：按 SDK 回传 `est_cost_usd` 执行 `max_cost_usd` 闭环      | done |
| W1.4.13   | PydanticAIAdapter builtin function registry：内置工具 callable → SDK `FunctionToolset`          | done |
| W1.4.14   | PydanticAIAdapter evidence lookup tool：`EvidencePack` → SDK `FunctionToolset` 自动注入         | done |
| W1.4.15   | PydanticAIAdapter SDK stream cancel：主动取消触达活动 `run_stream_events` stream                | done |
| W1.4.16   | ClaudeCodeAdapter SDK session：默认 lazy `claude_agent_sdk` 会话入口与 fake SDK 覆盖            | done |
| W1.4.17   | ClaudeCodeAdapter allowed tools projection：CW 工具白名单 → Claude `allowedTools`               | done |
| W1.4.18   | ClaudeCodeAdapter MCP server config handoff：请求内 MCP server → SDK `mcp_servers`              | done |
| W1.4.19   | ClaudeCodeAdapter permission lockdown：`allowed_tools` → `permission_mode="dontAsk"`            | done |
| W1.4.20   | PydanticAIAdapter builtin `web_fetch`：默认 SDK session 暴露安全 HTTP(S) fetch                  | done |
| W1.4.21   | PydanticAIAdapter builtin `file_io`：默认 SDK session 暴露只读项目文件工具                      | done |
| W1.4.22   | PydanticAIAdapter builtin `python_sandbox`：默认 SDK session 暴露表达式沙箱工具                 | done |
| W1.4.23   | ModelRouter builtin tool-name matching：按 Adapter metadata 过滤未支持内置工具                  | done |
| W1.4.24   | Runtime usage ledger：每个 terminal attempt 追加 `usage.jsonl` 用量记录                         | done |
| W1.4.25   | Project Skill/MCP availability context：项目 manifest 驱动 workflow L4 校验                     | done |
| W1.4.26   | Run Skill/MCP lock snapshot：Run 启动时写入 `skill_lock.json` / `mcp_lock.json`                 | done |
| W1.4.27   | Project Skill version registry：项目 Skill manifest 驱动版本化 L4 与 Adapter 校验               | done |
| W1.4.28   | Project MCP config handoff：项目 `mcp.config.json` 投影到 ClaudeCodeAdapter SDK 配置            | done |
| W1.4.29   | Runtime usage budget guard：`usage.jsonl` 累计用量驱动 run 级预算失败闭环                       | done |
| W1.4.30   | Model cost estimation：按选中 ModelProfile 静态价格补全 `RunUsage.est_cost_usd`                 | done |
| W1.4.31   | Adapter execution bridge：显式 AdapterFactory 执行 `execution_task` 并写入 usage ledger         | done |
| W1.4.32   | MCP tool discovery lock seam：注入式工具发现结果写入 `mcp_lock.tools_snapshot`                  | done |
| W1.4.33   | Project MCP secret resolver seam：注入式 resolver 投影到 Claude MCP `headers/env`               | done |
| W1.4.34   | Project MCP discovery lifecycle runner：显式 provider start/health/discover/close               | done |
| W1.4.35   | Project MCP stdio discovery client：本地 stdio JSON-RPC initialize/tools/list smoke             | done |
| W1.4.36   | Project MCP HTTP discovery client：Streamable HTTP JSON/SSE initialize/tools/list smoke         | done |
| W1.4.37   | Project MCP legacy HTTP+SSE fallback：旧版 endpoint event discovery smoke                       | done |
| W1.4.38   | Project MCP secure store secret resolver：secure SQLite decryptor → Claude headers/env          | done |
| W1.4.39   | Adapter runner bridge：evaluation/repair Adapter outcome → review/patch records                 | done |
| W1.4.40   | Adapter runner bridge：长运行 adapter await 释放 runtime.lock                                   | done |
| W1.4.41   | ClaudeCodeAdapter `can_use_tool` bridge：MCP approval prompt → CW human gate                    | done |
| W1.4.42   | Project MCP approval bridge：`mcp.config.json requires_approval` → Claude human gate            | done |
| W1.4.43   | Project secure secret envelope：PBKDF2 + injected AES-GCM/key-provider seam                     | done |
| W1.4.44   | Project MCP tool invocation smoke：本地 stdio/HTTP/legacy `tools/call` 闭环                     | done |
| W1.4.45   | Project secure AES-GCM provider：Windows CNG AEAD provider 接入                                 | done |
| W1.4.46   | Project secure Windows keychain provider：Credential Manager master key 接入                    | done |
| W1.4.47   | Project MCP external HTTP provider smoke：子进程 HTTP provider discovery + invocation           | done |
| W1.4.48   | Project MCP HTTP secure header injection：secure-store header -> discovery/invocation client    | done |
| W1.4.49   | Project MCP stdio secure env injection：secure-store env -> discovery/invocation client         | done |
| W1.4.50   | Project MCP response size hardening：stdio/SSE aggregate response guard                         | done |
| W1.4.51   | Project config API reads：skills/mcps/adapters accepted GET endpoints                           | done |
| W1.4.52   | System capabilities API wiring：AdapterRegistry static capability projection                    | done |
| W1.4.53   | Run node action API wiring：`run-once` / `re-evaluate` → injected AdapterRegistry bridge        | done |
| W1.4.54   | Run repair action API wiring：manual `:repair` request → RepairPatch response                   | done |
| W1.4.55   | Repair prompt overlay scope：`until_pass` / `persistent_for_run` runtime semantics              | done |
| W1.4.56   | Project MCP toolset bridge：PydanticAIAdapter project MCP `tools/call` → AdapterRegistry/API    | done |
| W1.4.57   | Project MCP secure adapter bridge：默认 PydanticAIAdapter `secret_ref` secure-store 注入        | done |
| W1.4.58   | Repair prompt overlay scope：`this_attempt_only` next-attempt overlay binding                   | done |
| W1.4.59   | Repair one-shot hardening：consumed/mismatched overlay and invalid workflow scope regressions   | done |
| W1.4.60   | Repair one-shot expiry：`expires_at` runtime skip / consume semantics                           | done |

## W1.4.1 范围

本切片启动 M1.4 AgentAdapter 工作，先关闭 `agent_adapter.md` 中已经声明的 `pydantic_ai` entry point 指向缺失模块的问题，并建立可测试的 PydanticAIAdapter foundation；不新增 production dependency、不要求默认环境安装 `pydantic-ai-slim`、不实现真实 SDK Agent/Toolset 构造：

- 新增 `cw_runtime.adapters.pydantic_ai_adapter`，导出 `PydanticAIAdapter`、`PydanticAIRunRequest`、`PydanticAIResumeRequest`、`PydanticAISession`、`PydanticAISessionFactory`、`RawPydanticAIEvent` 与 `build_pydantic_ai_descriptor()`。
- `PydanticAIAdapter.capabilities()` 引入 PydanticAI 能力声明 scaffold；W1.4.2 已按当前真实实现收敛路由面能力，避免 ModelRouter 选择未完成的 Toolset/HITL/cancel 路径。
- `prepare()` 只消费 `ExecutionPack`，生成 `AttemptHandle(state=prepared)`，并缓存 pack；不通过 side channel 注入参数。
- `run()` 经注入的 `PydanticAISession` seam 转译 raw events 到 spec StreamEvent：`model.text_delta`、`model.thinking_delta`、`tool.call_started`、`tool.call_completed`、`tool.approval_required`、`human.gate_required`、`model.request_completed`、`attempt.completed`、`attempt.failed`；若 session iterator 耗尽但未产出 terminal raw event，则兜底产出 `AA_RUN_INTERNAL` 的 `attempt.failed`。
- `resume()` 支持 `human_decision` 与 `deferred_tool` 两类 resumption；human decision 先发 `human.gate_resolved`，deferred tool result 不额外发 human event。
- `cancel()` 标记 handle cancelled 并委托 session cancel；`finalize()` 返回 `AttemptOutcome`，包含 output hash、AdapterError、AttemptProvenance 与 Pydantic AI traceparent 占位。
- 输出 schema 在 foundation 层覆盖 object required/type 校验，失败映射 `AA_RUN_OUTPUT_VALIDATION_FAILED`。

边界说明：本切片只建立 adapter 协议与事件转译闭环，不直接 import `pydantic_ai` SDK。当前默认 `.venv` 中 `cw-runtime.exe` 被已有进程占用，`uv run --package cw_runtime --extra agents ...` 无法完成环境切换；真实 SDK-backed `Agent.iter()`、Toolset/MCP/ApprovalRequiredToolset 构造、usage/messages 回填、≤5s cancel 实测留给后续 M1.4 切片。

## W1.4.2 范围

本切片把 `PydanticAIAdapter(session_factory=None)` 从 foundation-only 报错路径推进为默认 SDK-backed session 入口；仍保持 optional dependency lazy-load，不让默认 runtime import 依赖 `pydantic_ai`：

- 新增 `PydanticAISDKSession`，仅在实际运行默认 session 时 `importlib.import_module("pydantic_ai")`；缺少 optional `agents` extra 时仍转为结构化 `AA_RUN_INTERNAL`。
- 默认 SDK session 使用 Pydantic AI 1.107.0 的最小稳定接口：`Agent(model, output_type=StructuredDict(schema), system_prompt=..., instructions=..., model_settings=..., defer_model_check=True)` 与 `await agent.run(user_prompt, model_settings=...)`。
- 将 `ExecutionPack.node_contract_snapshot.output_schema` 映射为 `StructuredDict`；空 schema 时使用 permissive object schema，保持 CW Adapter output 为 object。
- 将 SDK `AgentRunResult.output` 转为 `completed` raw event，并把 usage/messages/traceparent 回填到 `AttemptOutcome.usage`、`AttemptOutcome.messages` 与 `AttemptProvenance.pydantic_ai_traceparent`。
- usage 回填显式映射 Pydantic AI `cache_write_tokens/cache_read_tokens` 到 CW `cache_creation_input_tokens/cache_read_input_tokens`，避免外部 SDK extra 字段进入 `RunUsage(extra=forbid)`。
- `capabilities()` 当前仅声明已由 W1.4.2 默认 SDK session 实现的路由面能力：`chat`、structured output、cloud/private/local provider、model settings passthrough；`streaming/tool_call/mcp/human_in_the_loop/deferred_tool_results/multi_modal/cancel/evidence_lookup_tool` 先保持 false/empty，后续切片实现真实语义后再打开。

边界说明：W1.4.2 不实现 `Agent.iter()` 细粒度事件流、Toolset/MCP/ApprovalRequiredToolset 构造、human decision resume、deferred tool result SDK resume、usage limit、真实 API key 调用或 cancel ≤5s 实测；这些仍留在后续 M1.4 切片。

## W1.4.3 范围

本切片把 W1.4.2 的默认 SDK-backed session 从一次性 `agent.run(...)` 推进到 Pydantic AI 1.107.0 的 `agent.run_stream_events(...)` 事件流桥接；仍保持 optional dependency lazy-load：

- `PydanticAISDKSession.run()` 优先使用 `async with agent.run_stream_events(...) as stream`，按 SDK 推荐生命周期消费事件流；缺少该接口时回退到 W1.4.2 的 `agent.run(...)` 路径。
- 新增 SDK 事件对象到现有 raw event seam 的转换：`PartStartEvent/PartDeltaEvent/PartEndEvent` 的 text/thinking delta 与 completed、`FunctionToolCallEvent`、`FunctionToolResultEvent`、`BuiltinToolCallEvent/BuiltinToolResultEvent`、`AgentRunResultEvent`。
- raw event 到 StreamEvent 的 adapter 层补齐 `model.text_completed`、`model.thought_completed`、`tool.call_failed`，继续复用 `StreamEvent` spec 内类型，不新增事件名。
- `capabilities().streaming=True` 现在由默认 SDK session 的真实 stream bridge 支撑；`tool_call/mcp/human_in_the_loop/deferred_tool_results/multi_modal/cancel/evidence_lookup_tool` 仍保持 false/empty，因为本切片未构造 Toolset/MCP/ApprovalRequiredToolset，也未实现 SDK resume/cancel 实测。
- 保留注入式 `PydanticAISession` raw-event seam，便于后续 M1.4 子阶段分别接入 Toolset/MCP/HITL/deferred resume 与 cancel。

边界说明：W1.4.3 不实现 `Agent.iter()` run graph 控制、Toolset/MCP 构造、ApprovalRequiredToolset、human decision SDK resume、deferred tool result SDK resume、usage limit、真实 API key 调用或 cancel ≤5s 实测；这些仍留在后续 M1.4 切片。

## W1.4.4 范围

本切片关闭 `ExecutionPack.usage_limits` 在默认 Pydantic AI SDK session 中被忽略的问题；范围只覆盖 SDK 可原生执行的 token usage limits，不打开 Toolset/MCP/HITL/deferred/cancel 等能力：

- `PydanticAIRunRequest` 带上 `usage_limits` 投影，来自 `ExecutionPack.usage_limits.model_dump(exclude_none=True)`。
- 默认 SDK session 在 `agent.run_stream_events(...)` 与 fallback `agent.run(...)` 两条路径都传入 Pydantic AI `UsageLimits`。
- CW 字段映射到 Pydantic AI 1.107.0 字段：`max_input_tokens -> input_tokens_limit`、`max_output_tokens -> output_tokens_limit`、`max_total_tokens -> total_tokens_limit`，并设置 `request_limit=None`，避免引入 CW spec 外的 request-count 限流语义。
- SDK 抛出的 `UsageLimitExceeded` 转译为结构化 `AA_RUN_USAGE_LIMIT` attempt failure，不再落入 `AA_RUN_INTERNAL`。
- `max_cost_usd` 当前不能由 Pydantic AI SDK 原生执行；本切片显式以 `AA_PREPARE_INCOMPATIBLE_ADAPTER` 拒绝，而不是静默忽略。

边界说明：W1.4.4 不实现成本统计/预算扣减、usage-limit UI、Toolset/MCP、ApprovalRequiredToolset、human/deferred SDK resume、真实 cancel ≤5s、multimodal input 或 evidence lookup tool。

## W1.4.4 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.5 范围

本切片关闭 Pydantic AI 默认 SDK session 对 `ExecutionPack.effective_toolsets` / `NodeContract` 工具声明静默忽略的问题；范围只覆盖可验证的 Toolset 构造边界，不伪造 ToolRegistry / SkillRegistry / MCP registry：

- `PydanticAIRunRequest` 新增序列化的 `PydanticAIToolsetRequest` 投影，覆盖 `builtin_tools`、`skill_ids_resolved` 与 `mcp_tools(server_id/tool_name/requires_approval)`。
- `PydanticAIAdapter` 支持注入 `PydanticAIToolsetFactory`；默认未配置 factory 时，声明了工具的 attempt 以 `AA_RUN_TOOL_NOT_FOUND` fail closed，不再静默裸模型运行。
- 默认 SDK session 将 factory 返回的 function toolsets / MCP toolsets 传入 `Agent(toolsets=...)`。
- 当 MCP tool ref 标记 `requires_approval=true` 时，adapter 用 Pydantic AI `ApprovalRequiredToolset` 包装 MCP toolsets，并按 `tool_name` / `*` 生成 approval predicate。
- `capabilities()` 仅在配置 `PydanticAIToolsetFactory` 时打开 `tool_call/mcp` 与 `max_tool_iterations=16`；`human_in_the_loop/deferred_tool_results/cancel/evidence_lookup_tool` 仍保持 false/empty，因为本切片未实现 SDK resume、deferred result 回填、cancel ≤5s 或 evidence lookup tool。

边界说明：W1.4.5 不新增 production dependency，不实现真实内置工具、Skill、MCP server registry，不实现 ApprovalRequiredToolset 后的 human/deferred SDK resume，不打开 evidence lookup，不处理成本预算/扣减，也不修改 spec/ADR。

## W1.4.5 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.6 范围

本切片关闭 Pydantic AI 默认 SDK session 对 `ExecutionPack.retry_policy` 中 SDK 可直接承载维度的忽略问题；范围只覆盖 Pydantic AI 1.107.0 `Agent(retries=...)` 的真实能力：

- `PydanticAIRunRequest` 新增 `PydanticAIRetryPolicy` 投影，当前用来承载 SDK 可映射字段以及不支持项的 fail-closed 检查。
- 默认 SDK session 将 `output_validation_retries -> retries["output"]`，整数型 `tool_retries -> retries["tools"]` 传入 `Agent(...)`。
- `tool_retries` 为 per-tool map 时，当前 SDK `AgentRetries` 无法直接表达；本切片以 `AA_PREPARE_INCOMPATIBLE_ADAPTER` fail closed，不静默降级成全局工具重试。
- Pydantic AI 1.107.0 的 `AgentRetries` 只有 `tools/output`，没有 spec 中早期设想的 `model` 字段；W1.4.6 先对显式非零 `model_retries` fail closed，避免无法由 SDK 执行却静默丢弃。W1.4.11 已把该字段迁移到 adapter-owned retry，不再传给 SDK。

边界说明：W1.4.6 不实现 `retry_policy.max_attempts`（NodeRunner 已负责 attempt 级上限）、`backoff/escalation_after`（ModelRouter/runner 责任）、`timeout_seconds`（后续 cancel/timeout 切片处理）、per-tool retry map、成本预算、human/deferred SDK resume 或真实 registry。

## W1.4.6 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.7 范围

本切片关闭 Pydantic AI 默认 SDK session 对 human/deferred resume 的缺口；范围只覆盖 Pydantic AI 1.107.0 `DeferredToolRequests` / `DeferredToolResults` 的真实 API，不实现真实 registry、cancel 时限或成本预算：

- 当 `ExecutionPack` 声明 toolset 且配置了 `PydanticAIToolsetFactory` 时，默认 SDK session 将 `output_type` 扩展为 `[StructuredDict(...), DeferredToolRequests]`，允许 SDK 以 deferred control-flow output 结束。
- 默认 SDK session 捕获上一轮 SDK `message_history`，在 resume 时传入 `message_history=...` 与 `deferred_tool_results=DeferredToolResults(...)`。
- SDK `DeferredToolRequests.calls` 转译为 `tool.call_started` + `human.gate_required`，等待外部工具结果回填。
- SDK `DeferredToolRequests.approvals` 转译为 `tool.approval_required` + `human.gate_required`，支持 `human_decision` 生成 approval result。
- `PydanticAIAdapter.capabilities()` 仅在配置 toolset factory 时打开 `human_in_the_loop/deferred_tool_results`，避免没有工具执行面的裸模型路径被 ModelRouter 误选。
- malformed deferred result payload 以 `AA_RESUME_INVALID_KIND` fail closed，不把不完整结果传给 SDK。

边界说明：W1.4.7 不实现真实 ToolRegistry / SkillRegistry / MCP registry，不实现 `cancel()` ≤5s 实测，不处理 `RetryPolicy.timeout_seconds`，不实现成本预算/扣减，不接入 model HTTP retry utilities，也不推进 ClaudeCodeAdapter 真实 SDK/session。

## W1.4.7 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.8 范围

本切片关闭 `ExecutionPack.retry_policy.timeout_seconds` 在 PydanticAIAdapter 默认/注入 session 路径中被忽略的问题；范围只覆盖 `node_contract.md` §11 明确要求的 adapter 层 attempt 总时限控制：

- `PydanticAIRetryPolicy` 增加 `timeout_seconds` 投影，来自 `ExecutionPack.retry_policy.timeout_seconds`。
- `PydanticAIAdapter.run()` / `resume()` 在 session async iterator 外层按总 elapsed 执行 `asyncio.wait_for` 控制，超时后调用 `session.cancel(handle_id, CancelReason.IDLE_TIMEOUT)`。
- 超时使用 spec 内既有 `AA_RUN_CANCELLED`，payload 带 `reason=idle_timeout`、`timeout_seconds` 与 `operation`，StreamEvent 仍按 `stream_event.md` 已接受事件类型产出 `attempt.failed`，`AttemptOutcome.state=CANCELLED`。
- `capabilities().cancel` 继续保持 `False`，因为本切片只证明 timeout 能终止 adapter 迭代器，不宣称用户主动 cancel 已实测满足 `agent_adapter.md` D-AA-6 的上游优雅取消语义。

边界说明：W1.4.8 不实现真实 ToolRegistry / SkillRegistry / MCP registry，不实现用户主动 `cancel()` ≤5s 实测，不接入 model HTTP retry utilities，不实现成本预算/扣减，也不推进 ClaudeCodeAdapter 真实 SDK/session。

## W1.4.8 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.9 范围

本切片关闭 PydanticAIAdapter 主动 `cancel()` 只能设置 handle 状态、但无法唤醒正在等待 session 下一帧的 `run()` / `resume()` iterator 的问题；范围只覆盖 adapter seam 内的 iterator 退出控制，不宣称真实 SDK 上游推理已被实测终止：

- 为每个 prepared handle 建立 cancel event，`cancel()` 设置 `cancellation_requested`、记录 `CancelReason`，并唤醒正在等待 session event 的 adapter iterator。
- `run()` / `resume()` 的 session event 消费同时等待下一帧、cancel event 与 `retry_policy.timeout_seconds` deadline；主动 cancel 会取消当前 `__anext__()` future，并以既有 `AA_RUN_CANCELLED` 产出终端 `attempt.failed`。
- `resume()` 路径同样支持主动取消，确保 `human.gate_resolved` 后阻塞在 session resume 时也能退出。
- `capabilities().cancel` 继续保持 `False`，因为当前默认环境未安装 optional `pydantic_ai`，本切片未对真实 SDK-backed upstream cancellation 做端到端实测。

边界说明：W1.4.9 不实现真实 ToolRegistry / SkillRegistry / MCP registry，不接入 model HTTP retry utilities，不实现真实 SDK 上游 `cancel()` ≤5s 实测，不实现成本预算/扣减，也不推进 ClaudeCodeAdapter 真实 SDK/session。

## W1.4.9 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.10 范围

本切片把 W1.4.5 的 `PydanticAIToolsetFactory` seam 收敛成可复用的 registry-backed resolver；范围只覆盖"已经由运行时上游构造好的 SDK toolset"如何按 `ExecutionPack` 声明解析到 Pydantic AI `Agent(toolsets=...)`：

- 新增 `PydanticAIToolsetRegistry`，支持注册 builtin toolset、`skill_id@version` resolved skill toolset 与 MCP server toolset，并通过 `as_factory()` 复用既有 SDK session seam。
- registry 按 `PydanticAIToolsetRequest` 的声明顺序解析 builtin、resolved skill、MCP server，去重后保持确定性输出。
- 任一声明的 builtin / skill / MCP server 未注册时，以 `AA_RUN_TOOL_NOT_FOUND` fail closed，并在 error payload 中列出缺失项；不会退化成裸模型运行。
- MCP server toolset 仍经过既有 `ApprovalRequiredToolset` 包装逻辑，保持 server 作用域和 `tool_name` / `*` approval predicate。
- 默认 adapter 没有配置 factory/registry 时行为不变；capabilities 仍只在显式配置 toolset factory 时打开 tool/MCP/HITL/deferred 能力，`cancel=False` 不变。

边界说明：W1.4.10 不实现真实内置工具函数、SkillRegistry 加载器、MCP client/server lifecycle，不新增 production dependency，不接入 evidence lookup，不处理 model HTTP retry utilities、成本预算/扣减、真实 SDK 上游 `cancel()` <=5s 实测，也不推进 ClaudeCodeAdapter 真实 SDK/session。

## W1.4.10 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.11 范围

本切片关闭 W1.4.6 留下的 `retry_policy.model_retries` 缺口：Pydantic AI SDK `AgentRetries` 不能表达 model retry，因此由 PydanticAIAdapter 在 session 边界执行 adapter-owned retry。

- `_sdk_agent_retries()` 继续只向 SDK 投影 `tools/output`，不把 `model_retries` 传给 Pydantic AI SDK。
- `run()` 对 retryable model/session exceptions 执行 `retry_policy.model_retries` 次重试；retry 前关闭并丢弃失败 session，重新创建 session，避免复用不确定状态。
- retryable model/session exceptions 包括 Python `TimeoutError` / `ConnectionError`、常见 HTTP/API transport exception class name，以及 HTTP status `408` / `429` / `5xx`；其它 `4xx` 模型 HTTP 错误按 `AA_RUN_STREAM_INTERRUPTED` 失败但不重试。
- Agent / toolset 构造与 registry factory 失败不进入 model retry，避免把工具面配置或 MCP/Skill registry 问题误判为模型请求瞬态失败。
- 已经发出任何 raw stream event 后发生的异常不重试，避免前端收到 partial delta 后 adapter 重放整次请求导致重复输出。
- 重试耗尽后以 `AA_RUN_STREAM_INTERRUPTED` 失败，payload 记录 `model_retries`、`model_retry_attempts` 与 `model_retries_exhausted=true`。

边界说明：W1.4.11 不实现 `retry_policy.backoff` 的真实等待策略，不处理 `escalation_after` / ModelRouter 升级，不实现 per-tool retry map、成本预算/扣减、真实 SDK 上游 `cancel()` <=5s 实测、真实内置工具 / SkillRegistry / MCP lifecycle，也不推进 ClaudeCodeAdapter 真实 SDK/session。

## W1.4.11 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.12 范围

本切片关闭 PydanticAIAdapter 对 `UsageLimits.max_cost_usd` 的最小可验证闭环：SDK 不原生执行成本预算，因此 adapter 只在 SDK/raw result 已回传 `RunUsage.est_cost_usd` 时按该已审计数值判定；若配置了 `max_cost_usd` 但完成事件没有成本估算，必须 fail closed。

- `_sdk_usage_limits()` 继续只向 Pydantic AI SDK 投影 token limits：`max_input_tokens`、`max_output_tokens`、`max_total_tokens`；只配置 `max_cost_usd` 时不构造 SDK `UsageLimits`。
- `attempt.completed` 前执行 adapter-side cost guard：`est_cost_usd <= max_cost_usd` 才落成功 output / messages / usage。
- `est_cost_usd > max_cost_usd` 映射为 `AA_RUN_USAGE_LIMIT` / `usage_limit_exceeded`，payload 记录 `usage_limit=max_cost_usd`、`limit` 与 `est_cost_usd`。
- 配置 `max_cost_usd` 但无 `est_cost_usd` 时同样映射为 `AA_RUN_USAGE_LIMIT`，payload 记录 `reason=missing_est_cost_usd`，避免无法审计的成功结果绕过成本边界。

边界说明：W1.4.12 不实现模型价格表、成本估算器、跨 attempt/run 的预算 ledger 或扣减，不改变 NodeRunner / ModelRouter 的全局预算语义，也不推进真实内置工具 / SkillRegistry / MCP lifecycle、真实 SDK 上游 `cancel()` <=5s 实测或 ClaudeCodeAdapter 真实 SDK/session。

## W1.4.12 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.13 范围

本切片推进 W1.4.10 留下的“真实内置工具函数”缺口：`PydanticAIToolsetRegistry` 除了已构造 SDK toolset，也可以注册 CW builtin tool id 到 Python callable；默认 SDK session 在运行期 lazy 构造 Pydantic AI `FunctionToolset` 并把 callable 按 CW tool id 暴露给 SDK。

- 新增 `PydanticAIBuiltinToolFunc` type alias，以及 `PydanticAIToolsetRegistry.register_builtin_tool_function(tool_id, func)`。
- registry resolution 对 `builtin_tools` 先保留已注册 SDK toolset 兼容路径；未命中 toolset 但命中 callable 时，构造一个 SDK `FunctionToolset(id="cw_builtin_tools")`。
- callable 通过 `FunctionToolset.add_function(func, name=tool_id)` 注册，保证 SDK tool 名使用 CW NodeContract / ToolsetSpec 中的 builtin tool id，而不是依赖 Python 函数名。
- 未注册 builtin、Skill、MCP 仍按既有 `AA_RUN_TOOL_NOT_FOUND` fail closed；SDK 缺少 `FunctionToolset` 或 `add_function` 时按 `AA_RUN_INTERNAL` fail closed。

边界说明：W1.4.13 不实现具体内置工具函数清单、不提供 Python sandbox / file_io / web_fetch 的运行实现，不实现 SkillRegistry loader、MCP client/server lifecycle、evidence lookup 自动注入、真实 SDK 上游 `cancel()` <=5s 实测、模型价格估算 / budget ledger / 成本扣减，也不推进 ClaudeCodeAdapter 真实 SDK/session。

## W1.4.13 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.14 范围

本切片关闭 W1.4.13 留下的 `evidence_lookup` 具体内置工具缺口：当 attempt 已携带 `ExecutionPack.evidence_pack`，且节点声明了 `evidence_requirements` 或显式请求 `evidence_lookup` builtin tool 时，PydanticAIAdapter 默认 SDK session 自动注入 adapter-owned `FunctionToolset`。

- `PydanticAIRunRequest` 携带可序列化的 `evidence_pack` snapshot，只在 `_should_enable_evidence_lookup(...)` 成立时投影。
- 默认 SDK session 把 `evidence_lookup(evidence_id)` 注册为 Pydantic AI `FunctionToolset(id="cw_builtin_tools")` 中的函数工具，返回 `EvidencePack.evidences` 中匹配 `evidence_id` 的 serialized Evidence dict。
- 未命中 evidence id 时抛出 `LookupError("EP_RUNTIME_EVIDENCE_LOOKUP_MISS: ...")`，不退化为空结果或外部检索。
- `evidence_lookup` 由 adapter 内部消费，不传给外部 `PydanticAIToolsetFactory`；同一请求中的其它 builtin / Skill / MCP 声明仍按既有 factory / registry 路径 fail closed 或解析。
- `capabilities()` 现在声明 `evidence_lookup_tool=True`；generic `tool_call` 与 `max_tool_iterations` 仍保持 factory-gated，避免把 `web_fetch` / `file_io` / `python_sandbox` 等未实现工具误报为可路由，`cancel=False` 不变。

边界说明：W1.4.14 不实现 `web_fetch` / `file_io` / `python_sandbox`，不做引用检索、rerank、EvidencePack 构建或 mutation，不实现 SkillRegistry loader / MCP client lifecycle，不处理模型价格估算 / budget ledger / 成本扣减、真实 SDK 上游 `cancel()` <=5s 实测，也不推进 ClaudeCodeAdapter 真实 SDK/session。

## W1.4.14 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.15 范围

本切片推进 W1.4.9 留下的默认 SDK session 上游取消缺口：`PydanticAIAdapter.cancel()` 已能唤醒 adapter iterator；现在默认 `PydanticAISDKSession` 还会记录活动 `agent.run_stream_events(...)` stream，并把主动取消转发给该 stream。

- `PydanticAISDKSession` 在 `async with agent.run_stream_events(...)` 生命周期内按 `handle_id` 记录 stream context 与 active stream，退出时确定性清理。
- `PydanticAISDKSession.cancel(handle_id, reason)` 优先调用活动 stream 的 `cancel()`；如果 SDK stream 不支持或抛 `NotImplementedError`，降级调用 `aclose()` / `close_stream()` / `close()`。
- `PydanticAISDKSession.aclose()` 会对仍登记的活动 stream 执行同一取消/关闭路径，再清空 agent、message history、run request 与 usage limit 缓存。
- 当 SDK stream 被取消后以 `StopAsyncIteration` 自然结束，adapter 仍产出 spec 内 `attempt.failed` 终端事件，并保持 `AttemptOutcome.state=CANCELLED` 与已记录的 `AA_RUN_CANCELLED`。
- `capabilities().cancel` 继续保持 `False`：当前默认环境未安装 optional `pydantic_ai`，本切片只用 fake SDK 证明 adapter 会触达 SDK stream 取消入口，尚未证明真实 provider 上游推理在 `<=5s` 内停止。

边界说明：W1.4.15 不实现 `web_fetch` / `file_io` / `python_sandbox`，不实现 SkillRegistry loader / MCP client lifecycle，不处理模型价格估算 / budget ledger / 成本扣减，不推进 ClaudeCodeAdapter 真实 SDK/session，也不声明真实外部 provider 端到端 cancel 能力已达标。

## W1.4.15 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.16 范围

本切片推进 M1.4 中 ClaudeCodeAdapter 的真实 SDK/session 接入缺口：`ClaudeCodeAdapter(session_factory=None)` 不再只是测试 seam 报错路径，而是默认创建一个 lazy optional `ClaudeCodeSDKSession`，通过 Anthropic Claude Agent SDK 的持续会话客户端执行一次 CW attempt。

- 新增 `ClaudeCodeSDKSession`，仅在默认 session 运行时 `importlib.import_module("claude_agent_sdk")`；缺少 optional SDK 时转成结构化 `AA_RUN_INTERNAL`，不让 ImportError 外泄。
- 默认 SDK session 构造 `ClaudeAgentOptions` 与 `ClaudeSDKClient`，禁用本地/user/project settings 自动加载（`setting_sources=[]`），只投影 CW prompt 与 `allowed_tools`，保持可复现输入边界。
- 默认 SDK session 使用 `connect()`、`query(prompt)`、`receive_response()` 消费持续会话消息，并将 SDK Assistant/Result/tool block 映射为现有 Claude raw event seam，再复用已审查的 StreamEvent 转译。
- `cancel()` 对默认 SDK client 优先调用 `interrupt()`；若 SDK 不支持该方法则关闭 client。fake SDK 覆盖证明 adapter cancel 会触达 SDK interrupt 入口。
- 保留注入式 `ClaudeCodeSession`/`SessionFactory`，现有 permission prompt → `tool.approval_required` + `human.gate_required`、resume、output validation、finalize 行为不变。

边界说明：W1.4.16 不新增 production dependency，不实现 `web_fetch` / `file_io` / `python_sandbox`，不实现 SkillRegistry loader / MCP client lifecycle，不把 Claude Agent SDK permission callback/HITL 适配推进到真实 provider 路径，不声明真实外部 provider 上游 `cancel()` <=5s 已实测；`capabilities().cancel=True` 沿用 W1.3.12 ClaudeCodeAdapter 已锁能力快照，本切片只增加默认 SDK session 入口的 fake SDK 证据。

## W1.4.16 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.17 范围

本切片关闭 ClaudeCodeAdapter 对 CW 工具白名单投影不完整的问题：`agent_adapter.md` §9.1 要求 ClaudeCodeAdapter 将 `effective_toolsets` 限制到 ClaudeCode permissions/allowed tools 表；W1.4.16 默认 SDK session 只把 `effective_toolsets.builtin_tools` 传给 SDK，遗漏了 NodeContract 层的 `allowed_tools` 与 MCP tool refs。

- `ClaudeCodeAdapter._run_request()` 现在通过 `_claude_allowed_tools(pack)` 构造 `ClaudeCodeRunRequest.allowed_tools`。
- 内置工具来源合并 `NodeContract.allowed_tools` 与 `ExecutionPack.effective_toolsets.builtin_tools`，去重并保留顺序。
- `ExecutionPack.effective_toolsets.mcp_server_ids` 投影为 Claude Agent SDK/Claude Code documented naming pattern `mcp__<server>__*`。
- `NodeContract.mcp_tools` 投影为 `mcp__<server>__<tool>`；当 `tool_name="*"` 时投影为 `mcp__<server>__*`。
- 默认 `ClaudeCodeSDKSession` 继续把 request 的 `allowed_tools` 传入 `ClaudeAgentOptions(allowed_tools=...)`；fake SDK 覆盖证明默认 SDK options 收到同一投影结果。

边界说明：W1.4.17 不实现 `web_fetch` / `file_io` / `python_sandbox` 工具本体，不新增 SkillRegistry loader，不把 SkillRef 映射为 Claude tools，不配置或启动 MCP servers，不改变 `tools` availability preset，也不声明未配置 MCP server 能被实际调用。本切片只关闭 CW tool permission allow-list 到 Claude `allowedTools` 的可验证投影。

## W1.4.17 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.18 范围

本切片关闭 W1.4.17 后留下的 ClaudeCodeAdapter MCP 可见性缺口：`allowed_tools` 只负责 auto-approve，不能让 MCP server 自身出现在 Claude Agent SDK session 中；当 CW pack 声明 MCP server/tool 时，默认 SDK session 还必须收到对应 `mcp_servers` 配置。

- `ClaudeCodeRunRequest` 新增内部 `mcp_servers` 字段，用于把当前 attempt 的 MCP server 配置交给默认 `ClaudeCodeSDKSession`。
- `ClaudeCodeAdapter._run_request()` 现在从 `ExecutionPack.effective_toolsets.mcp_server_ids` 与 `NodeContract.mcp_tools[*].server_id` 收集本次实际请求的 MCP server id，去重保序。
- MCP server 配置来源限定为已有 `AdapterConfig.settings["mcp_servers"]`；该字段是 adapter-local SDK 配置桥，不修改 accepted schema，也不把项目级 `mcp.config.json` 的 lifecycle/secret 解析纳入本切片。
- 默认 SDK options 现在传入 `mcp_servers={...}`，且只包含本次 pack 实际请求的 server，不把 adapter config 中未请求的 server 暴露给 SDK。
- 当 pack 声明了 MCP server/tool 但 adapter config 没有对应 server 配置时，adapter 在调用 SDK/session 前以 `AA_RUN_TOOL_NOT_FOUND` 结构化失败，避免只传 allowed tool 名称却实际不可调用。

边界说明：W1.4.18 不实现 MCP client/server 启动与健康检查，不解析 `mcp.config.json` / `secret_ref`，不实现 OAuth/headers/env secret 注入，不实现 SDK MCP server 对象构造，不改变 Claude permission callback/HITL 路径，不声明真实 provider smoke 或 MCP tool 调用已实测。本切片只证明 adapter-local MCP server config 会按 ExecutionPack 权限边界传给 Claude Agent SDK。

## W1.4.18 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.19 范围

本切片关闭 W1.4.17/W1.4.18 后留下的 ClaudeCodeAdapter 权限语义缺口：Claude Agent SDK 的 `allowed_tools` 只表示 auto-approve，不会把未列出的工具从执行面移除；但 `node_contract.md` 明确 `allowed_tools` / `mcp_tools` 是节点权限上限，超出必须拒绝。

- 默认 `ClaudeCodeSDKSession` 构造 `ClaudeAgentOptions` 时现在传入 `permission_mode="dontAsk"`。
- 结合 W1.4.17 的 `allowed_tools` 投影，列表内工具 auto-approve，列表外工具由 SDK 直接拒绝，不再进入交互式询问或宽松 permission mode。
- 结合 W1.4.18 的 requested-only `mcp_servers` 投影，MCP server 可见性与工具 auto-approve 都由当前 `ExecutionPack` 权限声明限定。
- fake SDK 覆盖证明无工具默认路径和带 builtin/MCP tools 路径都会收到 `permission_mode="dontAsk"`。

边界说明：W1.4.19 不实现 `can_use_tool` 交互回调，不实现 Claude Agent SDK permission callback → CW `human.gate_required` 的真实 provider 桥接，不新增 PreToolUse hook / streaming input workaround，不改变既有注入式 fake session 的 `permission_prompt` 事件转译回归，不实现项目级 MCP config/secret 解析或真实 provider smoke。本切片只把默认 SDK session 的权限模式收敛为符合 NodeContract 上限语义的 locked-down 模式。

## W1.4.19 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.20 范围

本切片开始关闭 M1.4 后续“具体内置工具函数”缺口，但只实现一个可独立验证的默认 builtin：`web_fetch`。

- 新增 `cw_runtime.adapters.builtin_tools.web_fetch`，使用标准库执行 HTTP(S) GET，不新增 production dependency。
- `web_fetch` 只允许 public HTTP(S) URL：拒绝非 http/https scheme、URL credentials、localhost、private/link-local/reserved/multicast 等非 public unicast 解析结果；解析并校验后只连接已验证 IP，避免 hostname 二次解析 / DNS rebinding；不使用环境代理、不跟随 HTTP redirect；读取响应时限制字节数并返回 text payload、status、content-type、final_url、truncated 等结构化字段。
- 默认 `PydanticAISDKSession` 在 `ExecutionPack` 请求 `web_fetch` 时自动构造 adapter-owned `FunctionToolset`，并把已处理的 builtin 从外部 `toolset_factory` 请求里剥离。
- `ExecutionPack.effective_toolsets.builtin_tools` 不能越过 `NodeContract.allowed_tools` 权限上限；effective builtin 未被 allowed whitelist 授权时在 Adapter 层以 `AA_PREPARE_INVALID_PACK` fail closed。
- 未实现 builtin（例如 `python_sandbox` / `file_io`）继续保持 fail-closed：没有外部 factory 时仍以 `AA_RUN_TOOL_NOT_FOUND` 失败，不伪造工具可用性。
- 默认 `PydanticAIAdapter` descriptor 现在可以声明简单 `tool_call=True`，但 `mcp` / `human_in_the_loop` / `deferred_tool_results` 仍只在外部 `toolset_factory` 配置时声明。

边界说明：W1.4.20 不实现 `file_io`、`python_sandbox`、ToolRegistry/SkillRegistry loader、MCP client/server lifecycle、Pydantic AI ApprovalRequiredToolset 真实权限策略、真实 provider smoke，也不改变 ModelRouter 对具体工具名的匹配策略；未知或未实现工具必须继续在 Adapter 层 fail closed。

## W1.4.20 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/builtin_tools.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/builtin_tools.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.21 范围

本切片继续关闭 M1.4 具体内置工具函数缺口，在 W1.4.20 `web_fetch` 之后新增一个受控的默认 builtin：`file_io`。

- 新增 `cw_runtime.adapters.builtin_tools.file_io_for_project_root(project_root)`，生成只读、项目根目录受限的 callable；默认支持 `read_text` / `list_dir` / `stat` 三个操作，不新增 production dependency。
- `file_io` 的根目录只来自 `ExecutionPack.metadata["cw"]["project_root"]`，不新增 schema 字段；根目录必须是已初始化的 CW project root（存在 `.agent-workflow/`）。
- 路径解析使用真实路径校验：相对路径以 project root 为基准，绝对路径也必须 resolve 后留在 project root 内；symlink escape / `..` escape 会被拒绝。
- `.agent-workflow/` 与 `.git/` 属于内部目录，不能读取或 stat；项目根目录列表也过滤这些内部目录，避免模型看到 runtime/secure/cache/VCS 内部路径。
- 默认 `PydanticAISDKSession` 在 `ExecutionPack` 请求 `file_io` 时自动构造 adapter-owned `FunctionToolset`；缺失或无效 `metadata.cw.project_root` 时在 SDK Agent 构造前以 `AA_PREPARE_INVALID_PACK` fail closed。
- 已由 adapter 处理的 `file_io` 会从外部 `toolset_factory` 请求中剥离；`python_sandbox` 等未实现 builtin 仍保持外部 factory / fail-closed 语义。

边界说明：W1.4.21 不实现写文件 / 删除文件 / 递归目录遍历 / glob 搜索，不读取 `.agent-workflow` / `.git` 内部目录，不实现 `python_sandbox`、ToolRegistry/SkillRegistry loader、MCP client/server lifecycle、真实 provider smoke、ModelRouter 具体工具名匹配或跨 attempt budget ledger。

## W1.4.21 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/builtin_tools.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/builtin_tools.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.22 范围

本切片补齐 M1.4 默认 Pydantic AI SDK session 的第三个具体内置工具：`python_sandbox`。

- 新增 `cw_runtime.adapters.builtin_tools.python_sandbox`，作为 adapter-owned `FunctionToolset` callable 暴露给 Pydantic AI。
- `python_sandbox` 当前只支持 `action="eval_expr"`：解析并求值一个受限 Python 表达式子集，不使用 `eval()` / `exec()` / 子进程，不新增 production dependency。
- 安全边界：只允许 literals、变量名、算术/布尔/比较、条件表达式、下标/切片，以及 allow-list 函数（如 `sum` / `len` / `range` / `sorted` 等）；拒绝 import、attribute access、lambda、comprehension、assignment、未知函数、keyword args、文件/网络/系统能力。
- 资源边界：限制表达式长度、AST 步数、容器大小、range 项数、嵌套深度、整数 bit 长度和结果 JSON 字符数；变量输入必须是 safe identifier + JSON-like 数据。
- 默认 `PydanticAISDKSession` 在 `ExecutionPack` 请求 `python_sandbox` 时自动构造 adapter-owned `FunctionToolset`，并从外部 `toolset_factory` 请求里剥离；未知 builtin 仍保持 external factory / fail-closed 语义。

边界说明：W1.4.22 不实现任意 Python 脚本执行、不支持 import / IO / network / package access / attribute reflection / mutable state / notebook-style cell，也不实现 ToolRegistry/SkillRegistry loader、MCP client/server lifecycle、真实 provider smoke、ModelRouter 具体工具名匹配或跨 attempt budget ledger。

## W1.4.22 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/builtin_tools.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/builtin_tools.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_20_builtin_tools.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.23 范围

本切片关闭 W1.4.20-W1.4.22 留下的 ModelRouter 具体工具名匹配缺口：默认 Pydantic AI SDK session 已实现 adapter-owned `web_fetch` / `file_io` / `python_sandbox` / `evidence_lookup`，Router 不能只凭 `tool_call=True` 把请求未知 builtin 的节点路由给默认 `pydantic_ai`，再等 Adapter 层失败。

- `AdapterCapabilities` 对齐 `agent_adapter.md` 的 `metadata` 扩展字段，保持 `extra="forbid"`，不新增 spec 字段。
- 默认 `pydantic_ai` Router descriptor 收敛到真实默认 adapter 能力：不声明 MCP / HITL / deferred tool results / cancel / multimodal；仅声明已实现的简单 builtin tool calling。
- `PydanticAIAdapter.capabilities()` 在 `metadata.cw` 下声明 `supported_builtin_tools` 与 `supports_unlisted_builtin_tools`。默认 SDK session 是闭合集合；配置外部 `toolset_factory` 时允许外部工厂解析未列入的 builtin。
- ModelRouter capability filter 仅在 Adapter 明确声明闭合 `supported_builtin_tools` 时检查 `NodeContract.allowed_tools`；未声明 metadata 的 Adapter 保持原有 `tool_call` 布尔语义，避免破坏外部 Adapter。
- 未支持 builtin 被剔除为 `MR_CAPABILITY_NOT_MET:unsupported_builtin_tools`，总体路由错误仍归并到既有 `MR_CAPABILITY_NOT_MET`。

边界说明：W1.4.23 不实现 ToolRegistry/SkillRegistry loader、MCP client/server lifecycle、Pydantic AI ApprovalRequiredToolset 真实权限策略、真实 provider smoke、项目级 MCP config/secret 解析或跨 attempt budget ledger。

## W1.4.23 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_9_model_router.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/model_router/router.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_3_9_model_router.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/model_router/router.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_3_9_model_router.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.24 范围

本切片关闭 runtime harness 中 `usage.jsonl` 已建文件但 terminal attempt 不写入用量 ledger 的缺口。`runtime_harness.md` §3.6 要求每 5 分钟或每 attempt 完成写 `{run_id, input_tokens, output_tokens, est_cost_usd?, at}`；当前 deterministic runner 只把 usage 放进 attempt / event payload，未落 `usage.jsonl`。

- `ExecutionAdvanceInput` / `EvaluationAdvanceInput` / `RepairAdvanceInput` 新增 `usage: RunUsage`，默认 `RunUsage()`，供 deterministic runner 或后续 Adapter bridge 投影已知用量。
- `_completed_attempt()` / `_failed_attempt()` 使用传入 usage，避免 `attempts.jsonl` 与 StreamEvent payload 分叉。
- 每次 terminal attempt 写入 `attempts.jsonl` 后追加一条 `usage.jsonl` record，包含 `usage_id`、`run_id`、`node_id`、`attempt_id`、`attempt_index`、`model_profile_id`、`at` 与 `RunUsage` 字段；`runtime_store` 继续按既有 `usage_id / attempt_id` 投影到 SQLite 索引。
- EvaluationResult.usage 与 RepairProvenance.usage 同步 deterministic input usage，保证 evaluation/repair 专用记录与 NodeAttempt ledger 一致。

边界说明：W1.4.24 不实现模型价格表、token 估算器、跨 attempt/run 的预算扣减、usage limit UI、实时 5 分钟周期 flush、真实 Adapter 调用接线或 provider smoke；只持久化调用方/adapter 已提供的 `RunUsage`。

## W1.4.24 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_5_node_runner.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
uv run ruff check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.25 范围

本切片关闭项目级 `skills.config.json` / `mcp.config.json` 已存在但 workflow 编译与运行入口不会使用它们做 L4 availability 校验的缺口。`runtime_harness.md` 将这两个 manifest 定义为项目级 Skill 启用 + 版本固化、MCP Server 启用 + 凭证引用；当前 compiler 已有 `WorkflowValidationContext.available_skill_ids / available_mcp_server_ids`，但项目加载路径没有把 manifest 投影进去。

- `cw_runtime.harness.project` 新增只读 availability loader，从 `.agent-workflow/skills.config.json` / `mcp.config.json` 提取 `enabled != false` 的 `skill_id` / `server_id`。
- loader 只解释 top-level array、entry object、非空 ID 与 `enabled` bool；不可读、不合形或字段非法的 manifest / entry 不启用任何对应工具，最终由已有 `WG_L4_UNKNOWN_SKILL / WG_L4_UNKNOWN_MCP` 在 workflow 引用处 fail closed，不扩大 RH 错误码语义。
- `load_and_compile_workflow()` 在未显式传入 context 时使用项目 availability context；`create_workflow_run()`、`advance_workflow_run()`、`resolve_human_decision()` 复用同一 context，防止运行路径绕过 `WG_L4_UNKNOWN_SKILL / WG_L4_UNKNOWN_MCP`。

边界说明：W1.4.25 不实现 SkillRegistry loader、MCP client/server 启动、工具快照、OAuth/header/env secret 注入、`secure/secrets.encrypted.sqlite` 读取、列表型 manifest 写 API、Adapter Toolset 构造、真实 MCP 调用或 provider smoke；只把项目 manifest 的启用 ID 投影到已有 workflow L4 校验。

## W1.4.25 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_3_workflow_compiler.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/engine/compiler.py apps/runtime/src/cw_runtime/runs/lifecycle.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_3_workflow_compiler.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/engine/compiler.py apps/runtime/src/cw_runtime/engine/__init__.py apps/runtime/src/cw_runtime/runs/lifecycle.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_3_workflow_compiler.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.26 范围

本切片关闭 `runtime_harness.md` §3.7 中 `skill_lock.json` / `mcp_lock.json` 已创建但始终为空的缺口。W1.4.25 已把项目 `skills.config.json` / `mcp.config.json` 投影为 workflow L4 availability；W1.4.26 在 Run 启动时把同一项目配置的启用项固化为 run-scoped replay lock。

- `cw_runtime.harness.project` 新增 `ProjectToolLockSnapshot`，从项目 manifest 生成 `skills` 与 `mcps` lock entry。
- Skill lock entry 只复制 `skill_id`、`version`；MCP lock entry 只复制 `server_id`，并写入临时 `version="latest"` / `tools_snapshot=[]` 占位，等待后续真实 MCP lifecycle / 工具发现切片替换为实测值。
- 同一 ID 只保留首次启用项；`enabled=false`、无效 ID、非法 entry 继续按 W1.4.25 的 empty availability 处理。
- `create_workflow_run()` 在持有 runtime lock 创建 run 目录时写入启动瞬间的 `skill_lock.json` / `mcp_lock.json`，不随之后项目 manifest 变化回写。
- `secret_ref`、transport、command_or_url、requires_approval 等 MCP 配置不进入 run lock，避免把凭证引用或后续 lifecycle 配置混入 replay artifact。

边界说明：W1.4.26 不实现真实 SkillRegistry 解析、checksum 计算、MCP client/server 启动、MCP server 版本解析、工具发现、OAuth/header/env secret 注入、`secure/secrets.encrypted.sqlite` 读取、Adapter Toolset 构造或真实 MCP 调用；只固化 project config 中已锁定的 Skill 版本和 MCP server id。

## W1.4.26 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/runs/lifecycle.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/runs/lifecycle.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.27 范围

本切片关闭 W1.4.25/26 后仍存在的 Skill 版本漂移缺口：项目 `skills.config.json` 已经是 Skill 启用 + 版本固化来源，但 workflow L4 只校验 `skill_id`，PydanticAIAdapter 直接调用时也没有按项目 manifest 限制 `skill_id@version`。

- `cw_runtime.harness.project` 的 `ProjectToolAvailability` 增加 `skill_refs`，从 enabled `skills.config.json` 条目生成 `skill_id@version`；版本缺失或空字符串时沿用 `"latest"`。
- `WorkflowValidationContext` 增加 `available_skill_refs`，项目加载路径会同时传入 enabled skill id 和 enabled versioned refs。
- `compile_workflow_graph()` 继续用 `WG_L4_UNKNOWN_SKILL` 处理未知 Skill；当 Skill id 存在但 `SkillRef.version` 未启用时，details 额外包含 `version` 与 `skill_ref`。
- `PydanticAIAdapter` 默认 SDK session 在 `ExecutionPack.metadata.cw.project_root` 存在且请求 `skill_ids_resolved` 时，先按项目 enabled `skill_id@version` 校验，再调用外部 `toolset_factory`。
- Adapter 端项目 Skill 校验失败以 `AA_RUN_TOOL_NOT_FOUND` fail closed，并在 SDK `Agent` 构造前终止。

边界说明：W1.4.27 不定义新的 Skill 文件/包格式，不加载项目内 Python 代码，不解释 `skills.config.json[*].params`，不计算 checksum，不实现 MCP client/server lifecycle、secret 读取、真实 provider smoke、模型价格估算或跨 attempt budget ledger；外部构造好的 SDK Skill toolset 仍由既有 `PydanticAIToolsetRegistry` / `toolset_factory` 提供。

## W1.4.27 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_3_workflow_compiler.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/engine/compiler.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_3_workflow_compiler.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/engine/compiler.py apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_3_workflow_compiler.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.28 范围

本切片关闭 W1.4.18/W1.4.25 后仍存在的项目 MCP 配置桥接缺口：ClaudeCodeAdapter 已支持 adapter-local `AdapterConfig.settings["mcp_servers"]`，workflow L4 也能用项目 `mcp.config.json` 校验 server id，但默认 run/attempt 路径尚不能把项目级 `mcp.config.json` 投影为 Claude Agent SDK 可消费的 `mcp_servers`。

- `cw_runtime.harness.project` 新增只读 `ProjectMCPServerConfig` 与 `load_project_mcp_server_configs()`，从 enabled `mcp.config.json` 条目读取 `server_id / transport / command_or_url / requires_approval / secret_ref`。
- MCP config loader 只复制 `runtime_harness.md §2.8` 已锁字段；`version` / `tools_snapshot` 等非 spec 字段不进入运行时配置。
- ClaudeCodeAdapter 在 `ExecutionPack.metadata.cw.project_root` 存在时，优先按项目 `mcp.config.json` 解析本次 requested MCP server config；无项目 root 时保留 W1.4.18 的 adapter-local settings fallback。
- 项目 MCP config 支持投影 `transport=http|sse` 到 `{type, url}`，`transport=stdio` 到 `{command}`；转换只覆盖本次 requested server。
- 带 `secret_ref` 或 `requires_approval=true` 的项目 MCP server 暂时 fail closed，避免在 secret resolver 与 `can_use_tool`/HITL 桥未完成前把凭证或审批语义静默丢失。

边界说明：W1.4.28 不启动 MCP client/server，不做健康检查，不解密或读取 `secure/secrets.encrypted.sqlite`，不实现 OAuth/header/env secret 注入，不发现 MCP tools，不替换 `mcp_lock.json.tools_snapshot=[]` 占位，不改变 Claude Agent SDK `can_use_tool`/HITL provider 桥，也不声明真实 MCP 调用或 provider smoke 已完成。

## W1.4.28 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.29 范围

本切片关闭 W1.4.24 后留下的 `usage.jsonl` 只追加、不参与预算闭环的问题。`runtime_harness.md §3.6` 已要求 terminal attempt 写 usage ledger，`agent_adapter.md §3.1/§13` 已锁定 `UsageLimits` 与 `AA_RUN_USAGE_LIMIT`；W1.4.29 在 deterministic runner 内实现可本地验证的 run 级累计预算守卫：

- Run 启动 metadata 中的 `metadata.cw.usage_limits` 复用既有 `UsageLimits` schema；不新增 `WorkflowRunStartRequest` 字段、不修改 accepted spec。
- `_prepare_attempt()` 将 run 级 usage limits 投影进静态 `ExecutionPack.usage_limits`，让 attempt artifact 暴露本次 run 的预算口径。
- execution / evaluation / repair 三类 attempt 在消费 usage 后，从 `usage.jsonl` 权威 ledger 聚合历史用量，再加当前 attempt usage，检查累计 `max_input_tokens / max_output_tokens / max_total_tokens / max_cost_usd`。
- 命中预算后写入 failed `NodeAttempt` 与本次 usage ledger，发 `attempt.failed`、`node.state_changed(to=failed)`、`error.budget_exhausted`、`run.failed`，并把 `failure_summary.error_code` 记录为 `AA_RUN_USAGE_LIMIT`。
- 配置 `max_cost_usd` 但历史或当前 usage 缺失 `est_cost_usd` 时 fail closed，避免不可审计成本绕过预算。
- 每次追加 usage 后更新 `run.json.summary_metrics.usage` 作为从 `usage.jsonl` 派生的累计缓存；`usage.jsonl` 仍是权威来源。

边界说明：W1.4.29 不实现模型价格表、token 估算器、实时 5 分钟 flush、UI budget 设置页、真实 Adapter bridge 接线、外部 provider smoke 或跨 run / 项目级预算；只在 deterministic runner 已收到 `RunUsage` 的前提下执行 run 内累计 guard。

## W1.4.29 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_3_6_static_builders.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/builders/static_packs.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
uv run ruff check apps/runtime/src/cw_runtime/builders/static_packs.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.30 范围

本切片关闭 W1.4.29 后留下的“有 token usage 和 ModelProfile 价格，但缺失 `RunUsage.est_cost_usd` 仍只能 fail-closed”的缺口。`model_router.md §1.3` 已锁定 `ModelCostProfile.input_per_million_usd / output_per_million_usd`；W1.4.30 在 deterministic runner 内把已选 ModelProfile 的静态价格投影到 terminal attempt usage：

- `ModelRouter` 新增纯函数 `estimate_usage_cost_usd()`，按 `RunUsage` token 与 `ModelCostProfile` 每百万 token 价格估算 USD；cache creation/read token 暂按 input rate 保守计入，避免成本预算低估。
- `_prepare_attempt()` 将 routing 选中的 `ModelCostProfile` 存入 runner 内部 `_AttemptArtifacts`，后续 budget/ledger 不重新读取项目 settings。
- execution / evaluation / repair 三类 terminal attempt 在 budget guard、`attempts.jsonl`、`usage.jsonl`、attempt event、`EvaluationResult.usage`、`RepairProvenance.usage` 写入前共享同一个 cost-enriched `RunUsage`。
- 已有 Adapter/调用方回传的 `est_cost_usd` 优先；只在本次输入显式提供 `usage` 且 cost 缺失时估算。
- 若选中 profile 的 input/output 价格任一为 `null`，仍保留缺失 cost，并让 `max_cost_usd` 按 W1.4.29 继续 fail closed。

边界说明：W1.4.30 不修改 accepted spec / ADR / public schema，不实现外部模型价格同步、provider-specific cache 折扣、实时 usage flush、UI budget 设置、真实 Adapter 执行桥接、外部 provider smoke 或跨 run / 项目级预算；只把已有 ModelRouter 静态价格用于 deterministic runner 已显式收到的 usage。

## W1.4.30 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_3_9_model_router.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/model_router apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
uv run ruff check apps/runtime/src/cw_runtime/model_router apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.31 范围

本切片关闭 W1.4.24-W1.4.30 后仍存在的“Adapter 已能产生 `AttemptOutcome.usage`，但 runner 只能消费 hand-supplied deterministic input”的缺口。W1.4.31 增加显式 adapter-driven execution advance，让真实 `AgentAdapter` outcome 进入 `NodeAttempt` 与 `usage.jsonl`：

- 新增 `advance_workflow_run_with_adapters(project_root, run_id, adapter_factory, ...)`；默认 `advance_workflow_run()` 仍保持 deterministic 行为，避免既有测试和手工推进语义漂移。
- 当前节点为 `execution_task` 且没有显式 `ExecutionAdvanceInput` 时，新入口按 ModelRouter 选中的 `adapter_id/model_profile_id` 构造 `ExecutionPack`，通过传入的 `AdapterFactory` 调用 `prepare/run/finalize`。
- Adapter stream 中 model/tool 等事件会重新分配 run-scoped `seq/event_id` 后写入 run event log；runner-owned lifecycle 事件仍由 runner 自己产生，避免重复 `attempt.completed/failed`。
- `AttemptOutcome` 被归并为 `NodeAttempt`，保留真实 `adapter_id/adapter_version/model_profile_id/output_hash/usage/errors/outcome_hash`；`usage` 继续复用 W1.4.30 的 cost estimation 与 W1.4.29 的 run budget guard。
- Adapter outcome 成功时追加 `attempts.jsonl` 与 `usage.jsonl` 并按 normal edge 路由；budget 超限或 adapter failure 时写 failed attempt + usage ledger，并沿用 `AA_RUN_USAGE_LIMIT` / AdapterError 失败闭环。

边界说明：W1.4.31 不修改 accepted spec / ADR / public schema，不新增默认 AdapterRegistry，不改变 HTTP API，不默认加载 Pydantic AI / Claude SDK，不实现 evaluation / repair 节点的真实 adapter 执行，不实现 async 长运行锁拆分、cancel/resume 桥、真实 provider smoke、MCP lifecycle / secret resolver 或跨 run / 项目级预算。

## W1.4.31 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py apps/runtime/tests/test_m1_3_5_node_runner.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/runner/__init__.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
uv run ruff check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/runner/__init__.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.32 范围

本切片关闭 W1.4.26 后 `mcp_lock.json` 中 `tools_snapshot=[]` 只能作为硬编码占位的问题，但不越界实现真实 MCP client/server lifecycle。W1.4.32 增加注入式 MCP tool discovery seam，让显式 discovery provider 的实测结果进入 run-scoped replay lock：

- 新增 runtime 内部 `ProjectMCPDiscoveredTools` 与 `ProjectMCPToolDiscovery` seam；discovery provider 返回的 `version/tools_snapshot` 会写入 `ProjectMCPLockEntry`。
- `load_project_tool_lock_snapshot(..., mcp_tool_discovery=...)` 只对 spec-valid project MCP config 调用 discovery；缺 `transport` / `command_or_url` 的 enabled server 仍只锁 `server_id`，保持 `version="latest"` / `tools_snapshot=[]`。
- `create_workflow_run(..., mcp_tool_discovery=...)` 透传同一个 seam，Run 启动时把 discovery snapshot 固化到 `runs/<run_id>/mcp_lock.json`。
- 默认 `mcp_tool_discovery=None` 时行为保持 W1.4.26 兼容：不启动 MCP、不做工具发现、不读 secret、不从非 spec 的 `mcp.config.json.tools_snapshot` 字段取值。

边界说明：W1.4.32 不修改 accepted spec / ADR / public schema，不新增 dependency，不启动真实 MCP server，不实现 health check、OAuth/header/env secret resolver、Claude `can_use_tool`/HITL bridge、真实 provider smoke，也不把 `mcp.config.json` 扩展为工具快照来源。

## W1.4.32 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/runs/lifecycle.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/runs/lifecycle.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.33 范围

本切片关闭 W1.4.28 后项目级 `mcp.config.json[*].secret_ref` 只能 fail closed 的一部分缺口，但不越界实现真实 secure store、OAuth 或 provider smoke。W1.4.33 增加 ClaudeCodeAdapter 内部注入式 secret resolver seam，让显式 resolver 返回的运行时 secret material 进入本次 requested MCP server 的 SDK-local config：

- 新增 runtime 内部 `ClaudeCodeMCPSecretResolution` 与 `ClaudeCodeMCPSecretResolver` seam；resolver 接收已解析的 `ProjectMCPServerConfig`，返回可投影到 Claude SDK MCP server config 的 `headers/env`。
- `ClaudeCodeAdapter` 在 `AdapterConfig.settings["project_mcp_secret_resolver"]` 存在且 callable 时使用该 resolver；未配置 resolver 时，带 `secret_ref` 的项目 MCP server 仍以 `AA_RUN_TOOL_NOT_FOUND` fail closed。
- resolver 返回 `None`、抛出普通异常或返回非 `ClaudeCodeMCPSecretResolution` 对象时同样 fail closed，不把带 `secret_ref` 的 MCP server 静默降级为无认证配置。
- secret projection 只作用于当前 `ExecutionPack` 实际 requested MCP server；无项目 root 时仍保留 W1.4.18 adapter-local `settings["mcp_servers"]` fallback。

边界说明：W1.4.33 不修改 accepted spec / ADR / public schema，不新增 dependency，不读取或解密 `secure/secrets.encrypted.sqlite`，不实现 OAuth refresh / token exchange，不启动真实 MCP server，不做 health check、tool discovery provider 或真实 provider smoke，也不改变 Claude `can_use_tool`/HITL bridge。

## W1.4.33 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/src/cw_runtime/adapters/__init__.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/src/cw_runtime/adapters/__init__.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.34 范围

本切片推进 W1.4.32 的注入式 MCP tool discovery seam：把裸 callable 升级为可复用的 provider client lifecycle runner，使显式 provider 能以 `start -> health_check -> discover_tools -> close` 的顺序产生 `mcp_lock.tools_snapshot`。默认 run-start 路径仍不启动 MCP、不读 secret、不做 discovery：

- 新增 runtime 内部 `ProjectMCPHealthCheck`、`ProjectMCPDiscoveryClient`、`ProjectMCPDiscoveryClientFactory`、`ProjectMCPDiscoveryRunner` 与 `ProjectMCPDiscoveryError`。
- `ProjectMCPDiscoveryRunner` 通过 provider client factory 创建 client，按顺序执行 `start(config)`、`health_check()`、`discover_tools()`、`close()`；健康检查失败会在 discovery 前 fail closed，且仍保证 `close()` 被调用。
- `load_project_tool_lock_snapshot(..., mcp_tool_discovery=...)` 继续接收任意 `ProjectMCPToolDiscovery` callable；provider 抛普通异常或返回非 `ProjectMCPDiscoveredTools` 对象时，统一包装为 sanitized `ProjectMCPDiscoveryError`，不把异常消息、`command_or_url` 或 `secret_ref` 放入 details。
- `mcp_lock.json` 仍只写 spec 已有的 `server_id/version/tools_snapshot`；没有扩展 lock schema，也没有新增 RH/AA 错误码。

边界说明：W1.4.34 不修改 accepted spec / ADR / public schema，不新增 dependency，不实现具体 stdio/http/sse MCP 协议客户端，不启动默认 MCP server，不解密或读取 `secure/secrets.encrypted.sqlite`，不实现 OAuth/token exchange，不声明真实 provider smoke 或真实 MCP tool invocation。

## W1.4.34 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.35 范围

本切片关闭 W1.4.34 后“具体 stdio MCP client”缺口的一部分：在显式注入的 `ProjectMCPDiscoveryRunner` 后面新增本地 stdio JSON-RPC discovery client，用 fake stdio server smoke 证明 `initialize -> notifications/initialized -> tools/list -> close` 能产生 `mcp_lock.tools_snapshot`。默认 run-start 路径仍不启动 MCP、不读 secret、不做 discovery：

- 新增 runtime 内部 `ProjectMCPStdioDiscoveryClient`，只支持 `ProjectMCPServerConfig.transport="stdio"`，通过 stdlib `subprocess.Popen` 启动显式配置的本地命令。
- client 发送 MCP `initialize`，校验响应中的 `protocolVersion`，用 `serverInfo.version` 作为 lock version，缺失时回落 protocol version；随后发送 `notifications/initialized`。
- `tools/list` 响应中的 `name`、`title`、`description` 与 `inputSchema` 被归一化为 lock snapshot 的 `name`、`title`、`description`、`input_schema`，仍只写 `runtime_harness.md` 既有 lock 字段。
- JSON-RPC error、非 JSON/object response、缺失 protocol/tool 字段、stdio timeout/write/read 错误均转为 sanitized `ProjectMCPDiscoveryError`；error details 不包含 `command_or_url`、`secret_ref` 或 server error message。
- fake Python stdio server 覆盖 happy path 与 JSON-RPC error sanitization，close 路径显式收尾 stdin/stdout/reader thread，避免测试环境 ResourceWarning。

边界说明：W1.4.35 不修改 accepted spec / ADR / public schema，不新增 dependency，不实现 HTTP/SSE MCP transport、不读取或解密 `secure/secrets.encrypted.sqlite`、不实现 OAuth/token exchange、不实现真实外部 provider smoke、不实现 MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.35 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.36 范围

本切片关闭 W1.4.35 后“HTTP/SSE MCP discovery client”缺口的一部分：新增显式注入的 Streamable HTTP discovery client，用本地 fake HTTP MCP server smoke 证明 `initialize -> notifications/initialized -> tools/list -> close` 能通过 HTTP JSON response 与 SSE response 两种响应形态产生 `mcp_lock.tools_snapshot`。默认 run-start 路径仍不启动 MCP、不读 secret、不做 discovery：

- 新增 runtime 内部 `ProjectMCPHttpDiscoveryClient`，只支持 `ProjectMCPServerConfig.transport="http"`，只接受 `http://` / `https://` endpoint URL。
- client 通过 stdlib `urllib` POST 单个 JSON-RPC message，并发送 `Content-Type: application/json` 与 `Accept: application/json, text/event-stream`；初始化成功后，后续请求带 `MCP-Protocol-Version` 与可选 `Mcp-Session-Id`。
- `initialize` 校验响应中的 `protocolVersion`，用 `serverInfo.version` 作为 lock version，缺失时回落 protocol version；随后发送 `notifications/initialized` 并要求 HTTP 202。
- `tools/list` 支持 `application/json` 单对象响应与 `text/event-stream` SSE 响应；SSE response 中的 unrelated notification 会被忽略，直到匹配 request id 的 JSON-RPC response 到达或 request deadline 超时。
- JSON-RPC error、HTTP status error、非 JSON/object response、unsupported content type、invalid session id、SSE timeout/read/parse 错误均转为 sanitized `ProjectMCPDiscoveryError`；error details 不包含 `command_or_url`、`secret_ref`、HTTP response body 或 server error message。

边界说明：W1.4.36 不修改 accepted spec / ADR / public schema，不新增 dependency，不实现旧版 HTTP+SSE GET endpoint fallback、不读取或解密 `secure/secrets.encrypted.sqlite`、不实现 OAuth/token exchange 或 Authorization header 注入、不实现真实外部 provider smoke、不实现 MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.36 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.37 范围

本切片关闭 W1.4.36 后“旧版 HTTP+SSE GET endpoint fallback”缺口：在 `ProjectMCPHttpDiscoveryClient` 内实现 MCP 2025-03-26 backwards compatibility 路径，用本地 fake legacy HTTP+SSE MCP server smoke 证明 Streamable HTTP `initialize` POST 遇到 4xx 后，可以 GET 同 URL 打开旧版 SSE endpoint，读取 `endpoint` event，并把后续 JSON-RPC POST 到该 endpoint，响应从 SSE `message` event 返回。默认 run-start 路径仍不启动 MCP、不读 secret、不做 discovery：

- `ProjectMCPHttpDiscoveryClient` 保持首选 W1.4.36 Streamable HTTP；只有 `initialize` POST 收到 4xx 时才进入 legacy fallback。
- legacy fallback 的 GET 只发送 `Accept: text/event-stream`，要求 `Content-Type: text/event-stream`，并在 request deadline 内读取首个非空 SSE event。
- legacy `endpoint` event 的 `data` 被解析为相对或绝对 POST endpoint；只接受解析后的 `http://` / `https://` URL，不把 endpoint URL 写入错误 details。
- legacy POST endpoint 只发送 JSON-RPC body 与 `Content-Type: application/json`，不发送 W1.4.36 的 `MCP-Protocol-Version` / `Mcp-Session-Id` header，避免把 Streamable HTTP session 语义混入旧协议。
- legacy SSE `message` event 中的 JSON-RPC response 按 request id 匹配；unrelated notification 会被忽略，JSON-RPC error 继续走 sanitized `ProjectMCPDiscoveryError`。

边界说明：W1.4.37 不修改 accepted spec / ADR / public schema，不新增 dependency，不实现 OAuth/token exchange 或 Authorization header 注入、不读取或解密 `secure/secrets.encrypted.sqlite`、不实现真实外部 provider smoke、不实现 MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.37 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.38 范围

本切片关闭 W1.4.33 后“`secret_ref` 只能依赖外部注入 resolver”的一部分缺口：在不新增 production dependency、不实现 OS keychain / OAuth 的前提下，补齐项目级 `secure/secrets.encrypted.sqlite` 到 Claude MCP `headers/env` 的本地后端。默认 run-start 路径仍不读取 secure store；只有 ClaudeCodeAdapter 收到带 `secret_ref` 的 requested project MCP server，且 AdapterConfig 显式提供 `settings["project_mcp_secret_decryptor"]` 时，才读取该项目的 secure SQLite。

- 新增 `cw_runtime.harness.secrets`：按 `runtime_harness.md` §6 读取 `.agent-workflow/secure/secrets.encrypted.sqlite` 的 `secrets(secret_id, value_encrypted)`；`secret_ref` 作为 `secret_id` 查询，缺库或缺行返回 `None`，由 adapter fail closed。
- 加密原语仍由调用方注入：`ProjectSecretDecryptor` 接收 `value_encrypted` bytes，返回 UTF-8 JSON 明文；本切片不引入 AES-GCM / PBKDF2 / OS keychain 依赖。
- 解密后的 MCP secret material 严格校验为 JSON object，字段只允许 `headers: dict[str, str]` 与 `env: dict[str, str]`，且至少包含一个非空映射；非法 payload 只抛 sanitized `ProjectSecretStoreError`。
- `ClaudeCodeAdapter` 保持原有 `project_mcp_secret_resolver` callable 优先；未配置 resolver 但配置 `project_mcp_secret_decryptor` 时，自动构造 project secure-store backed resolver，把 material 投影为 SDK-local MCP `headers/env`。
- resolver / decryptor / secure store 错误继续走 `AA_RUN_TOOL_NOT_FOUND` fail closed；错误 payload 不包含 `command_or_url`、`secret_ref`、明文 token、密文内容或 SQLite 路径。

边界说明：W1.4.38 不修改 accepted spec / ADR / public schema，不新增 dependency，不写入或迁移 secure store，不实现 AES-GCM-256 / PBKDF2 / OS keychain 主密钥，不实现 OAuth refresh / token exchange，不启动真实 MCP server，不做真实外部 provider smoke，也不改变 Claude `can_use_tool`/HITL 桥。

## W1.4.38 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/secrets.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/src/cw_runtime/adapters/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/harness/secrets.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/src/cw_runtime/adapters/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.39 范围

本切片关闭 W1.4.31 后 adapter runner bridge 只覆盖 `execution_task` 的缺口，把显式 `advance_workflow_run_with_adapters(...)` 扩展到 `evaluation_task` 与 `repair_task`；默认 `advance_workflow_run(...)` 仍保持 deterministic foundation 路径。

- `advance_workflow_run_with_adapters(...)` 在当前节点为 `evaluation_task` 且请求未提供 deterministic `evaluation` input 时，使用 AdapterFactory 执行该 evaluation attempt，并把 `AttemptOutcome.output` 严格校验为 `EvaluationAdvanceInput`。
- 当前节点为 `repair_task` 且请求未提供 deterministic `repair` input 时，使用 AdapterFactory 执行 repair attempt，并把 `AttemptOutcome.output` 严格校验为 `RepairAdvanceInput`。
- evaluation adapter 成功后继续写入 `evaluations.jsonl`、evaluation reflections、adapter provenance attempt、`usage.jsonl` 与 `evaluation.completed` 事件，并按 pass / repair / human / fail 路由。
- repair adapter 成功后继续写入 `repairs.jsonl`、pending prompt overlay、adapter provenance attempt、`usage.jsonl` 与 `repair.patch_proposed` / `repair.patch_applied` 事件，并回流到 repair target。
- adapter 输出不匹配 bridge contract 时复用 `AA_RUN_OUTPUT_VALIDATION_FAILED` fail closed；payload 只记录 node kind、输出类型与 validation error 数量，不持久化原始输出内容。
- evaluation / repair 结果级 metadata 标记为 `cw.adapter_bridge=true`；deterministic foundation 路径继续保留 `cw.foundation_runner=true`。

边界说明：W1.4.39 不修改 accepted spec / ADR / public schema，不新增错误码或 dependency，不实现默认 AdapterRegistry / HTTP API 接线，不拆分 runtime lock，不实现 adapter resume/cancel 桥、真实 provider smoke、MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.39 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
uv run ruff check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.40 范围

本切片关闭 W1.4.39 后 adapter runner bridge 仍在 `runtime.lock` 内 await adapter `prepare/run/finalize` 的问题。目标是缩短锁占用：adapter attempt 的开始状态与 pack 写入仍在锁内完成；adapter 长运行 iterator 在锁外执行；adapter stream event 与最终 attempts/evaluations/repairs/usage 持久化继续用短临界区写入。

- `advance_workflow_run_with_adapters(...)` 对 execution/evaluation/repair adapter 节点拆为 begin -> run -> complete 三段：begin 在锁内写 `attempt.started` 和节点状态，run 在锁外 await adapter，complete 重新获取锁后归并 outcome。
- adapter 产出的非 runner-owned StreamEvent 逐条用短 `runtime.lock` 重新编号并写入，保持 StreamEvent `seq` 单调递增与 run.json 更新边界。
- 若 adapter await 期间 run 被暂停，当前 adapter attempt 仍可 finalize 并把 current node 推进到下一节点，但 run state 保持 `paused`，恢复后从下一节点继续。
- 若 adapter await 期间 run 被取消或 current node 已改变，completion 阶段不覆盖新的 run 状态，并补写 terminal `cancelled` attempt row 与清理 in-flight node metadata；本切片不把这类 preemption 声称为 adapter-level cancel。
- 新增阻塞 fake adapter 回归：adapter `run()` 已进入且尚未释放时，测试必须能获取 `runtime.lock` 并读取 running node state；同一 current node 的第二个 advance 会被 in-flight guard 拒绝；pause 夹入后 attempt 完成、run 保持 paused，resume 后可继续到下一节点。

边界说明：W1.4.40 不修改 accepted spec / ADR / public schema，不新增错误码或 dependency，不实现默认 AdapterRegistry / HTTP API 接线，不实现外部 cancel -> `adapter.cancel()` 的运行中 registry，不改变 deterministic runner 锁语义，不实现真实 provider smoke、MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.40 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
uv run ruff check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.40 审查结果

- 聚焦实现自检已通过：`test_m1_4_31_adapter_runner_bridge.py`、focused mypy、focused ruff；覆盖 adapter `run()` 阶段释放 `runtime.lock`、in-flight current node 拒绝二次 advance、pause 夹入后完成当前 attempt 并保持 run paused。
- 独立 subagent 首轮审查 FAIL 指出 pause/cancel 抢占会留下无 terminal `attempts.jsonl` row 与 running node metadata；已修复为 pause 成功 finalize 后保留 paused 状态，cancel/current-node-change 补写 cancelled attempt 并清理 in-flight node state。
- 本地差异自检确认未修改 accepted spec / ADR / public schema / dependency 文件；旧的未调用 `_advance_*_with_adapter` 锁内 await 私有路径已删除，adapter bridge 统一走 begin -> unlocked run -> short-lock complete。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 402 passed, 1 skipped，codegen 无 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：本切片不实现默认 AdapterRegistry / HTTP API 接线，不实现外部 cancel -> `adapter.cancel()` 的运行中 registry，不实现真实 provider smoke、MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.41 范围

本切片关闭 W1.4.19 留下的 Claude Agent SDK `can_use_tool` 回调未接入缺口，把 `NodeContract.mcp_tools[].requires_approval=true` 的 MCP tool 从 Claude 自动批准列表移出，并通过 SDK permission callback 转成 CW `tool.approval_required -> human.gate_required` 流。

- `ClaudeCodeRunRequest` 区分 SDK `allowed_tools`（自动批准）、`permitted_tools`（CW NodeContract 权限上限）与 `approval_required_tools`（需 human gate 的 MCP tool）。
- 默认路径保持 W1.4.19 锁定行为：未显式配置 `enable_can_use_tool_bridge=true` 时仍传 `permission_mode="dontAsk"`，不注入 `can_use_tool` 或 hook。
- bridge 开启时，默认 SDK session 传 `permission_mode="default"`、`can_use_tool` callback 与 Python SDK 所需的 `PreToolUse` keepalive hook；越权工具直接 deny，非审批工具 allow，审批工具排队生成 `permission_prompt` raw event。
- approval-required tool 会从 SDK 自动批准列表中按重叠关系扣减，避免 `mcp__<server>__*` wildcard 覆盖 `mcp__<server>__<tool>` 后绕过 human gate。
- adapter 继续复用既有 `permission_prompt` 翻译逻辑，保证事件顺序为 `tool.approval_required` parent 后接 `human.gate_required` child；`resume(human_decision)` 先发 `human.gate_resolved` + `tool.approved` / `tool.rejected`，再唤醒 pending SDK callback 并把 approve/reject 映射为 `PermissionResultAllow` / `PermissionResultDeny`。
- fake SDK 回归覆盖 callback 挂起、server wildcard 扣减、run 产出 human gate、approve/reject resume 后 callback 返回 allow/deny 并完成 attempt；保持 optional dependency lazy-load，不新增 production dependency。

边界说明：W1.4.41 不修改 accepted spec / ADR / public schema，不实现真实 Claude provider smoke，不实现长时间跨进程持久化 permission callback，不放开 project `mcp.config.json requires_approval` lifecycle 拒绝，不改变 PydanticAIAdapter HITL 逻辑。

## W1.4.41 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.41 审查结果

- 聚焦实现自检已通过：`test_m1_3_12_claude_code_adapter.py`、focused mypy、focused ruff；覆盖默认 `dontAsk` 无 callback、bridge approve/reject、越权工具 deny、server wildcard 扣减与 legacy `permission_prompt` 路径。
- 独立 subagent 首轮审查 FAIL：修复了 `mcp__<server>__*` wildcard 仍会自动批准 approval-required MCP tool 的绕过问题，并补齐 `human.gate_resolved` 后的 `tool.approved` / `tool.rejected` 事件。
- 独立 subagent 复审 PASS，确认 W1.4.41 diff 中无剩余 actionable finding；归档见 `docs/reviews/m1.4.41-claude-code-can-use-tool-bridge-review.md`。
- 本地差异自检确认未修改 accepted spec / ADR / public schema / dependency 文件；project `mcp.config.json requires_approval` lifecycle 拒绝、真实 Claude provider smoke 与跨进程 pending permission 持久化仍为后续边界。

## W1.4.42 范围

本切片关闭 W1.4.41 明确遗留的 project `mcp.config.json requires_approval` 生命周期拒绝：当 ClaudeCodeAdapter 显式启用 `enable_can_use_tool_bridge=true` 时，项目级 `requires_approval=true` MCP server 会投影为 `approval_required_tools` 中的 `mcp__<server>__*`，并复用 W1.4.41 的 `tool.approval_required -> human.gate_required -> resume` 审批闭环。

- 默认路径保持 fail closed：未开启 `enable_can_use_tool_bridge=true` 时，project `requires_approval=true` 仍返回 `AA_RUN_TOOL_NOT_FOUND`，避免静默丢失审批语义。
- bridge 开启时，project `requires_approval=true` server 不再被 lifecycle gate 拒绝；对应 server wildcard 会从 SDK `allowed_tools` 自动批准列表中扣减，防止 `mcp__<server>__*` 绕过 human gate。
- `secret_ref` 行为不变：没有 resolver / decryptor 时仍 fail closed；本切片只放开 approval lifecycle，不实现 OAuth、secure store 主密钥、MCP health check、tool invocation 或真实 provider smoke。
- project config 仍只读取 `runtime_harness.md §2.8` 已接受字段，不扩展 `mcp.config.json` schema，不修改 accepted spec / ADR / public schema / dependency 文件。

边界说明：W1.4.42 不改变 NodeContract 显式 `mcp_tools[].requires_approval` 语义，不改变 PydanticAIAdapter HITL 逻辑，不实现跨进程 pending permission 持久化，不声明真实 Claude/MCP provider 已验证。

## W1.4.42 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.42 审查结果

- 聚焦实现自检已通过：`test_m1_3_12_claude_code_adapter.py` 27 passed、focused strict mypy、focused ruff；新增覆盖 project `mcp.config.json requires_approval=true` 在 bridge 开启时进入 human gate/resume 路径，既有测试继续覆盖 bridge 未开启时 fail closed。
- 独立 subagent Cicero 审查 PASS，确认当前 diff 无 actionable finding；归档见 `docs/reviews/m1.4.42-project-mcp-approval-bridge-review.md`。
- 本地差异自检确认未修改 accepted spec / ADR / public schema / dependency 文件；真实 Claude/MCP provider smoke、真实 MCP tool invocation、OAuth/secure-store lifecycle 与跨进程 pending permission 持久化仍为后续边界。

## W1.4.43 范围

本切片推进 `runtime_harness.md §6` 的 secure-store 加密区契约，但不擅自新增生产依赖或平台 keychain 绑定。`cw_runtime.harness.secrets` 新增 spec-shaped secret envelope 与 PBKDF2-HMAC-SHA256 派生逻辑，AES-GCM 与 OS keychain 主密钥仍由显式注入的 provider 承载：

- `encrypt_project_secret_value()` 生成 `value_encrypted` envelope：`version=0.1.0`、`algorithm=AES-GCM-256`、`kdf=PBKDF2-HMAC-SHA256`、项目 `project_id` salt、12-byte nonce 与 base64 ciphertext。
- `decrypt_project_secret_value()` 校验 envelope schema、project salt、nonce 与 size limit，再用 PBKDF2 派生 256-bit key，并调用注入式 AEAD decryptor 还原 plaintext。
- `build_project_secret_decryptor()` 从 `.agent-workflow/project.json` 读取 `project_id`，调用注入式 master-key provider，并返回既有 `load_project_mcp_secret_material()` 可消费的 decryptor。
- `ClaudeCodeAdapter` 在没有显式 `project_mcp_secret_decryptor` 时，可通过 `project_mcp_secret_master_key_provider` + `project_mcp_secret_aead_decryptor` 构造 secure-store backed resolver；显式 resolver / decryptor 仍保持优先级。
- 所有错误继续 fail closed，不暴露 `secret_ref`、明文、密文、SQLite 路径或 project id。

边界说明：W1.4.43 不修改 accepted spec / ADR / public schema，不新增 dependency，不实现真实 Windows Credential Manager / macOS Keychain / Linux libsecret 接入，不引入真实 AES-GCM provider，不迁移或写入现有 secure SQLite，不实现 OAuth refresh / token exchange、真实外部 provider smoke 或 MCP tool invocation。

## W1.4.43 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/secrets.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/harness/secrets.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/src/cw_runtime/adapters/claude_code_adapter.py apps/runtime/tests/test_m1_3_2_project_harness.py apps/runtime/tests/test_m1_3_12_claude_code_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.43 审查结果

- 聚焦实现自检已通过：project harness + ClaudeCodeAdapter pytest、focused strict mypy、focused ruff；覆盖 envelope round-trip、project salt mismatch、direct oversized envelope/ciphertext rejection、secure SQLite material load with crypto decryptor，以及 ClaudeCodeAdapter 通过 key-provider/AEAD seam 投影 headers/env。
- 独立 subagent Nietzsche 初审 FAIL：指出 `decrypt_project_secret_value()` direct 调用路径缺少 envelope / decoded ciphertext size limit；已修复并补充 oversized 回归。
- 独立 subagent Nietzsche 复审 PASS，确认 P2 已关闭且无剩余 actionable finding；归档见 `docs/reviews/m1.4.43-secure-secret-envelope-review.md`。
- 本地差异自检确认未修改 accepted spec / ADR / public schema / dependency 文件；真实 OS keychain provider、真实 AES-GCM provider、secure SQLite 迁移/写 API、OAuth/token exchange、真实外部 provider smoke 与 MCP tool invocation 仍为后续边界。

## W1.4.44 范围

本切片关闭 W1.4.35-W1.4.37 留下的“只发现工具、不能调用工具”的本地 MCP smoke 缺口。范围限定在已有 runtime harness concrete MCP clients，不修改 accepted spec / ADR / public schema，不新增 dependency：

- `ProjectMCPStdioDiscoveryClient` 新增 active session 内 `invoke_tool(tool_name, arguments)`，通过 JSON-RPC `tools/call` 调用 MCP tool，并复用既有 request deadline、response id 匹配、JSON-RPC error 清洗与 close 行为。
- `ProjectMCPHttpDiscoveryClient` 新增同名 `invoke_tool(...)`，覆盖 Streamable HTTP JSON response、Streamable HTTP SSE response，以及 W1.4.37 legacy HTTP+SSE fallback 的 message response 匹配。
- `tools/call` result 只接受 MCP content list，并可保留 `isError` / `structuredContent`；异常路径继续只暴露 sanitized `server_id`、`transport`、stage 与类型信息，不暴露 `command_or_url`、secret、参数原文或 provider 原始 error message。
- fake stdio / HTTP / legacy MCP server tests 证明本地 invocation smoke；本切片不把 invocation 接入 default AdapterRegistry、HTTP API、Claude SDK real provider 或 Pydantic AI MCP toolset。

边界说明：W1.4.44 不实现真实外部 HTTP MCP provider smoke，不实现 OAuth / secure-store credential injection 到 MCP discovery/invocation client，不实现真实 provider 上游 cancel，不修改 `NodeContract`/`StreamEvent`/runtime_harness spec，也不声明 Claude Agent SDK real-provider HITL 已验证。

## W1.4.44 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/harness/project.py apps/runtime/tests/test_m1_3_2_project_harness.py
uv run ruff check apps/runtime/src/cw_runtime/harness/project.py apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.44 审查结果

- 聚焦实现自检已通过：`test_m1_3_2_project_harness.py`、focused strict mypy、focused ruff；覆盖 stdio `tools/call`、Streamable HTTP JSON/SSE `tools/call`、legacy HTTP+SSE fallback `tools/call`，以及 HTTP invocation error sanitization。
- Python 全仓门禁已通过等价直跑：`.venv\Scripts\ruff.exe check .`、`.venv\Scripts\mypy.exe packages/schemas apps/runtime`、`.venv\Scripts\pytest.exe`（417 passed, 1 skipped）。
- JS/TS 门禁已通过：`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`。
- Codegen 已通过：`.venv\Scripts\python.exe scripts/codegen/generate-json-schemas.py` + `pnpm run codegen`，无 `packages/schemas-ts/src/generated/**` 实质 diff。
- 格式与 hygiene：`.venv\Scripts\ruff.exe format --check .`、targeted Prettier for touched runbook、`git diff --check` 均通过；`pnpm run format:check` 仍只命中 38 个既有未触碰 Prettier baseline 文件。
- 独立 subagent Bernoulli 审查 PASS，确认无 actionable finding；归档见 `docs/reviews/m1.4.44-mcp-tool-invocation-smoke-review.md`。
- 本地差异自检确认未修改 accepted spec / ADR / public schema / dependency 文件；真实外部 MCP provider smoke、OAuth/secure-store credential injection、AdapterRegistry/API 集成、真实 provider cancel 与 aggregate SSE/tool-result size hardening 仍为后续边界。

## W1.4.45 范围

本切片关闭 W1.4.43 留下的“只有注入式 AEAD seam、没有真实 AES-GCM provider”的一部分缺口。范围限定在 runtime harness 的无新增依赖 Windows CNG 绑定，不修改 accepted spec / ADR / public schema / dependency：

- `cw_runtime.harness.secrets` 新增 `windows_cng_encrypt_aes_gcm()` / `windows_cng_decrypt_aes_gcm()`，实现 AES-GCM-256，返回/消费 `ciphertext || tag`，可直接作为 `ProjectSecretAeadEncryptor` / `ProjectSecretAeadDecryptor` 注入。
- provider 使用 Windows `bcrypt.dll` / CNG；非 Windows 或 CNG 不可用时 fail closed 为 `ProjectSecretStoreError`。
- 真实 round-trip 测试覆盖 `encrypt_project_secret_value()` / `decrypt_project_secret_value()` 的 spec-shaped envelope、PBKDF2 project salt、Windows CNG AEAD、密文不含明文 token，以及错误 master key 的 authentication failure 清洗路径。

边界说明：W1.4.45 不修改 `runtime_harness.md`，不新增 Python crypto/keyring dependency，不实现 Windows Credential Manager / macOS Keychain / Linux libsecret 主密钥 provider，不实现 secure SQLite 写入/迁移 API，不实现 OAuth refresh / token exchange、真实外部 MCP provider smoke、MCP invocation 的 AdapterRegistry/API 集成或真实 provider cancel。

## W1.4.45 验证计划

```powershell
.venv\Scripts\pytest.exe apps/runtime/tests/test_m1_3_2_project_harness.py -q
.venv\Scripts\mypy.exe --strict apps/runtime/src/cw_runtime/harness/secrets.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
.venv\Scripts\ruff.exe check apps/runtime/src/cw_runtime/harness/secrets.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.45 审查结果

- 聚焦实现自检已通过：`test_m1_3_2_project_harness.py`、focused strict mypy、focused ruff；覆盖 Windows CNG AES-GCM envelope round-trip、密文 token 非明文、错误 master key authentication failure 清洗。
- Python 全仓门禁已通过等价直跑：`.venv\Scripts\ruff.exe check .`、`.venv\Scripts\mypy.exe packages/schemas apps/runtime`、`.venv\Scripts\pytest.exe`（418 passed, 1 skipped）。
- JS/TS 门禁已通过：`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`。
- Codegen 已通过：`.venv\Scripts\python.exe scripts/codegen/generate-json-schemas.py` + `pnpm run codegen`，无 `packages/schemas-ts/src/generated/**` 实质 diff。
- 格式与 hygiene：`.venv\Scripts\ruff.exe format --check .`、targeted Prettier for touched runbook/review、`git diff --check` 均通过；`pnpm run format:check` 仍只命中 38 个既有未触碰 Prettier baseline 文件。
- 独立 subagent Singer 审查 PASS，确认无 actionable finding；归档见 `docs/reviews/m1.4.45-secure-aes-gcm-provider-review.md`。
- 审查残余风险：非 Windows / CNG unavailable 路径在代码中 fail closed，但本轮 focused test 在 Windows 上验证真实 CNG 成功与 authentication failure。

## W1.4.46 范围

本切片关闭 `runtime_harness.md §6` 中“主密钥来自用户 OS keychain”的 Windows 侧一部分缺口。范围限定在 runtime harness 的无新增依赖 Windows Credential Manager helper，不修改 accepted spec / ADR / public schema / dependency：

- `cw_runtime.harness.secrets` 新增 `windows_credential_manager_master_key_provider()`，按 project id 从 Windows Credential Manager Generic Credential 读取 master key，并可直接作为 `ProjectSecretMasterKeyProvider` 注入 `build_project_secret_decryptor()`。
- 新增 `write_windows_credential_manager_master_key()` / `delete_windows_credential_manager_master_key()`，用于后续初始化路径和本地真实 keychain smoke；credential 默认使用 local-machine persistence，不跨设备漫游。
- 真实 Windows smoke 测试创建唯一 test credential，读取 master key，经 PBKDF2 + Windows CNG AES-GCM 解开 `.agent-workflow/secure/secrets.encrypted.sqlite` 中的 envelope，并清理 credential；缺失 credential fail closed 为 `ProjectSecretStoreError`，错误文本不暴露 project id、target 或明文 token。

边界说明：W1.4.46 不实现 macOS Keychain / Linux libsecret，不修改 `runtime_harness.md`，不新增 Python keyring/crypto dependency，不实现 secure SQLite 写入/迁移 API，不实现 OAuth refresh / token exchange、真实外部 MCP provider smoke、MCP invocation 的 AdapterRegistry/API 集成或真实 provider cancel。

## W1.4.46 验证计划

```powershell
.venv\Scripts\pytest.exe apps/runtime/tests/test_m1_3_2_project_harness.py -q
.venv\Scripts\mypy.exe --strict apps/runtime/src/cw_runtime/harness/secrets.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
.venv\Scripts\ruff.exe check apps/runtime/src/cw_runtime/harness/secrets.py apps/runtime/src/cw_runtime/harness/__init__.py apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.46 审查结果

- 聚焦实现自检已通过：`test_m1_3_2_project_harness.py`、focused strict mypy、focused ruff；覆盖 Windows Credential Manager 写入/读取/删除、keychain-backed `build_project_secret_decryptor()`、PBKDF2 + Windows CNG AES-GCM 解密 secure-store envelope，以及缺失 credential 的 sanitized fail-closed。
- Python 全仓门禁已通过等价直跑：`.venv\Scripts\ruff.exe check .`、`.venv\Scripts\mypy.exe packages/schemas apps/runtime`、`.venv\Scripts\pytest.exe`（419 passed, 1 skipped）。
- JS/TS 门禁已通过：`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`。
- Codegen 已通过：`.venv\Scripts\python.exe scripts/codegen/generate-json-schemas.py` + `pnpm run codegen`，无 `packages/schemas-ts/src/generated/**` 实质 diff。
- 格式与 hygiene：`.venv\Scripts\ruff.exe format --check .`、targeted Prettier for touched runbook/review、`git diff --check` 均通过；`pnpm run format:check` 仍只命中 38 个既有未触碰 Prettier baseline 文件。
- 独立 subagent Halley 审查 PASS，确认无 actionable finding；归档见 `docs/reviews/m1.4.46-windows-credential-manager-review.md`。
- 审查残余风险：非 Windows 路径未被本轮 Windows focused test 执行；若测试进程被硬中断，唯一 test credential 可能绕过正常 `finally` 清理。

## W1.4.47 范围

本切片关闭 W1.4.36/W1.4.44 留下的“HTTP MCP 仍主要是 in-process fake server/unit 级 smoke”的一部分缺口。范围限定在本地 stdlib 子进程 HTTP MCP provider，不修改 accepted spec / ADR / public schema / dependency：

- `test_m1_3_2_project_harness.py` 新增外部 Python 子进程 MCP HTTP provider，provider 在独立进程内绑定 `127.0.0.1:0`，发布 endpoint 后由现有 `ProjectMCPHttpDiscoveryClient` 访问。
- 测试覆盖 `ProjectMCPDiscoveryRunner + ProjectMCPHttpDiscoveryClient` 对子进程 provider 的 `initialize -> notifications/initialized -> tools/list -> close` lock snapshot 路径。
- 同一测试再用 concrete `ProjectMCPHttpDiscoveryClient` 对同一外部 provider 执行 `tools/call`，验证 `content` / `isError` / `structuredContent` 结果闭环。

边界说明：W1.4.47 不实现 OAuth / secure-store credential injection 到 MCP HTTP client，不接入 AdapterRegistry / HTTP API，不声明公网或第三方真实 provider 已验证，不实现真实 provider 上游 `cancel()`、Claude Agent SDK real-provider HITL 或 MCP invocation 的端到端 runtime API 集成。

## W1.4.47 验证计划

```powershell
.venv\Scripts\pytest.exe apps/runtime/tests/test_m1_3_2_project_harness.py -q
.venv\Scripts\mypy.exe --strict apps/runtime/tests/test_m1_3_2_project_harness.py
.venv\Scripts\ruff.exe check apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.47 审查结果

- 聚焦实现自检已通过：`test_m1_3_2_project_harness.py`、focused strict mypy、focused ruff；覆盖子进程 HTTP MCP provider 的 discovery lock snapshot 与 `tools/call` invocation。
- Python 门禁已通过：`.venv\Scripts\ruff.exe check .`、`.venv\Scripts\mypy.exe packages/schemas apps/runtime`、`.venv\Scripts\pytest.exe`（`420 passed, 1 skipped in 218.57s`）。
- JS/TS 与 codegen 门禁已通过：`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`、`.venv\Scripts\python.exe scripts/codegen/generate-json-schemas.py` + `pnpm run codegen`；codegen 未产生 `packages/schemas-ts/src/generated/**` 实质差异。
- 格式与 hygiene：`.venv\Scripts\ruff.exe format --check .`、`pnpm exec prettier --check docs/04_runbook/m1.4-progress.md`、`git diff --check` 均通过；`pnpm run format:check` 仍命中 38 个未触碰基线文件的既有 Prettier debt。
- 独立 subagent Gibbs 审查 PASS，确认无 actionable finding；归档见 `docs/reviews/m1.4.47-external-http-mcp-provider-review.md`。
- 审查残余风险：provider 刻意保持协议最小化，未校验 request path/session/header；5s startup 与 2s client timeout 在严重过载的 Windows runner 上仍可能 flaky。

## W1.4.48 范围

本切片关闭 W1.4.47 后仍未覆盖的“secure-store credential injection 到 MCP HTTP discovery/invocation client”缺口的一部分。范围限定为显式 secure-store backed HTTP header 注入，不修改 accepted spec / ADR / public schema / dependency：

- `ProjectMCPHttpDiscoveryClient` 新增 `secret_headers` seam，用于把已解出的 `ProjectMCPSecretMaterial.headers` 加入 Streamable HTTP 与 legacy HTTP+SSE 请求。
- HTTP client 拒绝 secret headers 覆盖 `Content-Type`、`Accept`、`MCP-Protocol-Version`、`Mcp-Session-Id` 等协议控制头，避免凭证材料破坏 MCP 协议状态。
- `build_project_mcp_http_discovery_client_factory()` 复用现有 `load_project_mcp_secret_material()`，按 `ProjectMCPServerConfig.secret_ref` 从 `.agent-workflow/secure/secrets.encrypted.sqlite` 读取并解密 header material，再构造 HTTP discovery client。
- 新增测试覆盖 encrypted secure-store envelope -> HTTP client factory -> discovery lock snapshot -> concrete `tools/call` invocation 的 header 透传，并确认 `env` 材料不会注入 HTTP 请求。

边界说明：W1.4.48 不实现 OAuth refresh/token exchange，不实现 stdio MCP `env` 注入，不实现 AdapterRegistry / HTTP API 接线，不声明公网或第三方真实 provider 已验证，不实现真实 provider 上游 `cancel()`、Claude Agent SDK real-provider HITL 或 MCP invocation 的端到端 runtime API 集成。

## W1.4.48 验证计划

```powershell
.venv\Scripts\pytest.exe apps/runtime/tests/test_m1_3_2_project_harness.py -q
.venv\Scripts\mypy.exe --strict apps/runtime/src/cw_runtime/harness apps/runtime/tests/test_m1_3_2_project_harness.py
.venv\Scripts\ruff.exe check apps/runtime/src/cw_runtime/harness apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.48 审查结果

- 聚焦实现自检已通过：`test_m1_3_2_project_harness.py`、focused strict mypy、focused ruff；覆盖 secure-store backed HTTP header 注入 discovery lock snapshot 与 `tools/call` invocation，以及协议保留 header 覆盖的 fail-closed。
- Python 门禁已通过：`.venv\Scripts\ruff.exe check .`、`.venv\Scripts\mypy.exe packages/schemas apps/runtime`、`.venv\Scripts\pytest.exe`（`422 passed, 1 skipped in 223.92s`）。
- JS/TS 与 codegen 门禁已通过：`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`、`.venv\Scripts\python.exe scripts/codegen/generate-json-schemas.py` + `pnpm run codegen`；codegen 未产生 `packages/schemas-ts/src/generated/**` 实质差异。
- 格式与 hygiene：`.venv\Scripts\ruff.exe format --check .`、`pnpm exec prettier --check docs/04_runbook/m1.4-progress.md`、`git diff --check` 均通过；`pnpm run format:check` 仍命中 38 个未触碰基线文件的既有 Prettier debt。
- 独立 subagent Zeno 初审发现 P2：测试声称 `env` 不注入 HTTP，但 fake server 未记录全量 headers；已修复为记录 full header map，并断言 `CW_MCP_TOKEN` / `fake-env-token` 不出现在任一 HTTP header key/value。
- 独立 subagent Zeno 复审 PASS，确认无 remaining actionable finding；归档见 `docs/reviews/m1.4.48-http-secure-header-injection-review.md`。
- 审查残余风险：本切片仍为本地 fake HTTP server smoke，不是公网或第三方 provider compatibility proof；`env` 不注入断言覆盖当前 fixture，不做所有 env-to-header 归一化形式的参数化穷尽。

## W1.4.49 范围

本切片关闭 W1.4.48 后仍未覆盖的“secure-store `env` credential injection 到 MCP stdio discovery/invocation client”缺口的一部分。范围限定为显式 secure-store backed stdio env 注入，不修改 accepted spec / ADR / public schema / dependency：

- `ProjectMCPStdioDiscoveryClient` 新增 `secret_env` seam，用于把已解出的 `ProjectMCPSecretMaterial.env` 叠加到子进程环境。
- stdio client 对 secret env 做最小 fail-closed 校验：拒绝空 env 名、包含 `=` / NUL 的 env 名，以及包含 NUL 的 env 值；错误不暴露 secret value。
- `build_project_mcp_stdio_discovery_client_factory()` 复用现有 `load_project_mcp_secret_material()`，按 `ProjectMCPServerConfig.secret_ref` 从 `.agent-workflow/secure/secrets.encrypted.sqlite` 读取并解密 env material，再构造 stdio discovery client。
- 新增测试覆盖 encrypted secure-store envelope -> stdio client factory -> discovery lock snapshot -> concrete `tools/call` invocation 的 env 透传，并确认 `headers` 材料不会注入子进程环境。

边界说明：W1.4.49 不实现 OAuth refresh/token exchange，不实现 HTTP MCP header 注入以外的新 HTTP 行为，不实现 AdapterRegistry / HTTP API 接线，不声明公网或第三方真实 provider 已验证，不实现真实 provider 上游 `cancel()`、Claude Agent SDK real-provider HITL 或 MCP invocation 的端到端 runtime API 集成。

## W1.4.49 验证计划

```powershell
.venv\Scripts\pytest.exe apps/runtime/tests/test_m1_3_2_project_harness.py -q
.venv\Scripts\mypy.exe --strict apps/runtime/src/cw_runtime/harness apps/runtime/tests/test_m1_3_2_project_harness.py
.venv\Scripts\ruff.exe check apps/runtime/src/cw_runtime/harness apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.49 审查结果

- 聚焦实现自检已通过：`test_m1_3_2_project_harness.py`、focused strict mypy、focused ruff；覆盖 secure-store backed stdio env 注入 discovery lock snapshot 与 `tools/call` invocation、`headers` 不注入 stdio env，以及非法 env 名 fail-closed。
- 独立 subagent 初审发现 P2：测试只记录 uppercase/underscore header-like env variants，可能漏掉 exact `Authorization` / `X-CW-MCP-Token` 被误注入 stdio env 的回归；已修复为记录并断言 exact header env names。
- 独立 subagent 复审 PASS 后提出 host-env flake 残余风险；已用 `monkeypatch.delenv(...)` 在测试开头清理所有被断言的 env key，最终复审 PASS，无 remaining actionable finding；归档见 `docs/reviews/m1.4.49-stdio-secure-env-injection-review.md`。
- 阶段门禁已通过：full `ruff check .`、full `mypy packages/schemas apps/runtime`、full pytest `424 passed, 1 skipped`、`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`、`pnpm run codegen`；codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.49-stdio-secure-env-injection-review.md` 已通过 targeted Prettier；全局 `pnpm run format:check` 仍命中 38 个既有 docs/spec/config 格式债。
- 审查残余风险：本切片仍为本地 fake stdio server smoke，不是外部 stdio provider compatibility proof；NUL env-name / env-value 拒绝路径由共享 validator 覆盖，新增直接回归测试覆盖 `BAD=NAME` invalid-name path。

## W1.4.50 范围

本切片关闭 W1.4 MCP client 在 HTTP JSON 单体响应之外仍缺少响应总量 guard 的 hardening 缺口。范围限定在 project harness 的 MCP discovery/invocation client，不修改 accepted spec / ADR / public schema / dependency：

- 将 MCP response size limit 收敛为 transport-neutral `_MCP_MAX_RESPONSE_BYTES`，继续保持默认 `4 MiB`。
- stdio client 读取 stdout 时使用 bounded `readline()`，并在 JSON parse 前按 UTF-8 byte size 拒绝超限 JSON-RPC response；错误只暴露 `response_size_limit`。
- Streamable HTTP SSE response 对 `data:` 多行聚合后的事件总量做 byte size guard，避免多个小 `data:` 行绕过单行限制。
- Legacy HTTP+SSE fallback 对 endpoint / message event 的 `data:` 多行聚合结果同样做 byte size guard。
- 新增测试覆盖 stdio oversized response、HTTP SSE split oversized event、legacy HTTP+SSE split oversized event；测试用 monkeypatch 缩小 limit，避免构造 4 MiB fixture。

边界说明：W1.4.50 不实现真实外部 provider 兼容矩阵，不实现 OAuth/token exchange，不接入 AdapterRegistry / HTTP API，不改变 MCP tool result schema，不声明真实 provider upstream `cancel()` 或 Claude Agent SDK real-provider HITL 已验证。

## W1.4.50 验证计划

```powershell
.venv\Scripts\pytest.exe apps/runtime/tests/test_m1_3_2_project_harness.py -q
.venv\Scripts\mypy.exe --strict apps/runtime/src/cw_runtime/harness apps/runtime/tests/test_m1_3_2_project_harness.py
.venv\Scripts\ruff.exe check apps/runtime/src/cw_runtime/harness apps/runtime/tests/test_m1_3_2_project_harness.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.50 审查结果

- 聚焦实现自检已通过：`test_m1_3_2_project_harness.py`、focused strict mypy、focused ruff；覆盖 stdio oversized response、HTTP SSE split oversized event、legacy HTTP+SSE split oversized event 三条 fail-closed 路径。
- 独立 subagent Nash 只读复审 PASS，确认 stdio JSON-RPC response 在 JSON parse 前有 bounded read + UTF-8 byte guard，Streamable HTTP SSE 与 legacy HTTP+SSE 均覆盖多 `data:` 聚合总量 guard，且错误 details 不包含 response 原文、`command_or_url` 或 `secret_ref`；归档见 `docs/reviews/m1.4.50-response-size-hardening-review.md`。
- 阶段门禁已通过：full `ruff check .`、full `mypy packages/schemas apps/runtime`、full pytest `427 passed, 1 skipped`、`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`、`pnpm run codegen`；codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier；全局 `pnpm run format:check` 仍命中 38 个既有 docs/spec/config 格式债。
- 审查残余风险：stdio 仍使用 `text=True` stdout，`readline(size)` 先按字符数限制，再由 UTF-8 byte guard 在 JSON parse 前拒绝超限响应；这不是严格 raw-byte memory ceiling。覆盖仍为 fake provider smoke，不是默认 4 MiB limit 下的真实外部 provider compatibility matrix。

## W1.4.51 范围

本切片补齐 `specs/api/http_sse.md` §2 已接受的项目配置只读 API：`GET /cw/v1/projects/{project_id}/skills`、`GET /cw/v1/projects/{project_id}/mcps`、`GET /cw/v1/projects/{project_id}/adapters`。范围限定在 runtime API route 与项目端点测试，不修改 accepted spec / ADR / public schema / dependency：

- 三个端点只读取当前 runtime 进程已注册项目下 `.agent-workflow/skills.config.json`、`.agent-workflow/mcp.config.json`、`.agent-workflow/adapters.config.json` 的原始 JSON manifest。
- 未注册项目、缺失配置文件与不可读取配置 JSON 统一返回既有 `RES_NOT_FOUND` envelope，避免在 spec 未锁定前新增项目 manifest 损坏错误码或复用 run 目录错误码。
- 新增 endpoint 测试覆盖三类配置读取、未知项目配置读取、缺失与 malformed `mcp.config.json`。

边界说明：W1.4.51 不实现 PATCH enable/disable/version/connect，不做 manifest schema canonicalization，不启动 MCP/provider、不解析 `secret_ref`、不接入 AdapterRegistry 或真实 provider E2E；端点只是 accepted API surface 的只读文件投影。

## W1.4.51 验证计划

```powershell
.venv\Scripts\pytest.exe apps/runtime/tests/test_m1_3_2_project_endpoints.py -q
.venv\Scripts\mypy.exe --strict apps/runtime/src/cw_runtime/api apps/runtime/tests/test_m1_3_2_project_endpoints.py
.venv\Scripts\ruff.exe check apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_3_2_project_endpoints.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.51 审查结果

- 聚焦实现自检已通过：project endpoints pytest、focused strict mypy、focused ruff；覆盖三类 accepted GET 端点、未知项目、缺失配置与 malformed JSON。
- 独立 subagent 初审发现 P2：配置 JSON 损坏路径误用了 `RH_RUN_DIR_CORRUPTED`，该错误码在 `runtime_harness.md` 中只锁定为 `runs/<run_id>/` 关键文件损坏。已修复为既有 API 通用 `RES_NOT_FOUND`，并同步 runbook 边界与 malformed JSON 回归测试。
- 独立 subagent 复审 PASS，确认缺失 / malformed 项目配置不再使用 `RH_RUN_DIR_CORRUPTED` 或新错误码，三条 route 对齐 `http_sse.md` §2，auth/origin/security headers 仍由 `/cw/v1` middleware 统一覆盖；归档见 `docs/reviews/m1.4.51-project-config-api-reads-review.md`。
- 阶段门禁已通过：full `ruff check .`、full `mypy packages/schemas apps/runtime`、full pytest `431 passed, 1 skipped`、`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`、`pnpm run codegen`；codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.51-project-config-api-reads-review.md` 已通过 targeted Prettier；全局 `pnpm run format:check` 仍命中 38 个既有 docs/spec/config 格式债。

## W1.4.52 范围

本切片关闭 `GET /cw/v1/system/capabilities` 仍返回空数组、无法把已实现 Adapter 静态能力暴露给 API/UI 的缺口。范围限定在 runtime API 的只读 capabilities 投影，不修改 accepted spec / ADR / public schema / dependency：

- `create_app()` 增加可注入 `AdapterRegistry` 参数；默认 registry 注册 PydanticAIAdapter 与 ClaudeCodeAdapter 的静态 descriptors。
- `/system/capabilities` 返回 `AdapterCapabilities[]`，只投影 descriptor 的 `capabilities`，不暴露 `AdapterDescriptor.default_config` 或 adapter_id。
- 系统端点测试覆盖默认两家能力边界：ClaudeCodeAdapter 继续声明 coding-agent / MCP / HITL / cancel；PydanticAIAdapter 继续声明 chat / structured / streaming，`mcp=False` 与 `cancel=False`，并暴露当前默认 builtin tool 集合。

边界说明：W1.4.52 不实现默认 execution `AdapterRegistry` 接线，不改变 `advance_workflow_run()` 默认 deterministic 路径，不启动 SDK/provider，不验证真实 provider cancel，不新增 capability 字段，也不改变 PydanticAI / ClaudeCode 的能力声明真值。

## W1.4.52 验证计划

```powershell
.venv\Scripts\pytest.exe apps/runtime/tests/test_m1_3_1_system_endpoints.py -q
.venv\Scripts\mypy.exe --strict apps/runtime/src/cw_runtime/api apps/runtime/tests/test_m1_3_1_system_endpoints.py
.venv\Scripts\ruff.exe check apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_3_1_system_endpoints.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.52 审查结果

- 聚焦实现自检已通过：system endpoints pytest、focused strict mypy、focused ruff；覆盖 `/system/capabilities` 返回非空 `AdapterCapabilities[]`，且不暴露 `adapter_id` / `default_config`。
- 独立 subagent 审查 PASS，确认 endpoint 只投影 `descriptor.capabilities`，PydanticAIAdapter 仍保持 `mcp=False` / `cancel=False`，ClaudeCodeAdapter 暴露既有 MCP / HITL / cancel snapshot，API import/default registry 不要求 `pydantic_ai` 或 `claude_agent_sdk`；归档见 `docs/reviews/m1.4.52-system-capabilities-api-review.md`。
- 阶段门禁已通过：full `ruff check .`、full `mypy packages/schemas apps/runtime`、full pytest `431 passed, 1 skipped`、`pnpm -r --if-present run lint`、`pnpm -r --if-present run typecheck`、`pnpm -r --if-present run test`、`pnpm run codegen`；codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.52-system-capabilities-api-review.md` 已通过 targeted Prettier；全局 `pnpm run format:check` 仍命中 38 个既有 docs/spec/config 格式债。

## W1.4.53 范围

本切片关闭 accepted API surface 中节点级执行动作仍未接入 runtime runner 的缺口，把 `http_sse.md` 已列出的 `POST /cw/v1/runs/{run_id}/nodes/{node_id}:run-once` 与 `:re-evaluate` 接到 W1.4.31/W1.4.39 已完成的 adapter-aware runner bridge。不修改 accepted spec / ADR / public schema / dependency：

- `create_app(..., adapter_registry=...)` 注入的 `AdapterRegistry` 现在不仅用于 `/system/capabilities`，也用于 run node action API 的 adapter attempt 执行。
- `:run-once` 与 `:re-evaluate` 复用现有 bearer auth、idempotency cache、`RunError` → `ErrorEnvelope`、`NodeAdvanceRequest` / `NodeAdvanceResult` 序列化路径。
- path `node_id` 是单一目标来源；若 body 同时带 `node_id`，必须与 path 一致。
- 测试覆盖 HTTP API 从 `n_start` 推进到 execution/evaluation 节点，再通过注入 fake AdapterRegistry 执行 adapter attempt，并验证 idempotent replay 不重复运行 adapter；空 registry 仍按既有 adapter bridge 语义持久化 `AA_PREPARE_INCOMPATIBLE_ADAPTER` 失败结果。

边界说明：W1.4.53 不实现 `POST /runs/{run_id}/nodes/{node_id}:repair` 的手动 repair API（该端点请求体与当前 `RepairAdvanceInput` 不同，需单独对齐）、不改变 `advance_workflow_run()` 默认 deterministic 路径、不启动 SDK/provider、不实现真实 provider cancel/HITL smoke，也不声明 MCP invocation + AdapterRegistry + API + real-provider 完整端到端闭环。

## W1.4.53 验证计划

```powershell
.venv\Scripts\python.exe -m pytest apps/runtime/tests/test_m1_4_53_node_action_api.py -q
.venv\Scripts\python.exe -m ruff check apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_53_node_action_api.py
.venv\Scripts\python.exe -m ruff format --check apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_53_node_action_api.py
.venv\Scripts\python.exe -m mypy --strict apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_53_node_action_api.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.53 审查结果

- 聚焦实现自检已通过：新增 node action API pytest `5 passed`、focused ruff check/format-check、focused strict mypy；覆盖 `:run-once` / `:re-evaluate` 的 injected AdapterRegistry bridge、idempotency replay、空 registry fail-closed 持久化、非 evaluation 节点 `:re-evaluate` 拒绝，以及 path/body `node_id` 冲突拒绝。
- 独立 subagent 初审发现两个问题：`:re-evaluate` 不能作为任意当前节点的 alias、path/body `node_id` 冲突不应伪装成 schema-version 错误；均已修复为 `NL_STATE_FORBIDDEN_TRANSITION` 路径，并经同一 subagent 复审 PASS。
- 阶段门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；full pytest `436 passed, 1 skipped`，codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；全局 `pnpm run format:check` 仍命中既有 docs/spec/config Prettier 债。`git diff --check` 通过。

## W1.4.54 范围

本切片关闭 `http_sse.md` 已列出的 `POST /cw/v1/runs/{run_id}/nodes/{node_id}:repair` 仍缺 HTTP seam 的缺口，把手动 repair API 请求体对齐到 spec 字段，并复用 W1.4.53 的 node action / idempotency / error envelope 路径。不修改 accepted spec / ADR / public schema / dependency：

- 新增 `RunNodeRepairRequest`，只接收 `schema_version`、`based_on_evaluation_id`、`preferred_strategy`、`scope`；不接收 `RepairPatch.operations`，符合 D-API-10。
- `:repair` 只允许 path node 是 `repair_task`，且 `based_on_evaluation_id` 必须匹配该 repair target 当前记录的 evaluation，避免陈旧修复请求落错评价依据。
- 当前 foundation 只支持 `preferred_strategy=prompt_patch` 与 `scope=until_pass`；其它 spec enum 值 fail closed，不写入错误语义不真实的 patch。
- `RepairAdvanceInput.scope` 收窄为 `until_pass` 并传入 `RepairPatch.scope`，避免 generic `:run-once` 写出未实现的 persistent repair 语义；API 响应返回 `NodeAdvanceResult` 加 `repair_patch` 与 `applied=true`，对齐 `http_sse.md` §3.7 的 "RepairPatch + 应用结果"。
- 测试覆盖 happy path、idempotent replay、非 repair node 拒绝、stale evaluation id 拒绝、unsupported strategy/scope 拒绝，以及 generic `:run-once` 对 persistent repair scope 的回归拒绝。

边界说明：W1.4.54 不实现无 evaluation 依据的任意修复触发，不实现 `persistent_for_run` / `persistent_for_workflow` 的实际 overlay/graph 持久化语义，不启动 SDK/provider，不实现真实 provider cancel/HITL smoke，也不声明 MCP invocation + AdapterRegistry + API + real-provider 完整端到端闭环。

## W1.4.54 验证计划

```powershell
.venv\Scripts\python.exe -m pytest apps/runtime/tests/test_m1_4_54_repair_action_api.py -q
.venv\Scripts\python.exe -m ruff check apps/runtime/src/cw_runtime/api/app.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_4_54_repair_action_api.py
.venv\Scripts\python.exe -m ruff format --check apps/runtime/src/cw_runtime/api/app.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_4_54_repair_action_api.py
.venv\Scripts\python.exe -m mypy --strict apps/runtime/src/cw_runtime/api/app.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_4_54_repair_action_api.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.54 审查结果

- 聚焦实现自检已通过：manual repair API pytest `6 passed`、W1.4.53/W1.4.54 adjacent API pytest `11 passed`、focused ruff check/format-check、focused strict mypy；覆盖 `:repair` request/response、idempotency replay、evaluation id guard、unsupported strategy/scope fail-closed 与 generic `:run-once` persistent scope 绕过拒绝。
- 独立 subagent 初审 NOT PASS，指出 `RepairAdvanceInput.scope` 允许 generic `:run-once` 写出未实现 persistent scope，且 `RP_BUILD_KIND_NOT_ALLOWED` HTTP status 应为 `422`；已修复为 `Literal[PatchScope.UNTIL_PASS]` 与显式 `status_code=422`，并新增绕过回归测试。
- 独立 subagent 复审 PASS，确认 `persistent_for_run` / `persistent_for_workflow` 均在 `NodeAdvanceRequest` 校验阶段拒绝，`:repair preferred_strategy=model_escalation` 返回 `RP_BUILD_KIND_NOT_ALLOWED` / `422`。
- 阶段门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；full pytest `442 passed, 1 skipped`，codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；全局 `pnpm run format:check` 仍命中既有 38 个 docs/spec/config Prettier 债。Touched runbook targeted Prettier 与 `git diff --check` 通过。

## W1.4.55 范围

本切片关闭 W1.4.54 后 repair prompt overlay scope 仍只有半真 foundation 的缺口，把 `repair_patch.md` §3 中可在当前 runner 内真实表达的两类 scope 做实：`until_pass` 与 `persistent_for_run`。不修改 accepted spec / ADR / public schema / dependency：

- `RepairAdvanceInput.scope` 从仅 `until_pass` 扩展为 `until_pass | persistent_for_run`；`persistent_for_workflow` 与 `this_attempt_only` 仍 fail closed，避免声明尚未实现的 Workflow SemVer 或精确 next-attempt 语义。
- `until_pass` 改为写入 run metadata 的 `active_prompt_overlays`，每次目标节点 attempt 都应用 overlay，直到该目标节点进入 `passed` 时清理。
- `persistent_for_run` 写入 `runs/<run_id>/run_overlay.json`，并在后续目标节点 attempt 中持续合成到 `ExecutionPack.effective_prompt_overlay` 与 `overlays/<attempt_id>.json`。
- deterministic repair 与 adapter repair 共用 `_apply_repair_prompt_overlay(...)`，保证同一 scope 语义不会因入口不同而漂移。
- overlay artifact 从单 patch 字段扩展为 `patch_ids` + `source_overlays`，保留每次 attempt 的可回放来源。

边界说明：W1.4.55 不实现 `persistent_for_workflow` 的 Workflow 新版本 / SemVer 提升，不实现 `this_attempt_only` 的精确 future attempt 绑定，不改变 `RepairPatch` public schema，不启动 SDK/provider，不声明真实 provider cancel/HITL smoke，也不声明 MCP invocation + AdapterRegistry + API + real-provider 完整端到端闭环。

## W1.4.55 验证计划

```powershell
.venv\Scripts\python.exe -m pytest apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py apps/runtime/tests/test_m1_4_54_repair_action_api.py -q
.venv\Scripts\python.exe -m ruff check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_54_repair_action_api.py apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
.venv\Scripts\python.exe -m ruff format --check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_54_repair_action_api.py apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
.venv\Scripts\python.exe -m mypy --strict apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_54_repair_action_api.py apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.55 审查结果

- 聚焦实现自检已通过：受影响 runtime pytest、focused ruff check/format-check、focused strict mypy；覆盖 `until_pass` 跨 attempt 持续与目标 passed 清理、adapter repair active overlay 写入、manual `persistent_for_run` 写 `run_overlay.json` 并进入后续 `ExecutionPack.effective_prompt_overlay`。
- 独立 subagent 初审 NOT PASS，指出 `persistent_for_run` 的 `RepairPatch.applies_to_attempts=[target_attempt_id]` 会覆盖 scope 语义，且缺少 `repairs.jsonl` 契约断言与 adapter `persistent_for_run` 回归测试；已修复为 `applies_to_attempts=[]` 由 scope 推导作用域，并补充 API / adapter 断言。
- 独立 subagent 复审 PASS，确认 `persistent_for_run` 写 `run_overlay.json` 并进入后续 attempt，`until_pass` 在目标 passed 后清理，`this_attempt_only` / `persistent_for_workflow` 仍 fail closed。
- 阶段门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；full pytest `444 passed, 1 skipped`，codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；全局 `pnpm run format:check` 仍命中既有 38 个 docs/spec/config Prettier 债。Touched runbook targeted Prettier 与 `git diff --check` 通过。

## W1.4.56 范围

本切片关闭 W1.4.44 / W1.4.53 后“本地 MCP `tools/call` 已有 client，但默认 PydanticAIAdapter + AdapterRegistry/API 路径无法使用项目 MCP”的缺口。不修改 accepted spec / ADR / public schema / dependency：

- `PydanticAIAdapter` 默认 SDK session 现在能从 `ExecutionPack.metadata.cw.project_root` 读取项目 `mcp.config.json`，对已启用且无 `secret_ref` 的 stdio/http MCP server 生成 SDK `FunctionToolset`。
- project MCP tool 函数每次调用按 `start -> health_check -> tools/call -> close` 执行，复用 `ProjectMCPStdioDiscoveryClient` / `ProjectMCPHttpDiscoveryClient.invoke_tool(...)` 的 JSON-RPC / SSE / response-size / sanitization 语义。
- 显式 `NodeContract.mcp_tools[].tool_name` 直接生成对应 tool；`tool_name="*"` 或仅 `effective_toolsets.mcp_server_ids` 时通过 `discover_tools()` 建立 tool name 列表。
- 现有 MCP approval wrapping 继续生效：`requires_approval=true` 的 project MCP toolset 仍经 SDK `ApprovalRequiredToolset` 包装。
- runner 在 `StaticAttemptPackRequest.metadata.cw.project_root` 中写入项目根，并由 `ExecutionPack.metadata` 透传给 adapter；ModelRouter / system capabilities 同步声明默认 PydanticAIAdapter 的 project MCP / deferred tool capability，node-level `human_in_the_loop=False` 与 `cancel=False` 不变，避免把 MCP tool approval 包装误报为 `human_gate` 节点能力。

边界说明：W1.4.56 不实现 `secret_ref` 的默认 secure-store resolver 注入，不实现 OAuth refresh/token exchange，不声明公网或第三方真实 provider 已验证，不证明真实外部 provider 上游 `cancel()` ≤5s，也不实现 manual repair 的 `this_attempt_only` / `persistent_for_workflow`。

## W1.4.56 验证计划

```powershell
.venv\Scripts\python.exe -m pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
.venv\Scripts\python.exe -m pytest apps/runtime/tests/test_m1_4_53_node_action_api.py apps/runtime/tests/test_m1_3_1_system_endpoints.py -q
.venv\Scripts\python.exe -m ruff check apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/src/cw_runtime/builders/static_packs.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/model_router/router.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_53_node_action_api.py apps/runtime/tests/test_m1_3_1_system_endpoints.py
.venv\Scripts\python.exe -m mypy --strict apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/src/cw_runtime/builders/static_packs.py apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/model_router/router.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_53_node_action_api.py apps/runtime/tests/test_m1_3_1_system_endpoints.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.56 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter 单测覆盖 project MCP explicit tool、wildcard discovery、`secret_ref` fail-closed 与 approval wrapping；node action API 测试覆盖默认 AdapterRegistry + fake SDK 下 `:run-once` 生成 project MCP FunctionToolset；system endpoint 测试覆盖 PydanticAIAdapter MCP/deferred 能力投影与 node-level HITL 不声明。
- 独立 subagent 初审 NOT PASS，指出默认 PydanticAIAdapter 误将 project MCP tool approval 包装声明为 node-level `human_in_the_loop=True`，会让 ModelRouter 把 `human_gate` / `requires_human_approval` 节点路由给 Pydantic AI；已修复为默认 `human_in_the_loop=False`，仅保留外部 toolset factory 路径的既有声明。
- 独立 subagent 最终复审 PASS，确认默认 `PydanticAIAdapter` 与 ModelRouter 内置 `adapter_id="pydantic_ai"` descriptor 均保持 node-level `human_in_the_loop=False`，deterministic foundation adapter 的既有 `human_in_the_loop=True` 未被破坏。
- 阶段门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；最终 full pytest `448 passed, 1 skipped`，codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；全局 `pnpm run format:check` 仍命中既有 38 个 docs/spec/config Prettier 债。Touched runbook targeted Prettier 与 `git diff --check` 通过。

## W1.4.57 范围

本切片关闭 W1.4.56 留下的默认 PydanticAIAdapter project MCP `secret_ref` 缺口；范围只覆盖已有 project secure-store decryptor seam 到默认 Pydantic SDK FunctionToolset discovery/invocation 的注入，不新增 spec/ADR/public schema，不声明 OAuth 或公网真实 provider E2E：

- `PydanticAIAdapter(project_mcp_secret_decryptor=...)` 将注入式 `ProjectSecretDecryptor` 传给默认 `PydanticAISDKSession`；自定义 `session_factory` 路径保持原有外部负责语义。
- 默认 SDK session 的 project MCP toolset 构造改为按 `ExecutionPack.metadata.cw.project_root` 生成 client factory；无 `secret_ref` 时仍使用既有 stdio/http clients，有 `secret_ref` 时调用 harness secure-store backed stdio/http discovery client factory。
- 未配置 decryptor 的 `secret_ref` server 仍 fail closed，保持 `AA_RUN_TOOL_NOT_FOUND` 与 `mcp_secret_ref_server_ids` payload。
- `ProjectSecretStoreError` 在 adapter toolset discovery / tool invocation 边界被重包为 sanitized `AA_RUN_TOOL_NOT_FOUND`，payload 只保留 server/transport/exception_type，不携带 `secret_ref`、明文 token、SQLite 路径或 provider 原始消息。

边界说明：本切片复用 W1.4.48/W1.4.49/W1.4.43/W1.4.45/W1.4.46 已有 secure-store、header/env、AES-GCM 与 Windows keychain seam；不新增真实 OAuth resolver/provider smoke，不证明公网真实 MCP invocation，不改变 PydanticAIAdapter `human_in_the_loop=False` / `cancel=False` 能力边界。

## W1.4.57 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run ruff format --check apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run mypy --strict apps/runtime/src/cw_runtime/adapters/pydantic_ai_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.57 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter 单测覆盖无 decryptor 的 `secret_ref` fail-closed、HTTP `secret_ref` decryptor 注入、stdio `secret_ref` decryptor 注入、secret-store error sanitized payload，以及既有 no-secret explicit/wildcard project MCP toolset。
- 独立 subagent 审查 PASS，确认当前 diff 没有阻塞问题；指出 adapter-level stdio `secret_ref` 分支覆盖弱于 HTTP，本轮已补充对应测试。
- 聚焦门禁已通过：`uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q`、touched-file `ruff format --check`、touched-file `ruff check`、touched-file `mypy --strict`。
- 阶段门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 451 passed, 1 skipped，codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；全局 `pnpm run format:check` 仍命中既有 38 个 docs/spec/config Prettier 债。本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。

## W1.4.58 范围

本切片关闭 W1.4.55 留下的 manual/adapter repair `this_attempt_only` scope 缺口；范围只覆盖 `repair_patch.md` §3 已定义的“仅下一次目标 attempt 生效”语义，不修改 accepted spec / ADR / public schema / dependency：

- `RepairAdvanceInput.scope` 与手动 `POST /cw/v1/runs/{run_id}/nodes/{node_id}:repair` 接受 `this_attempt_only`，但 `persistent_for_workflow` 继续 fail closed，避免声明尚未实现的 Workflow SemVer 持久化语义。
- 构建 `RepairPatch` 时为 `this_attempt_only` 预留下一次 target node `attempt_id`，并把该 id 写入 `applies_to_attempts`；同一字段参与 patch hash 输入，保持 patch 内容可审计。
- runtime metadata 写入 `active_prompt_overlays[target_node_id]` 与 `reserved_attempt_ids[target_node_id]`；目标节点下一次 `_prepare_attempt()` 使用预留 attempt id，生成 `overlays/{attempt_id}.json` 与 `ExecutionPack.effective_prompt_overlay`，随后消费一次性 overlay 与 reserved id。
- deterministic repair、手动 API repair 与 adapter repair outcome 共用同一 patch 构建和 overlay 应用语义；`until_pass` / `persistent_for_run` 既有行为保持不变。

边界说明：W1.4.58 不实现 `persistent_for_workflow` 的 Workflow 新版本 / SemVer 提升，不改变 `RepairPatch` public schema，不启动 SDK/provider，不声明真实 provider cancel/HITL smoke，也不声明公网或 OAuth-backed MCP invocation 端到端闭环。

## W1.4.58 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_54_repair_action_api.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py apps/runtime/tests/test_m1_3_5_node_runner.py -q
uv run ruff format --check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_54_repair_action_api.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
uv run ruff check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_54_repair_action_api.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
uv run mypy --strict apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_4_54_repair_action_api.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.58 审查结果

- 聚焦实现自检已通过：manual repair API、adapter repair bridge 与 node runner 回归 pytest 覆盖 `this_attempt_only` patch `applies_to_attempts`、reserved target attempt id、一次性 overlay 写入 attempt pack / execution pack，以及 retry 后 runtime metadata 清理。
- 聚焦静态门禁已通过：touched-file `ruff format --check`、touched-file `ruff check`、touched-file `mypy --strict`。
- 独立 subagent 审查 PASS，确认 `this_attempt_only` 由 `applies_to_attempts` 绑定下一次目标 attempt，deterministic repair 与 adapter repair 共享 patch/overlay 语义，`until_pass` / `persistent_for_run` 未被破坏，`persistent_for_workflow` 仍 fail closed，runbook 未越界声明真实 provider/OAuth/cancel/HITL。
- 阶段门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 453 passed, 1 skipped，codegen 未留下 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；全局 `pnpm run format:check` 仍命中既有 38 个 docs/spec/config Prettier 债。本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。

## W1.4.59 范围

本切片关闭 W1.4.58 独立审查提出的 one-shot repair overlay 负向覆盖缺口；范围只增加回归测试，不修改 accepted spec / ADR / public schema / runtime production code：

- 覆盖 `scope=this_attempt_only` overlay 在被预留的 target attempt 消费后，后续 `retry_same` 触发的同一 target node 新 attempt 不再携带 `effective_prompt_overlay_ref`。
- 覆盖 active one-shot overlay 的 `applies_to_attempts` 与当前 attempt id 不匹配时，runner 不把该 overlay 写入 `ExecutionPack.effective_prompt_overlay`。
- 覆盖 adapter repair 输出 `scope=persistent_for_workflow` 时仍 fail closed，且不向 `repairs.jsonl` 持久化 patch。

边界说明：W1.4.59 不实现 `expires_at` runtime 过期判断，不实现 `persistent_for_workflow` Workflow SemVer / 快照写入，也不启动 SDK/provider 或声明真实 provider/OAuth/cancel/HITL 已验证。

## W1.4.59 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py -q
uv run ruff format --check apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
uv run ruff check apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
uv run mypy --strict apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_4_31_adapter_runner_bridge.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.59 审查结果

- 聚焦实现自检已通过：node runner 与 adapter bridge pytest 覆盖 consumed one-shot overlay 不泄漏、非匹配 `applies_to_attempts` 不应用、adapter `persistent_for_workflow` 输出 fail closed 且不写 `repairs.jsonl`。
- 聚焦静态门禁已通过：touched-file `ruff format --check`、touched-file `ruff check`、touched-file `mypy --strict`。
- 独立 subagent 审查 PASS，确认本切片只修改测试与 runbook，未触碰 runtime production code / schema / spec / ADR / dependency；新增三条负向测试有效覆盖 W1.4.58 风险，runbook 未越界声明 `expires_at`、`persistent_for_workflow`、真实 provider/OAuth/cancel/HITL。
- 阶段 Python 门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`；`test-py` 为 455 passed, 1 skipped。
- `dev.ps1 codegen` 已通过，未留下 `packages/schemas-ts/src/generated/**` diff。
- 阶段门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 455 passed, 1 skipped。
- `format-check` 中 Python `ruff format --check .` 通过；全局 `pnpm run format:check` 仍命中既有 38 个 docs/spec/config Prettier 债。本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。

## W1.4.60 范围

本切片关闭 `repair_patch.md` §1.1 中 `expires_at` 对 `scope=this_attempt_only` 的 runtime 消费缺口；范围只覆盖已有 `RepairPatch.expires_at` 字段在 prompt overlay 应用前的过期判断，不修改 accepted spec / ADR / public schema / HTTP request contract / dependency：

- `_StoredPromptOverlay` 持久化 `expires_at`，由 `RepairPatch.expires_at` 原样进入 active / run overlay source，保持 patch 审计信息可追踪。
- `_prompt_overlay_applies_to_attempt()` 对 `scope=this_attempt_only` 且 `expires_at` 已过期的 overlay 返回不适用；malformed timestamp 视为过期，避免坏 metadata 绕过失效语义。
- 已过期但绑定当前 reserved target attempt 的 one-shot overlay 仍在 attempt 准备后消费，避免 runtime metadata 残留；未过期 one-shot overlay 继续生成 `overlays/{attempt_id}.json` 与 `ExecutionPack.effective_prompt_overlay`。
- `until_pass` / `persistent_for_run` 既有行为不因 `expires_at` 改变；`persistent_for_workflow` 仍按 W1.4.58 fail closed。

边界说明：W1.4.60 不把 `expires_at` 暴露到 manual `:repair` HTTP request，不实现 `persistent_for_workflow` Workflow SemVer / 快照写入，不启动 SDK/provider，不声明真实 provider/OAuth/cancel/HITL 已验证。

## W1.4.60 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_5_node_runner.py -q
uv run ruff format --check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
uv run ruff check apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
uv run mypy --strict apps/runtime/src/cw_runtime/runner/node_runner.py apps/runtime/tests/test_m1_3_5_node_runner.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.60 审查结果

- 聚焦实现自检已通过：node runner pytest 覆盖 expired one-shot overlay skip + reserved attempt consume，以及 unexpired one-shot overlay apply + reserved attempt consume。
- 聚焦静态门禁已通过：touched-file `ruff format --check`、touched-file `ruff check`、touched-file `mypy --strict`。
- 独立 subagent 审查 PASS，确认本切片只对 `scope=this_attempt_only` 执行 `expires_at` 过滤，没有扩展 HTTP/API/spec/public schema/dependency，runbook 未越界声明真实 provider/OAuth/cancel/HITL 或 `persistent_for_workflow` 已实现。
- 阶段 Python 门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`；`test-py` 为 457 passed, 1 skipped。
- `dev.ps1 codegen` 已通过，未留下 `packages/schemas-ts/src/generated/**` diff。
- 阶段门禁已通过：`dev.ps1 lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 457 passed, 1 skipped。
- `format-check` 中 Python `ruff format --check .` 通过；全局 `pnpm run format:check` 仍命中既有 38 个 docs/spec/config Prettier 债。本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：malformed `expires_at` 分支当前按代码检查确认 fail closed，未单独补回归测试；真实 `:repair` HTTP request 暴露 `expires_at`、`persistent_for_workflow` Workflow SemVer / 快照写入、真实 provider/OAuth/cancel/HITL 仍留给后续切片。

## W1.4.3 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.2 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.1 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run pytest apps/runtime/tests/test_m1_3_12_claude_code_adapter.py apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
uv run ruff check apps/runtime/src/cw_runtime/adapters apps/runtime/tests/test_m1_4_1_pydantic_ai_adapter.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.4.1 审查结果

- 聚焦实现自检已通过：W1.4.1 pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 独立 subagent 第一轮审查 FAIL：修复了 `kinds` 误含 `model_only` 的 spec 漂移，并补齐 run/resume 空 session 兜底 terminal `attempt.failed`。
- 独立 subagent 复审 PASS，归档见 `docs/reviews/m1.4.1-pydantic-ai-adapter-review.md`。

## W1.4.2 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 独立 subagent 第一轮审查 FAIL：修复了 `capabilities()` 继续宣称未实现 Toolset/MCP/HITL/deferred/cancel/evidence/multimodal 的路由风险。
- 独立 subagent 复审 PASS，归档见 `docs/reviews/m1.4.2-pydantic-ai-sdk-session-review.md`。

## W1.4.3 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、focused mypy、focused ruff。
- 独立 subagent 第一轮审查 FAIL：修复了 `RetryPromptPart` 被误映射为 `tool.call_completed` 的问题，并按 Pydantic AI UI stream 行为忽略 deprecated `BuiltinToolCallEvent`，避免与 native `PartStartEvent[builtin-tool-call]` 双发。
- 独立 subagent 复审 PASS，归档见 `docs/reviews/m1.4.3-pydantic-ai-stream-events-review.md`。

## W1.4.4 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 独立 subagent 审查 PASS，确认 token usage limits 映射、`max_cost_usd` 拒绝、`UsageLimitExceeded` 转译与能力边界均符合 W1.4.4 范围；归档见 `docs/reviews/m1.4.4-pydantic-ai-usage-limits-review.md`。

## W1.4.5 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、focused mypy、focused ruff。
- 独立 subagent 第一轮审查 FAIL：修复了 MCP approval 只按 `tool_name` 判断、丢失 `server_id` 作用域的问题。
- 独立 subagent 复审 FAIL：修复了 toolset factory 多返回未请求 MCP server 时会被传给 SDK Agent 的权限上限问题。
- 独立 subagent 最终复审 PASS，归档见 `docs/reviews/m1.4.5-pydantic-ai-toolsets-review.md`。

## W1.4.6 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、focused mypy、focused ruff。
- 独立 subagent 首轮审查 FAIL：修复了 `model_retries` 被投影后无法由 Pydantic AI `AgentRetries` 执行却静默丢弃的问题。
- 独立 subagent 最终复审 PASS，归档见 `docs/reviews/m1.4.6-pydantic-ai-retry-policy-review.md`。

## W1.4.7 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、focused mypy、focused ruff。
- 独立 subagent 首轮审查 FAIL：修复了 deferred resume payload 只按 shape 校验，未按 pending `DeferredToolRequests` 校验 id/kind/completeness 的问题。
- 独立 subagent 复审 FAIL：修复了 mixed calls+approvals 时 `human_decision` 只提交 approval results、遗漏 pending call results 的问题。
- 独立 subagent 最终复审 PASS，归档见 `docs/reviews/m1.4.7-pydantic-ai-deferred-resume-review.md`。

## W1.4.8 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 独立 subagent 首轮审查 FAIL：修复了 SDK/tool 层 `TimeoutError` 会被误分类为 CW `retry_policy.timeout_seconds` 取消的问题。
- 独立 subagent 最终复审 PASS，归档见 `docs/reviews/m1.4.8-pydantic-ai-attempt-timeout-review.md`。

## W1.4.9 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 独立 subagent 审查 PASS，确认主动 `cancel()` 会唤醒阻塞中的 run/resume iterator，且 `capabilities().cancel=False` 仍与真实 SDK 上游取消未实测的边界一致。
- 审查报告归档见 `docs/reviews/m1.4.9-pydantic-ai-user-cancel-iterator-exit-review.md`。

## W1.4.10 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 独立 subagent 审查 PASS，确认 registry fail-closed、request-bounded deterministic projection、MCP approval wrapping、factory-gated capabilities 与 W1.4.10 范围一致。
- 审查报告归档见 `docs/reviews/m1.4.10-pydantic-ai-toolset-registry-review.md`。

## W1.4.11 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 独立 subagent 首轮审查 FAIL：修复了 retry 分类过宽的问题，避免非瞬态 HTTP `4xx` 和 toolset factory/setup 异常被误当作 model retry 候选。
- 独立 subagent 复审 PASS，确认 `model_retries` 由 adapter-owned retry 执行，SDK `AgentRetries` 仍只承载 `tools/output`，HTTP status retry 只覆盖 `408/429/5xx`，partial stream 和 toolset factory failure 不重试。
- 审查报告归档见 `docs/reviews/m1.4.11-pydantic-ai-model-retry-review.md`。

## W1.4.12 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.12-pydantic-ai-reported-cost-guard-review.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认 token-only SDK `UsageLimits` 投影、cost-only SDK limit 为空、missing / above-limit `est_cost_usd` fail closed、within-limit 完成，以及 runbook 未越界声明价格估算或 budget ledger。
- 审查报告归档见 `docs/reviews/m1.4.12-pydantic-ai-reported-cost-guard-review.md`。

## W1.4.13 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.13-pydantic-ai-builtin-function-registry-review.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认 callable builtin 注册、原有 toolset/Skill/MCP registry 路径、缺失注册 fail closed、`FunctionToolset.add_function(func, name=tool_id)` 命名，以及 runbook 边界均符合 W1.4.13 范围。
- 审查后补充旧 SDK fail-closed 回归：缺少 `FunctionToolset` 或缺少 `add_function` 时在 SDK `Agent` 构造前以 `AA_RUN_INTERNAL` 失败。
- 审查报告归档见 `docs/reviews/m1.4.13-pydantic-ai-builtin-function-registry-review.md`。

## W1.4.14 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.14-pydantic-ai-evidence-lookup-tool-review.md` 已通过 targeted Prettier。
- 独立 subagent 首轮审查 FAIL：修复了 missing evidence id 只由直接 callable 测试覆盖、未证明 SDK run/finalize 结构化失败的问题；修复了默认 adapter 无 toolset factory 时过度声明 generic `tool_call` / `max_tool_iterations` 的能力边界问题。
- 独立 subagent 复审 PASS，确认 `EP_RUNTIME_EVIDENCE_LOOKUP_MISS` 已进入 failed attempt payload，默认能力只声明 `evidence_lookup_tool=True` 而 generic tool routing 保持 factory-gated。
- 审查报告归档见 `docs/reviews/m1.4.14-pydantic-ai-evidence-lookup-tool-review.md`。

## W1.4.15 审查结果

- 聚焦实现自检已通过：PydanticAIAdapter pytest、ClaudeCodeAdapter 回归、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.15-pydantic-ai-sdk-stream-cancel-review.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认主动 `cancel()` 会触达默认 SDK session 的活动 `run_stream_events` stream，取消后自然结束仍产出 `attempt.failed` 与 `AttemptOutcome.state=CANCELLED`，且 `capabilities().cancel=False` 保持真实外部 provider 未实测的边界。
- 审查报告归档见 `docs/reviews/m1.4.15-pydantic-ai-sdk-stream-cancel-review.md`。

## W1.4.16 审查结果

- 聚焦实现自检已通过：ClaudeCodeAdapter pytest、ClaudeCodeAdapter + PydanticAIAdapter 联合回归、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.16-claude-code-sdk-session-review.md` 已通过 targeted Prettier。
- 独立 subagent 初审 FAIL：修复了默认 SDK receive loop 在 cancel 后不能唤醒阻塞 iterator 的问题；修复了未知 SDK message 或未知 content block 会静默空事件结束、导致非终态 finalize 的问题。
- 独立 subagent 最终复审 PASS，确认 lazy optional SDK、结构化 `AA_RUN_INTERNAL` fail closed、cancel wake-up、exports/tests/runbook 边界符合 W1.4.16 范围。
- 审查报告归档见 `docs/reviews/m1.4.16-claude-code-sdk-session-review.md`。

## W1.4.17 审查结果

- 聚焦实现自检已通过：ClaudeCodeAdapter pytest、ClaudeCodeAdapter + PydanticAIAdapter 联合回归、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.17-claude-code-allowed-tools-review.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认 NodeContract allowed tools、effective builtin tools、MCP server wildcards 与 MCP tool refs 均进入 Claude `allowed_tools`，且去重保序；runbook 未越界声明工具本体、SkillRegistry、MCP lifecycle 或真实 provider smoke。
- 审查报告归档见 `docs/reviews/m1.4.17-claude-code-allowed-tools-review.md`。

## W1.4.18 审查结果

- 聚焦实现自检已通过：ClaudeCodeAdapter pytest、ClaudeCodeAdapter + PydanticAIAdapter 联合回归、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.18-claude-code-mcp-server-config-review.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认 MCP server ids 从 effective toolsets 与 NodeContract MCP refs 收集、去重保序、只从 `AdapterConfig.settings["mcp_servers"]` 解析，且只把当前 pack 请求的 server 传给 SDK；缺失 config 会以 `AA_RUN_TOOL_NOT_FOUND` fail closed。
- 审查报告归档见 `docs/reviews/m1.4.18-claude-code-mcp-server-config-review.md`。

## W1.4.19 审查结果

- 聚焦实现自检已通过：ClaudeCodeAdapter pytest、ClaudeCodeAdapter + PydanticAIAdapter 联合回归、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.19-claude-code-permission-lockdown-review.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认默认 SDK options 只新增 `permission_mode="dontAsk"`，W1.4.17 `allowed_tools` 投影与 W1.4.18 requested-only `mcp_servers` handoff 保持不变；该模式让未列出的工具由 SDK 拒绝，符合 NodeContract 权限上限语义。
- 审查报告归档见 `docs/reviews/m1.4.19-claude-code-permission-lockdown-review.md`。

## W1.4.20 审查结果

- 聚焦实现自检已通过：builtin tools pytest、PydanticAIAdapter pytest、PydanticAIAdapter + ClaudeCodeAdapter 联合回归、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 与 `docs/reviews/m1.4.20-pydantic-ai-web-fetch-builtin-review.md` 已通过 targeted Prettier。
- 独立 subagent 最终审查 PASS；前两轮审查发现的 redirect/proxy、multicast、unauthorized effective builtin 与 DNS rebinding TOCTOU 阻塞项均已修复。最终确认 `web_fetch` 解析并校验 host 后只连接已验证 IP，HTTPS 保留原 host 做 SNI/证书校验，且 effective builtin tools 不得越过 `NodeContract.allowed_tools` 权限上限。
- 审查报告归档见 `docs/reviews/m1.4.20-pydantic-ai-web-fetch-builtin-review.md`。

## W1.4.21 审查结果

- 聚焦实现自检已通过：builtin tools pytest、PydanticAIAdapter pytest、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 独立 subagent 初审 FAIL：修复了 nested `.git` / `.agent-workflow` 内部路径可读的问题；raw path 与 resolved path 任意组件命中内部目录都会拒绝，目录列表也过滤任意层级内部目录。
- 独立 subagent 复审 PASS，确认 `file_io` 只读、根目录受限、缺失/无效 `metadata.cw.project_root` 在 SDK Agent 构造前 fail closed，且外部 factory 剥离语义保持正确。

## W1.4.22 审查结果

- 聚焦实现自检已通过：builtin tools pytest、PydanticAIAdapter pytest、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`；完整 `lint` / `typecheck` / `test` / `git diff --check` 见本切片收尾。
- 独立 subagent 审查 PASS，确认 `python_sandbox` 仅使用 AST evaluator，无 `eval` / `exec` / subprocess；import、attribute access、keyword args、assignment、comprehension、oversized range、step overflow、非 JSON 值、深层嵌套、大整数、重复放大和结果大小超限均被拒绝。
- 审查确认默认 Pydantic AI SDK session 会把 `python_sandbox` 作为 adapter-owned `FunctionToolset` 注入并从外部 factory 请求中剥离；未知 builtin 仍走 external factory / `AA_RUN_TOOL_NOT_FOUND` fail-closed。

## W1.4.23 审查结果

- 聚焦实现自检已通过：ModelRouter + PydanticAIAdapter pytest、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；`git diff --check` 见本切片收尾。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认 accepted spec / ADR 无本地 diff，`AdapterCapabilities.metadata` 使用 `agent_adapter.md` 已有命名空间化扩展字段，默认 `pydantic_ai` Router descriptor 不再过度声明 MCP / HITL / deferred / cancel / multimodal。
- 独立 subagent 指出 P3 边界：显式 `supported_builtin_tools=[]` 曾被当作未声明。已修复为显式空闭合集合会拒绝所有 requested builtin，并补充 Router 回归测试。

## W1.4.24 审查结果

- 聚焦实现自检已通过：NodeRunner pytest、NodeRunner + persistence pytest、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`；完整 `lint` / `typecheck` / `test` / `git diff --check` 见本切片收尾。
- 独立 subagent 审查 PASS，确认 terminal `NodeAttempt` 写入 `attempts.jsonl` 后追加 `usage.jsonl`，覆盖 execution / evaluation / repair / failed execution 的写入路径；`NodeAttempt.usage`、`attempt.completed` payload、`EvaluationResult.usage`、`RepairProvenance.usage` 均来自同一 input usage。
- 审查确认本切片没有实现价格估算、预算扣减、真实 Adapter 调用接线或 spec 变更；`usage.jsonl` record 继续复用 runtime_store 既有 `usage_id` / `attempt_id` 索引投影。
- 审查残余风险中提到 failed execution 与 repair usage 缺少直接测试覆盖；已补充 failed execution usage ledger 与 repair provenance / attempt / ledger 一致性回归测试。

## W1.4.25 审查结果

- 聚焦实现自检已通过：project harness / workflow compiler / run lifecycle pytest、focused mypy、focused ruff。
- 独立 subagent 第一轮审查 FAIL：修复了 `update_manifest_json()` 对 list payload 放得过宽的问题；最终保留 object manifest writer，不把列表型 Skill/MCP manifest 写 API 纳入本切片。
- 独立 subagent 第二轮审查 FAIL：修复了 untyped/list payload 仍可在运行时经 `dict([])` 把 object manifest 写成 `{}` 并 bump revision 的问题；新增 runtime `Mapping` guard 与 revision 不变回归测试。
- 独立 subagent 最终复审 PASS，确认非 Mapping payload 会在写入前被拒绝、project Skill/MCP malformed manifest 不再使用 `RH_RUN_DIR_CORRUPTED`、MCP run lifecycle 覆盖已补充为 missing Skill -> missing MCP -> success。
- 审查残余风险：`advance_workflow_run()` / `resolve_human_decision()` 已接入 project validation context，但未单独测试 run 创建后 manifest 变化；malformed Skill/MCP manifest 当前按 empty availability fail closed，如需显式暴露项目 manifest 损坏，需要后续 spec-backed 错误码。

## W1.4.26 审查结果

- 聚焦实现自检已通过：project harness / run lifecycle pytest、focused mypy、focused ruff。
- 独立 subagent 初审 FAIL：修复了 run start 在获取 `runtime.lock` 前完成 workflow validation、但 lock snapshot 在锁内读取的竞态；现在 workflow load、project validation context、compile、tool lock snapshot 和 run lock 文件写入都在同一个 `runtime.lock` 临界区内完成。
- 独立 subagent 初审 FAIL：修复了从 project config 读取非 `runtime_harness.md` §2.8 字段的问题；Skill lock 只读取 `skill_id/version`，MCP lock 只读取 `server_id` 并写临时 `version="latest"` / `tools_snapshot=[]`。
- 独立 subagent 最终复审 PASS，确认 `secret_ref` 不进入 `mcp_lock.json`、重复 enabled ID 由首个条目确定、object manifest runtime guard 保持有效，且本切片未实现 registry/lifecycle/secret/adapter toolset。
- 审查残余风险：MCP lock 中 `version="latest"` 与空 `tools_snapshot` 只是占位，真实 MCP lifecycle / 工具发现切片需要替换；重复 MCP entry 行为由共享 first-enabled-ID 逻辑保证，但没有单独测试。

## W1.4.27 审查结果

- 聚焦实现自检已通过：project harness / workflow compiler / PydanticAIAdapter pytest、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认版本化 `ProjectToolAvailability.skill_refs`、compiler L4 `SkillRef.version` 校验、PydanticAIAdapter 项目范围 `skill_ids_resolved` gating 均符合 W1.4.27 范围。
- 审查确认本切片未修改 spec/ADR，未定义新 Skill 包格式，未加载项目 Python 代码，未实现 `params` 语义、MCP lifecycle 或 secret 解析。
- 审查残余风险：`version` 缺失/空字符串回落 `"latest"` 的新路径没有单独测试；malformed Skill manifest 仍沿用 W1.4.25 的 empty availability fail-closed 策略，未引入新 manifest 错误码。

## W1.4.28 审查结果

- 聚焦实现自检已通过：project harness / ClaudeCodeAdapter pytest、focused mypy、focused ruff。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- 独立 subagent 审查 PASS，确认项目 `mcp.config.json` 只经只读 harness loader 进入 ClaudeCodeAdapter，且只把本次 requested MCP server 投影到 SDK `mcp_servers`。
- 审查确认项目 MCP config 仅在 `metadata.cw.project_root` 存在时优先生效，无项目 root 时 W1.4.18 adapter-local settings fallback 保持不变。
- 审查确认 `secret_ref` / `requires_approval=true` / unsupported transport 均 fail closed，且本切片未实现 MCP lifecycle、health check、secret resolution、tool discovery、lock snapshot、provider smoke、ADR/spec 或 dependency 变更。
- 审查残余风险：覆盖仍为 unit/fake-session 级别，不声明真实 Claude/MCP provider smoke；非 spec-shaped malformed manifest 值类型没有穷尽测试；Claude `can_use_tool`/HITL bridge 仍留给后续切片。

## W1.4.29 审查结果

- 聚焦实现自检已通过：NodeRunner + static builders pytest、focused mypy、focused ruff；独立审查建议补齐 `max_input_tokens` / `max_output_tokens` 单独覆盖后，已补参数化回归并复跑 23 个聚焦测试通过。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 独立 subagent 审查 PASS，确认本切片未修改 accepted spec / ADR / public schema，`metadata.cw.usage_limits` 复用既有 `UsageLimits`，且 `usage.jsonl` 仍是累计预算判断的权威来源。
- 审查确认 execution / evaluation / repair 三类 deterministic attempt 均执行累计 budget guard；超限或 cost 不可审计时写 failed attempt + usage ledger，并发 `attempt.failed`、`node.state_changed(to=failed)`、`error.budget_exhausted`、`run.failed`。
- 审查确认本切片未实现模型价格表、token 估算器、实时 flush、UI budget 设置页、真实 Adapter bridge、provider smoke 或跨 run / 项目级预算。

## W1.4.30 审查结果

- 聚焦实现自检已通过：NodeRunner + ModelRouter pytest、focused mypy、focused ruff。
- 独立 subagent 审查 PASS，确认本切片未修改 accepted spec / ADR / public schema / dependency 文件，`estimate_usage_cost_usd()` 仅复用 `ModelCostProfile` 静态价格，且 runbook 未越界声明真实 Adapter bridge、provider smoke 或跨 run / 项目级预算。
- 审查确认 execution / evaluation / repair 三类 deterministic attempt 都在 budget guard、attempt / usage ledger、结果/provenance/event 写入前使用同一 cost-enriched `RunUsage`。
- 审查确认已上报的 `est_cost_usd` 优先于 profile 估算；profile input/output 价格缺失时仍按 W1.4.29 fail closed；未显式提供 usage 不会被默认 0 成本绕过。
- 审查残余风险中的 evaluation / repair 专项断言、reported-cost 优先、未显式 usage 不默认 0 成本已补窄测试并复跑聚焦门禁通过。

## W1.4.31 审查结果

- 聚焦实现自检已通过：`test_m1_4_31_adapter_runner_bridge.py` + `test_m1_3_5_node_runner.py`、focused mypy、focused ruff。
- 独立 subagent 初审发现两处实现缺口：`AdapterFactory.create()` 失败未进入结构化失败路径，以及 budget forced-failed attempt 会丢失 adapter `output_hash`。已修复并补测试覆盖 factory-create failure、budget failure `output_hash` 保留、adapter-emitted runner-owned lifecycle de-dup。
- 独立 subagent 复核 PASS，确认 `adapter_factory.create(...)` 已纳入 `AdapterRuntimeError` 处理，`_attempt_from_adapter_outcome()` 会保留 `outcome.output_hash`，且新增测试覆盖前述回归。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 369 passed, 1 skipped。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查确认本切片未修改 accepted spec / ADR / public schema / dependency 文件；默认 `advance_workflow_run()` 仍为 deterministic，adapter 执行路径仅通过显式 `advance_workflow_run_with_adapters(...)` 进入。
- 审查确认本切片不声明默认 AdapterRegistry、HTTP API、真实 provider smoke、async 长运行锁拆分、cancel/resume 桥、evaluation/repair adapter 执行、MCP lifecycle / secret resolver 或跨 run / 项目级预算。

## W1.4.32 审查结果

- 聚焦实现自检已通过：project harness + run lifecycle pytest、focused mypy、focused ruff。
- 独立 subagent 审查 PASS，确认本切片未修改 accepted spec / ADR / public schema / dependency 文件，且 `mcp.config.json` 仍只按 `runtime_harness.md §2.8` 的 spec 字段读取；非 spec 的 `version/tools_snapshot` 字段不会作为 discovery 来源。
- 审查确认 discovery seam 仅通过显式 `mcp_tool_discovery` 注入进入 `load_project_tool_lock_snapshot()` / `create_workflow_run()`，API 默认 run-start 路径不传 discovery provider，因此默认仍不启动 MCP、不做工具发现、不读 secret。
- 审查确认只有 parsed `ProjectMCPServerConfig` 会触发 discovery；缺 `transport` / `command_or_url` 的 enabled MCP server 仍写入 `version="latest"` / `tools_snapshot=[]`，不会调用 provider。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；`test-py` / `test` 均为 371 passed, 1 skipped。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：discovery provider 自身异常本切片不转换为结构化 runtime failure；真实 MCP process startup、health check、secret resolver、provider smoke 与真实 tool invocation validation 仍留给后续切片。

## W1.4.33 审查结果

- 聚焦实现自检已通过：ClaudeCodeAdapter pytest、focused mypy、focused ruff；resolver 成功、无 resolver、resolver 返回 `None`、resolver 抛普通异常、resolver 返回非 `ClaudeCodeMCPSecretResolution` 对象、`requires_approval`、unsupported transport 与 missing config 均有结构化路径覆盖。
- 独立 subagent 初审 PASS，确认本切片未修改 accepted spec / ADR / public schema，不读取 secure store，不启动 MCP，不声明 OAuth / health check / tool discovery provider / provider smoke。
- 独立 subagent 发现非阻塞 hardening 点：resolver 返回错误对象类型时缺显式校验。已补为 `AA_RUN_TOOL_NOT_FOUND` fail closed，payload 只含 `unresolved_secret_mcp_server_ids` 与 `resolver_result_type`，并补测试覆盖。
- 独立 subagent 增量复核 PASS，确认 hardening 仍在 SDK/session 前失败，且无 secret message / token 泄漏到 error payload。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 375 passed, 1 skipped，codegen 无 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 不在失败列表。
- 审查残余风险：本切片仍为 unit/fake-session 级别，不实现真实 secure store / OAuth token exchange / provider smoke；真实 MCP lifecycle、health check、provider-backed discovery、Claude `can_use_tool`/HITL 桥仍留给后续切片。

## W1.4.34 审查结果

- 聚焦实现自检已通过：project harness pytest、focused mypy、focused ruff；覆盖 lifecycle happy path、unhealthy health fail-closed + close、raw discovery 普通异常、raw discovery 错误返回、dirty `ProjectMCPDiscoveryError` 重新清洗。
- 独立 subagent 初审 FAIL：发现 provider/client 可主动抛未清洗的 `ProjectMCPDiscoveryError` 并携带 `command_or_url` / `secret_ref` dirty details，存在泄漏路径。
- 已修复初审阻塞项：`ProjectMCPDiscoveryError` 增加 harness sanitizer 标记；`ProjectMCPDiscoveryRunner` 与 `_discover_mcp_tools()` 对外部未清洗同类 error 统一重包，只保留 `server_id`、`transport`、`exception_type` 和 allowlisted stage。
- 独立 subagent 复核 PASS，确认 dirty client error 与 dirty raw callable error 均会被重包，unsafe `command_or_url`、`secret_ref` 与 provider exception message 不进入最终 error details / message。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 381 passed, 1 skipped，codegen 无 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：`_cw_sanitized` 是 Python 约定标记，不是对恶意 in-process provider 的硬安全边界；本切片仍不实现具体 stdio/http/sse MCP 协议 client、secure store / OAuth backend 或真实 provider smoke。

## W1.4.35 审查结果

- 聚焦实现自检已通过：project harness pytest、focused mypy、focused ruff；覆盖 stdio initialize/tools-list happy path、JSON-RPC error sanitization、wrong-id/unrelated-message request deadline timeout。
- 独立 subagent 初审 FAIL：发现 `_request()` 对 wrong-id/notification 消息逐次重置 `queue.get(timeout=...)`，持续噪声 server 可让 discovery 不 fail closed。
- 已修复初审阻塞项：`ProjectMCPStdioDiscoveryClient._request()` 使用单个 monotonic per-request deadline，`_read_json_rpc()` 只消费剩余时间；fake stdio server 新增 noise 模式并补回归测试。
- 独立 subagent 增量复核 PASS，确认 wrong-id/unrelated-message hang 已由 request deadline 修复，默认路径仍保持显式注入才启动 discovery。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；`test-py` / `test` 均为 384 passed, 1 skipped，codegen 无 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：本切片只证明本地 fake stdio server smoke，不实现 HTTP/SSE MCP transport、secure store / OAuth backend、真实外部 provider smoke、MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.36 审查结果

- 聚焦实现自检已通过：project harness pytest、focused mypy、focused ruff；覆盖 Streamable HTTP JSON response、SSE response、session/protocol headers、JSON-RPC error sanitization、HTTP status error sanitization。
- 独立 subagent 初审 FAIL：发现 SSE `response.readline()` 仍按 urllib socket timeout 阻塞，未严格使用剩余 request deadline；发现 HTTP discovery client 复用时旧 `_protocol_version` / `_session_id` 可串到下一台 server 的 `initialize`。
- 已修复初审阻塞项：SSE 读取改为 reader thread + queue，主线程按 monotonic per-request deadline 等待并将 socket timeout 映射为 sanitized timeout；`start()` / `close()` 清理 endpoint、request id、version、protocol version 与 session id。
- 已补回归：fake HTTP server `sse_hang` 只发 unrelated SSE event 并保持连接，断言按 `timeout_seconds` 超时；复用同一个 `ProjectMCPHttpDiscoveryClient` 连接两台 fake server，断言第二次 `initialize` 不带旧 session/protocol headers。
- 独立 subagent 增量复核 PASS，确认两个初审问题均已修复，未发现新的 FAIL 级问题。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；`test-py` / `test` 均为 390 passed, 1 skipped，codegen 无 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：本切片只证明本地 fake HTTP/SSE response smoke，不实现旧版 HTTP+SSE GET endpoint fallback、OAuth/secure store、真实外部 provider smoke、MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.37 审查结果

- 聚焦实现自检已通过：project harness pytest、focused mypy、focused ruff；覆盖 Streamable HTTP `initialize` POST 4xx 后 legacy HTTP+SSE fallback happy path、legacy POST header 边界、unrelated SSE notification 忽略，以及 legacy JSON-RPC error sanitization。
- 独立 subagent 审查 PASS：确认 fallback 只挂在 `initialize` POST 4xx 后，Streamable HTTP 仍为首选；legacy POST 不携带 `Accept` / `MCP-Protocol-Version` / `Mcp-Session-Id`，SSE `message` response 按 JSON-RPC id 匹配，server/JSON-RPC error 保持 sanitized，且未新增 dependency 或 schema/ADR/public contract 变化。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；`test-py` / `test` 均为 392 passed, 1 skipped，codegen 无 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：本切片只证明本地 fake legacy HTTP+SSE MCP server smoke，不实现 OAuth/secure store/Authorization、真实外部 provider smoke、MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.38 审查结果

- 聚焦实现自检已通过：project harness + ClaudeCodeAdapter pytest、focused mypy、focused ruff；覆盖 secure SQLite secret material 成功读取、缺库/缺行返回 `None`、非法解密 payload sanitized failure、Claude adapter `project_mcp_secret_decryptor` 成功投影 headers/env，以及 decryptor/secure-store 错误 fail closed。
- 独立 subagent 审查 PASS：确认 secure store scope 对齐 `runtime_harness.md` §6 的 `secrets(secret_id, value_encrypted)` 契约，crypto 仍由注入式 decryptor 提供；`ProjectMCPSecretMaterial` 严格禁止额外字段，explicit `project_mcp_secret_resolver` 优先于 decryptor fallback，错误路径不暴露 `command_or_url`、`secret_ref`、明文/密文或 SQLite 路径。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`；`test-py` / `test` 均为 397 passed, 1 skipped，codegen 无 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：本切片只证明本地 secure SQLite + fake-session adapter smoke，不实现 AES-GCM-256 / PBKDF2 / OS keychain 主密钥，不实现 OAuth refresh / token exchange、真实外部 provider smoke、MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## W1.4.39 审查结果

- 聚焦实现自检已通过：`test_m1_4_31_adapter_runner_bridge.py`、focused mypy、focused ruff；覆盖 adapter evaluation 写入 `evaluations.jsonl` 并路由 repair、adapter repair 写入 `repairs.jsonl` 与 pending prompt overlay、无效 adapter evaluation output 以 `AA_RUN_OUTPUT_VALIDATION_FAILED` fail closed。
- 本地差异自检确认未修改 accepted spec / ADR / public schema / dependency 文件；evaluation / repair adapter bridge 只通过显式 `advance_workflow_run_with_adapters(...)` 进入，默认 `advance_workflow_run(...)` 仍保持 deterministic foundation 路径。
- 阶段门禁已通过：`lint-py`、`typecheck-py`、`test-py`、`codegen`、`lint`、`typecheck`、`test`、`git diff --check`；`test-py` / `test` 均为 400 passed, 1 skipped，codegen 无 `packages/schemas-ts/src/generated/**` diff。
- `format-check` 中 Python `ruff format --check .` 通过；Prettier 仍命中 38 个既有 docs/spec/config 格式债，本轮触碰的 `docs/04_runbook/m1.4-progress.md` 已通过 targeted Prettier。
- 审查残余风险：本切片不实现默认 AdapterRegistry / HTTP API 接线、不拆分 runtime lock、不实现 adapter resume/cancel 桥、真实 provider smoke、MCP tool invocation 或 Claude `can_use_tool`/HITL 桥。

## 下一步

- 2026-06-19 handoff：W1.4.20-W1.4.44 已完成 PydanticAIAdapter 默认 builtin `web_fetch` / read-only `file_io` / expression-only `python_sandbox`、ModelRouter 对默认 Pydantic AI builtin tool-name 的闭合集合过滤、runtime terminal attempt usage ledger append 与 deterministic runner run 内累计 budget guard、deterministic runner 按选中 ModelProfile 静态价格补全 `RunUsage.est_cost_usd`、显式 AdapterFactory 驱动 execution/evaluation/repair attempt 并把 `AttemptOutcome` 归并到 attempts/evaluations/repairs/usage 持久化闭环、adapter bridge begin/run/complete 锁拆分与 pause preemption 收口、注入式 MCP tool discovery 结果写入 `mcp_lock.tools_snapshot`、项目 Skill/MCP availability context 投影到 workflow L4 校验、Run 启动时 `skill_lock.json` / `mcp_lock.json` 快照写入、项目 Skill 版本化 registry 校验、项目 `mcp.config.json` 到 ClaudeCodeAdapter SDK `mcp_servers` 的 request-bounded 投影、注入式 project MCP secret resolver 到 Claude MCP `headers/env` 的 fail-closed 投影、显式 provider client `start/health/discover/close` lifecycle runner、本地 stdio MCP JSON-RPC discovery client、Streamable HTTP JSON/SSE response discovery client、旧版 HTTP+SSE GET endpoint fallback、project secure SQLite backed MCP secret resolver backend、Claude Agent SDK `can_use_tool`/HITL fake-SDK bridge、project `mcp.config.json requires_approval` 到 human gate 的 bridge 投影、project secure secret envelope/PBKDF2/key-provider seam，以及本地 stdio/HTTP/legacy MCP `tools/call` invocation smoke。
- W1.4.51-W1.4.59 已补齐只读 project config API、system capabilities API、`run-once` / `re-evaluate` 节点动作到 injected/default AdapterRegistry bridge 的 HTTP seam、手动 `:repair` 的 request-body / RepairPatch response seam、repair prompt overlay 的 `until_pass` / `persistent_for_run` / `this_attempt_only` runtime 语义及 one-shot overlay 负向回归、默认 PydanticAIAdapter 对无 `secret_ref` project MCP `tools/call` 的 AdapterRegistry/API fake-SDK 闭环，以及注入式 secure-store decryptor 到默认 Pydantic project MCP FunctionToolset 的 fail-closed 投影；仍不声明公网真实 provider 或 OAuth-backed MCP invocation 端到端闭环。
- M1.4 后续 Adapter 工作：真实外部 HTTP MCP provider smoke、真实 OS keychain provider 与真实 AES-GCM provider 接入、OAuth resolver provider smoke、真实外部 provider 上游 `cancel()` ≤5s 实测、Claude Agent SDK real-provider HITL smoke，以及 manual repair 的 `persistent_for_workflow` 语义仍需单独切片。
