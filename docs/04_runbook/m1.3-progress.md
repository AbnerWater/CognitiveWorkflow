# M1.3 Runtime 核心 · 进度日志

> 本文档跟踪 M1.3 milestone 的阶段切片；与 `docs/roadmap.md §5.2` 的 Runtime 核心计划对照。

| Work item | 描述                                                                                              | 状态    |
| --------- | ------------------------------------------------------------------------------------------------- | ------- |
| W1.3.1    | Runtime API foundation：settings / auth / ErrorEnvelope / system endpoints / CLI READY foundation | done    |
| W1.3.2    | Project runtime harness skeleton：`.agent-workflow/` 初始化、manifest revision、锁边界            | done    |
| W1.3.3    | WorkflowGraph validator + compile boundary：Graph → Engine IR / LangGraph adapter seam            | done    |
| W1.3.4    | Run state and StreamEvent bus：Run lifecycle、SSE、jsonl 持久化边界                               | done    |
| W1.3.5    | Node runner foundation：execution / evaluation / repair / human_checkpoint / start / end          | done    |
| W1.3.6    | ContextBuilder + EvidenceBuilder Phase 1 static implementation                                    | done    |
| W1.3.7    | Persistence / observability foundation：SQLite index、trace.sqlite exporter、git snapshot hooks   | done    |
| W1.3.8    | HITL decision resolve foundation：`/runs/{run_id}/decisions`、`human.gate_resolved`、决策路由恢复 | done    |

## W1.3.1 范围

本切片只建立 Runtime sidecar 的 API 基础面，不实现 Engine、持久化、SSE replay 或 Adapter：

- `RuntimeSettings` 固定 `127.0.0.1` 绑定，要求 `CW_RUNTIME_AUTH_TOKEN` 注入，符合 `http_sse.md` D-API-1 / D-API-2。
- `ErrorEnvelope` 对齐 `http_sse.md §1.5` 字段：`schema_version / error_code / message / details / cw_failure_type / retry_after_ms / trace_id`。
- `validate_bearer_authorization()` 对 `Authorization: Bearer <token>` 做常量时间比较；缺失或错误 token 返回 `AUTH_FORBIDDEN`。
- `create_app()` 通过 runtime extra 动态加载 FastAPI，避免默认 `cw_runtime` import 依赖 runtime extras。
- 已落地 system 端点基础响应：
  - `GET /cw/v1/system/info`
  - `GET /cw/v1/system/health`
  - `GET /cw/v1/system/capabilities`
  - `POST /cw/v1/system/shutdown`
- CLI 启动时绑定 `127.0.0.1:<port>`，成功监听后输出 `READY <port>`；缺 token 或缺 runtime extra 时以错误退出，不输出假 READY。

## 验证计划

```powershell
uv run pytest apps/runtime/tests -q
uv run --package cw_runtime --extra runtime pytest apps/runtime/tests/test_m1_3_1_system_endpoints.py -q
uv run mypy --strict apps/runtime/src apps/runtime/tests
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
git diff --check
```

## W1.3.1 审查结果

- 独立 subagent 初审结论：`CONDITIONAL_PASS`，指出 `http_sse.md §8` 的外部 `Origin` 拒绝未实现。
- 已修复：runtime middleware 现在拒绝外部 `Origin: http://...`，接受无 Origin、`app://...` 与 `http://127.0.0.1:<port>`；测试覆盖顶层 `AUTH_FORBIDDEN` envelope、无 FastAPI `detail` wrapper、CSP header。
- 独立 subagent 复核结论：`PASS`。

## W1.3.2 范围

本切片建立项目级 Runtime Harness 初始化能力，并接入项目创建/读取 API：

- `cw_runtime.harness` 新增纯 Python 项目初始化逻辑：
  - 创建 `.agent-workflow/` 骨架和用户可见的 `references/`、`workflow/`、`outputs/`。
  - 写入 `project.json`、`settings.json`、`workflow.flow.json`、`workflow_history.json`、`memory.json`、`references.manifest.json`、`skills.config.json`、`mcp.config.json`、`adapters.config.json`、`manifest_revision.json`。
  - 追加 `.gitignore` 与 `.gitattributes` 中 runtime harness 必需规则。
  - 初始化 Git 仓库，安装最小 pre-commit hook，并提交 `chore(cw): initialize CognitiveWorkflow project <project_id>`。
- `manifest_revision.json` 初始记录 8 个 manifest；`update_manifest_json()` 在 `runtime.lock` 下原子写入并递增 revision。
- `memory.json` 默认禁止通过普通 manifest 写入，保留 D-RH-2 的 `memory_task` / 显式 UI 边界。
- `POST /cw/v1/projects` 接入项目创建；`GET /cw/v1/projects/{project_id}` 返回当前 runtime 进程注册的项目资源。
- `POST /cw/v1/projects` 支持 `Idempotency-Key`：同 key + 同 body 重放结果，不同 body 返回 `IDEMPOTENCY_KEY_BODY_MISMATCH`，TTL 外重用返回 `IDEMPOTENCY_KEY_REUSE_OUTSIDE_WINDOW`。

## W1.3.2 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_2_project_harness.py -q
uv run --package cw_runtime --extra runtime pytest apps/runtime/tests/test_m1_3_2_project_endpoints.py -q
uv run mypy --strict apps/runtime/src apps/runtime/tests
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
git diff --check
```

## W1.3.2 审查结果

- 独立 subagent 初审结论：`FAIL`，发现 3 项问题：
  - 初始 CW commit 可能包含已有 repo 中用户预先 staged 的文件。
  - `skills.config.json`、`mcp.config.json`、`adapters.config.json` 形状是 `{ "entries": [] }`，偏离 spec 的 list-of-objects。
  - `POST /cw/v1/projects` malformed JSON 会返回 plain 500 而非 `ErrorEnvelope`。
- 已修复：初始提交改为 `git commit --only -- <CW paths>`；三份 config manifest 初始化为 `[]`；项目创建端点捕获 malformed JSON 并以顶层 `ErrorEnvelope` 返回 `SCHEMA_VERSION_MISSING`。
- 独立 subagent 复核结论：`PASS`。

## W1.3.3 范围

本切片建立 Runtime 的 WorkflowGraph validation / compile boundary，不实现 LangGraph `StateGraph`、节点 Runner 或 Adapter 调用：

- 新增 `cw_runtime.engine`，提供 `load_workflow_graph()`、`validate_workflow_graph_payload()`、`compile_workflow_graph()`、`load_and_compile_workflow()`。
- L1 文件/JSON 入口映射到 `workflow_graph.md §8` 已锁错误码：`WG_L1_INVALID_JSON`、`WG_L1_NODES_NOT_ARRAY`、`WG_L1_EDGES_NOT_ARRAY`。
- L2 schema 与节点/边声明一致性继续复用 `cw_schemas.WorkflowGraph`，runtime 保留 `WG_*` / `NC_*` custom error code，并把常见 Pydantic discriminator / enum 错误映射回 spec 错误码。
- `compile_workflow_graph()` 输出内部 `EngineWorkflowIR`，并把 `evaluation_task` 的 pass/fail、`repair_task` 的 repair/retry、`human_checkpoint` 的 decision routes 合并为确定性 Engine 边。
- L3 编译校验覆盖 orphan、unreachable、fail dead-end、uncontrolled loop；合法 retry loop 需要 `retry/loop` 边以及 evaluation `max_retry` guard。
- L4 可行性校验覆盖 enabled node types、global context refs、ModelProfile、Skill、MCP server 外部注册表，不在 schema 层强行实现运行时状态。
- 默认启用节点类型为 Phase 1 runtime 范围：`start / end / execution_task / evaluation_task / repair_task / human_checkpoint`；`tool_task / memory_task / subflow` 仍由 L4 阻止。

边界说明：W1.3.3 只完成 Graph → Engine IR / LangGraph adapter seam；`workflow_graph.md §7` 与 ADR-0004 要求的 WorkflowGraph → LangGraph `StateGraph` 一对一编译仍属于后续 M1.3 Engine/Runner 完成条件，不能用本切片替代最终 StateGraph compiler。

## W1.3.3 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_3_workflow_compiler.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/engine apps/runtime/tests/test_m1_3_3_workflow_compiler.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
git diff --check
```

## W1.3.3 审查结果

- 独立 subagent 初审结论：`FAIL`，指出 5 项问题：W1.3.3 scope 与最终 LangGraph StateGraph compiler 目标容易混淆；runtime 错误码归一化丢失 `NC_*` / `WG_L2_METADATA_NOT_NAMESPACED`；human route 合并会丢 decision key；非 LLM 节点的 contract model policy 被 L4 model registry 错误检查；repair-task 回流把 `repair` 边当作 `retry` 边兼容。
- 已修复：文档显式声明本切片不是最终 LangGraph StateGraph compiler；`WorkflowValidationError` 保留 `WG_*` / `NC_*` L1-L4 custom code；human route 按 `route_key` 合并；节点级 model registry 仅检查 execution/evaluation/repair；repair-task 回流只接受/生成 `retry` 语义。
- 回归测试新增覆盖：schema custom error code passthrough、human decision route key、repair vs retry 语义、non-LLM contract model policy。
- 独立 subagent 复核结论：`CONDITIONAL_PASS`；代码级阻塞已关闭，条件是完整 WorkflowGraph → LangGraph `StateGraph` compiler 仍为后续 M1.3 完成项。

## W1.3.4 范围

本切片建立 WorkflowRun lifecycle 与 StreamEvent bus 的服务端/落盘边界，不执行节点、不启动 Adapter、不推进 LangGraph：

- 新增 `cw_runtime.runs`，提供 `create_workflow_run()`、`read_workflow_run()`、`pause_workflow_run()`、`resume_workflow_run()`、`cancel_workflow_run()`、`list_stream_events()`、`stream_sse_events()`、`format_sse_event()`。
- `POST /cw/v1/workflows/{workflow_id}/run` 创建 `runs/<run_id>/`，写入 `run.json`、空的 attempts/evaluations/repairs/decisions/usage/metrics jsonl、`skill_lock.json`、`mcp_lock.json`，并发出 `run.started`。
- `GET /cw/v1/runs/{run_id}` 返回 `run.json` 投影；`POST /cw/v1/workflows/{workflow_id}/pause|resume|cancel` 驱动当前 active Run 状态迁移；`POST /cw/v1/runs/{run_id}/cancel` 保留为 run-level 取消入口。
- `GET /cw/v1/runs/{run_id}/stream` 与 `/observability/runs/{run_id}/stream` 使用 live `StreamingResponse`，按 `stream_event.md` D-SE-2 输出 SSE 帧，支持 `Last-Event-ID` replay、`since_seq / until_seq`、`category / level` 过滤与 `system.heartbeat`。
- 创建 Run 时执行 W1.3.3 的 WorkflowGraph L1-L4 validation / Engine IR compile boundary，并阻止同一 Workflow 的并发非终态 Run（`WR_CONCURRENT_RUN_FORBIDDEN`）。
- Run 创建、状态迁移与 StreamEvent append 在 `runtime.lock` 内串行化；generic resume 只允许 `paused -> running`，`waiting_user / repairing` 仍由后续 `decision.resolved / patch.applied` 路径触发。
- Project 初始化追加 `.agent-workflow/runs/*/stream-events/` 与 planning stream-events 的 git ignore/pre-commit 阻断，符合 D-RH-10。
- 副作用 POST 复用 API idempotency cache：同 path + 同 `Idempotency-Key` + 同 body 在 24h 内重放响应。

边界说明：W1.3.4 是 Run 壳层和事件总线，不生成 NodeAttempt 内容，不写 EvaluationResult/RepairPatch/HumanDecision 业务记录，也不创建 git run snapshot/tag；这些由 W1.3.5+ Runner、Repair/HITL 与 Persistence slice 补齐。

## W1.3.4 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_4_run_lifecycle.py apps/runtime/tests/test_m1_3_4_run_endpoints.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/runs apps/runtime/src/cw_runtime/harness apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_3_4_run_lifecycle.py apps/runtime/tests/test_m1_3_4_run_endpoints.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
git diff --check
```

## W1.3.4 审查结果

- 独立 subagent 初审结论：`FAIL`，指出 6 项问题：run-level pause/resume API drift、SSE replay-only 非 live stream、active Run 检查无锁、generic resume 绕过 `waiting_user / repairing` 触发条件、`stream-events/` 未阻止 Git tracking、`run.json` 持久化 spec 外 `schema_version`。
- 已修复：pause/resume/cancel 改为 workflow-level active Run 操作并保留 run-level cancel；SSE 改为 `StreamingResponse` + replay preflight + heartbeat/poll；Run 创建/状态/事件写入使用 `runtime.lock`；generic resume 收紧为 `paused`；project `.gitignore` 与 pre-commit hook 阻止 stream-events 入 Git；`run.json` 移除 spec 外 `schema_version`。
- 回归测试新增覆盖：workflow-level endpoint、并发启动串行化、`waiting_user / repairing` resume 阻断、live heartbeat、stream-events Git 防护。
- 独立 subagent 复核结论：`PASS`。

## W1.3.5 范围

本切片建立确定性 Node runner foundation，让 W1.3.4 的 Run lifecycle / StreamEvent bus 能推进最小节点闭环；不调用真实模型、不引入 LangGraph 运行依赖、不实现 ContextBuilder/EvidenceBuilder 或 Human Decision resolve：

- 新增 `cw_runtime.runner.advance_workflow_run()`，从 `run.json.current_node_ids` 取单个当前节点，在 `runtime.lock` 内推进一步。
- 覆盖 Phase 1 节点类型：`start / end / execution_task / evaluation_task / repair_task / human_checkpoint`；`tool_task / memory_task / subflow` 仍不在本切片执行范围。
- `execution_task` 使用显式 deterministic `ExecutionAdvanceInput` 生成 `NodeAttempt`，写入 `attempts.jsonl`，并写入最小有效 `context_packs/<id>.json` 与 `execution_packs/<id>.json` foundation stub，避免 attempt 引用悬空。
- `evaluation_task` 使用 deterministic `EvaluationAdvanceInput` 生成完整 `EvaluationResult`，写入 `evaluations.jsonl`，按 `pass_to_next / repair_with_patch / human_checkpoint / retry_same` 的可判定路由推进。
- `repair_task` 使用 deterministic `RepairAdvanceInput` 生成 `RepairPatch`，写入 `repairs.jsonl`，登记目标节点 pending prompt overlay；目标 execution retry attempt 创建时写入 `overlays/<target_attempt_id>.json`，并挂到该目标 attempt 的 `ExecutionPack.effective_prompt_overlay` 与 `NodeAttempt.effective_prompt_overlay_ref`。
- `human_checkpoint` 进入 `waiting_user`，发出 `human.gate_required` + `run.paused`，不实现 `decision.resolved`；`waiting_user -> running` 仍留给后续 HITL resolve 切片。
- 节点运行态与 attempt 计数放在 `run.json.metadata.cw.node_states / attempt_counts / last_attempt_ids / last_evaluation_by_target / last_patch_ids`，不扩展 `run.json` 顶层字段。
- StreamEvent 严格使用已锁事件类型：`node.state_changed`、`attempt.started/completed/failed`、`evaluation.started/completed`、`repair.started/patch_proposed/patch_applied`、`human.gate_required`、`run.paused/completed/failed`。

边界说明：W1.3.5 是确定性 runner foundation，不等同于最终 WorkflowGraph → LangGraph `StateGraph` 执行器；真实 Adapter 调用、Context/Evidence pack 构建、Human Decision resolve、SQLite/OTel/git snapshot 都仍属于 W1.3.6+ / W1.3.7。

## W1.3.5 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_5_node_runner.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/runner apps/runtime/src/cw_runtime/runs apps/runtime/tests/test_m1_3_5_node_runner.py
uv run ruff check apps/runtime/src/cw_runtime/runner apps/runtime/src/cw_runtime/runs/lifecycle.py apps/runtime/tests/test_m1_3_5_node_runner.py
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
git diff --check
```

## W1.3.5 审查结果

- 独立 subagent 初审结论：`FAIL`，指出 5 项问题：repair overlay 错绑到 repair attempt；failed evaluation 默认路由忽略 failure taxonomy 且可把 terminal 当 repair target；EvaluationResult criteria 未从 EvaluationContract 回填；human_checkpoint 未写 pending decision；新增 StreamEvent payload 缺关键字段。
- 已修复：overlay 改为 pending 后绑定下一次目标 retry attempt；failure_type 默认路由按 W1.3.5 已支持能力收敛，`model_capability_limit` 在 model escalation 未实现前降级到 human/abort；repair/human target 做节点类型校验；EvaluationResult 由 contract.criteria 生成；waiting_user 写 `decisions.jsonl` pending 记录；attempt/evaluation/repair/human/run 事件补齐 spec 关键 payload。
- 独立 subagent 复核结论：`CONDITIONAL_PASS`，条件为记录 model escalation 降级边界与 overlay 时机；已在本节范围说明中同步。
- 独立 subagent 最终复核结论：`PASS`；无 required fixes。

## W1.3.6 范围

本切片把 W1.3.5 的 foundation stub pack 替换为 deterministic static ContextBuilder / EvidenceBuilder Phase 1，不调用真实模型、不读取 ReferenceLibrary 索引、不调用工具、不写缓存数据库：

- 新增 `cw_runtime.builders`，提供 `build_static_attempt_packs()`，一次性构建 `EvidencePack | None`、`ContextPack` 与 `ExecutionPack`。
- Static ContextBuilder 覆盖 `node_goal`、`context_requirements.kind=static_text`、`context_requirements.kind=user_input`，从 `run.metadata.cw.initial_input` 读取确定性输入，并填充 `template_inputs.deps`。
- Static EvidenceBuilder 仅在 `NodeContract.evidence_requirements` 非空时运行；Phase 1 只接受 `initial_input.user_assertions` / `initial_input.evidence` 的显式用户断言作为 `user_input` evidence。
- 必填 evidence requirement 无可用用户断言时以 `EP_BUILD_REQUIREMENT_UNRESOLVED` 构建期失败；不生成空 EvidencePack，不伪造证据。
- EvidencePack 构建成功后，ContextBuilder 将每条 Evidence 投影为 `kind=evidence` 的 `ContextFragment`，并在 `deps.evidence` 中提供引用列表。
- Runner 在 attempt 启动前写入 `evidence_packs/<id>.json`、`context_packs/<id>.json`、`execution_packs/<id>.json`，并发出 `context.build_started`、`evidence.build_completed`、`context.build_completed`。
- `NodeAttempt.evidence_pack_id` 与 `EvaluationResult.provenance.evidence_pack_id` 从本次 bundle 透传，不再硬编码为空。

边界说明：W1.3.6 不是最终 MCCL 检索实现。`reference`、`project_memory`、`upstream_artifact`、ReflectionMemory 注入、压缩摘要、cache SQLite、敏感 secure 旁路、真实 EvidenceBuilder retrieve/rerank/consolidate 与 CitationChecker 仍属于后续 M1.3/W1.4 切片。

## W1.3.6 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_3_6_static_builders.py -q
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.3.6 审查结果

- 独立 subagent 初审结论：`FAIL`，指出 5 项问题：user assertion 与 evidence requirement 匹配过宽且空断言可变成伪 evidence；pack build failure 直接抛异常而非进入 repair/human/fail 路由；`context.build_started` 早于 EvidenceBuilder；EvidencePack hash 含时间字段；ContextFragment 未按 priority 降序。
- 已修复：user assertion 必须有非空 `claim / quote` 且显式匹配 requirement；Pydantic schema 层 `CP_BUILD_* / EP_BUILD_*` ValidationError 包装为可路由 `PackBuildError`；pack build failure 进入 explicit repair、human fallback 或 run.failed；`evidence_packs/<id>.json` 先落盘再发 `evidence.build_completed`，且 evidence completed 早于 context started；EvidencePack hash 排除时间戳；ContextFragment 按 priority 稳定排序。
- 独立 subagent 最终复核结论：`PASS`；无剩余 required fixes。复核命令覆盖 W1.3.5/W1.3.6 聚焦测试、focused mypy、`git diff --check` 与 oversized context + valid evidence smoke。
- 门禁说明：`.\dev.ps1 format-check` 的 Python formatter 通过；仓库既有 Prettier baseline 仍报告 39 个 Markdown/YAML 文件，不在本切片范围内批量改动。

## W1.3.7 范围

本切片建立 Persistence / observability foundation，只落本地派生索引、`trace.sqlite` exporter 基础表与 run 关键点 git snapshot hook；不实现远端 OTLP、SQLCipher secure store、GC、完整 LangGraph executor 或真实 Adapter trace：

- 新增 `cw_runtime.persistence`，创建 `.agent-workflow/cache/runtime_index.sqlite` 与 `.agent-workflow/traces/trace.sqlite`。
- Runtime index 保持派生视图：`runtime_runs`、`runtime_stream_events`、`runtime_jsonl_records` 从 `run.json`、StreamEvent JSONL 与 run JSONL 追加记录重建；权威仍是文件。
- `trace.sqlite` 按 `observability.md §6` 建立 `spans / resources / metrics` 表与索引；StreamEvent 按 `observability.md §5.1` 投影，`model.thinking_delta / model.text_delta` 不建独立 span，`metric.snapshot / usage.delta` 只写 `metrics` 表。
- OTel attributes 只写 spec 白名单里的结构化元信息，例如 `cw.run.id`、`cw.event.type/category/display_level`、`cw.workflow.id/version`、`cw.attempt.id/index`；不写 StreamEvent payload、prompt 原文、模型输出原文或 quote 原文。
- `metric.snapshot` / `usage.delta` 事件同时投影到 `metrics` 表，作为 metrics.jsonl 的本地查询视图基础。
- Project 初始化创建 runtime/trace SQLite 文件，并在 `snapshots/snapshots.jsonl` 记录 initial commit。
- `run.started`、`attempt.completed`、`run.completed / run.failed / run.cancelled` 触发自动 git snapshot；run 终态创建 `run-<run_id>-<state>` tag。
- 自动 commit 只提交 `.agent-workflow/runs/<run_id>` 与 `snapshots/snapshots.jsonl`，仍由 `.gitignore` / pre-commit 阻止 `stream-events/`、`traces/`、`cache/`、`secure/` 入 Git。

边界说明：W1.3.7 是本地持久化/观测基础，不把 SQLite 提升为权威副本。`snapshots.jsonl` 中的 snapshot record 指向对应 commit；由于 commit hash 不能自包含写入同一 commit，最新 snapshot record 会作为后续 snapshot 的可提交元数据保留在工作区。

## W1.3.7 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_7_persistence_observability.py -q
uv run pytest apps/runtime/tests/test_m1_3_4_run_lifecycle.py apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_3_6_static_builders.py apps/runtime/tests/test_m1_3_7_persistence_observability.py -q
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.3.7 审查结果

- 独立 subagent 初审结论：`FAIL`，指出 4 项问题：`trace_id` 未使用 `StreamEvent.correlation_id`；`model.thinking_delta / model.text_delta` 与 `metric.snapshot / usage.delta` 被错误建成独立 span；`resources.resource_hash` 使用了非 spec 的 blake2b；review/runbook 文档仍为 pending。
- 已修复：trace exporter 优先使用 32-hex `correlation_id` 作为 OTel TraceID；非 hex correlation fallback 使用 blake3 派生；model delta 只保留 runtime index，不创建独立 span；metric/usage 只写 `metrics` 表；resource hash 改为 blake3 canonical JSON；`blake3` 改为 lazy import，避免裸 import harness 依赖 runtime extra。
- 独立 subagent 复核结论：`CONDITIONAL_PASS`；剩余条件为 lazy import 与文档同步，已完成。
- 独立 subagent 最终复核结论：`PASS`；无剩余 required fixes。复核命令覆盖 W1.3.7 聚焦测试、focused mypy、focused ruff、scoped `git diff --check` 与 base harness import smoke。

## W1.3.8 范围

本切片关闭 W1.3.5 留下的 Human Checkpoint `decision.resolved` 基础路径，让等待中的 run 能通过显式人工决策恢复；不实现真实 UI、timeout fallback、edit resumption、Adapter 继续调用或 LangGraph executor：

- 新增 `cw_runtime.runner.HumanDecisionRequest / HumanDecisionRecord` 与 `resolve_human_decision()`，只允许 `WorkflowRun.state=waiting_user` 且当前节点为 `human_checkpoint` 时提交决策。
- 决策必须来自当前 `HumanCheckpointNode.decisions[*].key`，并按 `routing_map` 编译出的 `EdgeType.HUMAN + route_key` 推进到下一节点。
- `decisions.jsonl` 保持 append-only：W1.3.5 写入 pending 行，本切片追加 resolved 行并保留原始 `requested_at`。
- 决策成功发出 `human.gate_resolved`、`node.state_changed` 与 `run.resumed`，并把 run 恢复为 `running`，`current_node_ids` 指向 decision route target。
- 新增 `POST /cw/v1/runs/{run_id}/decisions`，复用 bearer auth、顶层 `ErrorEnvelope`、`Idempotency-Key` cache 与 runtime 进程内 run registry；idempotency replay 先于状态检查，避免重复提交在 run 已恢复后误报。

边界说明：`reject / edit` 的最终语义仍需后续 HITL/Adapter resumption 切片处理；本切片只实现 Run 层 `decision.resolved -> running` 与声明式 decision route。Node 层 `reject -> failed`、`edit -> retrying` 是已知未关闭项，不在 W1.3.8 内用半成品语义替代。`decision.timeout`、fallback decision、cancel/edit artifact mutation 不在本切片范围。

## W1.3.8 验证计划

```powershell
uv run pytest apps/runtime/tests/test_m1_3_8_human_decision_resolve.py -q
uv run mypy --strict apps/runtime/src/cw_runtime/runner apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_3_8_human_decision_resolve.py
uv run ruff check apps/runtime/src/cw_runtime/runner apps/runtime/src/cw_runtime/api/app.py apps/runtime/tests/test_m1_3_8_human_decision_resolve.py
uv run pytest apps/runtime/tests/test_m1_3_4_run_lifecycle.py apps/runtime/tests/test_m1_3_5_node_runner.py apps/runtime/tests/test_m1_3_8_human_decision_resolve.py -q
.\dev.ps1 lint-py
.\dev.ps1 typecheck-py
.\dev.ps1 test-py
.\dev.ps1 codegen
.\dev.ps1 format-check
.\dev.ps1 lint
.\dev.ps1 typecheck
.\dev.ps1 test
git diff --check
```

## W1.3.8 审查结果

- 聚焦实现自检已通过：W1.3.8 pytest、focused mypy、focused ruff。
- 独立 subagent 初审结论：`FAIL`，指出 3 项问题：`POST /runs/{run_id}/decisions` 对缺失 `schema_version` 的请求会被模型默认值误放行；成功响应缺少 HTTP body 必填的 `schema_version`；`reject/edit` Node 层最终语义仍未实现。
- 已修复：`HumanDecisionRequest.schema_version` 改为必填；decision endpoint 成功响应补齐 `schema_version`；测试新增缺失/不支持版本与成功响应版本覆盖。
- 已记录边界：W1.3.8 只关闭 Run 层 decision resolve 与声明式路由，`reject/edit` 的 Node 层 failed/retrying resumption 留给后续完整 HITL/Adapter 切片。
- 独立 subagent 复核结论：`PASS`；无剩余 required fixes。复核确认 P1 已关闭，`reject/edit` 作为 W1.3.8 明确残留边界可接受。

## 下一步

- M1.3 剩余 runtime 核心：完整 WorkflowGraph → LangGraph `StateGraph` executor、ModelRouter Phase 1、ReflectionMemory v0、ClaudeCodeAdapter 与更完整 HITL resumption。
