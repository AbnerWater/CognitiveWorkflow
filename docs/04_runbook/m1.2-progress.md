# M1.2 共享 Schema 包 · 进度日志

> 本文档跟踪 M1.2 milestone（W1.2.1 ~ W1.2.8）的实际推进；与 `docs/roadmap.md §5.2` 的计划清单对照。

| Work item | 描述 | 状态 |
|---|---|---|
| W1.2.1 | cw_schemas.types / ids / metadata | ✅ done |
| W1.2.2 | cw_schemas.workflow（Graph + 8 类 Node + 8 类 Edge + 3 类 Policy） | ✅ done |
| W1.2.3 | cw_schemas.contract（NodeContract 6 类 + 子对象） | ✅ done |
| W1.2.4 | cw_schemas.packs（ContextPack / EvidencePack / ExecutionPack） | ✅ done |
| W1.2.5 | cw_schemas.runtime（EvaluationResult / RepairPatch / NodeAttempt） | ✅ done |
| W1.2.6 | cw_schemas.events（StreamEvent） | ✅ done |
| W1.2.7 | __exported_models__ 完整 + codegen 真实输出 | 🟡 partial（已注册 90 个模型；TS 仍为 schema-name stub，真实类型生成器待补） |
| W1.2.8 | 契约测试覆盖错误码 | 🟡 partial（WG_/NC_/CP_/EP_/ER_/RP_/SE_ 共 40+ 类已覆盖） |

## W1.2.1 + W1.2.2 产出（本轮）

新增文件：

```
packages/schemas/src/cw_schemas/
├── types.py                                # 30+ 枚举（FailureType / NodeType / RepairKind / ...）
├── ids.py                                  # ULID/UUID v7/Draft ID 校验 + StrictId/LooseId
├── metadata.py                             # 命名空间化 metadata helper
└── workflow/
    ├── __init__.py                         # 子模块 re-export
    ├── policies.py                         # ExecutionPolicy / ReviewPolicy / WorkflowModelPolicy
    ├── nodes.py                            # 9 类 WorkflowNode + ArchiveAction + HumanDecisionDef
    └── graph.py                            # WorkflowGraph + WorkflowEdge + EdgeCondition + DraftSource

packages/schemas/tests/
├── fixtures.py                             # MINIMAL_GRAPH_DICT（含 6 节点 + 6 边的最小可执行 Workflow）
└── test_w1_2_workflow_graph.py             # 30+ 契约测试，覆盖 WG_L2_* 11 类错误码
```

修改：

- `packages/schemas/src/cw_schemas/__init__.py` — 顶层 re-export + `__exported_models__` 注册 12 个 workflow 模型
- `packages/schemas/README.md` — 子模块布局图
- 删除占位 `packages/schemas-ts/src/generated/_placeholder.{json,ts}`（codegen 现在产真实输出）

## W1.2.5 + W1.2.6 产出

新增：

```
packages/schemas/src/cw_schemas/
├── runtime/          # EvaluationResult / RepairPatch / NodeAttempt / RunUsage
└── events/           # StreamEvent 12 大 category envelope + validate_stream_event

packages/schemas/tests/
├── test_w1_2_5_runtime.py
└── test_w1_2_6_events.py
```

StreamEvent 已对齐 `specs/schemas/stream_event.md`：顶层字段使用 `seq / type / created_at`，事件类型使用 `human.gate_required`、`model.text_delta`、`repair.patch_applied` 等 spec 口径；schema 层覆盖 `SE_BUILD_BAD_TYPE / SE_BUILD_PAYLOAD_TOO_LARGE / SE_BUILD_BINARY_IN_PAYLOAD`。

## 错误码覆盖度（M1.2 起新建）

| Spec | 错误码 | 测试 | 状态 |
|---|---|---|---|
| workflow_graph.md §11 | WG_L2_DUP_NODE_ID | test_wg_l2_dup_node_id | ✅ |
| workflow_graph.md §11 | WG_L2_DUP_EDGE_ID | test_wg_l2_dup_edge_id | ✅ |
| workflow_graph.md §11 | WG_L2_BAD_SCHEMA_VERSION | test_wg_l2_bad_schema_version | ✅ |
| workflow_graph.md §11 | WG_L2_MISSING_ENTRY_NODE | test_wg_l2_missing_entry_node x2 | ✅ |
| workflow_graph.md §11 | WG_L2_MISSING_TERMINAL_NODES | test_wg_l2_missing_terminal_nodes | ✅ |
| workflow_graph.md §11 | WG_L2_EVAL_NO_PASS_ROUTE | test_wg_l2_eval_no_pass_route | ✅ |
| workflow_graph.md §11 | WG_L2_EVAL_NO_FAIL_ROUTE | test_wg_l2_eval_no_fail_route | ✅ |
| workflow_graph.md §11 | WG_L2_EVAL_PASS_ROUTE_MISMATCH | test_wg_l2_eval_pass_route_mismatch | ✅ |
| workflow_graph.md §11 | WG_L2_EVAL_FAIL_ROUTE_MISMATCH | test_wg_l2_eval_fail_route_mismatch | ✅ |
| workflow_graph.md §11 | WG_L2_REPAIR_MISSING_TARGET | test_wg_l2_repair_missing_target | ✅ |
| workflow_graph.md §11 | WG_L2_METADATA_NOT_NAMESPACED | test_wg_l2_metadata_not_namespaced | ✅ |
| workflow_graph.md §11 | WG_L3_MULTIPLE_ENTRIES | test_wg_l3_multiple_entries | ✅ |
| workflow_graph.md §11 | WG_L2_UNKNOWN_NODE_TYPE | (Pydantic discriminator 自动处理) | ✅ |
| workflow_graph.md §11 | WG_L2_UNKNOWN_EDGE_TYPE | (Pydantic enum 自动处理) | ✅ |
| workflow_graph.md §11 | WG_L1_INVALID_JSON / NODES_NOT_ARRAY / EDGES_NOT_ARRAY | (Pydantic schema 自动处理) | ✅ |

## 本轮验证

```powershell
.\dev.ps1 lint        # ruff check 应当全绿
.\dev.ps1 typecheck   # mypy --strict 全绿（pydantic v2 plugin 已加）
.\dev.ps1 test        # pytest 应当 30+ pass
.\dev.ps1 codegen     # 应当写出 12 份 JSON Schema + TS（每个 __exported_models__ 一份）
```

预期：测试数从 2 → 35+（含 17 类型 / 4 ID / 4 metadata / 12 WG_* + 3 happy path）。

## 下一步

- **W1.2.3 cw_schemas.contract**：NodeContract（最大的一份 spec：6 类 contract_kind + Prompt 三层 + 5 类 ContextRequirement.kind + 3 类 EvaluationCriterion.kind 等）
- 完成后 M1.2 进度从 25% → 50%
